import { describe, it } from '@jest/globals';
describe('useFlag', () => {
  it.todo('returns false when flag is missing');
  it.todo('returns true when feature_flags row has enabled=true');
  it.todo('updates within 5 minutes after dashboard toggle (cache staleTime)');
});
