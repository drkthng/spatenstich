// Phase 09.1 Wave 0 RED: it.todo() pins; Wave 3 GREEN fills.
// Plan 03 GREEN-fill: computeCornerHandles + computeRotationHandle geometry math.
// Pins RESEARCH §Pattern 3 + MVP carve-out A1 (hidden handles when rotateDeg !== 0).

describe('lib/editor/handleGeometry', () => {
  it.todo('computeCornerHandles returns 4 corner positions in garden meters');
  it.todo('computeCornerHandles tl == (xM - widthM/2, yM - heightM/2)');
  it.todo('computeCornerHandles br == (xM + widthM/2, yM + heightM/2)');
  it.todo('computeRotationHandle returns position above top-center with offsetPx scaled by viewport');
  it.todo('MVP carve-out: rotated-resize math TODO; resize handles hidden when rotateDeg !== 0 (A1)');
});
