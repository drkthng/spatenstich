// Phase 09.1 Wave 0 RED: it.todo() pins; Wave 3 GREEN fills.
// Plan 04 GREEN-fill: EditorCanvas Skia Group rotation transform rendering.
// Pins ROTATE-VIS (Skia transform array) + T-09.1-RENDER-ORDER.

describe('EditorCanvas > rotation rendering (ROTATE-VIS)', () => {
  it.todo('Skia <Group> receives transform array [translate,rotate,translate] when provenance.rotateDeg set');
  it.todo('sortByZOrder applied to infrastructure and seasonal layers before map renders');
});
