// Phase 09.1 Wave 0 RED: it.todo() pins; Wave 3 GREEN fills.
// Plan 04 GREEN-fill: WebPlanEditor SVG rotation transform rendering.
// Pins T-09.1-RENDER-ORDER (sortByZOrder applied before render) + ROTATE-VIS (SVG transform attribute).

describe('WebPlanEditor > rotation rendering (ROTATE-VIS)', () => {
  it.todo('SVG <G> receives transform="rotate(deg, cxPx, cyPx)" attribute when provenance.rotateDeg set');
  it.todo('sortByZOrder applied to visibleElements before map renders elements in z-ascending order');
});
