// Phase 09.1 Wave 0 RED: it.todo() pins; Wave 2 GREEN fills.
// Plan 02 GREEN-fill: WebPlanEditor onDoubleClick handler sets editingElementId.
// Pins D-01 (Web trigger) + RESEARCH §Open Q 2 (mousedown threshold).

describe('WebPlanEditor > dblclick (D-01)', () => {
  it.todo('onDoubleClick on SVG element <G> sets editingElementId via setEditingElementId(el.id)');
  it.todo('onDoubleClick stops propagation (does not trigger parent SVG mousedown)');
  it.todo('mousedown threshold of 5px must be exceeded to start drag — leaves dblclick window open (RESEARCH §Open Q 2)');
});
