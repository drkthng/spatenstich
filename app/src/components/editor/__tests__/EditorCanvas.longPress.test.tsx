// Phase 09.1 Wave 0 RED: it.todo() pins; Wave 2 GREEN fills.
// Plan 02 GREEN-fill: EditorCanvas LongPress gesture composition + hit-test dispatch.
// Pins D-01/D-02 (Skia Long-Press, 500ms, maxDistance 10px, findElementAtPoint).

describe('EditorCanvas > longPress (D-01/D-02)', () => {
  it.todo('Gesture.LongPress minDuration is 500ms (D-02)');
  it.todo('Gesture.LongPress maxDistance is 10px — cancels on drag intent (D-02)');
  it.todo('longPress hit-test calls setEditingElementId(hit.id) when element underneath (via findElementAtPoint)');
});
