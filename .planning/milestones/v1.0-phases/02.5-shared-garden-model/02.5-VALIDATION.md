---
phase: 02.5
slug: shared-garden-model
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-23
---

# Phase 02.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 1.x (app/unit) + Supabase SQL pgTAP-style via `supabase db execute` for migration + RLS tests |
| **Config file** | `app/vitest.config.ts` (existing Phase 1) + `supabase/tests/*.sql` (new in this phase) |
| **Quick run command** | `pnpm -C app test --run` (app unit) / `supabase test db` (SQL) |
| **Full suite command** | `pnpm -C app test --run && supabase test db` |
| **Estimated runtime** | ~40 seconds (unit ~15 s, SQL via local supabase ~25 s) |

---

## Sampling Rate

- **After every task commit:** Run `pnpm -C app test --run` (or the relevant subset) — max 20 s feedback
- **After every plan wave:** Run full suite (app + SQL tests)
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 40 seconds

---

## Per-Task Verification Map

> Populated by planner. Every task that modifies production code must have a unit/integration/SQL test or a Wave 0 stub dependency. Pure docs / migration-only scaffolding tasks may be marked N/A with justification.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 02.5-xx-xx | xx | x | GARDEN-01..04 | T-02.5-xx / — | {expected} | unit/integration/sql | `{command}` | ✅ / ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Populated by planner. Expected stubs (anticipated by research):

- [ ] `supabase/tests/rls_member_check.sql` — RLS isolation tests for GARDEN-01
- [ ] `supabase/tests/member_limit.sql` — 2-member DB trigger limit tests
- [ ] `supabase/tests/invite_code.sql` — generation + single-use consume atomicity
- [ ] `supabase/tests/migration_003_atomic.sql` — default-garten seeding + garden_id backfill idempotency
- [ ] `app/src/lib/__tests__/gardenRepo.test.ts` — repo unit tests (create/join/leave)
- [ ] `app/src/lib/__tests__/inviteCodeRepo.test.ts` — code validation + error mapping
- [ ] `app/src/lib/__tests__/migrateLocalToAccount.test.ts` — extended with garden_id stamping

*If none of these files exist yet they are Wave 0 stubs.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Invite-Code share sheet opens native share dialog on iOS/Android | GARDEN-02 | OS-level share sheet cannot be asserted in headless test | Run on device, tap "Teilen" next to code, confirm iOS/Android system share sheet appears |
| "Zuletzt bearbeitet von <Name>" inline label renders correctly on iPhone + Web in Settings → Vereinsregeln | GARDEN-04 | Visual regression requires real device/browser | Edit row on account A, observe account B after Pull-to-Refresh — label shows A's `display_name` + relative time |
| Onboarding flow still works end-to-end after Phase-2.5 pivot (no regression on Phase-2 UX) | GARDEN-01..04 | Cross-flow UX test | Walk: Splash → "Account erstellen" → Onboarding PLZ/Archetyp → Settings zeigt "Mein Garten" mit user als Owner |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 40s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
