---
phase: 03-offline-sync-2-user-shared-state
plan: 04
type: execute
wave: 4
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - app/src/lib/sync/SyncWorker.ts (neu)
  - app/src/lib/sync/SyncTriggers.ts (neu)
  - app/src/lib/sync/backoff.ts (neu)
  - app/src/lib/sync/events.ts (neu)
  - app/src/lib/sync/__tests__/SyncWorker.test.ts (neu)
  - app/src/lib/sync/__tests__/SyncTriggers.test.ts (neu)
  - app/src/lib/sync/__tests__/backoff.test.ts (neu)
  - app/src/lib/sync/__tests__/reconnect-2user.integration.test.ts (neu)
  - app/app/_layout.tsx
  - app/src/stores/authStore.ts
  - app/src/lib/gardenRepo.ts
  - app/src/lib/vereinsregelnRepo.ts
  - app/src/lib/profileRepo.ts
autonomous: true
requirements: [SYNC-02, SYNC-03, SYNC-04]
must_haves:
  truths:
    - "SyncWorker ist eine Klasse mit Constructor-Injection { storage, supabase, sentry? } — keine Module-Funktionen"
    - "SyncWorker-Instanz ist Singleton via getSyncWorker()-Accessor (lazy-initialized); setSyncWorker() für Tests"
    - "Public API: push(), pull(entity), pullAll(), syncAll(), retryOp(opId), discardOp(opId) — alle async"
    - "pullAll() iteriert PULL_ENTITIES-Array und ruft intern pull(entity) pro Entity, aggregiert Results"
    - "syncAll() = push() + pullAll() — für Reconnect-Trigger"
    - "retryOp(opId) setzt attempts=0, clear lastError, status='pending'; Throws 'outbox_entry_not_found' wenn ID fehlt"
    - "discardOp(opId) löscht Outbox-Entry und triggert pull(entity) für Delta-Korrektur; idempotent bei fehlender ID"
    - "pushInFlight-Serialisation (instance-state) verhindert parallele push()-Aufrufe"
    - "push() iteriert sync_outbox FIFO und pusht jeden Eintrag mit entity-spezifischem Mapper zu Supabase"
    - "LWW-P9011-Fehler führen NICHT zu retry — Eintrag wird verworfen und ConflictError via EventEmitter emittiert"
    - "42501-Fehler (RLS/permission) → Eintrag als 'failed' markiert, attempts++ mit Backoff"
    - "pull(entity) nutzt delta-query `WHERE updated_at > lastPullAt` + aktualisiert sync_state auf server_now()"
    - "Pull-Ergebnisse werden via upsertRowsFromServer eingespielt (KEINE Outbox-Einträge)"
    - "Exponential backoff: 1s, 2s, 4s, 8s, ..., capped bei 60s (full jitter)"
    - "Nach 10 erfolglosen Attempts: Eintrag wird in UI (Plan 03-06) als 'permanent failed' gezeigt"
    - "SyncTriggers: NetInfo offline→online, AppState background→active, 500ms debounced after write (D-16/D-26)"
    - "Root-Layout instanziiert SyncWorker via getSyncWorker() und bootet Initial-Pull beim Start, wenn account-mode + online"
    - "Bootstrap-Pull bei erstem Account-Login (fresh device) synct alle 6 Entities binnen 5s"
  artifacts:
    - path: "app/src/lib/sync/SyncWorker.ts"
      provides: "Push-/Pull-Worker als Klasse mit Outbox-Iteration + Delta-Pull + Retry/Discard + Singleton-Accessor"
      contains: "export class SyncWorker"
    - path: "app/src/lib/sync/SyncTriggers.ts"
      provides: "Trigger-Subscriptions: NetInfo + AppState + writeDebounce (500ms per D-16/D-26)"
      contains: "registerSyncTriggers"
    - path: "app/src/lib/sync/backoff.ts"
      provides: "Exponential-Backoff mit Full-Jitter"
      contains: "nextBackoffMs"
    - path: "app/src/lib/sync/events.ts"
      provides: "SyncEventChannel für UI + Sentry"
      contains: "syncEvents"
    - path: "app/app/_layout.tsx"
      provides: "SyncWorker-Bootstrap im Root-Layout (nach auth + garden ready)"
      contains: "registerSyncTriggers"
    - path: "app/src/stores/authStore.ts"
      provides: "Event: onAccountModeActivated → SyncWorker initial pull (via layout-effect)"
      contains: "setAccountMode"
  key_links:
    - from: "app/src/lib/sync/SyncWorker.ts"
      to: "storage.listOutboxEntries / deleteOutboxEntry / updateOutboxEntry"
      via: "FIFO-Iteration des Outbox — instance-method push()"
      pattern: "listOutboxEntries"
    - from: "app/src/lib/sync/SyncTriggers.ts"
      to: "NetInfo.addEventListener + AppState.addEventListener"
      via: "Event-driven Sync-Auslösung"
      pattern: "addEventListener"
    - from: "app/app/_layout.tsx"
      to: "getSyncWorker() + registerSyncTriggers"
      via: "Bootstrap-Aufruf nach ensureDefaultGardenForUser()"
      pattern: "getSyncWorker"
    - from: "scheduleWriteDebounced"
      to: "worker.push()"
      via: "500ms debounce (D-16/D-26) — exakt testet via jest.useFakeTimers"
      pattern: "500"
---

<objective>
Implementierung des SyncWorker-Subsystems als **Klasse** (nicht Modul-Funktionen): Constructor-Injection
von `{ storage, supabase, sentry? }`, Singleton-Accessor `getSyncWorker()`, Instance-Methoden
`push()/pull(entity)/pullAll()/syncAll()/retryOp(opId)/discardOp(opId)`. Push-side iteriert die
Outbox und führt entity-spezifische Supabase-Writes aus mit korrektem LWW-Error-Handling
(P9011/P9010/42501/23505). Pull-side macht Delta-Pulls pro Entity mit `server_now()`-Checkpoints.
Dazu kommen die SyncTriggers (NetInfo-Reconnect, AppState-foreground, 500ms writeDebounce per D-16/D-26),
der Exponential-Backoff und das Bootstrap-Wiring im Root-Layout.

Purpose: Ohne Worker + Triggers wären SYNC-02, SYNC-03, SYNC-04 nicht erreichbar. Die Klassen-
Struktur ist nötig, weil (a) pushInFlight als Instance-State serialisiert werden muss, (b)
retryOp/discardOp instance-scoped an Outbox + SyncTriggers koppeln, und (c) Constructor-Injection
von { storage, supabase } die Testbarkeit (Jest-Mocks) für Plan 03-04 Task 03 Integration-Test
dramatisch verbessert. Plan 03-06 Tasks 02/04/05 nutzt die Klassen-API explizit.

Output:
- `app/src/lib/sync/` mit SyncWorker **class** + SyncTriggers + backoff + events (+ Tests)
- Root-Layout-Wiring mit registerSyncTriggers nach auth+garden ready
- Sentry-Instrumentierung für push/pull-Errors
- sync_state wird pro erfolgreichem Pull auf server_now() gesetzt
- EventEmitter-basiertes Status-Channel für UI (Plan 03-06)
- Repos (gardenRepo, vereinsregelnRepo, profileRepo) patchen `scheduleWriteDebounced()` nach writeWithOutbox
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-offline-sync-2-user-shared-state/03-CONTEXT.md
@.planning/phases/03-offline-sync-2-user-shared-state/03-RESEARCH.md
@.planning/phases/03-offline-sync-2-user-shared-state/03-PATTERNS.md
@.planning/phases/03-offline-sync-2-user-shared-state/03-VALIDATION.md
@.planning/phases/03-offline-sync-2-user-shared-state/03-01-SUMMARY.md
@.planning/phases/03-offline-sync-2-user-shared-state/03-02-SUMMARY.md
@.planning/phases/03-offline-sync-2-user-shared-state/03-03-SUMMARY.md

@app/src/storage/index.ts
@app/src/lib/supabase.ts
@app/src/lib/mappers/rowMappers.ts
@app/src/lib/errors.ts
@app/src/stores/authStore.ts
@app/app/_layout.tsx
@app/src/lib/gardenRepo.ts
@app/src/lib/vereinsregelnRepo.ts
@app/src/lib/profileRepo.ts

<interfaces>
<!-- StorageAdapter (Plan 03-02) — Outbox-API -->

```typescript
interface StorageAdapter {
  listOutboxEntries(limit?): Promise<OutboxEntry[]>;
  deleteOutboxEntry(id): Promise<void>;
  updateOutboxEntry(id, { attempts, lastError }): Promise<void>;
  upsertRowFromServer<T>(entity, row): Promise<void>;
  upsertRowsFromServer<T>(entity, rows[]): Promise<void>;
  getSyncState(entity): Promise<SyncStateEntry | null>;
  setSyncState(state): Promise<void>;
}
```

<!-- Server-RPCs (Plan 03-01) -->

```sql
-- server_now() → ISO-Timestamp für Pull-Checkpoint (clock-skew-safe)
CREATE FUNCTION public.server_now() RETURNS timestamptz

-- LWW-Trigger (alle entity-Tables) wirft:
-- P9010 → updated_at fehlt im UPDATE-Payload (Client-Bug)
-- P9011 → älterer Write (updated_at < existing.updated_at)
```

<!-- Supabase-Client-Type (Injection-Argument für SyncWorker-Constructor) -->

```typescript
import type { SupabaseClient } from '@supabase/supabase-js';
// Der default-Client wird aus `app/src/lib/supabase.ts` importiert:
import { supabase } from '../supabase';
```

<!-- SyncWorker-Klassen-API (DIESE WIRD IN DIESEM PLAN ERSTELLT) -->

```typescript
// app/src/lib/sync/SyncWorker.ts
import type { EntityName } from '@spatenstich/shared';
import type { StorageAdapter } from '@spatenstich/shared';
import type { SupabaseClient } from '@supabase/supabase-js';

export interface SyncWorkerDeps {
  storage: StorageAdapter;
  supabase: SupabaseClient;
  sentry?: typeof import('@sentry/react-native');
}

export class SyncWorker {
  constructor(deps: SyncWorkerDeps);
  push(): Promise<void>;
  pull(entity: EntityName): Promise<void>;
  pullAll(): Promise<void>;         // iteriert PULL_ENTITIES
  syncAll(): Promise<void>;          // push() + pullAll()
  retryOp(opId: string): Promise<void>;
  discardOp(opId: string): Promise<void>;
}

// Singleton-Accessor (lazy-initialized mit Default-Adapter + Default-Supabase-Client)
export function getSyncWorker(): SyncWorker;
export function setSyncWorker(worker: SyncWorker | null): void; // für Tests + layout-Bootstrap
```

<!-- Mapper-Funktionen (Plan 03-03) -->

```typescript
// app/src/lib/mappers/rowMappers.ts
gardenFromDb / profileFromDb / vereinsregelnFromDbRows /
gardenMemberFromDb / inviteCodeFromDb / photoQueueFromDb
vereinsregelnToDbRows(row, userId) → Array<DbInsert>
```

<!-- NetInfo + AppState APIs -->

```typescript
import NetInfo from '@react-native-community/netinfo';
NetInfo.addEventListener((state) => { state.isConnected ... });

import { AppState } from 'react-native';
AppState.addEventListener('change', (state) => { state === 'active' ... });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 03-04-01: Exponential-Backoff + Event-Channel + SyncWorker Klasse + Singleton-Accessor</name>
  <files>
    - app/src/lib/sync/backoff.ts (neu)
    - app/src/lib/sync/events.ts (neu — SyncEventChannel)
    - app/src/lib/sync/SyncWorker.ts (neu — KLASSE mit Singleton-Accessor)
    - app/src/lib/sync/__tests__/backoff.test.ts (neu)
    - app/src/lib/sync/__tests__/SyncWorker.test.ts (neu)
  </files>

  <read_first>
    - .planning/phases/03-offline-sync-2-user-shared-state/03-PATTERNS.md (S-6 Reentrancy-Guard Pattern + S-7 SyncWorker Pattern)
    - app/src/storage/index.ts (storage-Singleton)
    - app/src/lib/supabase.ts
    - app/src/lib/mappers/rowMappers.ts (nach Plan 03-03)
    - app/src/lib/errors.ts (ConflictError, OutboxEnqueueError)
    - .planning/phases/03-offline-sync-2-user-shared-state/03-RESEARCH.md §3 "SyncWorker-Architecture" + §4 "Delta-Pull-Idiom"
    - packages/shared/src/types/entities.ts (OutboxEntry, EntityName, SyncStateEntry)
  </read_first>

  <behavior>
    - Test 1: nextBackoffMs(0) == 0; nextBackoffMs(1) ∈ [0, 1000]; nextBackoffMs(10) ≤ 60000 (cap)
    - Test 2: Backoff nutzt "full jitter": random ∈ [0, 2^(attempts-1) * 1000] mit cap
    - Test 3: `new SyncWorker({ storage, supabase }).push()` iteriert Outbox in FIFO-Order, delegiert an entity-Pusher
    - Test 4: gardens-Pusher übersetzt lokale GardenRow → Supabase-upsert({id, name, plz, ..., updated_at, updated_by_user_id})
    - Test 5: Bei P9011 (ConflictError): Outbox-Eintrag wird gelöscht, ConflictError via events.emit('push_conflict', ...)
    - Test 6: Bei 42501/23505 (permission/unique-violation): attempts++ mit Backoff, bleibt in Queue
    - Test 7: Bei Network-Error: attempts++ + Backoff
    - Test 8: Nach attempts >= 10: events.emit('permanent_failure', entry); bleibt in Queue
    - Test 9: `worker.pull('gardens')` nutzt select() mit `gt('updated_at', lastPullAt)` + aktualisiert sync_state
    - Test 10: Pull speichert via upsertRowsFromServer (keine Outbox-Einträge)
    - Test 11: Parallele `worker.push()`-Aufrufe (gleichzeitig) werden via pushInFlight-instance-flag serialisiert
    - Test 12: `worker.pullAll()` ruft pull() für alle PULL_ENTITIES; Fehler einzelner Entities stoppen die Iteration NICHT (partial-sync erlaubt)
    - Test 13: `worker.syncAll()` ruft pullAll() + push() in dieser Reihenfolge
    - Test 14: `getSyncWorker()` liefert dieselbe Instanz bei wiederholten Aufrufen (Singleton); `setSyncWorker(null)` + `getSyncWorker()` liefert neue Instanz
    - Test 15: `worker.retryOp(opId)` setzt attempts=0, lastError=null, nextAttemptAt=now und triggert syncAll()
    - Test 16: `worker.retryOp('does-not-exist')` wirft Error('outbox_entry_not_found')
    - Test 17: `worker.discardOp(opId)` löscht Entry + triggert pull(entity) für Delta-Korrektur
    - Test 18: `worker.discardOp('does-not-exist')` ist idempotent (kein throw, keine pull)
  </behavior>

  <action>
**1. `app/src/lib/sync/backoff.ts` (NEU):**

```typescript
// Exponential backoff with full jitter (AWS-style).
// Research §3: 2^(attempts-1) * base, cap at 60s, random ∈ [0, cap].

const BASE_MS = 1000;   // 1 Sekunde erste Verzögerung
const CAP_MS = 60_000;  // 60s-Cap

export const BACKOFF_BASE_MS = BASE_MS;
export const BACKOFF_CAP_MS = CAP_MS;
export const MAX_ATTEMPTS = 10;

export function nextBackoffMs(attempts: number, rng: () => number = Math.random): number {
  if (attempts <= 0) return 0;
  const exp = Math.min(CAP_MS, BASE_MS * Math.pow(2, attempts - 1));
  return Math.floor(rng() * exp);
}
```

**2. `app/src/lib/sync/events.ts` (NEU) — EventEmitter für UI + Sentry:**

```typescript
import type { OutboxEntry } from '@spatenstich/shared';
import { ConflictError } from '../errors';

export type SyncEvent =
  | { type: 'push_start'; entry: OutboxEntry }
  | { type: 'push_success'; entry: OutboxEntry }
  | { type: 'push_retry'; entry: OutboxEntry; nextAttempt: number; nextDelayMs: number }
  | { type: 'push_conflict'; entry: OutboxEntry; error: ConflictError }
  | { type: 'push_permanent_failure'; entry: OutboxEntry; lastError: string }
  | { type: 'pull_start'; entity: string }
  | { type: 'pull_success'; entity: string; rowsFetched: number; serverNow: string }
  | { type: 'pull_failure'; entity: string; error: string }
  | { type: 'status_change'; status: 'idle' | 'syncing' | 'offline' | 'degraded' };

type Listener = (event: SyncEvent) => void;

const listeners = new Set<Listener>();

export const syncEvents = {
  on(listener: Listener): () => void {
    listeners.add(listener);
    return () => listeners.delete(listener);
  },
  emit(event: SyncEvent): void {
    for (const l of listeners) {
      try {
        l(event);
      } catch (e) {
        if (__DEV__) console.error('[syncEvents] listener threw', e);
      }
    }
  },
  _reset(): void {
    listeners.clear();
  },
};
```

**3. `app/src/lib/sync/SyncWorker.ts` (NEU — KLASSE mit Singleton-Accessor):**

```typescript
import * as SentryDefault from '@sentry/react-native';
import type { SupabaseClient } from '@supabase/supabase-js';
import { storage as defaultStorage } from '../../storage';
import { supabase as defaultSupabase } from '../supabase';
import { ConflictError } from '../errors';
import { useAuthStore } from '../../stores/authStore';
import { syncEvents } from './events';
import { nextBackoffMs, MAX_ATTEMPTS } from './backoff';
import type {
  EntityName,
  OutboxEntry,
  StorageAdapter,
  GardenRow,
  GardenMemberRow,
  ProfileRow,
  VereinsregelnRow,
  InviteCodeRow,
  PhotoQueueRow,
} from '@spatenstich/shared';
import {
  gardenFromDb,
  profileFromDb,
  vereinsregelnFromDbRows,
  gardenMemberFromDb,
  inviteCodeFromDb,
  vereinsregelnToDbRows,
} from '../mappers/rowMappers';

// Entities, die gepullt werden (photo_queue ist upload-driven, nicht pull-driven)
export const PULL_ENTITIES: EntityName[] = [
  'gardens',
  'garden_members',
  'profiles',
  'vereinsregeln',
  'invite_codes',
];

export interface SyncWorkerDeps {
  storage: StorageAdapter;
  supabase: SupabaseClient;
  sentry?: typeof SentryDefault;
}

/**
 * SyncWorker: Offline-First Sync-Engine für Phase 3.
 *
 * KLASSE (nicht Module-Funktionen), weil:
 *   - pushInFlight-Serialisation braucht Instance-State
 *   - retryOp/discardOp sind instance-scoped und koppeln an Outbox + SyncTriggers
 *   - Constructor-Injection { storage, supabase } erlaubt saubere Jest-Mocks
 *     (insbesondere für reconnect-2user.integration.test.ts in Task 03)
 *
 * Singleton-Pattern: getSyncWorker() im App-Code, setSyncWorker() in Tests + _layout.tsx.
 */
export class SyncWorker {
  private readonly storage: StorageAdapter;
  private readonly supabase: SupabaseClient;
  private readonly sentry: typeof SentryDefault;
  private pushInFlight = false;

  constructor(deps: SyncWorkerDeps) {
    this.storage = deps.storage;
    this.supabase = deps.supabase;
    this.sentry = deps.sentry ?? SentryDefault;
  }

  // ── Public API ──────────────────────────────────────────────────────────────

  /**
   * Push alle pending Outbox-Einträge sequentiell.
   * Idempotent bei parallelem Aufruf (zweiter Aufruf ist No-Op wenn erster noch läuft — S-6 Pattern).
   */
  async push(): Promise<void> {
    if (this.pushInFlight) return;
    this.pushInFlight = true;
    syncEvents.emit({ type: 'status_change', status: 'syncing' });
    try {
      const entries = await this.storage.listOutboxEntries(50);
      for (const entry of entries) {
        await this.pushOne(entry);
      }
    } finally {
      this.pushInFlight = false;
      const remaining = await this.storage.listOutboxEntries(1);
      syncEvents.emit({
        type: 'status_change',
        status: remaining.length > 0 ? 'degraded' : 'idle',
      });
    }
  }

  /**
   * Delta-Pull für eine einzelne Entity.
   * Nutzt sync_state.lastPullAt als `updated_at > X`-Filter.
   * Aktualisiert sync_state auf server_now() nach Erfolg.
   */
  async pull(entity: EntityName): Promise<void> {
    syncEvents.emit({ type: 'pull_start', entity });
    try {
      const state = await this.storage.getSyncState(entity);
      const lastPullAt = state?.lastPullAt;

      const { data: nowData, error: nowErr } = await this.supabase.rpc('server_now');
      if (nowErr) throw nowErr;
      const serverNow = nowData as string;

      const rowsFetched = await this.pullEntity(entity, lastPullAt);

      await this.storage.setSyncState({
        entity,
        lastPullAt: serverNow,
        lastPushAt: state?.lastPushAt ?? null,
      });
      syncEvents.emit({ type: 'pull_success', entity, rowsFetched, serverNow });
    } catch (e) {
      const msg = e instanceof Error ? e.message : String(e);
      this.sentry.captureException(e, { tags: { sync_phase: 'pull', entity } });
      syncEvents.emit({ type: 'pull_failure', entity, error: msg });
      throw e;
    }
  }

  /**
   * Delta-Pull für ALLE PULL_ENTITIES.
   * Fehler einzelner Entities stoppen die Iteration NICHT — partial-sync ist erlaubt.
   * Wird von syncAll() + Plan 03-06 Task 05 (worker.pullAll()) genutzt.
   */
  async pullAll(): Promise<void> {
    for (const entity of PULL_ENTITIES) {
      try {
        await this.pull(entity);
      } catch (e) {
        if (__DEV__) console.warn(`[pullAll] pull ${entity} failed`, e);
        // Continue mit nächster Entity
      }
    }
  }

  /**
   * Voller Sync: alle Entities pullen + Outbox pushen.
   * Wird beim Reconnect + App-Start (nach auth) aufgerufen + von retryOp().
   */
  async syncAll(): Promise<void> {
    await this.pullAll();
    await this.push();
  }

  /**
   * Retry eines failed/stuck Outbox-Eintrags (Plan 03-06 Detail-Screen Retry-Button).
   * Setzt attempts=0, clear lastError, triggert syncAll().
   * Throws 'outbox_entry_not_found' wenn opId nicht existiert.
   */
  async retryOp(opId: string): Promise<void> {
    const entries = await this.storage.listOutboxEntries();
    const entry = entries.find((e) => e.id === opId);
    if (!entry) {
      throw new Error('outbox_entry_not_found');
    }
    await this.storage.updateOutboxEntry(opId, {
      attempts: 0,
      lastError: null,
    });
    syncEvents.emit({ type: 'status_change', status: 'syncing' });
    await this.syncAll();
  }

  /**
   * Verwerfen eines Outbox-Eintrags (Plan 03-06 Detail-Screen Verwerfen-Button).
   * Löscht die Op, triggert pull(entity) für Delta-Korrektur — Server-Stand überschreibt lokalen Wert (D-22).
   * Idempotent: fehlende IDs führen NICHT zu Exception.
   */
  async discardOp(opId: string): Promise<void> {
    const entries = await this.storage.listOutboxEntries();
    const entry = entries.find((e) => e.id === opId);
    await this.storage.deleteOutboxEntry(opId);
    if (entry) {
      try {
        await this.pull(entry.entity);
      } catch {
        // Pull-Fehler still — nächster Trigger wird versuchen
      }
    }
  }

  /** Test-utility: reset internal state between tests. */
  _resetForTest(): void {
    this.pushInFlight = false;
  }

  // ── Private: Push Handlers ─────────────────────────────────────────────────

  private async pushOne(entry: OutboxEntry): Promise<void> {
    syncEvents.emit({ type: 'push_start', entry });
    this.sentry.addBreadcrumb({
      category: 'sync',
      level: 'info',
      message: `push ${entry.entity}.${entry.rowId} op=${entry.operation}`,
      data: {
        entity: entry.entity,
        row_id: entry.rowId,
        operation: entry.operation,
        attempts: entry.attempts,
      },
    });
    try {
      await this.dispatchPush(entry);
      await this.storage.deleteOutboxEntry(entry.id);
      syncEvents.emit({ type: 'push_success', entry });
      this.sentry.addBreadcrumb({
        category: 'sync',
        level: 'info',
        message: `push success ${entry.entity}.${entry.rowId}`,
      });
    } catch (e) {
      await this.handlePushError(entry, e);
    }
  }

  private async dispatchPush(entry: OutboxEntry): Promise<void> {
    switch (entry.entity) {
      case 'gardens':     return this.pushGarden(entry);
      case 'profiles':    return this.pushProfile(entry);
      case 'vereinsregeln': return this.pushVereinsregeln(entry);
      case 'garden_members': return this.pushGardenMember(entry);
      case 'invite_codes': return this.pushInviteCode(entry);
      case 'photo_queue':  return this.pushPhotoQueue(entry);
      default:
        throw new Error(`Unknown entity for push: ${entry.entity}`);
    }
  }

  private async pushGarden(entry: OutboxEntry): Promise<void> {
    const row = entry.payload as GardenRow & { plz?: string | null; klimazone?: string | null; archetype?: string | null };
    const userId = useAuthStore.getState().userId;
    if (!userId) throw new Error('no_user');
    if (entry.operation === 'delete') {
      const { error } = await this.supabase
        .from('gardens')
        .update({
          deleted_at: new Date().toISOString(),
          updated_at: row.updatedAt,
          updated_by_user_id: userId,
        })
        .eq('id', entry.rowId);
      if (error) throw error;
      return;
    }
    const { error } = await this.supabase.from('gardens').upsert(
      {
        id: row.id,
        name: row.name,
        plz: row.plz ?? null,
        klimazone: row.klimazone ?? null,
        archetype: row.archetype ?? null,
        updated_at: row.updatedAt,
        updated_by_user_id: userId,
        deleted_at: row.deletedAt,
      },
      { onConflict: 'id' },
    );
    if (error) throw error;
  }

  private async pushProfile(entry: OutboxEntry): Promise<void> {
    const row = entry.payload as ProfileRow;
    const userId = useAuthStore.getState().userId;
    if (!userId) throw new Error('no_user');
    const { error } = await this.supabase.from('profiles').upsert(
      {
        id: row.userId,
        display_name: row.displayName,
        updated_at: row.updatedAt,
        updated_by_user_id: userId,
        deleted_at: row.deletedAt,
      },
      { onConflict: 'id' },
    );
    if (error) throw error;
  }

  private async pushVereinsregeln(entry: OutboxEntry): Promise<void> {
    const row = entry.payload as VereinsregelnRow;
    const userId = useAuthStore.getState().userId;
    if (!userId) throw new Error('no_user');
    const dbRows = vereinsregelnToDbRows(row, userId);
    if (dbRows.length === 0) {
      const { error } = await this.supabase
        .from('vereinsregeln')
        .delete()
        .eq('garden_id', row.gardenId);
      if (error) throw error;
      return;
    }
    const { error: upsertErr } = await this.supabase
      .from('vereinsregeln')
      .upsert(dbRows, { onConflict: 'id' });
    if (upsertErr) throw upsertErr;
    const currentIds = dbRows.map((r) => r.id);
    const { error: deleteErr } = await this.supabase
      .from('vereinsregeln')
      .delete()
      .eq('garden_id', row.gardenId)
      .not('id', 'in', `(${currentIds.map((id) => `'${id}'`).join(',')})`);
    if (deleteErr) throw deleteErr;
  }

  private async pushGardenMember(_entry: OutboxEntry): Promise<void> {
    throw new Error('garden_members push not supported in Phase 3 — use RPCs directly');
  }

  private async pushInviteCode(_entry: OutboxEntry): Promise<void> {
    throw new Error('invite_codes push not supported — use RPCs');
  }

  private async pushPhotoQueue(entry: OutboxEntry): Promise<void> {
    const row = entry.payload as PhotoQueueRow;
    const userId = useAuthStore.getState().userId;
    if (!userId) throw new Error('no_user');
    const { error } = await this.supabase.from('photo_queue').upsert(
      {
        id: row.id,
        garden_id: row.gardenId,
        storage_path: row.storagePath,
        geo_lat: row.geoLat,
        geo_lng: row.geoLng,
        upload_status: row.uploadStatus,
        upload_error: row.uploadError,
        job_id: row.jobId,
        updated_at: row.updatedAt,
        updated_by_user_id: userId,
        deleted_at: row.deletedAt,
      },
      { onConflict: 'id' },
    );
    if (error) throw error;
  }

  // ── Private: Error Handling ─────────────────────────────────────────────────

  private async handlePushError(entry: OutboxEntry, e: unknown): Promise<void> {
    const err = e as { code?: string; message?: string };
    const code = err.code;
    const msg = err.message ?? String(e);

    if (code === 'P9011') {
      const conflictError = new ConflictError(entry.entity, entry.rowId, e);
      await this.storage.deleteOutboxEntry(entry.id);
      syncEvents.emit({ type: 'push_conflict', entry, error: conflictError });
      this.sentry.captureException(e, {
        level: 'warning',
        tags: {
          sync_phase: 'push',
          entity: entry.entity,
          error_kind: 'lww_conflict',
        },
        extra: { row_id: entry.rowId, attempts: entry.attempts },
      });
      return;
    }

    if (code === 'P9010') {
      await this.storage.deleteOutboxEntry(entry.id);
      this.sentry.captureException(e, {
        tags: { sync_phase: 'push', entity: entry.entity, error_kind: 'missing_updated_at' },
      });
      syncEvents.emit({ type: 'push_permanent_failure', entry, lastError: 'P9010 missing updated_at' });
      return;
    }

    const nextAttempts = entry.attempts + 1;

    if (nextAttempts >= MAX_ATTEMPTS) {
      await this.storage.updateOutboxEntry(entry.id, { attempts: nextAttempts, lastError: msg });
      syncEvents.emit({ type: 'push_permanent_failure', entry: { ...entry, attempts: nextAttempts, lastError: msg }, lastError: msg });
      this.sentry.captureException(e, {
        tags: { sync_phase: 'push', entity: entry.entity, error_kind: 'max_attempts' },
      });
      return;
    }

    const delayMs = nextBackoffMs(nextAttempts);
    await this.storage.updateOutboxEntry(entry.id, { attempts: nextAttempts, lastError: msg });
    syncEvents.emit({
      type: 'push_retry',
      entry: { ...entry, attempts: nextAttempts, lastError: msg },
      nextAttempt: nextAttempts,
      nextDelayMs: delayMs,
    });
  }

  // ── Private: Pull Handlers ─────────────────────────────────────────────────

  private async pullEntity(entity: EntityName, lastPullAt: string | null): Promise<number> {
    const userId = useAuthStore.getState().userId;
    const activeGardenId = useAuthStore.getState().activeGardenId;
    if (!userId) throw new Error('no_user');

    switch (entity) {
      case 'gardens': {
        let q = this.supabase.from('gardens').select('*');
        if (lastPullAt) q = q.gt('updated_at', lastPullAt);
        const { data, error } = await q;
        if (error) throw error;
        const rows = (data ?? []).map(gardenFromDb);
        if (rows.length > 0) await this.storage.upsertRowsFromServer('gardens', rows);
        return rows.length;
      }
      case 'profiles': {
        let q = this.supabase
          .from('profiles')
          .select('id, display_name, created_at, updated_at, updated_by_user_id, deleted_at, locale');
        if (lastPullAt) q = q.gt('updated_at', lastPullAt);
        const { data, error } = await q;
        if (error) throw error;
        const rows = (data ?? []).map(profileFromDb);
        if (rows.length > 0) await this.storage.upsertRowsFromServer('profiles', rows);
        return rows.length;
      }
      case 'vereinsregeln': {
        if (!activeGardenId) return 0;
        let q = this.supabase.from('vereinsregeln').select('*').eq('garden_id', activeGardenId);
        if (lastPullAt) q = q.gt('updated_at', lastPullAt);
        const { data, error } = await q;
        if (error) throw error;
        if ((data ?? []).length === 0) return 0;
        const { data: allData, error: allErr } = await this.supabase
          .from('vereinsregeln')
          .select('*')
          .eq('garden_id', activeGardenId);
        if (allErr) throw allErr;
        const aggregated = vereinsregelnFromDbRows(allData ?? [], activeGardenId);
        if (aggregated) await this.storage.upsertRowFromServer('vereinsregeln', aggregated);
        return (allData ?? []).length;
      }
      case 'garden_members': {
        if (!activeGardenId) return 0;
        const { data, error } = await this.supabase
          .from('garden_members')
          .select('garden_id, user_id, role, joined_at')
          .eq('garden_id', activeGardenId);
        if (error) throw error;
        const rows = (data ?? []).map(gardenMemberFromDb);
        await this.storage.upsertRowsFromServer('garden_members', rows);
        return rows.length;
      }
      case 'invite_codes': {
        if (!activeGardenId) return 0;
        const { data, error } = await this.supabase
          .from('invite_codes')
          .select('*')
          .eq('garden_id', activeGardenId);
        if (error) throw error;
        const rows = (data ?? []).map(inviteCodeFromDb);
        await this.storage.upsertRowsFromServer('invite_codes', rows);
        return rows.length;
      }
      default:
        return 0;
    }
  }
}

// ── Singleton-Accessor ────────────────────────────────────────────────────────

let _instance: SyncWorker | null = null;

/**
 * Lazy-initialized Singleton-Accessor.
 * Default-Injection: storage aus '../../storage', supabase aus '../supabase'.
 * Für Tests: setSyncWorker(null) vor Test + setSyncWorker(customWorker) zur Injection.
 */
export function getSyncWorker(): SyncWorker {
  if (!_instance) {
    _instance = new SyncWorker({
      storage: defaultStorage,
      supabase: defaultSupabase,
    });
  }
  return _instance;
}

export function setSyncWorker(worker: SyncWorker | null): void {
  _instance = worker;
}
```

**4. `app/src/lib/sync/__tests__/backoff.test.ts` (NEU):**

```typescript
import { nextBackoffMs, MAX_ATTEMPTS, BACKOFF_BASE_MS, BACKOFF_CAP_MS } from '../backoff';

describe('nextBackoffMs', () => {
  it('returns 0 für attempts=0', () => {
    expect(nextBackoffMs(0)).toBe(0);
  });

  it('skaliert exponentiell bis zum Cap (rng=1.0)', () => {
    const maxRng = () => 1.0;
    // Da nextBackoffMs Math.floor(rng * exp) rechnet, liefert rng=1.0 den Wert (exp-1) am Rand.
    // Wir testen mit rng=0.999999 für "nahe max" zur Vermeidung von Off-by-One.
    const almostMax = () => 0.999999;
    expect(nextBackoffMs(1, almostMax)).toBeGreaterThanOrEqual(999);
    expect(nextBackoffMs(1, almostMax)).toBeLessThanOrEqual(1000);
    expect(nextBackoffMs(7, almostMax)).toBeLessThanOrEqual(BACKOFF_CAP_MS);
    expect(nextBackoffMs(10, almostMax)).toBeLessThanOrEqual(BACKOFF_CAP_MS);
    // rng=1.0 liefert Math.floor(1.0 * exp) = exp exakt
    expect(nextBackoffMs(1, maxRng)).toBe(1000);
    expect(nextBackoffMs(7, maxRng)).toBe(BACKOFF_CAP_MS);
  });

  it('jitter: rng=0 → 0', () => {
    expect(nextBackoffMs(5, () => 0)).toBe(0);
  });

  it('Konstanten: BASE=1000, CAP=60000, MAX_ATTEMPTS=10', () => {
    expect(BACKOFF_BASE_MS).toBe(1000);
    expect(BACKOFF_CAP_MS).toBe(60_000);
    expect(MAX_ATTEMPTS).toBe(10);
  });
});
```

**5. `app/src/lib/sync/__tests__/SyncWorker.test.ts` (NEU):**

```typescript
import 'fake-indexeddb/auto';
import { SyncWorker, getSyncWorker, setSyncWorker } from '../SyncWorker';
import { syncEvents } from '../events';
import { storage } from '../../../storage';

const supabaseMock = {
  from: jest.fn(),
  rpc: jest.fn(),
} as any;

jest.mock('../../../stores/authStore', () => ({
  useAuthStore: {
    getState: () => ({ userId: 'user-a', activeGardenId: 'garden-a', mode: 'account' }),
  },
}));

function makeWorker(overrides: any = {}) {
  return new SyncWorker({
    storage,
    supabase: overrides.supabase ?? supabaseMock,
    sentry: { addBreadcrumb: jest.fn(), captureException: jest.fn(), captureMessage: jest.fn() } as any,
  });
}

describe('SyncWorker — Klasse + Singleton', () => {
  beforeEach(() => {
    setSyncWorker(null);
    syncEvents._reset();
    jest.clearAllMocks();
  });

  it('getSyncWorker() liefert Singleton-Instanz', () => {
    const a = getSyncWorker();
    const b = getSyncWorker();
    expect(a).toBe(b);
  });

  it('setSyncWorker(null) erzeugt bei nächstem getSyncWorker() neue Instanz', () => {
    const a = getSyncWorker();
    setSyncWorker(null);
    const b = getSyncWorker();
    expect(a).not.toBe(b);
  });
});

describe('SyncWorker.push()', () => {
  beforeEach(() => {
    syncEvents._reset();
    jest.clearAllMocks();
  });

  it('leere Queue ist No-Op', async () => {
    const worker = makeWorker();
    await worker.push();
    expect(supabaseMock.from).not.toHaveBeenCalled();
  });

  it('Success entfernt Eintrag aus Outbox', async () => {
    await storage.writeWithOutbox('gardens', {
      id: 'g-1', createdAt: 'now', updatedAt: 'now', updatedByUserId: 'user-a',
      deletedAt: null, name: 'G', ownerUserId: 'user-a',
    } as any, { entity: 'gardens', rowId: 'g-1', operation: 'insert', payload: {} });

    supabaseMock.from.mockReturnValue({
      upsert: jest.fn().mockResolvedValue({ error: null }),
    });
    const worker = makeWorker();
    await worker.push();
    expect(await storage.listOutboxEntries()).toHaveLength(0);
  });

  it('P9011 löscht Eintrag + emittiert push_conflict', async () => {
    await storage.writeWithOutbox('gardens', {
      id: 'g-1', createdAt: 'now', updatedAt: 'now', updatedByUserId: 'user-a',
      deletedAt: null, name: 'G', ownerUserId: 'user-a',
    } as any, { entity: 'gardens', rowId: 'g-1', operation: 'update', payload: {} });

    supabaseMock.from.mockReturnValue({
      upsert: jest.fn().mockResolvedValue({ error: { code: 'P9011', message: 'older write rejected' } }),
    });

    const events: any[] = [];
    syncEvents.on((e) => events.push(e));
    const worker = makeWorker();
    await worker.push();

    expect(await storage.listOutboxEntries()).toHaveLength(0);
    expect(events.some((e) => e.type === 'push_conflict')).toBe(true);
  });

  it('parallele push()-Aufrufe werden via pushInFlight serialisiert', async () => {
    await storage.writeWithOutbox('gardens', {
      id: 'g-1', createdAt: 'now', updatedAt: 'now', updatedByUserId: 'user-a',
      deletedAt: null, name: 'G', ownerUserId: 'user-a',
    } as any, { entity: 'gardens', rowId: 'g-1', operation: 'insert', payload: {} });

    const upsertMock = jest.fn().mockResolvedValue({ error: null });
    supabaseMock.from.mockReturnValue({ upsert: upsertMock });
    const worker = makeWorker();

    await Promise.all([worker.push(), worker.push(), worker.push()]);
    expect(upsertMock).toHaveBeenCalledTimes(1);
  });
});

describe('SyncWorker.pull / pullAll / syncAll', () => {
  beforeEach(() => {
    syncEvents._reset();
    jest.clearAllMocks();
    supabaseMock.rpc.mockResolvedValue({ data: '2026-04-24T12:00:00Z', error: null });
  });

  it('pull(entity) aktualisiert sync_state.lastPullAt auf server_now()', async () => {
    supabaseMock.from.mockReturnValue({
      select: jest.fn().mockResolvedValue({ data: [], error: null }),
    });
    const worker = makeWorker();
    await worker.pull('gardens');
    const state = await storage.getSyncState('gardens');
    expect(state?.lastPullAt).toBe('2026-04-24T12:00:00Z');
  });

  it('pull speichert Rows via upsertRowsFromServer (KEINE Outbox)', async () => {
    supabaseMock.from.mockReturnValue({
      select: jest.fn().mockResolvedValue({
        data: [{
          id: 'g-1', name: 'Test', created_at: 'now', updated_at: 'now',
          updated_by_user_id: 'user-a', owner_user_id: 'user-a',
          deleted_at: null, plz: null, klimazone: null, archetype: null,
        }],
        error: null,
      }),
    });
    const worker = makeWorker();
    await worker.pull('gardens');
    const row = await storage.getRow('gardens', 'g-1');
    expect(row).toBeTruthy();
    expect(await storage.listOutboxEntries()).toHaveLength(0);
  });

  it('pullAll() iteriert alle PULL_ENTITIES und continued bei Einzel-Fehlern', async () => {
    // gardens OK, profiles wirft, rest OK → pullAll läuft trotzdem durch
    let callCount = 0;
    supabaseMock.from.mockImplementation(() => {
      callCount++;
      if (callCount === 2) {
        return { select: jest.fn().mockResolvedValue({ data: null, error: { message: 'boom' } }) };
      }
      return { select: jest.fn().mockResolvedValue({ data: [], error: null }) };
    });
    const worker = makeWorker();
    await expect(worker.pullAll()).resolves.not.toThrow();
  });

  it('syncAll() ruft erst pullAll() dann push()', async () => {
    const worker = makeWorker();
    const pullAllSpy = jest.spyOn(worker, 'pullAll').mockResolvedValue();
    const pushSpy = jest.spyOn(worker, 'push').mockResolvedValue();
    await worker.syncAll();
    const pullOrder = pullAllSpy.mock.invocationCallOrder[0];
    const pushOrder = pushSpy.mock.invocationCallOrder[0];
    expect(pullOrder).toBeLessThan(pushOrder);
  });
});

describe('SyncWorker.retryOp / discardOp', () => {
  beforeEach(() => {
    syncEvents._reset();
    jest.clearAllMocks();
    supabaseMock.rpc.mockResolvedValue({ data: '2026-04-24T12:00:00Z', error: null });
    supabaseMock.from.mockReturnValue({
      select: jest.fn().mockResolvedValue({ data: [], error: null }),
      upsert: jest.fn().mockResolvedValue({ error: null }),
    });
  });

  async function seedFailedEntry(): Promise<string> {
    await storage.writeWithOutbox('gardens', {
      id: 'g-1', createdAt: 'now', updatedAt: 'now', updatedByUserId: 'user-a',
      deletedAt: null, name: 'G', ownerUserId: 'user-a',
    } as any, { entity: 'gardens', rowId: 'g-1', operation: 'update', payload: {} });
    const [entry] = await storage.listOutboxEntries();
    await storage.updateOutboxEntry(entry.id, { attempts: 9, lastError: 'prev' });
    return entry.id;
  }

  it('retryOp setzt attempts=0, clear lastError', async () => {
    const id = await seedFailedEntry();
    const worker = makeWorker();
    await worker.retryOp(id);
    const entries = await storage.listOutboxEntries();
    // Bei erfolgreichem Push wird Entry gelöscht — dann ist Outbox leer
    // Wir testen hier nur den attempts-reset via pre-push-Seed:
    expect(entries.length === 0 || entries[0].attempts === 0).toBe(true);
  });

  it('retryOp("does-not-exist") wirft outbox_entry_not_found', async () => {
    const worker = makeWorker();
    await expect(worker.retryOp('does-not-exist')).rejects.toThrow('outbox_entry_not_found');
  });

  it('discardOp löscht Entry + triggert pull(entity)', async () => {
    const id = await seedFailedEntry();
    const worker = makeWorker();
    const pullSpy = jest.spyOn(worker, 'pull').mockResolvedValue();
    await worker.discardOp(id);
    expect(await storage.listOutboxEntries()).toHaveLength(0);
    expect(pullSpy).toHaveBeenCalledWith('gardens');
  });

  it('discardOp("does-not-exist") ist idempotent (kein throw, kein pull)', async () => {
    const worker = makeWorker();
    const pullSpy = jest.spyOn(worker, 'pull').mockResolvedValue();
    await expect(worker.discardOp('does-not-exist')).resolves.not.toThrow();
    expect(pullSpy).not.toHaveBeenCalled();
  });
});
```
  </action>

  <verify>
    <automated>pnpm --filter app test -- --runInBand app/src/lib/sync/__tests__/backoff.test.ts app/src/lib/sync/__tests__/SyncWorker.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `app/src/lib/sync/SyncWorker.ts` existiert und exportiert `export class SyncWorker`
    - `grep -q "export class SyncWorker" app/src/lib/sync/SyncWorker.ts` → 0
    - `grep -q "export function getSyncWorker" app/src/lib/sync/SyncWorker.ts` → 0
    - `grep -q "export function setSyncWorker" app/src/lib/sync/SyncWorker.ts` → 0
    - `grep -q "async push()" app/src/lib/sync/SyncWorker.ts` → 0
    - `grep -q "async pull(entity" app/src/lib/sync/SyncWorker.ts` → 0
    - `grep -q "async pullAll()" app/src/lib/sync/SyncWorker.ts` → 0
    - `grep -q "async syncAll()" app/src/lib/sync/SyncWorker.ts` → 0
    - `grep -q "async retryOp(opId" app/src/lib/sync/SyncWorker.ts` → 0
    - `grep -q "async discardOp(opId" app/src/lib/sync/SyncWorker.ts` → 0
    - `grep -q "private pushInFlight" app/src/lib/sync/SyncWorker.ts` → 0 (instance-state)
    - `grep -q "P9011" app/src/lib/sync/SyncWorker.ts` → 0
    - `grep -q "MAX_ATTEMPTS" app/src/lib/sync/backoff.ts` → 0
    - `grep -q "BACKOFF_BASE_MS" app/src/lib/sync/backoff.ts` → 0
    - `grep -q "syncEvents.emit" app/src/lib/sync/SyncWorker.ts` → 0
    - Jest-Tests grün (backoff.test.ts + SyncWorker.test.ts)
  </acceptance_criteria>

  <done>
    SyncWorker ist eine Klasse mit Constructor-Injection.
    Singleton-Accessor (getSyncWorker + setSyncWorker) wirkt.
    Public API: push/pull/pullAll/syncAll/retryOp/discardOp.
    P9011-Conflict-Handling korrekt, parallele push-Aufrufe serialisiert via instance-flag.
    retryOp + discardOp vollständig öffentlich ab Tag 1 (KEINE implizite API-Mutation über Plan-Grenzen).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-04-02: SyncTriggers (NetInfo + AppState + 500ms writeDebounce per D-16/D-26) + Root-Layout-Bootstrap + Repo-Patches</name>
  <files>
    - app/src/lib/sync/SyncTriggers.ts (neu)
    - app/src/lib/sync/__tests__/SyncTriggers.test.ts (neu)
    - app/app/_layout.tsx
    - app/src/stores/authStore.ts
    - app/src/lib/gardenRepo.ts
    - app/src/lib/vereinsregelnRepo.ts
    - app/src/lib/profileRepo.ts
  </files>

  <read_first>
    - .planning/phases/03-offline-sync-2-user-shared-state/03-PATTERNS.md (S-6 Reentrancy-Guard im _layout, S-8 Event-driven Sync)
    - .planning/phases/03-offline-sync-2-user-shared-state/03-CONTEXT.md (D-16 500ms Debounce, D-26 Sync-Trigger)
    - app/src/lib/sync/SyncWorker.ts (Task 03-04-01 — getSyncWorker() + Singleton)
    - app/src/lib/sync/events.ts (Task 03-04-01)
    - app/app/_layout.tsx (Root-Layout, Phase 02.5)
    - app/src/stores/authStore.ts (setAccountMode)
    - app/src/lib/gardenRepo.ts / vereinsregelnRepo.ts / profileRepo.ts (Plan 03-03 — Stellen mit writeWithOutbox)
    - .planning/phases/03-offline-sync-2-user-shared-state/03-RESEARCH.md §5 "Sync Triggers"
  </read_first>

  <behavior>
    - Test 1: registerSyncTriggers() abonniert NetInfo + AppState und gibt unregister-Fn zurück
    - Test 2: NetInfo: offline→online feuert worker.syncAll()
    - Test 3: AppState: background→active feuert worker.syncAll()
    - Test 4: **D-16/D-26 Debounce (500ms exakt)**: scheduleWriteDebounced() triggert worker.push() NACH 500ms — mit jest.useFakeTimers(): bei 499ms noch kein push-Call, bei 500ms genau 1 push-Call
    - Test 5: scheduleWriteDebounced() 5× hintereinander → push nur 1× (debounced)
    - Test 6: Root-Layout registriert Triggers NACH auth-Bootstrap (nicht während isLoading)
    - Test 7: Root-Layout ruft registerSyncTriggers nur EINMAL auf (useRef-Guard, S-6)
    - Test 8: registerSyncTriggers nutzt getSyncWorker() für Worker-Zugriff (Klassen-API)
  </behavior>

  <action>
**1. `app/src/lib/sync/SyncTriggers.ts` (NEU) — nutzt getSyncWorker() (Klassen-API):**

```typescript
// Subscribt auf 3 Event-Quellen und ruft Worker-Methoden:
//   - NetInfo offline→online: worker.syncAll()
//   - AppState background→active: worker.syncAll()
//   - scheduleWriteDebounced(): worker.push() nach 500ms (D-16/D-26)
import NetInfo, { type NetInfoSubscription } from '@react-native-community/netinfo';
import { AppState, type AppStateStatus } from 'react-native';
import { getSyncWorker } from './SyncWorker';

export const WRITE_DEBOUNCE_MS = 500; // D-16 / D-26

let writeDebounceTimer: ReturnType<typeof setTimeout> | null = null;

/** Debounced push() — 500ms nach letztem Write (D-16/D-26). */
export function scheduleWriteDebounced(): void {
  if (writeDebounceTimer) clearTimeout(writeDebounceTimer);
  writeDebounceTimer = setTimeout(() => {
    writeDebounceTimer = null;
    getSyncWorker().push().catch((e) => {
      if (__DEV__) console.warn('[SyncTriggers] debounced push failed', e);
    });
  }, WRITE_DEBOUNCE_MS);
}

/** Test-utility: reset debounce timer zwischen Tests. */
export function _resetSyncTriggers(): void {
  if (writeDebounceTimer) {
    clearTimeout(writeDebounceTimer);
    writeDebounceTimer = null;
  }
}

/**
 * Registriert NetInfo + AppState Subscriptions.
 * Gibt unregister-Funktion zurück (call bei unmount / logout).
 */
export function registerSyncTriggers(): () => void {
  let wasOffline = false;
  const netInfoUnsub: NetInfoSubscription = NetInfo.addEventListener((state) => {
    const isConnected = state.isConnected === true && state.isInternetReachable !== false;
    if (!isConnected) {
      wasOffline = true;
      return;
    }
    if (wasOffline && isConnected) {
      wasOffline = false;
      getSyncWorker().syncAll().catch((e) => {
        if (__DEV__) console.warn('[SyncTriggers] reconnect syncAll failed', e);
      });
    }
  });

  let lastState: AppStateStatus = AppState.currentState;
  const appStateUnsub = AppState.addEventListener('change', (state) => {
    if (lastState !== 'active' && state === 'active') {
      getSyncWorker().syncAll().catch((e) => {
        if (__DEV__) console.warn('[SyncTriggers] foreground syncAll failed', e);
      });
    }
    lastState = state;
  });

  return () => {
    netInfoUnsub();
    appStateUnsub.remove();
    _resetSyncTriggers();
  };
}
```

**2. `app/app/_layout.tsx` — SyncWorker-Bootstrap-Wiring mit getSyncWorker():**

Nach dem bestehenden `ensureDefaultGardenForUser`-Effect einen zweiten Effect anhängen. S-6 Pattern (useRef-Guard) gegen Doppel-Registrierung in React-18-Concurrent/StrictMode:

```tsx
import { registerSyncTriggers } from '@/src/lib/sync/SyncTriggers';
import { getSyncWorker } from '@/src/lib/sync/SyncWorker';

// innerhalb GuardedStack, nach dem bestehenden useEffect für ensureDefaultGardenForUser:

  // ── Phase 3 Plan 03-04 — SyncWorker-Bootstrap (S-6 Pattern) ────────────────
  const syncBooted = React.useRef(false);
  React.useEffect(() => {
    if (syncBooted.current) return;
    if (!identity || mode !== 'account' || !activeGardenId) return;
    syncBooted.current = true;
    const unregister = registerSyncTriggers();
    // Initial-Pull (Reconnect-Equivalent für Fresh-Boot) via Klassen-API
    getSyncWorker().syncAll().catch((e) => {
      if (__DEV__) console.warn('[layout] initial syncAll failed', e);
    });
    return () => {
      unregister();
      syncBooted.current = false;
    };
  }, [identity, mode, activeGardenId]);
```

**3. `app/src/stores/authStore.ts` — setAccountMode signal (kein direkter Worker-Call):**

```typescript
setAccountMode: (userId: string) => {
  set({ mode: 'account', userId });
  // Phase 3 Plan 03-04: layout-effect reagiert auf mode-change und ruft getSyncWorker().syncAll()
},
```

**4. Repo-Patches: `gardenRepo.ts`, `vereinsregelnRepo.ts`, `profileRepo.ts`**

In allen drei Repos nach erfolgreichem `storage.writeWithOutbox`-Call `scheduleWriteDebounced()` rufen. Konkret:

- `app/src/lib/gardenRepo.ts` → in `updateGarden`
- `app/src/lib/vereinsregelnRepo.ts` → in `saveVereinsregeln` + `deleteVereinsregel` (account-mode-Branch)
- `app/src/lib/profileRepo.ts` → in `saveProfile` (account-mode-Branch)

Import in jedem Repo hinzufügen: `import { scheduleWriteDebounced } from './sync/SyncTriggers';` (oder entsprechender Pfad). Nach dem `await storage.writeWithOutbox(...)`-Try-Block (NUR bei Success, nicht im catch) `scheduleWriteDebounced()` aufrufen.

**5. `app/src/lib/sync/__tests__/SyncTriggers.test.ts` (NEU) — mit D-16/D-26 exakt-500ms-Test:**

```typescript
import NetInfo from '@react-native-community/netinfo';
import { AppState } from 'react-native';
import {
  registerSyncTriggers,
  scheduleWriteDebounced,
  _resetSyncTriggers,
  WRITE_DEBOUNCE_MS,
} from '../SyncTriggers';
import * as SyncWorkerModule from '../SyncWorker';

jest.mock('@react-native-community/netinfo');
jest.mock('../SyncWorker', () => {
  const actual = jest.requireActual('../SyncWorker');
  const workerMock = {
    push: jest.fn().mockResolvedValue(undefined),
    pull: jest.fn().mockResolvedValue(undefined),
    pullAll: jest.fn().mockResolvedValue(undefined),
    syncAll: jest.fn().mockResolvedValue(undefined),
    retryOp: jest.fn().mockResolvedValue(undefined),
    discardOp: jest.fn().mockResolvedValue(undefined),
  };
  return {
    ...actual,
    getSyncWorker: jest.fn(() => workerMock),
    __workerMock: workerMock,
  };
});

describe('SyncTriggers', () => {
  let netInfoListener: (state: any) => void;
  let appStateListener: (state: string) => void;
  const workerMock = (SyncWorkerModule as any).__workerMock;

  beforeEach(() => {
    jest.clearAllMocks();
    _resetSyncTriggers();
    (NetInfo.addEventListener as jest.Mock).mockImplementation((cb) => {
      netInfoListener = cb;
      return () => {};
    });
    jest.spyOn(AppState, 'addEventListener').mockImplementation((_ev: any, cb: any) => {
      appStateListener = cb;
      return { remove: jest.fn() } as any;
    });
  });

  it('ruft worker.syncAll() bei offline→online', async () => {
    registerSyncTriggers();
    netInfoListener({ isConnected: false });
    netInfoListener({ isConnected: true, isInternetReachable: true });
    expect(workerMock.syncAll).toHaveBeenCalled();
  });

  it('ruft worker.syncAll() bei background→active', async () => {
    registerSyncTriggers();
    appStateListener('background');
    appStateListener('active');
    expect(workerMock.syncAll).toHaveBeenCalled();
  });

  it('D-16/D-26: Debounce 500ms — bei 499ms noch kein push, bei 500ms genau 1 push', async () => {
    jest.useFakeTimers();
    scheduleWriteDebounced();
    jest.advanceTimersByTime(499);
    expect(workerMock.push).not.toHaveBeenCalled();
    jest.advanceTimersByTime(1); // jetzt insgesamt 500ms
    // setTimeout-callback ist async via Promise — microtask-flush nötig
    await Promise.resolve();
    expect(workerMock.push).toHaveBeenCalledTimes(1);
    jest.useRealTimers();
  });

  it('scheduleWriteDebounced 5× → push nur 1×', async () => {
    jest.useFakeTimers();
    for (let i = 0; i < 5; i++) scheduleWriteDebounced();
    jest.advanceTimersByTime(WRITE_DEBOUNCE_MS + 1);
    await Promise.resolve();
    expect(workerMock.push).toHaveBeenCalledTimes(1);
    jest.useRealTimers();
  });

  it('unregister cleanup klappt', () => {
    const unregister = registerSyncTriggers();
    expect(() => unregister()).not.toThrow();
  });

  it('WRITE_DEBOUNCE_MS ist 500 (D-16/D-26)', () => {
    expect(WRITE_DEBOUNCE_MS).toBe(500);
  });
});
```
  </action>

  <verify>
    <automated>pnpm --filter app test -- --runInBand app/src/lib/sync/__tests__/SyncTriggers.test.ts && pnpm --filter app tsc --noEmit</automated>
  </verify>

  <acceptance_criteria>
    - `app/src/lib/sync/SyncTriggers.ts` existiert
    - `grep -q "registerSyncTriggers" app/src/lib/sync/SyncTriggers.ts` → 0
    - `grep -q "scheduleWriteDebounced" app/src/lib/sync/SyncTriggers.ts` → 0
    - `grep -q "WRITE_DEBOUNCE_MS = 500" app/src/lib/sync/SyncTriggers.ts` → 0 (D-16/D-26)
    - `grep -q "getSyncWorker()" app/src/lib/sync/SyncTriggers.ts` → 0 (Klassen-API)
    - `grep -q "NetInfo.addEventListener" app/src/lib/sync/SyncTriggers.ts` → 0
    - `grep -q "AppState.addEventListener" app/src/lib/sync/SyncTriggers.ts` → 0
    - `grep -q "registerSyncTriggers" app/app/_layout.tsx` → 0
    - `grep -q "getSyncWorker()" app/app/_layout.tsx` → 0
    - `grep -q "syncBooted" app/app/_layout.tsx` → 0 (S-6 useRef-Guard)
    - `grep -q "scheduleWriteDebounced" app/src/lib/gardenRepo.ts` → 0
    - `grep -q "scheduleWriteDebounced" app/src/lib/vereinsregelnRepo.ts` → 0
    - `grep -q "scheduleWriteDebounced" app/src/lib/profileRepo.ts` → 0
    - SyncTriggers.test.ts Jest-Suite grün
    - **D-16/D-26-Debounce-Test (499ms vs 500ms) grün — sichert gegen stille Regression**
    - TS-Build: `pnpm --filter app tsc --noEmit` exit 0
  </acceptance_criteria>

  <done>
    SyncTriggers abonniert NetInfo + AppState mit korrekter Unregister-Fn.
    scheduleWriteDebounced implementiert exakt 500ms (D-16/D-26, via fake-timers verifiziert).
    Root-Layout registriert Triggers genau einmal nach auth + garden ready (S-6 useRef-Guard).
    Repos (gardenRepo, vereinsregelnRepo, profileRepo) triggern debounced push nach erfolgreichen Writes.
    Alle Trigger-Calls nutzen getSyncWorker() (Klassen-API).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-04-03: 2-User-Reconnect-Integrationstest (SC-5 30s-Window) + Sentry-Breadcrumbs (Klassen-API)</name>
  <files>
    - app/src/lib/sync/__tests__/reconnect-2user.integration.test.ts (neu)
  </files>

  <read_first>
    - app/src/lib/sync/SyncWorker.ts (nach Task 03-04-01 — Klasse!)
    - app/src/lib/sync/SyncTriggers.ts (nach Task 03-04-02)
    - .planning/phases/03-offline-sync-2-user-shared-state/03-VALIDATION.md I-4 (30s-Reconnect-Propagation)
    - .planning/phases/03-offline-sync-2-user-shared-state/03-RESEARCH.md §§3-5 + SC-5 Acceptance
    - packages/shared/src/types/entities.ts
  </read_first>

  <behavior>
    - Test 1 (I-4 Integration): 2 SyncWorker-Instanzen (Gerät A + Gerät B) gegen SHARED supabase-Shim. Device A macht offline writeWithOutbox. A.syncAll() pushed. B.syncAll() pullt. B sieht den Wert. Delta ≤ 3s (Nyquist).
    - Test 2 (LWW-Szenario): Server hat bereits B-Write (t0+1s). Device A hat älteren Write in Outbox (t0). A.syncAll() → P9011 → push_conflict-Event + Outbox leer.
    - Test 3: Sentry-Breadcrumbs werden von worker-instance bei push/pull gesetzt (via injected sentry-Mock in Constructor)
    - Test 4: Sentry.captureException bei P9011 hat tag sync_phase='push', error_kind='lww_conflict'
    - Test 5: Ein Push-Cycle dauert < 3s
  </behavior>

  <action>
**`app/src/lib/sync/__tests__/reconnect-2user.integration.test.ts` (NEU):**

```typescript
import 'fake-indexeddb/auto';
import { SyncWorker } from '../SyncWorker';
import { syncEvents } from '../events';
import { storage as sharedStorage } from '../../../storage';
import type { GardenRow } from '@spatenstich/shared';

// Mock-Setup: in-memory supabase-shim teilt state zwischen beiden "Devices"
function createSupabaseShim(opts: { enforceLww?: boolean } = {}) {
  const sharedState: Record<string, any[]> = { gardens: [] };
  let clock = 1_000_000;
  const { enforceLww = true } = opts;

  const rpc = jest.fn((name: string) => {
    if (name === 'server_now') {
      clock += 100;
      return Promise.resolve({ data: new Date(clock).toISOString(), error: null });
    }
    return Promise.resolve({ data: null, error: null });
  });

  const from = jest.fn((table: string) => ({
    select: jest.fn().mockReturnValue({
      gt: jest.fn().mockImplementation((_col: string, ts: string) =>
        Promise.resolve({ data: sharedState[table].filter((r) => r.updated_at > ts), error: null }),
      ),
      eq: jest.fn().mockReturnValue({
        maybeSingle: jest.fn().mockImplementation(() =>
          Promise.resolve({ data: sharedState[table][0] ?? null, error: null }),
        ),
      }),
    }),
    upsert: jest.fn().mockImplementation((row: any) => {
      if (enforceLww) {
        const existing = sharedState[table].find((r) => r.id === row.id);
        if (existing && existing.updated_at >= row.updated_at) {
          return Promise.resolve({ data: null, error: { code: 'P9011', message: 'older write' } });
        }
      }
      sharedState[table] = sharedState[table].filter((r) => r.id !== row.id).concat(row);
      return Promise.resolve({ data: row, error: null });
    }),
    update: jest.fn().mockReturnValue({ eq: jest.fn().mockResolvedValue({ error: null }) }),
    delete: jest.fn().mockReturnValue({ eq: jest.fn().mockResolvedValue({ error: null }) }),
  }));

  return {
    client: { rpc, from } as any,
    _sharedState: sharedState,
    _seed: (table: string, row: any) => { sharedState[table].push(row); },
  };
}

jest.mock('../../../stores/authStore', () => ({
  useAuthStore: {
    getState: () => ({ userId: 'user-a', activeGardenId: 'garden-a', mode: 'account' }),
  },
}));

describe('2-user reconnect (I-4 / SC-5) — Klassen-API', () => {
  beforeEach(() => {
    syncEvents._reset();
    jest.clearAllMocks();
  });

  it('Device A offline-edit, reconnect, Device B pull → sieht Änderung (< 3s)', async () => {
    const shim = createSupabaseShim();
    const sentryMock = {
      addBreadcrumb: jest.fn(),
      captureException: jest.fn(),
      captureMessage: jest.fn(),
    };
    const workerA = new SyncWorker({ storage: sharedStorage, supabase: shim.client, sentry: sentryMock as any });

    const rowA: GardenRow = {
      id: 'garden-a',
      createdAt: '2026-04-24T10:00:00Z',
      updatedAt: '2026-04-24T10:00:05Z',
      updatedByUserId: 'user-a',
      deletedAt: null,
      name: 'Garten-A-Edit',
      ownerUserId: 'user-a',
    };
    await sharedStorage.writeWithOutbox('gardens', rowA, {
      entity: 'gardens', rowId: 'garden-a', operation: 'update', payload: rowA,
    });

    const t0 = Date.now();
    await workerA.syncAll();
    const elapsed = Date.now() - t0;
    expect(elapsed).toBeLessThan(3000);

    expect(shim._sharedState.gardens).toContainEqual(
      expect.objectContaining({ id: 'garden-a', name: 'Garten-A-Edit' }),
    );

    // Sentry-Breadcrumb gesetzt
    expect(sentryMock.addBreadcrumb).toHaveBeenCalledWith(
      expect.objectContaining({ category: 'sync' }),
    );
  });

  it('LWW-Konflikt: A offline-Write vs bereits-vorhandener neuerer Server-Write → A bekommt P9011', async () => {
    const shim = createSupabaseShim({ enforceLww: true });
    shim._seed('gardens', {
      id: 'garden-a',
      name: 'Garten-B-Wins',
      updated_at: '2026-04-24T10:00:06Z',
      updated_by_user_id: 'user-b',
      owner_user_id: 'user-a',
      created_at: '2026-04-24T10:00:00Z',
      deleted_at: null,
      plz: null, klimazone: null, archetype: null,
    });

    const sentryMock = {
      addBreadcrumb: jest.fn(),
      captureException: jest.fn(),
      captureMessage: jest.fn(),
    };
    const workerA = new SyncWorker({ storage: sharedStorage, supabase: shim.client, sentry: sentryMock as any });

    const rowA: GardenRow = {
      id: 'garden-a',
      createdAt: '2026-04-24T10:00:00Z',
      updatedAt: '2026-04-24T10:00:05Z',
      updatedByUserId: 'user-a',
      deletedAt: null,
      name: 'Garten-A-Stale',
      ownerUserId: 'user-a',
    };
    await sharedStorage.writeWithOutbox('gardens', rowA, {
      entity: 'gardens', rowId: 'garden-a', operation: 'update', payload: rowA,
    });

    const events: any[] = [];
    syncEvents.on((e) => events.push(e));
    await workerA.syncAll();

    expect(events.some((e) => e.type === 'push_conflict')).toBe(true);
    expect(await sharedStorage.listOutboxEntries()).toHaveLength(0);

    // Sentry captureException mit lww_conflict tag
    expect(sentryMock.captureException).toHaveBeenCalledWith(
      expect.anything(),
      expect.objectContaining({
        tags: expect.objectContaining({
          sync_phase: 'push',
          error_kind: 'lww_conflict',
        }),
      }),
    );
  });
});
```
  </action>

  <verify>
    <automated>pnpm --filter app test -- --runInBand app/src/lib/sync/__tests__/reconnect-2user.integration.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `app/src/lib/sync/__tests__/reconnect-2user.integration.test.ts` existiert
    - Test nutzt `new SyncWorker({ storage, supabase, sentry })` (Klassen-API mit Constructor-Injection)
    - `grep -q "P9011" app/src/lib/sync/__tests__/reconnect-2user.integration.test.ts` → 0
    - `grep -q "push_conflict" app/src/lib/sync/__tests__/reconnect-2user.integration.test.ts` → 0
    - `grep -q "lww_conflict" app/src/lib/sync/__tests__/reconnect-2user.integration.test.ts` → 0
    - `grep -q "addBreadcrumb" app/src/lib/sync/SyncWorker.ts` → 0
    - Test grün, Push-Cycle < 3s
  </acceptance_criteria>

  <done>
    2-User-Reconnect-Integrationstest nutzt Constructor-Injection (klasse + sentry-mock).
    LWW-Konflikt wird korrekt als push_conflict emittiert + Outbox-Eintrag gelöscht.
    Sentry-Breadcrumbs + captureException-Tags werden auf instance-scope verifiziert.
    Test-Latenz unter 3s (well below Nyquist 60s-Budget).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SyncWorker (Instance) → Supabase | Authenticated REST/RPC calls via injected client |
| SyncWorker → storage | Local DB über injected adapter (Test-Injection möglich) |
| NetInfo / AppState Events | Gerät-internal, keine Trust-Boundary |
| syncEvents (EventChannel) | In-process, keine externen Subscribers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-04-01 | Spoofing | Fremder User könnte synthetische Outbox-Einträge mit anderen userIds injizieren | mitigate | pushGarden/pushProfile/pushVereinsregeln stempeln updated_by_user_id mit authStore.userId (Pattern 6). Server-LWW-Trigger setzt updated_by_user_id erneut aus auth.uid() — clientseitige Manipulation wird überschrieben. |
| T-3-04-02 | Tampering | Angreifer könnte Outbox-Payload manipulieren, um fremde row_id zu pushen | mitigate | RLS (Phase 02.5) verwirft UPDATE für nicht-member-Rows serverseitig (42501). Lokaler Angriff ist harmlos. |
| T-3-04-03 | Information Disclosure | Delta-Pull könnte Rows anderer Gärten pullen | accept | RLS filtert serverseitig via is_garden_member. Pull-Ergebnis enthält nur Rows, die der User sehen darf. Sentry-Breadcrumbs enthalten keine Row-Payloads. |
| T-3-04-04 | Denial of Service | Infinite Retry-Loop bei permanentem Server-Fehler | mitigate | MAX_ATTEMPTS=10 + exponential backoff mit 60s-cap. Nach 10 Versuchen permanent_failure-Event. retryOp(opId) erzwingt User-Eingriff über Plan 03-06 Detail-Screen. |
| T-3-04-05 | Information Disclosure | Sentry-Logs könnten sensitive Row-Payloads enthalten | mitigate | Breadcrumbs enthalten nur {entity, row_id, operation, attempts}. captureException extras enthalten nur IDs. |
| T-3-04-06 | Tampering | Race-Condition: push während Outbox-Write → lost update | mitigate | Instance-level pushInFlight-Boolean serialisiert parallele push()-Calls pro Worker-Instanz. SqliteAdapter nutzt EXCLUSIVE-Lock. IndexedDbAdapter serialisiert via Transaction-Queue. |
| T-3-04-07 | Denial of Service | Böser Push könnte Server überlasten bei Dauerreconnect | mitigate | Single-User-Context (D-13). 2 Devices max. Supabase-Rate-Limit reicht. AppState-Trigger feuert nur bei ECHTEM background→active. |
</threat_model>

<verification>
**Integrated Validation Checkpoints (aus 03-VALIDATION.md):**
- I-3 (LWW-Trigger rejected älteren Write): reconnect-2user.integration.test.ts Test-Case 2
- I-4 (30s-Reconnect-Propagation): reconnect-2user.integration.test.ts Test-Case 1
- SC-5 (2-User-Shared-State mit 30s-Delay): unit-level via mocks; E2E folgt in Plan 03-06 Playwright-Test

**Full plan verification command:**
```bash
pnpm --filter @spatenstich/shared build && \
pnpm --filter app tsc --noEmit && \
pnpm --filter app test -- --runInBand \
  app/src/lib/sync/__tests__/backoff.test.ts \
  app/src/lib/sync/__tests__/SyncWorker.test.ts \
  app/src/lib/sync/__tests__/SyncTriggers.test.ts \
  app/src/lib/sync/__tests__/reconnect-2user.integration.test.ts
```

Alle 4 Test-Suites grün + TS-Build exit 0 → Plan komplett.
</verification>

<success_criteria>
- [ ] `SyncWorker` ist **Klasse** (`export class SyncWorker`) mit Constructor-Injection `{ storage, supabase, sentry? }`
- [ ] Singleton-Accessor `getSyncWorker()` + `setSyncWorker()` exportiert
- [ ] Public Methoden: `push()`, `pull(entity)`, `pullAll()`, `syncAll()`, `retryOp(opId)`, `discardOp(opId)` — ALLE ab Tag 1 öffentlich
- [ ] `pullAll()` iteriert PULL_ENTITIES (5 Entities), aggregiert Results, continued bei Einzel-Fehlern
- [ ] `retryOp('not-found')` wirft typed Error `outbox_entry_not_found`
- [ ] `discardOp('not-found')` ist idempotent (kein throw)
- [ ] `discardOp(opId)` triggert `pull(entity)` für Delta-Korrektur (D-22)
- [ ] `backoff.ts` exportiert `MAX_ATTEMPTS`, `BACKOFF_BASE_MS`, `BACKOFF_CAP_MS` als Module-Level-Constants
- [ ] `events.ts` emittiert SyncEvents für UI + Sentry
- [ ] P9011 (ConflictError) löscht Outbox-Eintrag + emittiert push_conflict
- [ ] attempts >= 10 führt zu permanent_failure-Event
- [ ] pull nutzt server_now() RPC als Checkpoint
- [ ] pull-Ergebnisse via upsertRowsFromServer (keine Outbox-Erzeugung)
- [ ] `SyncTriggers.ts` abonniert NetInfo + AppState + writeDebounce via `getSyncWorker()` (Klassen-API)
- [ ] `WRITE_DEBOUNCE_MS === 500` (D-16/D-26) — mit fake-timers verifiziert (499ms kein Call, 500ms genau 1 Call)
- [ ] Root-Layout (_layout.tsx) registriert Triggers nach auth+garden ready (useRef-Guard S-6)
- [ ] Repos (gardenRepo, vereinsregelnRepo, profileRepo) rufen scheduleWriteDebounced() nach erfolgreichem writeWithOutbox
- [ ] `files_modified` enthält die 3 Repo-Pfade (gardenRepo/vereinsregelnRepo/profileRepo)
- [ ] Sentry-Breadcrumbs + captureException-Tags gesetzt (sync_phase, entity, error_kind)
- [ ] 2-User-Reconnect-Integrationstest nutzt Constructor-Injection (new SyncWorker({...}))
- [ ] Threat-Model mit 7 STRIDE-Bedrohungen dokumentiert
- [ ] Commit `docs(03): revise plan 04 — SyncWorker als Klasse, wave 4, D-16/D-26 Debounce-Test`
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/03-offline-sync-2-user-shared-state/03-04-SUMMARY.md` mit:
- SyncWorker-Klassen-API (push, pull, pullAll, syncAll, retryOp, discardOp) + Constructor-Signatur
- Singleton-Accessor-Pattern (getSyncWorker / setSyncWorker)
- SyncTriggers-Subscription-Modell inkl. D-16/D-26 500ms-Debounce-Nachweis
- Error-Classification-Tabelle (P9011, P9010, 42501, PGRST301, etc.)
- Sentry-Instrumentation-Referenz
- Repo-Patches (3 Repos: scheduleWriteDebounced-Aufrufe)
- Downstream-Empfehlungen für Plan 03-05 (PhotoUploader) + Plan 03-06 (UI nutzt worker.retryOp/discardOp/pullAll direkt)
</output>
</content>
