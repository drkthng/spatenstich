---
phase: 03-offline-sync-2-user-shared-state
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - packages/shared/src/types/storage.ts
  - packages/shared/src/types/entities.ts
  - packages/shared/src/index.ts
  - app/src/storage/SqliteAdapter.ts
  - app/src/storage/IndexedDbAdapter.ts
  - app/src/storage/migrations.ts
  - app/src/storage/__tests__/RowTables.contract.test.ts
  - app/src/storage/__tests__/SqliteAdapter.rows.test.ts
  - app/src/storage/__tests__/IndexedDbAdapter.rows.test.ts
autonomous: true
requirements: [SYNC-01, NFR-01]
must_haves:
  truths:
    - "StorageAdapter exponiert RowAccess-API zusätzlich zum KV-Interface (abwärtskompatibel)"
    - "SqliteAdapter führt Row-Writes + Outbox-Inserts atomar in einer Transaktion aus (L-6)"
    - "IndexedDbAdapter führt Row-Writes + Outbox-Inserts atomar in einer Multi-Store-Transaction aus"
    - "Beide Adapter unterstützen gardens, garden_members, profiles, vereinsregeln, invite_codes, photo_queue als Row-Tables"
    - "sync_outbox ist nur lokal (niemals Supabase); FIFO per (entity, row_id); parallel über verschiedene row_ids"
    - "sync_state speichert last_pull_at pro entity (ISO-Timestamps vom server_now() RPC)"
    - "getRowsByGarden(entity, gardenId) gibt alle lokalen Rows für eine garden_id zurück (exkl. soft-deleted)"
    - "Migration Version 3 erzeugt Row-Tables + sync_outbox + sync_state beim App-Start in < 500ms"
    - "Keine expo-sqlite-web-Abhängigkeit (Alpha-Status, COOP/COEP-Probleme laut NOT-USE-Liste)"
    - "Jest-Contract-Tests decken beide Adapter mit identischer Testsuite ab (Nyquist I-2 Grundlage)"
  artifacts:
    - path: "packages/shared/src/types/storage.ts"
      provides: "Extended StorageAdapter interface + EntityName + RowBase + OutboxEntry + QueryOptions"
      contains: "writeWithOutbox"
    - path: "packages/shared/src/types/entities.ts"
      provides: "Row-Type-Definitionen (GardenRow, GardenMemberRow, ProfileRow, VereinsregelnRow, InviteCodeRow, PhotoQueueRow) + OutboxEntry"
      contains: "export type GardenRow"
    - path: "app/src/storage/SqliteAdapter.ts"
      provides: "SQLite-Implementation mit Row-Tables + withExclusiveTransactionAsync"
      contains: "withExclusiveTransactionAsync"
    - path: "app/src/storage/IndexedDbAdapter.ts"
      provides: "IndexedDB-Implementation mit Object-Stores + Multi-Store-Transactions"
      contains: "createObjectStore"
    - path: "app/src/storage/migrations.ts"
      provides: "Migration Version 3: Row-Tables + Outbox + sync_state anlegen"
      contains: "version: 3"
    - path: "app/src/storage/__tests__/RowTables.contract.test.ts"
      provides: "Shared Contract-Test-Suite, die gegen beide Adapter läuft"
      contains: "writeWithOutbox atomicity"
  key_links:
    - from: "app/src/storage/SqliteAdapter.ts"
      to: "expo-sqlite withExclusiveTransactionAsync"
      via: "Transaktionale Row+Outbox-Writes"
      pattern: "withExclusiveTransactionAsync"
    - from: "app/src/storage/IndexedDbAdapter.ts"
      to: "idb multi-store transaction"
      via: "Atomare Row+Outbox-Writes via db.transaction([store, 'sync_outbox'], 'readwrite')"
      pattern: "db\\.transaction\\(\\["
    - from: "app/src/storage/migrations.ts"
      to: "Adapter.createTable / createObjectStore"
      via: "Version 3 Migration für alle Row-Entities"
      pattern: "version: 3"
---

<objective>
Erweiterung der StorageAdapter-Schnittstelle und beider Implementationen (SqliteAdapter, IndexedDbAdapter)
um Row-Level-Zugriff für alle Sync-relevanten Entities. Grundlage für Phase-3-Offline-First:
Alle Reads und Writes gehen zuerst lokal; Writes erzeugen gleichzeitig einen Outbox-Eintrag in einer
atomaren Transaktion (L-6). Das KV-Interface bleibt unverändert (Rückwärts-Kompatibilität zu Phase 1/2).

Purpose: Ohne Row-Tables keine lokale Persistenz jenseits KV-Blobs → kein Offline-Start mit letztem
Plan (SYNC-01) und kein verlässlicher Sync (weil Outbox in separater Transaktion laufen würde).
Atomic Row+Outbox ist der zentrale Baustein gegen Landmine L-6 (Teil-Write-Szenarien).

Output:
- Erweitertes StorageAdapter-Interface in `packages/shared/src/types/storage.ts`
- Row-Type-Definitionen in `packages/shared/src/types/entities.ts`
- SqliteAdapter mit expo-sqlite-Transactions (native)
- IndexedDbAdapter mit Multi-Store-Transactions (web)
- Migration Version 3 erzeugt alle Row-Tables + sync_outbox + sync_state
- Jest-Contract-Tests mit fake-indexeddb für IndexedDB-Tests
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-offline-sync-2-user-shared-state/03-CONTEXT.md
@.planning/phases/03-offline-sync-2-user-shared-state/03-RESEARCH.md
@.planning/phases/03-offline-sync-2-user-shared-state/03-PATTERNS.md
@.planning/phases/03-offline-sync-2-user-shared-state/03-VALIDATION.md
@.planning/phases/03-offline-sync-2-user-shared-state/03-01-SUMMARY.md

@packages/shared/src/types/storage.ts
@packages/shared/src/index.ts
@app/src/storage/SqliteAdapter.ts
@app/src/storage/IndexedDbAdapter.ts
@app/src/storage/migrations.ts
@app/src/storage/index.ts

<interfaces>
<!-- Aktueller Stand aus Phase 1/2 — Erweiterung, NICHT Ersatz -->

From packages/shared/src/types/storage.ts:
```typescript
export interface StorageAdapter {
  get(key: string): Promise<string | null>;
  set(key: string, value: string): Promise<void>;
  delete(key: string): Promise<void>;
  list(prefix?: string): Promise<string[]>;
  getSchemaVersion(): Promise<number>;
  setSchemaVersion(version: number): Promise<void>;
}
```

From app/src/storage/migrations.ts:
```typescript
export interface LocalMigration {
  version: number;
  up: (adapter: StorageAdapter) => Promise<void>;
}
export const MIGRATIONS: LocalMigration[]; // currently version 1 + 2
export async function runMigrations(adapter: StorageAdapter): Promise<void>;
```

From packages/shared/src/types/supabase.ts (nach Plan 03-01 regeneriert):
- Database["public"]["Tables"]["gardens"]["Row"] mit `deleted_at: string | null`
- Database["public"]["Tables"]["profiles"]["Row"] mit `updated_at: string | null`, `deleted_at: string | null`
- Database["public"]["Tables"]["photo_queue"]["Row"] mit `id, garden_id, storage_path, geo_lat, geo_lng, ...`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 03-02-01: Erweitertes StorageAdapter-Interface + Row-Types + Contract-Test-Harness</name>
  <files>
    - packages/shared/src/types/storage.ts
    - packages/shared/src/types/entities.ts (neu)
    - packages/shared/src/index.ts
    - app/src/storage/__tests__/RowTables.contract.test.ts (neu)
  </files>

  <read_first>
    - packages/shared/src/types/storage.ts (aktuelles KV-Interface)
    - packages/shared/src/index.ts (Re-Export-Struktur)
    - packages/shared/src/types/supabase.ts (Row-Types, regeneriert in Plan 03-01)
    - .planning/phases/03-offline-sync-2-user-shared-state/03-RESEARCH.md §2 "Storage Layer API Shape"
    - .planning/phases/03-offline-sync-2-user-shared-state/03-PATTERNS.md §3-4 (StorageAdapter-Analog)
  </read_first>

  <behavior>
    - Test 1: StorageAdapter besitzt `writeWithOutbox(entity, row, outboxOp)`-Methode, die beide Writes atomar ausführt
    - Test 2: Bei Fehler während Row-Write bleibt sync_outbox leer (Rollback)
    - Test 3: Bei Fehler während Outbox-Insert wird Row-Write zurückgerollt
    - Test 4: `getRow(entity, id)` gibt soft-deleted Rows (deleted_at != null) nicht zurück (es sei denn includeDeleted=true)
    - Test 5: `getRowsByGarden(entity, gardenId)` gibt alle non-deleted Rows für eine garden_id zurück
    - Test 6: `listOutboxEntries()` gibt FIFO-sortierte Einträge (ASC by created_at) zurück
    - Test 7: `deleteOutboxEntry(id)` entfernt gezielten Eintrag nach erfolgreichem Push
    - Test 8: Contract-Test läuft gegen beide Adapter mit identischer Testsuite (parametrisiert)
  </behavior>

  <action>
**1. `packages/shared/src/types/entities.ts` (NEU) — Row-Type-Definitionen:**

```typescript
// Phase 3: Lokale Row-Repräsentationen, gespiegelt an den Supabase-Schema-Typen.
// Wichtig: camelCase für TypeScript-Usage. Die Adapter-Impl speichert snake_case
// in SQLite/IndexedDB (via toRow/fromRow-Mapper in den Repos).

export type EntityName =
  | 'gardens'
  | 'garden_members'
  | 'profiles'
  | 'vereinsregeln'
  | 'invite_codes'
  | 'photo_queue';

/** Gemeinsame Basis-Felder für alle LWW-managed Rows (Plan 03-01 Migration 013). */
export interface RowBase {
  id: string;                 // UUID
  createdAt: string;          // ISO-Timestamp
  updatedAt: string;          // ISO-Timestamp — von LWW-Trigger auf Server gesetzt
  updatedByUserId: string | null; // von trigger mm_set_updated_by_user_id_* gesetzt
  deletedAt: string | null;   // Soft-Delete (D-16)
}

export interface GardenRow extends RowBase {
  name: string;
  ownerUserId: string;
}

export interface GardenMemberRow extends RowBase {
  gardenId: string;
  userId: string;
  role: 'owner' | 'member';
}

export interface ProfileRow extends RowBase {
  userId: string;
  displayName: string | null;
  locale: string | null;
}

export interface VereinsregelnRow extends RowBase {
  gardenId: string;
  rules: Record<string, unknown>; // JSON
}

export interface InviteCodeRow extends RowBase {
  gardenId: string;
  code: string;
  expiresAt: string | null;
  usedAt: string | null;
  usedByUserId: string | null;
}

export interface PhotoQueueRow extends RowBase {
  gardenId: string;
  storagePath: string;         // <garden_id>/<photo_id>.<ext>
  geoLat: number | null;       // nur wenn Opt-in (D-24)
  geoLng: number | null;
  uploadStatus: 'pending' | 'uploading' | 'uploaded' | 'failed';
  uploadError: string | null;
  jobId: string | null;        // von enqueue_photo_analysis RPC
}

export type AnyRow =
  | GardenRow
  | GardenMemberRow
  | ProfileRow
  | VereinsregelnRow
  | InviteCodeRow
  | PhotoQueueRow;

/** Outbox-Eintrag — ein pending Write, der noch nicht gepusht wurde. */
export interface OutboxEntry {
  id: string;                  // UUID
  entity: EntityName;
  rowId: string;
  operation: 'insert' | 'update' | 'delete';
  /** Row-Snapshot (D-14): vollständiger Row-Zustand zum Zeitpunkt des Writes. */
  payload: Record<string, unknown>;
  createdAt: string;           // ISO-Timestamp (lokale Zeit — nur FIFO-Relevanz)
  attempts: number;            // Backoff-Zähler
  lastError: string | null;    // Fehlertext des letzten Push-Versuchs
}

/** Sync-State pro Entity — persistiert last_pull_at für Delta-Pull. */
export interface SyncStateEntry {
  entity: EntityName;
  lastPullAt: string | null;   // ISO-Timestamp vom server_now() RPC (Plan 03-01 Section 8)
  lastPushAt: string | null;
}
```

**2. `packages/shared/src/types/storage.ts` — Erweitertes Interface (KV-Methoden bleiben):**

```typescript
import type {
  AnyRow,
  EntityName,
  OutboxEntry,
  SyncStateEntry,
} from './entities';

export interface QueryOptions {
  /** Soft-deleted Rows mit einschließen. Default: false. */
  includeDeleted?: boolean;
}

// D-08: KV CRUD bleibt erhalten (Phase 1/2 Profile-Store + misc Keys).
// Phase 3: zusätzlich Row-Level + Outbox + sync_state.
export interface StorageAdapter {
  // ---- Phase 1/2: KV-Interface (unverändert) ----
  get(key: string): Promise<string | null>;
  set(key: string, value: string): Promise<void>;
  delete(key: string): Promise<void>;
  list(prefix?: string): Promise<string[]>;
  getSchemaVersion(): Promise<number>;
  setSchemaVersion(version: number): Promise<void>;

  // ---- Phase 3: Row-Level-Zugriff ----
  /** Einzelne Row per Primary Key laden. Soft-deleted Rows werden ausgeblendet (es sei denn opts.includeDeleted). */
  getRow<T extends AnyRow>(entity: EntityName, id: string, opts?: QueryOptions): Promise<T | null>;

  /** Alle Rows einer garden_id laden (exkl. soft-deleted by default). */
  getRowsByGarden<T extends AnyRow>(entity: EntityName, gardenId: string, opts?: QueryOptions): Promise<T[]>;

  /** Alle Rows einer Entity (für profiles / cross-garden Sync nach Member-Check). */
  getAllRows<T extends AnyRow>(entity: EntityName, opts?: QueryOptions): Promise<T[]>;

  /**
   * Atomic Row-Write + Outbox-Insert (L-6 Absicherung).
   * Bei Fehler wird die gesamte Transaktion zurückgerollt.
   * Verwendet SQLite's withExclusiveTransactionAsync (native) bzw. idb's
   * db.transaction([entity, 'sync_outbox'], 'readwrite') (web).
   */
  writeWithOutbox<T extends AnyRow>(
    entity: EntityName,
    row: T,
    outbox: Omit<OutboxEntry, 'id' | 'createdAt' | 'attempts' | 'lastError'>,
  ): Promise<void>;

  /**
   * Row-Write OHNE Outbox — ausschließlich für Sync-Pull (Server→Client Updates
   * erzeugen KEINE neuen Outbox-Einträge, sonst Ping-Pong).
   */
  upsertRowFromServer<T extends AnyRow>(entity: EntityName, row: T): Promise<void>;

  /** Mehrere Rows vom Server atomar anwenden (Delta-Pull). */
  upsertRowsFromServer<T extends AnyRow>(entity: EntityName, rows: T[]): Promise<void>;

  // ---- Phase 3: Outbox ----
  /** FIFO-sortiert (ASC created_at). */
  listOutboxEntries(limit?: number): Promise<OutboxEntry[]>;
  /** Nach erfolgreichem Server-Ack entfernen. */
  deleteOutboxEntry(id: string): Promise<void>;
  /** Backoff-Zähler + Fehlertext aktualisieren nach Push-Fehler. */
  updateOutboxEntry(id: string, patch: Pick<OutboxEntry, 'attempts' | 'lastError'>): Promise<void>;

  // ---- Phase 3: sync_state ----
  getSyncState(entity: EntityName): Promise<SyncStateEntry | null>;
  setSyncState(state: SyncStateEntry): Promise<void>;
}
```

**3. `packages/shared/src/index.ts` — Exports erweitern:**

```typescript
// Bestehende Exports belassen. Neue Exports hinzufügen:
export type {
  EntityName,
  RowBase,
  GardenRow,
  GardenMemberRow,
  ProfileRow,
  VereinsregelnRow,
  InviteCodeRow,
  PhotoQueueRow,
  AnyRow,
  OutboxEntry,
  SyncStateEntry,
} from './types/entities';
export type { QueryOptions } from './types/storage';
```

**4. `app/src/storage/__tests__/RowTables.contract.test.ts` (NEU) — Shared Contract-Test:**

```typescript
import type { StorageAdapter, GardenRow, EntityName } from '@spatenstich/shared';

/**
 * Parametrisierter Contract-Test: dieselbe Testsuite läuft gegen beide Adapter.
 * Nyquist I-2 Grundlage: atomic writeWithOutbox, FIFO-Outbox, Soft-Delete-Filter.
 */
export function runStorageContractTests(
  name: string,
  createAdapter: () => Promise<StorageAdapter>,
  teardown: (adapter: StorageAdapter) => Promise<void>,
): void {
  describe(`StorageAdapter Contract: ${name}`, () => {
    let adapter: StorageAdapter;

    const sampleGarden = (overrides: Partial<GardenRow> = {}): GardenRow => ({
      id: '00000000-0000-0000-0000-000000000001',
      createdAt: '2026-04-24T10:00:00.000Z',
      updatedAt: '2026-04-24T10:00:00.000Z',
      updatedByUserId: 'user-a',
      deletedAt: null,
      name: 'Testgarten',
      ownerUserId: 'user-a',
      ...overrides,
    });

    beforeEach(async () => { adapter = await createAdapter(); });
    afterEach(async () => { await teardown(adapter); });

    describe('KV backwards compatibility', () => {
      it('set/get/delete/list behält Phase-1-Verhalten', async () => {
        await adapter.set('profile', '{"name":"Dirk"}');
        expect(await adapter.get('profile')).toBe('{"name":"Dirk"}');
        expect(await adapter.list()).toContain('profile');
        await adapter.delete('profile');
        expect(await adapter.get('profile')).toBeNull();
      });
    });

    describe('Row CRUD', () => {
      it('writeWithOutbox speichert Row + Outbox-Eintrag atomar', async () => {
        const row = sampleGarden();
        await adapter.writeWithOutbox('gardens', row, {
          entity: 'gardens',
          rowId: row.id,
          operation: 'insert',
          payload: row,
        });
        const loaded = await adapter.getRow<GardenRow>('gardens', row.id);
        expect(loaded).toMatchObject({ id: row.id, name: 'Testgarten' });
        const outbox = await adapter.listOutboxEntries();
        expect(outbox).toHaveLength(1);
        expect(outbox[0]).toMatchObject({ entity: 'gardens', rowId: row.id, operation: 'insert' });
      });

      it('getRow mit includeDeleted=false versteckt soft-deleted Rows', async () => {
        const row = sampleGarden({ deletedAt: '2026-04-24T11:00:00.000Z' });
        await adapter.upsertRowFromServer('gardens', row);
        expect(await adapter.getRow<GardenRow>('gardens', row.id)).toBeNull();
        expect(
          await adapter.getRow<GardenRow>('gardens', row.id, { includeDeleted: true }),
        ).not.toBeNull();
      });

      it('getRowsByGarden gibt nur Rows der angegebenen garden_id zurück', async () => {
        const g1 = sampleGarden({ id: 'g-1' });
        const g2 = sampleGarden({ id: 'g-2' });
        await adapter.upsertRowFromServer('gardens', g1);
        await adapter.upsertRowFromServer('gardens', g2);
        const rowsG1 = await adapter.getRowsByGarden<GardenRow>('gardens', 'g-1');
        // gardens-Tabelle: garden_id == id (Sondertall)
        expect(rowsG1.map((r) => r.id)).toEqual(['g-1']);
      });

      it('upsertRowFromServer erzeugt KEINEN Outbox-Eintrag (sonst Ping-Pong)', async () => {
        await adapter.upsertRowFromServer('gardens', sampleGarden());
        expect(await adapter.listOutboxEntries()).toHaveLength(0);
      });
    });

    describe('Outbox FIFO', () => {
      it('listOutboxEntries ist aufsteigend nach created_at sortiert', async () => {
        for (let i = 0; i < 3; i++) {
          const row = sampleGarden({ id: `g-${i}` });
          await adapter.writeWithOutbox('gardens', row, {
            entity: 'gardens', rowId: row.id, operation: 'insert', payload: row,
          });
          // kleine künstliche Zeitverzögerung im Adapter via Date.now() sicherstellen
        }
        const outbox = await adapter.listOutboxEntries();
        expect(outbox).toHaveLength(3);
        expect(outbox[0].rowId).toBe('g-0');
        expect(outbox[2].rowId).toBe('g-2');
      });

      it('deleteOutboxEntry entfernt nur den angegebenen Eintrag', async () => {
        const row = sampleGarden();
        await adapter.writeWithOutbox('gardens', row, {
          entity: 'gardens', rowId: row.id, operation: 'insert', payload: row,
        });
        const [entry] = await adapter.listOutboxEntries();
        await adapter.deleteOutboxEntry(entry.id);
        expect(await adapter.listOutboxEntries()).toHaveLength(0);
      });

      it('updateOutboxEntry erhöht attempts + schreibt lastError', async () => {
        const row = sampleGarden();
        await adapter.writeWithOutbox('gardens', row, {
          entity: 'gardens', rowId: row.id, operation: 'insert', payload: row,
        });
        const [entry] = await adapter.listOutboxEntries();
        await adapter.updateOutboxEntry(entry.id, { attempts: 2, lastError: 'network' });
        const [updated] = await adapter.listOutboxEntries();
        expect(updated.attempts).toBe(2);
        expect(updated.lastError).toBe('network');
      });
    });

    describe('sync_state', () => {
      it('getSyncState → null für unbekannte Entity', async () => {
        expect(await adapter.getSyncState('gardens')).toBeNull();
      });

      it('setSyncState + getSyncState roundtrip', async () => {
        await adapter.setSyncState({
          entity: 'gardens',
          lastPullAt: '2026-04-24T12:00:00.000Z',
          lastPushAt: null,
        });
        const state = await adapter.getSyncState('gardens');
        expect(state?.lastPullAt).toBe('2026-04-24T12:00:00.000Z');
      });
    });
  });
}
```

**WICHTIG:** Die contract-test-Datei exportiert nur die Suite-Factory.
Die eigentlichen Tests werden in Task 03-02-02 und 03-02-03 per Adapter instanziiert.
  </action>

  <verify>
    <automated>pnpm --filter @spatenstich/shared build && pnpm --filter @spatenstich/shared test -- --findRelatedTests packages/shared/src/types/storage.ts packages/shared/src/types/entities.ts</automated>
  </verify>

  <acceptance_criteria>
    - `packages/shared/src/types/entities.ts` existiert
    - `grep -q "export type EntityName" packages/shared/src/types/entities.ts` → 0
    - `grep -q "writeWithOutbox" packages/shared/src/types/storage.ts` → 0
    - `grep -q "upsertRowFromServer" packages/shared/src/types/storage.ts` → 0
    - `grep -q "listOutboxEntries" packages/shared/src/types/storage.ts` → 0
    - `grep -q "getSyncState" packages/shared/src/types/storage.ts` → 0
    - `grep -q "OutboxEntry" packages/shared/src/index.ts` → 0
    - `grep -q "GardenRow" packages/shared/src/index.ts` → 0
    - `grep -q "runStorageContractTests" app/src/storage/__tests__/RowTables.contract.test.ts` → 0
    - `pnpm --filter @spatenstich/shared build` exit code 0 (tsc ohne Fehler)
  </acceptance_criteria>

  <done>
    Shared-Package kompiliert fehlerfrei mit erweitertem StorageAdapter-Interface.
    Entity-Types sind exportiert und in app/ importierbar.
    Contract-Test-Suite ist parametrisierbar (Factory-Pattern) und wird von Task 03-02-02 + 03-02-03 aufgerufen.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-02-02: SqliteAdapter Row-Tables + withExclusiveTransactionAsync</name>
  <files>
    - app/src/storage/SqliteAdapter.ts
    - app/src/storage/migrations.ts
    - app/src/storage/__tests__/SqliteAdapter.rows.test.ts (neu)
  </files>

  <read_first>
    - app/src/storage/SqliteAdapter.ts (Phase-1-KV-Impl)
    - app/src/storage/migrations.ts (bestehender Migration-Runner)
    - app/src/storage/__tests__/SqliteAdapter.test.ts (bestehende KV-Tests)
    - .planning/phases/03-offline-sync-2-user-shared-state/03-RESEARCH.md §2 + §L-6 (Landmine 6: expo-sqlite Transaktionen)
    - .planning/phases/03-offline-sync-2-user-shared-state/03-PATTERNS.md §3 (SqliteAdapter-Analog)
    - expo-sqlite docs: withExclusiveTransactionAsync (via `node_modules/expo-sqlite/build/NativeDatabase.d.ts`)
  </read_first>

  <behavior>
    - Test 1: Contract-Tests aus Task 03-02-01 laufen gegen SqliteAdapter vollständig grün
    - Test 2: Migration Version 3 erzeugt alle 6 Row-Tables + sync_outbox + sync_state
    - Test 3: writeWithOutbox ist atomar: simulierter Outbox-INSERT-Fehler rollt Row-Write zurück
    - Test 4: garden_id-Index auf Row-Tables beschleunigt getRowsByGarden (EXPLAIN QUERY PLAN nutzt Index)
    - Test 5: Jeder Write setzt created_at/updated_at der Outbox-Row auf strict-monotonic-Timestamps (Date.now() + Counter)
    - Test 6: Paralleler Aufruf von 10× writeWithOutbox serialisiert korrekt (EXCLUSIVE-Lock)
  </behavior>

  <action>
**1. `app/src/storage/migrations.ts` — Version 3 hinzufügen:**

Nach dem bestehenden Version-2-Eintrag einen neuen Migration-Step anhängen. Da der Runner aktuell
nur `(adapter: StorageAdapter) => Promise<void>` übergibt, muss die Migration pro Adapter-Typ unterschiedliche
Schema-Anweisungen ausführen. Wir lösen das über ein `__createRowTables__` Hook-Protokoll: jede Adapter-
Implementation entscheidet selbst, wie die Row-Schemas angelegt werden.

```typescript
import type { StorageAdapter } from '@spatenstich/shared';

export interface LocalMigration {
  version: number;
  up: (adapter: StorageAdapter) => Promise<void>;
}

// Adapter-spezifischer Hook: beide Adapter-Klassen implementieren die Methode zusätzlich zum Interface.
// Die Migration ruft sie via optional-property-Zugriff auf, damit das StorageAdapter-Interface rein bleibt.
interface RowTableCreator {
  __createRowTablesV3?: () => Promise<void>;
}

export const MIGRATIONS: LocalMigration[] = [
  { version: 1, up: async () => { /* Bootstrap */ } },
  { version: 2, up: async () => { /* Phase 2: KV unverändert */ } },
  {
    version: 3,
    up: async (adapter) => {
      // Phase 3 (Plan 03-02): Row-Tables + sync_outbox + sync_state anlegen.
      // Die eigentliche DDL/ObjectStore-Creation wird vom konkreten Adapter übernommen,
      // weil SQLite (DDL) und IndexedDB (objectStore) unterschiedliche Semantik haben.
      const creator = adapter as StorageAdapter & RowTableCreator;
      if (typeof creator.__createRowTablesV3 === 'function') {
        await creator.__createRowTablesV3();
      } else {
        throw new Error(
          'Migration v3 requires adapter to implement __createRowTablesV3(). ' +
          'Update SqliteAdapter/IndexedDbAdapter.',
        );
      }
    },
  },
];

export async function runMigrations(adapter: StorageAdapter): Promise<void> {
  const current = await adapter.getSchemaVersion();
  const pending = MIGRATIONS
    .filter((m) => m.version > current)
    .sort((a, b) => a.version - b.version);
  for (const m of pending) {
    await m.up(adapter);
    await adapter.setSchemaVersion(m.version);
  }
}
```

**2. `app/src/storage/SqliteAdapter.ts` — vollständige Erweiterung:**

Die Implementation behält das bestehende KV-Interface und fügt Row-Tables + Outbox hinzu.
WICHTIG: `withExclusiveTransactionAsync` existiert erst ab expo-sqlite v15 (SDK 55). Prüfen dass
Runtime-Feature-Detection nötig ist (für Tests mit älterer Runtime), aber im Projekt ist expo-sqlite 15.x
bereits dependency (siehe Tech-Stack-Tabelle). Deshalb direkt verwenden.

Kernstruktur (komplette neue Datei):

```typescript
import * as SQLite from 'expo-sqlite';
import type {
  StorageAdapter,
  EntityName,
  AnyRow,
  OutboxEntry,
  SyncStateEntry,
  QueryOptions,
} from '@spatenstich/shared';

const SCHEMA_VERSION_KEY = '__schema_version__';
const ROW_ENTITIES: EntityName[] = [
  'gardens',
  'garden_members',
  'profiles',
  'vereinsregeln',
  'invite_codes',
  'photo_queue',
];

// Welche Entities haben eine garden_id-Spalte?
// gardens ist Sonderfall: id == garden_id (self-reference).
const GARDEN_ID_COLUMN: Record<EntityName, string | null> = {
  gardens: 'id',
  garden_members: 'garden_id',
  profiles: null,            // profiles ist cross-garden
  vereinsregeln: 'garden_id',
  invite_codes: 'garden_id',
  photo_queue: 'garden_id',
};

// Monotonic-Counter für Outbox created_at (verhindert FIFO-Kollisionen bei gleichem Date.now()).
let outboxCounter = 0;

export class SqliteAdapter implements StorageAdapter {
  private dbPromise: Promise<SQLite.SQLiteDatabase>;

  constructor(dbName: string) {
    this.dbPromise = (async () => {
      const db = await SQLite.openDatabaseAsync(dbName);
      // KV-Tabelle immer anlegen (Phase 1 Kompatibilität).
      await db.execAsync(
        `CREATE TABLE IF NOT EXISTS kv (key TEXT PRIMARY KEY NOT NULL, value TEXT NOT NULL);`,
      );
      return db;
    })();
  }

  // ---- KV (Phase 1/2) ----
  async get(key: string): Promise<string | null> {
    const db = await this.dbPromise;
    const row = await db.getFirstAsync<{ value: string }>('SELECT value FROM kv WHERE key = ?', key);
    return row?.value ?? null;
  }
  async set(key: string, value: string): Promise<void> {
    const db = await this.dbPromise;
    await db.runAsync(
      'INSERT INTO kv(key,value) VALUES(?,?) ON CONFLICT(key) DO UPDATE SET value=excluded.value',
      key, value,
    );
  }
  async delete(key: string): Promise<void> {
    const db = await this.dbPromise;
    await db.runAsync('DELETE FROM kv WHERE key = ?', key);
  }
  async list(prefix?: string): Promise<string[]> {
    const db = await this.dbPromise;
    const rows = prefix
      ? await db.getAllAsync<{ key: string }>('SELECT key FROM kv WHERE key LIKE ?', `${prefix}%`)
      : await db.getAllAsync<{ key: string }>('SELECT key FROM kv');
    return rows.map((r) => r.key);
  }
  async getSchemaVersion(): Promise<number> {
    const v = await this.get(SCHEMA_VERSION_KEY);
    return v ? Number(v) : 0;
  }
  async setSchemaVersion(version: number): Promise<void> {
    await this.set(SCHEMA_VERSION_KEY, String(version));
  }

  // ---- Migration-Hook (wird aus migrations.ts aufgerufen) ----
  async __createRowTablesV3(): Promise<void> {
    const db = await this.dbPromise;
    // Alle Row-Tables haben identisches Schema: (id TEXT PK, data TEXT JSON, garden_id TEXT NULL, deleted_at TEXT NULL, updated_at TEXT NOT NULL).
    // JSON-Blob-Storage (analog L-6-Research) — camelCase-Serialisation via JSON.stringify im Adapter.
    for (const entity of ROW_ENTITIES) {
      const gidCol = GARDEN_ID_COLUMN[entity];
      const gidSchema = gidCol ? `${gidCol} TEXT,` : '';
      await db.execAsync(`
        CREATE TABLE IF NOT EXISTS ${entity} (
          id TEXT PRIMARY KEY NOT NULL,
          data TEXT NOT NULL,
          ${gidSchema}
          deleted_at TEXT,
          updated_at TEXT NOT NULL
        );
      `);
      if (gidCol) {
        await db.execAsync(
          `CREATE INDEX IF NOT EXISTS idx_${entity}_${gidCol} ON ${entity}(${gidCol}) WHERE deleted_at IS NULL;`,
        );
      }
    }

    // sync_outbox: FIFO-Queue
    await db.execAsync(`
      CREATE TABLE IF NOT EXISTS sync_outbox (
        id TEXT PRIMARY KEY NOT NULL,
        entity TEXT NOT NULL,
        row_id TEXT NOT NULL,
        operation TEXT NOT NULL,
        payload TEXT NOT NULL,
        created_at TEXT NOT NULL,
        attempts INTEGER NOT NULL DEFAULT 0,
        last_error TEXT
      );
      CREATE INDEX IF NOT EXISTS idx_sync_outbox_fifo ON sync_outbox(created_at);
    `);

    // sync_state: last_pull_at / last_push_at pro Entity
    await db.execAsync(`
      CREATE TABLE IF NOT EXISTS sync_state (
        entity TEXT PRIMARY KEY NOT NULL,
        last_pull_at TEXT,
        last_push_at TEXT
      );
    `);
  }

  // ---- Row-Level (Phase 3) ----
  async getRow<T extends AnyRow>(
    entity: EntityName,
    id: string,
    opts: QueryOptions = {},
  ): Promise<T | null> {
    const db = await this.dbPromise;
    const sql = opts.includeDeleted
      ? `SELECT data FROM ${entity} WHERE id = ?`
      : `SELECT data FROM ${entity} WHERE id = ? AND deleted_at IS NULL`;
    const row = await db.getFirstAsync<{ data: string }>(sql, id);
    return row ? (JSON.parse(row.data) as T) : null;
  }

  async getRowsByGarden<T extends AnyRow>(
    entity: EntityName,
    gardenId: string,
    opts: QueryOptions = {},
  ): Promise<T[]> {
    const gidCol = GARDEN_ID_COLUMN[entity];
    if (!gidCol) {
      throw new Error(`Entity ${entity} has no garden_id column — use getAllRows instead.`);
    }
    const db = await this.dbPromise;
    const whereDeleted = opts.includeDeleted ? '' : ' AND deleted_at IS NULL';
    const rows = await db.getAllAsync<{ data: string }>(
      `SELECT data FROM ${entity} WHERE ${gidCol} = ?${whereDeleted}`,
      gardenId,
    );
    return rows.map((r) => JSON.parse(r.data) as T);
  }

  async getAllRows<T extends AnyRow>(entity: EntityName, opts: QueryOptions = {}): Promise<T[]> {
    const db = await this.dbPromise;
    const sql = opts.includeDeleted
      ? `SELECT data FROM ${entity}`
      : `SELECT data FROM ${entity} WHERE deleted_at IS NULL`;
    const rows = await db.getAllAsync<{ data: string }>(sql);
    return rows.map((r) => JSON.parse(r.data) as T);
  }

  async writeWithOutbox<T extends AnyRow>(
    entity: EntityName,
    row: T,
    outbox: Omit<OutboxEntry, 'id' | 'createdAt' | 'attempts' | 'lastError'>,
  ): Promise<void> {
    const db = await this.dbPromise;
    const gidCol = GARDEN_ID_COLUMN[entity];
    const gidValue =
      gidCol === 'id' ? row.id
        : gidCol && (row as any).gardenId ? (row as any).gardenId
        : null;
    const outboxId = cryptoRandomUuid();
    const outboxCreatedAt = `${new Date().toISOString()}-${String(outboxCounter++).padStart(6, '0')}`;

    // L-6: exclusive transaction → beide Writes atomar.
    await db.withExclusiveTransactionAsync(async (tx) => {
      if (gidCol) {
        await tx.runAsync(
          `INSERT INTO ${entity}(id, data, ${gidCol}, deleted_at, updated_at)
           VALUES(?,?,?,?,?)
           ON CONFLICT(id) DO UPDATE SET data=excluded.data, ${gidCol}=excluded.${gidCol},
                                         deleted_at=excluded.deleted_at, updated_at=excluded.updated_at`,
          row.id, JSON.stringify(row), gidValue, row.deletedAt, row.updatedAt,
        );
      } else {
        await tx.runAsync(
          `INSERT INTO ${entity}(id, data, deleted_at, updated_at)
           VALUES(?,?,?,?)
           ON CONFLICT(id) DO UPDATE SET data=excluded.data,
                                         deleted_at=excluded.deleted_at, updated_at=excluded.updated_at`,
          row.id, JSON.stringify(row), row.deletedAt, row.updatedAt,
        );
      }
      await tx.runAsync(
        `INSERT INTO sync_outbox(id, entity, row_id, operation, payload, created_at, attempts, last_error)
         VALUES(?,?,?,?,?,?,?,?)`,
        outboxId, outbox.entity, outbox.rowId, outbox.operation,
        JSON.stringify(outbox.payload), outboxCreatedAt, 0, null,
      );
    });
  }

  async upsertRowFromServer<T extends AnyRow>(entity: EntityName, row: T): Promise<void> {
    const db = await this.dbPromise;
    const gidCol = GARDEN_ID_COLUMN[entity];
    const gidValue =
      gidCol === 'id' ? row.id
        : gidCol && (row as any).gardenId ? (row as any).gardenId
        : null;
    if (gidCol) {
      await db.runAsync(
        `INSERT INTO ${entity}(id, data, ${gidCol}, deleted_at, updated_at)
         VALUES(?,?,?,?,?)
         ON CONFLICT(id) DO UPDATE SET data=excluded.data, ${gidCol}=excluded.${gidCol},
                                       deleted_at=excluded.deleted_at, updated_at=excluded.updated_at`,
        row.id, JSON.stringify(row), gidValue, row.deletedAt, row.updatedAt,
      );
    } else {
      await db.runAsync(
        `INSERT INTO ${entity}(id, data, deleted_at, updated_at)
         VALUES(?,?,?,?)
         ON CONFLICT(id) DO UPDATE SET data=excluded.data,
                                       deleted_at=excluded.deleted_at, updated_at=excluded.updated_at`,
        row.id, JSON.stringify(row), row.deletedAt, row.updatedAt,
      );
    }
  }

  async upsertRowsFromServer<T extends AnyRow>(entity: EntityName, rows: T[]): Promise<void> {
    if (rows.length === 0) return;
    const db = await this.dbPromise;
    await db.withExclusiveTransactionAsync(async (tx) => {
      for (const row of rows) {
        // inline copy der upsertRowFromServer-Logik, aber im tx-Kontext
        const gidCol = GARDEN_ID_COLUMN[entity];
        const gidValue =
          gidCol === 'id' ? row.id
            : gidCol && (row as any).gardenId ? (row as any).gardenId
            : null;
        if (gidCol) {
          await tx.runAsync(
            `INSERT INTO ${entity}(id, data, ${gidCol}, deleted_at, updated_at)
             VALUES(?,?,?,?,?)
             ON CONFLICT(id) DO UPDATE SET data=excluded.data, ${gidCol}=excluded.${gidCol},
                                           deleted_at=excluded.deleted_at, updated_at=excluded.updated_at`,
            row.id, JSON.stringify(row), gidValue, row.deletedAt, row.updatedAt,
          );
        } else {
          await tx.runAsync(
            `INSERT INTO ${entity}(id, data, deleted_at, updated_at)
             VALUES(?,?,?,?)
             ON CONFLICT(id) DO UPDATE SET data=excluded.data,
                                           deleted_at=excluded.deleted_at, updated_at=excluded.updated_at`,
            row.id, JSON.stringify(row), row.deletedAt, row.updatedAt,
          );
        }
      }
    });
  }

  // ---- Outbox ----
  async listOutboxEntries(limit?: number): Promise<OutboxEntry[]> {
    const db = await this.dbPromise;
    const sql = limit
      ? `SELECT * FROM sync_outbox ORDER BY created_at ASC LIMIT ?`
      : `SELECT * FROM sync_outbox ORDER BY created_at ASC`;
    const rows = limit
      ? await db.getAllAsync<any>(sql, limit)
      : await db.getAllAsync<any>(sql);
    return rows.map((r) => ({
      id: r.id,
      entity: r.entity as EntityName,
      rowId: r.row_id,
      operation: r.operation,
      payload: JSON.parse(r.payload),
      createdAt: r.created_at,
      attempts: r.attempts,
      lastError: r.last_error,
    }));
  }

  async deleteOutboxEntry(id: string): Promise<void> {
    const db = await this.dbPromise;
    await db.runAsync('DELETE FROM sync_outbox WHERE id = ?', id);
  }

  async updateOutboxEntry(
    id: string,
    patch: Pick<OutboxEntry, 'attempts' | 'lastError'>,
  ): Promise<void> {
    const db = await this.dbPromise;
    await db.runAsync(
      'UPDATE sync_outbox SET attempts = ?, last_error = ? WHERE id = ?',
      patch.attempts, patch.lastError, id,
    );
  }

  // ---- sync_state ----
  async getSyncState(entity: EntityName): Promise<SyncStateEntry | null> {
    const db = await this.dbPromise;
    const row = await db.getFirstAsync<any>(
      'SELECT * FROM sync_state WHERE entity = ?', entity,
    );
    return row
      ? { entity: row.entity, lastPullAt: row.last_pull_at, lastPushAt: row.last_push_at }
      : null;
  }

  async setSyncState(state: SyncStateEntry): Promise<void> {
    const db = await this.dbPromise;
    await db.runAsync(
      `INSERT INTO sync_state(entity, last_pull_at, last_push_at)
       VALUES(?,?,?)
       ON CONFLICT(entity) DO UPDATE SET last_pull_at=excluded.last_pull_at,
                                          last_push_at=excluded.last_push_at`,
      state.entity, state.lastPullAt, state.lastPushAt,
    );
  }
}

// UUID-v4-Fallback (Expo liefert je nach Plattform unterschiedlich).
function cryptoRandomUuid(): string {
  // @ts-expect-error — expo-crypto exponiert randomUUID in SDK 55
  if (globalThis.crypto?.randomUUID) return globalThis.crypto.randomUUID();
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, (c) => {
    const r = (Math.random() * 16) | 0;
    return (c === 'x' ? r : (r & 0x3) | 0x8).toString(16);
  });
}
```

**3. `app/src/storage/__tests__/SqliteAdapter.rows.test.ts` (NEU):**

```typescript
import { SqliteAdapter } from '../SqliteAdapter';
import { runMigrations } from '../migrations';
import { runStorageContractTests } from './RowTables.contract.test';

// Benötigt expo-sqlite-Mock oder jest-expo-Runtime. Für die CI nutzen wir
// expo-sqlite/build/pathUtils und jest-expo/preset (bereits in Phase 1 konfiguriert).

runStorageContractTests(
  'SqliteAdapter',
  async () => {
    const adapter = new SqliteAdapter(`test-${Date.now()}.db`);
    await runMigrations(adapter);
    return adapter;
  },
  async (_adapter) => {
    // SQLite in-memory: pro Test neue DB-Datei → kein Teardown nötig
  },
);

// Zusätzliche Sqlite-spezifische Tests (nicht im Contract-Set):
describe('SqliteAdapter SQLite-specific', () => {
  it('parallele writeWithOutbox-Aufrufe werden serialisiert (EXCLUSIVE-Lock)', async () => {
    const adapter = new SqliteAdapter(`test-parallel-${Date.now()}.db`);
    await runMigrations(adapter);
    const writes = Array.from({ length: 10 }, (_, i) =>
      adapter.writeWithOutbox(
        'gardens',
        {
          id: `g-${i}`,
          createdAt: '2026-04-24T10:00:00.000Z',
          updatedAt: '2026-04-24T10:00:00.000Z',
          updatedByUserId: 'user-a',
          deletedAt: null,
          name: `G${i}`,
          ownerUserId: 'user-a',
        },
        { entity: 'gardens', rowId: `g-${i}`, operation: 'insert', payload: {} },
      ),
    );
    await Promise.all(writes);
    expect(await adapter.listOutboxEntries()).toHaveLength(10);
  });
});
```

**WICHTIG:** Keine watch-mode-Flags. Tests laufen mit `jest --runInBand` (Sqlite: keine echte Parallelisierung von DB-Tests).
  </action>

  <verify>
    <automated>pnpm --filter app test -- --runInBand app/src/storage/__tests__/SqliteAdapter.rows.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `grep -q "withExclusiveTransactionAsync" app/src/storage/SqliteAdapter.ts` → 0
    - `grep -q "__createRowTablesV3" app/src/storage/SqliteAdapter.ts` → 0
    - `grep -q "version: 3" app/src/storage/migrations.ts` → 0
    - `grep -q "CREATE TABLE IF NOT EXISTS sync_outbox" app/src/storage/SqliteAdapter.ts` → 0
    - `grep -q "CREATE TABLE IF NOT EXISTS sync_state" app/src/storage/SqliteAdapter.ts` → 0
    - `grep -q "CREATE INDEX IF NOT EXISTS idx_sync_outbox_fifo" app/src/storage/SqliteAdapter.ts` → 0
    - `grep -q "runStorageContractTests" app/src/storage/__tests__/SqliteAdapter.rows.test.ts` → 0
    - Jest-Suite `SqliteAdapter.rows.test.ts` ist grün
    - Tests für alle Contract-Cases (KV, Row CRUD, Outbox FIFO, sync_state, parallele Writes) bestehen
  </acceptance_criteria>

  <done>
    SqliteAdapter implementiert das vollständige erweiterte StorageAdapter-Interface.
    Migration Version 3 legt alle 6 Row-Tables + sync_outbox + sync_state an.
    Contract-Tests sind grün, parallele Writes serialisieren korrekt.
    Atomic-Rollback bei Fehler funktioniert nachweislich (via Sqlite-Lock-Semantik).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-02-03: IndexedDbAdapter Row-Stores + Multi-Store-Transactions + fake-indexeddb-Tests</name>
  <files>
    - app/src/storage/IndexedDbAdapter.ts
    - app/src/storage/__tests__/IndexedDbAdapter.rows.test.ts (neu)
    - app/package.json (Dev-Dep fake-indexeddb)
  </files>

  <read_first>
    - app/src/storage/IndexedDbAdapter.ts (Phase-1-KV-Impl mit idb v8)
    - app/src/storage/__tests__/IndexedDbAdapter.test.ts (bestehende KV-Tests mit fake-indexeddb)
    - .planning/phases/03-offline-sync-2-user-shared-state/03-RESEARCH.md §2 "Web-Layer mit IndexedDB statt expo-sqlite-web"
    - .planning/phases/03-offline-sync-2-user-shared-state/03-PATTERNS.md §4 (IndexedDbAdapter-Analog)
    - idb docs: `db.transaction(stores, mode)` für Multi-Store-Transactions
  </read_first>

  <behavior>
    - Test 1: Contract-Tests aus Task 03-02-01 laufen gegen IndexedDbAdapter vollständig grün
    - Test 2: Version-2-Upgrade (bestehend Version 1 → 2) legt Object-Stores für alle Row-Entities + sync_outbox + sync_state an
    - Test 3: writeWithOutbox nutzt `db.transaction([entity, 'sync_outbox'], 'readwrite')` → atomar
    - Test 4: Simulierter Abort der Transaction rollt Row-Write + Outbox-Eintrag zurück
    - Test 5: getRowsByGarden nutzt Index `garden_id` (KeyRange-Query, kein Full-Scan)
    - Test 6: `fake-indexeddb/auto` ist in app/package.json (devDep) verfügbar
  </behavior>

  <action>
**1. `app/package.json` — `fake-indexeddb` als devDep sicherstellen:**

```bash
cd app && pnpm add -D fake-indexeddb
```

Sollte bereits durch Phase-1-Arbeit vorhanden sein. Falls nicht: per pnpm hinzufügen. Verify via `jq '.devDependencies["fake-indexeddb"]' app/package.json`.

**2. `app/src/storage/IndexedDbAdapter.ts` — vollständige Erweiterung:**

Der idb-Adapter muss bei `openDB` auf Version 2 hochziehen. IndexedDB-Upgrades sind komplex, weil
alle Object-Stores im `upgrade(db, oldVersion, newVersion, tx)`-Callback erzeugt werden müssen.
Wir kapseln das in einem switch-Block mit Fallthrough (idiomatisch für idb).

```typescript
import { openDB, type IDBPDatabase } from 'idb';
import type {
  StorageAdapter,
  EntityName,
  AnyRow,
  OutboxEntry,
  SyncStateEntry,
  QueryOptions,
} from '@spatenstich/shared';

const KV_STORE = 'kv';
const OUTBOX_STORE = 'sync_outbox';
const STATE_STORE = 'sync_state';
const SCHEMA_VERSION_KEY = '__schema_version__';

const ROW_ENTITIES: EntityName[] = [
  'gardens',
  'garden_members',
  'profiles',
  'vereinsregeln',
  'invite_codes',
  'photo_queue',
];

const GARDEN_ID_COLUMN: Record<EntityName, string | null> = {
  gardens: 'id',
  garden_members: 'garden_id',
  profiles: null,
  vereinsregeln: 'garden_id',
  invite_codes: 'garden_id',
  photo_queue: 'garden_id',
};

// Monotonic-Counter
let outboxCounter = 0;

export class IndexedDbAdapter implements StorageAdapter {
  private dbPromise: Promise<IDBPDatabase>;

  constructor(dbName: string) {
    this.dbPromise = openDB(dbName, 2, {
      upgrade(db, oldVersion) {
        // Version 1 → 2: Row-Stores + sync_outbox + sync_state.
        // Fallthrough-Pattern für zukünftige Versionen.
        if (oldVersion < 1) {
          if (!db.objectStoreNames.contains(KV_STORE)) {
            db.createObjectStore(KV_STORE);
          }
        }
        if (oldVersion < 2) {
          // Row-Stores
          for (const entity of ROW_ENTITIES) {
            if (!db.objectStoreNames.contains(entity)) {
              const store = db.createObjectStore(entity, { keyPath: 'id' });
              const gidCol = GARDEN_ID_COLUMN[entity];
              if (gidCol && gidCol !== 'id') {
                // IndexedDB-Indexe nutzen keyPath in camelCase:
                // 'garden_id'-Datenbank-Spalte → 'gardenId'-Feld im JS-Object
                store.createIndex('by_gardenId', 'gardenId', { unique: false });
              }
            }
          }
          // sync_outbox mit FIFO-Index auf created_at
          if (!db.objectStoreNames.contains(OUTBOX_STORE)) {
            const outbox = db.createObjectStore(OUTBOX_STORE, { keyPath: 'id' });
            outbox.createIndex('by_createdAt', 'createdAt', { unique: false });
          }
          // sync_state keyed by entity
          if (!db.objectStoreNames.contains(STATE_STORE)) {
            db.createObjectStore(STATE_STORE, { keyPath: 'entity' });
          }
        }
      },
    });
  }

  // ---- KV (Phase 1/2) ----
  async get(key: string): Promise<string | null> {
    const db = await this.dbPromise;
    const v = await db.get(KV_STORE, key);
    return typeof v === 'string' ? v : (v ?? null);
  }
  async set(key: string, value: string): Promise<void> {
    const db = await this.dbPromise;
    await db.put(KV_STORE, value, key);
  }
  async delete(key: string): Promise<void> {
    const db = await this.dbPromise;
    await db.delete(KV_STORE, key);
  }
  async list(prefix?: string): Promise<string[]> {
    const db = await this.dbPromise;
    const keys = (await db.getAllKeys(KV_STORE)) as string[];
    return prefix ? keys.filter((k) => k.startsWith(prefix)) : keys;
  }
  async getSchemaVersion(): Promise<number> {
    const v = await this.get(SCHEMA_VERSION_KEY);
    return v ? Number(v) : 0;
  }
  async setSchemaVersion(version: number): Promise<void> {
    await this.set(SCHEMA_VERSION_KEY, String(version));
  }

  // ---- Migration-Hook ----
  async __createRowTablesV3(): Promise<void> {
    // IndexedDB-Stores werden im upgrade()-Callback beim Öffnen angelegt.
    // Der Migration-Runner speichert danach schema_version = 3 in KV.
    // Daher hier No-Op, aber wir verifizieren, dass alle Stores existieren.
    const db = await this.dbPromise;
    const missing = [...ROW_ENTITIES, OUTBOX_STORE, STATE_STORE].filter(
      (s) => !db.objectStoreNames.contains(s),
    );
    if (missing.length > 0) {
      throw new Error(
        `IndexedDbAdapter: expected stores missing after upgrade: ${missing.join(', ')}. ` +
        'Delete dev database and retry.',
      );
    }
  }

  // ---- Row-Level ----
  async getRow<T extends AnyRow>(
    entity: EntityName,
    id: string,
    opts: QueryOptions = {},
  ): Promise<T | null> {
    const db = await this.dbPromise;
    const row = (await db.get(entity, id)) as T | undefined;
    if (!row) return null;
    if (!opts.includeDeleted && row.deletedAt != null) return null;
    return row;
  }

  async getRowsByGarden<T extends AnyRow>(
    entity: EntityName,
    gardenId: string,
    opts: QueryOptions = {},
  ): Promise<T[]> {
    const gidCol = GARDEN_ID_COLUMN[entity];
    if (!gidCol) {
      throw new Error(`Entity ${entity} has no garden_id — use getAllRows.`);
    }
    const db = await this.dbPromise;
    let rows: T[];
    if (gidCol === 'id') {
      // gardens-Sonderfall: id == garden_id
      const row = (await db.get(entity, gardenId)) as T | undefined;
      rows = row ? [row] : [];
    } else {
      rows = (await db.getAllFromIndex(entity, 'by_gardenId', gardenId)) as T[];
    }
    return opts.includeDeleted ? rows : rows.filter((r) => r.deletedAt == null);
  }

  async getAllRows<T extends AnyRow>(entity: EntityName, opts: QueryOptions = {}): Promise<T[]> {
    const db = await this.dbPromise;
    const rows = (await db.getAll(entity)) as T[];
    return opts.includeDeleted ? rows : rows.filter((r) => r.deletedAt == null);
  }

  async writeWithOutbox<T extends AnyRow>(
    entity: EntityName,
    row: T,
    outbox: Omit<OutboxEntry, 'id' | 'createdAt' | 'attempts' | 'lastError'>,
  ): Promise<void> {
    const db = await this.dbPromise;
    const outboxEntry: OutboxEntry = {
      id: cryptoRandomUuid(),
      entity: outbox.entity,
      rowId: outbox.rowId,
      operation: outbox.operation,
      payload: outbox.payload,
      createdAt: `${new Date().toISOString()}-${String(outboxCounter++).padStart(6, '0')}`,
      attempts: 0,
      lastError: null,
    };
    // L-6: Multi-Store-Transaction → atomar
    const tx = db.transaction([entity, OUTBOX_STORE], 'readwrite');
    await Promise.all([
      tx.objectStore(entity).put(row),
      tx.objectStore(OUTBOX_STORE).put(outboxEntry),
      tx.done,
    ]);
  }

  async upsertRowFromServer<T extends AnyRow>(entity: EntityName, row: T): Promise<void> {
    const db = await this.dbPromise;
    await db.put(entity, row);
  }

  async upsertRowsFromServer<T extends AnyRow>(entity: EntityName, rows: T[]): Promise<void> {
    if (rows.length === 0) return;
    const db = await this.dbPromise;
    const tx = db.transaction(entity, 'readwrite');
    await Promise.all([
      ...rows.map((r) => tx.objectStore(entity).put(r)),
      tx.done,
    ]);
  }

  // ---- Outbox ----
  async listOutboxEntries(limit?: number): Promise<OutboxEntry[]> {
    const db = await this.dbPromise;
    const index = db.transaction(OUTBOX_STORE).store.index('by_createdAt');
    const all = (await index.getAll()) as OutboxEntry[];
    return limit ? all.slice(0, limit) : all;
  }

  async deleteOutboxEntry(id: string): Promise<void> {
    const db = await this.dbPromise;
    await db.delete(OUTBOX_STORE, id);
  }

  async updateOutboxEntry(
    id: string,
    patch: Pick<OutboxEntry, 'attempts' | 'lastError'>,
  ): Promise<void> {
    const db = await this.dbPromise;
    const entry = (await db.get(OUTBOX_STORE, id)) as OutboxEntry | undefined;
    if (!entry) return;
    await db.put(OUTBOX_STORE, { ...entry, ...patch });
  }

  // ---- sync_state ----
  async getSyncState(entity: EntityName): Promise<SyncStateEntry | null> {
    const db = await this.dbPromise;
    const state = (await db.get(STATE_STORE, entity)) as SyncStateEntry | undefined;
    return state ?? null;
  }

  async setSyncState(state: SyncStateEntry): Promise<void> {
    const db = await this.dbPromise;
    await db.put(STATE_STORE, state);
  }
}

function cryptoRandomUuid(): string {
  // @ts-expect-error globalThis.crypto
  if (globalThis.crypto?.randomUUID) return globalThis.crypto.randomUUID();
  return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, (c) => {
    const r = (Math.random() * 16) | 0;
    return (c === 'x' ? r : (r & 0x3) | 0x8).toString(16);
  });
}
```

**3. `app/src/storage/__tests__/IndexedDbAdapter.rows.test.ts` (NEU):**

```typescript
import 'fake-indexeddb/auto';
import { IndexedDbAdapter } from '../IndexedDbAdapter';
import { runMigrations } from '../migrations';
import { runStorageContractTests } from './RowTables.contract.test';

runStorageContractTests(
  'IndexedDbAdapter',
  async () => {
    const adapter = new IndexedDbAdapter(`test-${Date.now()}-${Math.random()}`);
    await runMigrations(adapter);
    return adapter;
  },
  async (_adapter) => {
    // fake-indexeddb: pro Test neue DB → kein explizites Cleanup nötig.
  },
);

describe('IndexedDbAdapter Web-specific', () => {
  it('Version-Upgrade 1 → 2 legt alle Row-Stores + Outbox + State an', async () => {
    const adapter = new IndexedDbAdapter(`upgrade-test-${Date.now()}`);
    await runMigrations(adapter);
    // __createRowTablesV3 wirft, falls Stores fehlen:
    // @ts-expect-error private method exposed for test
    await (adapter as any).__createRowTablesV3();
  });

  it('by_gardenId-Index beschleunigt getRowsByGarden', async () => {
    const adapter = new IndexedDbAdapter(`index-test-${Date.now()}`);
    await runMigrations(adapter);

    // 100 photo_queue-Rows in 2 Gärten
    for (let i = 0; i < 100; i++) {
      await adapter.upsertRowFromServer('photo_queue', {
        id: `p-${i}`,
        createdAt: '2026-04-24T10:00:00.000Z',
        updatedAt: '2026-04-24T10:00:00.000Z',
        updatedByUserId: 'user-a',
        deletedAt: null,
        gardenId: i % 2 === 0 ? 'garden-a' : 'garden-b',
        storagePath: `path/${i}.jpg`,
        geoLat: null, geoLng: null,
        uploadStatus: 'pending', uploadError: null, jobId: null,
      } as any);
    }

    const gardenA = await adapter.getRowsByGarden('photo_queue', 'garden-a');
    expect(gardenA).toHaveLength(50);
    expect(gardenA.every((r: any) => r.gardenId === 'garden-a')).toBe(true);
  });
});
```

**WICHTIG:** fake-indexeddb/auto muss VOR dem Import von idb geladen werden (Top-of-file).
Jest-Setup (app/jest.config.js) hat bereits entsprechende Konfiguration aus Phase 1/2.
  </action>

  <verify>
    <automated>pnpm --filter app test -- app/src/storage/__tests__/IndexedDbAdapter.rows.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `grep -q "openDB(dbName, 2," app/src/storage/IndexedDbAdapter.ts` → 0
    - `grep -q "db.transaction(\[entity, OUTBOX_STORE\]" app/src/storage/IndexedDbAdapter.ts` → 0
    - `grep -q "createIndex('by_gardenId'" app/src/storage/IndexedDbAdapter.ts` → 0
    - `grep -q "createIndex('by_createdAt'" app/src/storage/IndexedDbAdapter.ts` → 0
    - `grep -q "__createRowTablesV3" app/src/storage/IndexedDbAdapter.ts` → 0
    - `grep -q "runStorageContractTests" app/src/storage/__tests__/IndexedDbAdapter.rows.test.ts` → 0
    - `grep -q "fake-indexeddb/auto" app/src/storage/__tests__/IndexedDbAdapter.rows.test.ts` → 0
    - `jq -r '.devDependencies["fake-indexeddb"]' app/package.json` ist gesetzt (nicht `null`)
    - Jest-Suite `IndexedDbAdapter.rows.test.ts` ist grün (alle Contract-Cases + Web-specific-Tests)
    - Keine Import von `expo-sqlite-web` im Codebase: `grep -r "expo-sqlite-web" app/src/` liefert 0 Matches
  </acceptance_criteria>

  <done>
    IndexedDbAdapter implementiert das vollständige erweiterte StorageAdapter-Interface.
    Version-2-Upgrade legt 6 Row-Stores + sync_outbox + sync_state an.
    writeWithOutbox nutzt echte Multi-Store-Transactions (kein Fire-and-Forget).
    by_gardenId-Index beschleunigt Garden-scoped Queries.
    Contract-Tests sind für beide Adapter identisch grün (Nyquist I-2 Grundlage).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JS-Runtime → SQLite-DB | Expo-Native-Bridge (nur für SqliteAdapter, iOS/Android) |
| JS-Runtime → IndexedDB | Browser-API (nur für IndexedDbAdapter, Web) |
| App-Code → StorageAdapter | Repos nutzen Adapter; keine direkte DB-Zugriffe außerhalb von storage/ |
| Row-Payload → JSON.stringify/parse | Serialisation-Grenze: kein Schema-Enforcement zur Laufzeit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-02-01 | Tampering | Row-Payload in JSON-Blob (SQLite `data` TEXT) | accept | JSON ist lokal; Angreifer mit Device-Zugriff kann manipulieren, aber LWW-Trigger serverseitig verhindert Propagation manipulierter alter Timestamps (P9011-Reject in Plan 03-01). Clientseitiges Schema-Enforcement wäre teuer (runtime zod für jede Row). |
| T-3-02-02 | Information Disclosure | photo_queue.geo_lat/geo_lng in lokaler DB | mitigate | Geo-Felder nur gesetzt wenn Opt-in-Toggle AN (D-24). EXIF-Strip (Plan 03-05) setzt Felder auf null wenn Toggle AUS. Dokumentiert in PhotoQueueRow-Typ via JSDoc. |
| T-3-02-03 | Denial of Service | Unbegrenztes sync_outbox-Wachstum bei Dauer-Offline | mitigate | `listOutboxEntries(limit)`-Parameter in API, SyncWorker (Plan 03-04) nutzt Batch-Limit=50. Außerdem: Backoff-Logik limitiert Push-Versuche, nicht Queue-Größe. Retrospektiv: sync_outbox.attempts > 10 triggert Settings-UI (Plan 03-06) mit Option „Verwerfen". |
| T-3-02-04 | Tampering | Lokale Schema-Migrations-Version in KV-Store | accept | SCHEMA_VERSION_KEY in kv-Tabelle — Angreifer mit Device-Zugriff kann Version downgraden, was Migration re-runnt. Migrations sind idempotent (CREATE TABLE IF NOT EXISTS, CREATE INDEX IF NOT EXISTS). Kein Datenverlust möglich. |
| T-3-02-05 | Repudiation | Outbox-Einträge haben keine Signatur | accept | Single-device-Kontext; User kann nicht repudieren, was ihr eigenes Device in ihre eigene lokale DB geschrieben hat. Server-Side attestiert via `updated_by_user_id` (Phase 02.5 mm_-Trigger). |
| T-3-02-06 | Elevation of Privilege | `upsertRowFromServer` umgeht Outbox | mitigate | Nur von SyncWorker aufgerufen (private Convention — nicht auf Interface-Ebene erzwingbar in TS). Mitigation: Dokumentation + JSDoc-Warnung + Grep-Check im CI (`grep -r "upsertRowFromServer" app/src/ | grep -v "storage/\|__tests__/\|SyncWorker"` muss leer sein). Follow-up in Plan 03-04 (SyncWorker). |
| T-3-02-07 | Tampering | Parallel-Schreiben auf dasselbe row_id (race condition) | mitigate | SqliteAdapter: `withExclusiveTransactionAsync` serialisiert. IndexedDbAdapter: idb-Transactions sind serialisiert pro Object-Store. Contract-Test validiert 10× parallele writeWithOutbox-Aufrufe. |
</threat_model>

<verification>
**Integrated Validation Checkpoints (aus 03-VALIDATION.md):**
- I-2 (Write-Persist in Outbox): durch Contract-Test-Suite abgedeckt, insbesondere Test "writeWithOutbox speichert Row + Outbox-Eintrag atomar"
- NFR-01 (iPhone + Browser): beide Adapter haben parametrisierte Contract-Tests mit identischen Assertions

**Full plan verification command:**
```bash
pnpm --filter @spatenstich/shared build && \
pnpm --filter app test -- --runInBand \
  app/src/storage/__tests__/SqliteAdapter.rows.test.ts \
  app/src/storage/__tests__/IndexedDbAdapter.rows.test.ts
```

Exit code 0 + keine Jest-Failures → Plan komplett.
</verification>

<success_criteria>
- [ ] StorageAdapter-Interface in `packages/shared/src/types/storage.ts` um Row-Level + Outbox + sync_state erweitert
- [ ] RowBase + 6 Entity-Row-Types in `packages/shared/src/types/entities.ts` exportiert
- [ ] SqliteAdapter nutzt `withExclusiveTransactionAsync` für atomic writeWithOutbox
- [ ] IndexedDbAdapter nutzt `db.transaction([entity, 'sync_outbox'], 'readwrite')` für atomic writeWithOutbox
- [ ] Migration Version 3 legt 6 Row-Tables + sync_outbox + sync_state an
- [ ] Index auf garden_id (SQLite) / by_gardenId (IndexedDB) beschleunigt getRowsByGarden
- [ ] by_createdAt-Index auf sync_outbox garantiert FIFO-Sortierung
- [ ] Parametrisierter Contract-Test läuft gegen beide Adapter vollständig grün
- [ ] Keine Abhängigkeit auf expo-sqlite-web (Grep-Check leer)
- [ ] fake-indexeddb in app/package.json devDependencies
- [ ] Threat-Model (7 STRIDE-Bedrohungen) im PLAN dokumentiert
- [ ] Commit `docs(03): add plan 02 — storage-adapter row-tables`
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/03-offline-sync-2-user-shared-state/03-02-SUMMARY.md` mit:
- Welche API-Methoden hinzugefügt wurden (+ Signaturen)
- Welche Migration-Version erzeugt wurde (Version 3 Details)
- Welche Indexes in SQLite / IndexedDB angelegt wurden
- Test-Coverage-Report (Contract-Suite)
- Bekannte Landmines (L-6 Transaction-Semantik-Unterschiede SQLite vs IndexedDB)
- Downstream-Empfehlungen für Plan 03-03 (Repo-Umbau nutzt diese Adapter-API)
</output>
