---
plan: 03
phase: 1
wave_sequence: [2, 3]
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - app/eas.json
  - app/app.config.ts
  - app/app/_layout.tsx
  - .github/workflows/ci.yml
  - .github/workflows/eas-build.yml
  - .github/workflows/bundle-secret-scan.yml
  - supabase/functions/ai-job-consumer/index.ts
  - supabase/functions/ai-job-consumer/deno.json
  - supabase/functions/.env.example
  - scripts/check-claude-key-in-bundle.sh
  - scripts/e2e-pgmq-smoke.sql
autonomous: false
requirements: [FOUND-05, FOUND-06, FOUND-07, FOUND-08, NFR-08]
user_setup:
  - service: expo-eas
    why: "EAS Build für iOS + Web Export aus CI (FOUND-05)"
    env_vars:
      - name: EXPO_TOKEN
        source: "Expo Dashboard → Settings → Access Tokens (GitHub Actions Secret)"
    dashboard_config:
      - task: "EAS-Projekt initialisieren (eas build:configure) und iOS Credentials anlegen"
        location: "expo.dev → Projekt → Credentials (iOS Distribution)"
  - service: anthropic
    why: "Claude API-Key für Edge Function (server-seitig)"
    env_vars:
      - name: CLAUDE_API_KEY
        source: "console.anthropic.com → API Keys"
    dashboard_config:
      - task: "API-Key als Supabase-Function-Secret setzen (niemals in Client-Bundle)"
        location: "CLI: supabase secrets set CLAUDE_API_KEY=... --project-ref <ref>"
  - service: sentry
    why: "Crash-Reporting (NFR-08) — EU-Region"
    env_vars:
      - name: EXPO_PUBLIC_SENTRY_DSN
        source: "Sentry Dashboard (EU-Org) → Settings → Projects → DSN"
      - name: SENTRY_AUTH_TOKEN
        source: "Sentry Dashboard → Account → Auth Tokens (für Source-Map-Upload im EAS-Build)"
    dashboard_config:
      - task: "Sentry-Organisation in Region 'Europe' anlegen (DSN zeigt automatisch auf de.sentry.io)"
        location: "sentry.io → Create Organization → Data Storage Region: Europe"
must_haves:
  truths:
    - "Jeder PR lässt lint + typecheck + tests via GitHub Actions laufen (Job PR Checks)"
    - "Merge auf main startet EAS Build iOS + Web-Export (nicht auf PRs)"
    - "Claude API Key erscheint nicht im Web-Bundle (automatisierter grep im CI)"
    - "Edge Function ai-job-consumer liest aus pgmq und persistiert in ai_results"
    - "Sentry ist initialisiert und sendet Error-Events (einmaliger manueller Sentry-Test-Event)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "PR-Workflow: lint + typecheck + tests + bundle-secret-scan"
      contains: "pull_request"
    - path: ".github/workflows/eas-build.yml"
      provides: "Main-Branch-Workflow: EAS iOS + Web Export"
      contains: "branches: [main]"
    - path: ".github/workflows/bundle-secret-scan.yml"
      provides: "Reusable Job: Web-Bundle bauen und nach Claude-Key scannen"
      contains: "sk-ant"
    - path: "app/eas.json"
      provides: "EAS Build Profiles: development, preview, production (D-07)"
      contains: "production"
    - path: "supabase/functions/ai-job-consumer/index.ts"
      provides: "Edge Function Consumer: liest pgmq → persistiert ai_results"
      min_lines: 50
    - path: "scripts/check-claude-key-in-bundle.sh"
      provides: "CI-Script: exit 1 wenn Claude-Key im Bundle"
      contains: "sk-ant"
  key_links:
    - from: ".github/workflows/eas-build.yml"
      to: "app/eas.json"
      via: "eas build --profile production"
      pattern: "eas build"
    - from: "supabase/functions/ai-job-consumer/index.ts"
      to: "pgmq_public.read"
      via: "supabase.schema('pgmq_public').rpc('read')"
      pattern: "pgmq_public"
    - from: "supabase/functions/ai-job-consumer/index.ts"
      to: "ai_results"
      via: "supabase.from('ai_results').insert"
      pattern: "ai_results"
    - from: "app/app/_layout.tsx"
      to: "@sentry/react-native"
      via: "Sentry.init + Sentry.wrap"
      pattern: "Sentry\\."
---

# Plan 03: EAS CI + Edge Function Consumer + End-to-End Verification

> Liefert die CI-Pipeline (PRs: schnelle Checks; main: EAS iOS + Web — per D-06), den pgmq-Consumer als Edge Function mit server-seitigem Claude-API-Key (FOUND-06/07/08), Sentry (NFR-08) und End-to-End-Tests: Bundle-Scan auf API-Keys und pgmq-Round-Trip.

## Context

Dieser Plan schließt Phase 1 ab. Drei Jobs müssen zusammenkommen:

1. **CI (FOUND-05, D-06):** PR-Workflow = nur Lint + Typecheck + Tests + Bundle-Secret-Scan. Main-Workflow = EAS Build iOS + `expo export --platform web`. Spart EAS-Free-Tier-Minuten.
2. **Edge Function (FOUND-06/07/08):** Deno-Consumer liest `pgmq_public.read` → (in Phase 1 nur) persistiert einen Mock-Response in `ai_results`. Echte Claude-Calls werden erst in Phase 4 live geschaltet; der Pfad ist aber jetzt komplett live und die API-Key-Hülse existiert server-seitig.
3. **Sentry (NFR-08):** `@sentry/react-native` in Expo-App, EU-DSN, `Sentry.wrap(RootLayout)`.

**Der FOUND-06-Test ist der heikelste:** Der Claude-API-Key darf niemals im Client-Bundle landen. Wir verdrahten das dreifach: (a) Key nur in `Deno.env` der Edge Function, (b) CI-Grep auf `sk-ant` + `CLAUDE_API_KEY` im `dist/` des Web-Exports, (c) Keine `EXPO_PUBLIC_*`-Variable für den Claude-Key.

**pnpm + EAS Build** ist das größte bekannte Risiko (RESEARCH Pitfall 1, eas-cli Issue #3247). Deshalb wird der erste EAS-Build aktiv im CI getestet, nicht erst am Phase-Ende. Bei Blockierung: `shamefully-hoist=true` ist bereits in Plan 01 gesetzt als Fallback.

## Decisions Implemented

| ID | Decision | Task |
|----|----------|------|
| D-06 | EAS nur bei main-Merge; PRs = schnelle Checks | 1-03-02 |
| D-07 | dev + prod Environments (kein Staging) | 1-03-01 (eas.json) |
| FOUND-06 | API-Keys nur server-seitig; Key-Grep als CI-Gate | 1-03-04 + 1-03-05 |

## Tasks

### Wave 2: Konfiguration + Test-Stubs

#### Task 1-03-01: eas.json + app.config.ts Sentry-Plugin + Env-Wiring
- **files_modified:** app/eas.json, app/app.config.ts, app/app/_layout.tsx
- **depends_on:** 01, 02
- **type:** config
- **autonomous:** true
- **requirement:** FOUND-05, NFR-08

**What to do:**

1. `app/eas.json` — zwei Profile (D-07: dev + prod), plus `preview` für interne Tests:
   ```json
   {
     "cli": { "version": ">= 18.0.0", "appVersionSource": "local" },
     "build": {
       "development": {
         "developmentClient": true,
         "distribution": "internal",
         "env": { "ENVIRONMENT": "dev" },
         "ios": { "simulator": true }
       },
       "preview": {
         "distribution": "internal",
         "env": { "ENVIRONMENT": "dev" },
         "ios": { "simulator": true }
       },
       "production": {
         "env": { "ENVIRONMENT": "prod" },
         "ios": { "image": "macos-sonoma-14.6-xcode-16.1" }
       }
     },
     "submit": { "production": {} }
   }
   ```
2. `app/app.config.ts` erweitern (Sentry-Plugin + public env):
   ```ts
   import type { ExpoConfig } from 'expo/config';
   const config: ExpoConfig = {
     name: 'Spatenstich',
     slug: 'spatenstich',
     version: '0.1.0',
     orientation: 'portrait',
     scheme: 'spatenstich',
     userInterfaceStyle: 'automatic',
     ios: { bundleIdentifier: 'de.spatenstich.app', supportsTablet: true },
     web: {
       bundler: 'metro',
       output: 'static',
       headers: [
         { source: '/(.*)', headers: [
           { key: 'Cross-Origin-Embedder-Policy', value: 'credentialless' },
           { key: 'Cross-Origin-Opener-Policy', value: 'same-origin' },
         ]},
       ],
     },
     plugins: [
       'expo-router',
       ['@sentry/react-native/expo', {
         organization: process.env.SENTRY_ORG ?? 'spatenstich',
         project: 'spatenstich-app',
         url: 'https://sentry.io/',
       }],
     ],
     experiments: { typedRoutes: true },
     extra: {
       eas: { projectId: process.env.EAS_PROJECT_ID },
     },
   };
   export default config;
   ```
   `@sentry/react-native@8.7.0` als dep in `app/package.json` ergänzen.
3. `app/app/_layout.tsx` — Sentry initialisieren und `Sentry.wrap` anwenden:
   ```tsx
   import * as Sentry from '@sentry/react-native';
   import { Stack } from 'expo-router';

   Sentry.init({
     dsn: process.env.EXPO_PUBLIC_SENTRY_DSN,
     environment: process.env.ENVIRONMENT ?? 'dev',
     tracesSampleRate: 1.0,
     enabled: !!process.env.EXPO_PUBLIC_SENTRY_DSN, // kein Init ohne DSN (lokal)
   });

   function RootLayout() {
     return <Stack screenOptions={{ headerShown: false }} />;
   }

   export default Sentry.wrap(RootLayout);
   ```
4. `.env.example` erweitern (kein `CLAUDE_API_KEY` hier — wichtige Trennlinie):
   ```
   # app/.env.example (public-only)
   EXPO_PUBLIC_SUPABASE_URL=https://<project>.supabase.co
   EXPO_PUBLIC_SUPABASE_ANON_KEY=<anon-key>
   EXPO_PUBLIC_SENTRY_DSN=https://<eu-dsn>@o<org>.ingest.de.sentry.io/<project>
   # WICHTIG: CLAUDE_API_KEY darf NIEMALS hier stehen (FOUND-06).
   ```

**Verify:**
```bash
pnpm --filter app run typecheck
test -f app/eas.json
grep -q "production" app/eas.json
grep -q "Sentry.wrap" app/app/_layout.tsx
grep -q "Sentry.init" app/app/_layout.tsx
! grep -q "CLAUDE_API_KEY" app/.env.example
```

**Done:**
- `eas.json` existiert mit drei Profilen (development, preview, production)
- Sentry ist in `app/_layout.tsx` initialisiert und `Sentry.wrap` umschließt den RootLayout
- `CLAUDE_API_KEY` kommt an keiner Stelle in `app/`-Dateien vor

---

#### Task 1-03-02: GitHub Actions — PR-Checks + EAS-Build + Bundle-Secret-Scan
- **files_modified:** .github/workflows/ci.yml, .github/workflows/eas-build.yml, .github/workflows/bundle-secret-scan.yml, scripts/check-claude-key-in-bundle.sh
- **depends_on:** 1-03-01
- **type:** infrastructure
- **autonomous:** true
- **requirement:** FOUND-05, FOUND-06

**What to do (per D-06):**

1. Bundle-Secret-Scan-Script `scripts/check-claude-key-in-bundle.sh`:
   ```bash
   #!/usr/bin/env bash
   # Fails CI if a Claude API-Key-Muster im gebauten Web-Bundle gefunden wird.
   # Ausführen nach `expo export --platform web` (Output in dist/).
   set -euo pipefail
   BUNDLE_DIR="${1:-app/dist}"
   if [ ! -d "$BUNDLE_DIR" ]; then
     echo "Bundle dir '$BUNDLE_DIR' nicht gefunden" >&2
     exit 2
   fi
   PATTERNS=(
     "sk-ant-"              # Claude-API-Key-Prefix
     "CLAUDE_API_KEY"       # Env-Var-Name
     "SUPABASE_SERVICE_ROLE_KEY" # Server-Key
   )
   FOUND=0
   for p in "${PATTERNS[@]}"; do
     if grep -r --binary-files=text -l "$p" "$BUNDLE_DIR" >/dev/null; then
       echo "❌ Secret-Muster '$p' im Bundle gefunden" >&2
       grep -r --binary-files=text -n "$p" "$BUNDLE_DIR" | head -5 >&2
       FOUND=1
     fi
   done
   if [ "$FOUND" -eq 1 ]; then
     exit 1
   fi
   echo "✓ Keine Secret-Muster im Bundle."
   ```
   `chmod +x` im Script (oder `bash scripts/...` im Workflow).
2. `.github/workflows/ci.yml` — **PR-Workflow (schnelle Checks, kein EAS Build):**
   ```yaml
   name: CI — PR Checks
   on:
     pull_request:
       branches: [main]
   jobs:
     checks:
       runs-on: ubuntu-latest
       steps:
         - uses: actions/checkout@v5
         - uses: pnpm/action-setup@v4
           with: { version: 10.33.0 }
         - uses: actions/setup-node@v6
           with: { node-version: 22, cache: 'pnpm' }
         - run: pnpm install --frozen-lockfile
         - run: pnpm -r run typecheck
         - run: pnpm -r run lint
         - run: pnpm -r run test --passWithNoTests
         - name: Web export (for bundle scan)
           run: pnpm --filter app exec expo export --platform web
         - name: Scan bundle for secrets
           run: bash scripts/check-claude-key-in-bundle.sh app/dist
   ```
3. `.github/workflows/eas-build.yml` — **Main-Branch-Workflow (EAS + Web-Export):**
   ```yaml
   name: EAS Build (iOS + Web) — main only
   on:
     push:
       branches: [main]
   jobs:
     build:
       runs-on: ubuntu-latest
       steps:
         - uses: actions/checkout@v5
         - uses: pnpm/action-setup@v4
           with: { version: 10.33.0 }
         - uses: actions/setup-node@v6
           with: { node-version: 22, cache: 'pnpm' }
         - uses: expo/expo-github-action@v8
           with:
             eas-version: 18.7.0
             token: ${{ secrets.EXPO_TOKEN }}
         - name: Install
           run: pnpm install --frozen-lockfile
         - name: Web export
           run: pnpm --filter app exec expo export --platform web
         - name: Scan web bundle for secrets
           run: bash scripts/check-claude-key-in-bundle.sh app/dist
         - name: EAS Build iOS (non-interactive, no-wait)
           working-directory: app
           run: eas build --platform ios --profile production --non-interactive --no-wait
   ```
4. `.github/workflows/bundle-secret-scan.yml` — **reusable Workflow** (wird von ci.yml + eas-build.yml geteilt; kann später erweitert werden):
   ```yaml
   name: Bundle Secret Scan (reusable)
   on:
     workflow_call:
       inputs:
         bundle_dir:
           required: false
           type: string
           default: app/dist
   jobs:
     scan:
       runs-on: ubuntu-latest
       steps:
         - uses: actions/checkout@v5
         - run: bash scripts/check-claude-key-in-bundle.sh ${{ inputs.bundle_dir }}
   ```
5. **GitHub Secrets**, die Claude NICHT setzen kann (Mensch muss):
   - `EXPO_TOKEN` (Repo → Settings → Secrets)
   - `SUPABASE_ACCESS_TOKEN`
   - `SENTRY_AUTH_TOKEN`
   - Niemals `CLAUDE_API_KEY` im GitHub Actions — das kommt NUR in Supabase Edge-Function-Secrets.

**Verify:**
```bash
test -f .github/workflows/ci.yml && test -f .github/workflows/eas-build.yml && test -x scripts/check-claude-key-in-bundle.sh || chmod +x scripts/check-claude-key-in-bundle.sh
grep -q "branches: \[main\]" .github/workflows/eas-build.yml
grep -q "pull_request" .github/workflows/ci.yml
# Lokaler Bundle-Scan-Dry-Run gegen leeres Verzeichnis
mkdir -p /tmp/empty-bundle && bash scripts/check-claude-key-in-bundle.sh /tmp/empty-bundle
```

**Done:**
- `.github/workflows/ci.yml` triggert nur bei `pull_request`
- `.github/workflows/eas-build.yml` triggert nur bei `push` zu `main`
- Bundle-Scan-Script exitet 0 bei leerem Verzeichnis
- Secret-Pattern-Liste enthält `sk-ant-`, `CLAUDE_API_KEY`, `SUPABASE_SERVICE_ROLE_KEY`

---

### Wave 3: Edge Function Consumer + End-to-End-Tests

#### Task 1-03-03: pgmq-Consumer Edge Function (Phase-1-Skeleton mit Mock-Response)
- **files_modified:** supabase/functions/ai-job-consumer/index.ts, supabase/functions/ai-job-consumer/deno.json, supabase/functions/.env.example
- **depends_on:** 1-03-02, 02 (Migration 001 live)
- **type:** implementation
- **autonomous:** true
- **requirement:** FOUND-06, FOUND-07, FOUND-08

**What to do (per RESEARCH Pattern 4, aber: Phase 1 mockt Claude-Call):**

1. `supabase/functions/ai-job-consumer/deno.json`:
   ```json
   {
     "imports": {
       "@supabase/supabase-js": "npm:@supabase/supabase-js@2.103.2"
     }
   }
   ```
2. `supabase/functions/.env.example`:
   ```
   # Wird via `supabase secrets set` in die Supabase-Cloud geladen.
   # NIEMALS in ein Bundle oder in Git comitten.
   SUPABASE_URL=
   SUPABASE_SERVICE_ROLE_KEY=
   CLAUDE_API_KEY=
   ```
3. `supabase/functions/ai-job-consumer/index.ts`:
   ```ts
   // deno-lint-ignore-file
   import 'jsr:@supabase/functions-js/edge-runtime.d.ts';
   import { createClient } from 'npm:@supabase/supabase-js@2.103.2';

   // FOUND-06: Alle Secrets ausschließlich aus Deno.env. Kein Fallback.
   const SUPABASE_URL = Deno.env.get('SUPABASE_URL')!;
   const SERVICE_ROLE = Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!;
   const CLAUDE_KEY = Deno.env.get('CLAUDE_API_KEY'); // Phase 1: optional, Phase 4: required

   if (!SUPABASE_URL || !SERVICE_ROLE) {
     throw new Error('Missing SUPABASE_URL or SUPABASE_SERVICE_ROLE_KEY');
   }

   const supabase = createClient(SUPABASE_URL, SERVICE_ROLE);

   interface PgmqMessage {
     msg_id: number;
     read_ct: number;
     vt: string;
     enqueued_at: string;
     message: { job_id: string; job_type: string; payload: unknown };
   }

   Deno.serve(async (_req) => {
     const { data: messages, error: readErr } = await supabase
       .schema('pgmq_public')
       .rpc('read', { queue_name: 'ai_jobs', sleep_seconds: 30, n: 5 });

     if (readErr) {
       return new Response(JSON.stringify({ error: readErr.message }), { status: 500 });
     }
     if (!messages || (messages as PgmqMessage[]).length === 0) {
       return new Response(JSON.stringify({ processed: 0 }), { status: 200 });
     }

     const processedIds: number[] = [];
     for (const msg of messages as PgmqMessage[]) {
       try {
         // 1. Job-Row laden (Audit)
         const { data: job, error: jobErr } = await supabase
           .from('ai_jobs').select('user_id').eq('id', msg.message.job_id).single();
         if (jobErr || !job) throw new Error(`Job lookup failed: ${jobErr?.message}`);

         // 2. Phase 1: Mock-Response. Phase 4 tauscht diesen Block gegen echten Claude-Call.
         const rawResponse = CLAUDE_KEY
           ? { _phase1_placeholder: true, note: 'Claude call not yet wired' }
           : { _phase1_placeholder: true, note: 'CLAUDE_API_KEY not set — mock only' };

         // 3. Persist in ai_results (FOUND-08)
         await supabase.from('ai_results').insert({
           user_id: job.user_id,
           job_id: msg.message.job_id,
           raw_response: rawResponse,
           parsed_result: null,
           model_used: 'mock-phase1',
           latency_ms: 0,
         });

         // 4. Job-Status aktualisieren
         await supabase.from('ai_jobs').update({ status: 'done' }).eq('id', msg.message.job_id);

         // 5. Message archivieren (Audit-Trail)
         await supabase.schema('pgmq_public').rpc('archive', {
           queue_name: 'ai_jobs',
           msg_id: msg.msg_id,
         });
         processedIds.push(msg.msg_id);
       } catch (err) {
         console.error(`Job ${msg.message.job_id} failed:`, err);
         await supabase.from('ai_jobs').update({
           status: 'failed', last_error: (err as Error).message,
         }).eq('id', msg.message.job_id);
         // Message bleibt in Queue (visibility timeout = retry)
       }
     }

     return new Response(JSON.stringify({ processed: processedIds.length, ids: processedIds }), {
       status: 200, headers: { 'content-type': 'application/json' },
     });
   });
   ```
4. Deploy:
   ```bash
   supabase functions deploy ai-job-consumer --project-ref $SUPABASE_PROJECT_REF
   supabase secrets set SUPABASE_URL="$EXPO_PUBLIC_SUPABASE_URL" --project-ref $SUPABASE_PROJECT_REF
   # SERVICE_ROLE_KEY aus Dashboard → Settings → API (secret section)
   supabase secrets set SUPABASE_SERVICE_ROLE_KEY=... --project-ref $SUPABASE_PROJECT_REF
   # Claude-Key in Phase 1 optional; setzen falls vorhanden:
   # supabase secrets set CLAUDE_API_KEY=sk-ant-... --project-ref $SUPABASE_PROJECT_REF
   ```

**Verify:**
```bash
# Function existiert remote
supabase functions list --project-ref $SUPABASE_PROJECT_REF | grep ai-job-consumer
# Secrets-Liste (nur Namen sichtbar)
supabase secrets list --project-ref $SUPABASE_PROJECT_REF
# Invoke gibt 200 zurück (auch bei leerer Queue)
supabase functions invoke ai-job-consumer --project-ref $SUPABASE_PROJECT_REF
```

**Done:**
- Function deployed und per `supabase functions invoke` erreichbar
- Secrets `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` gesetzt
- Leere-Queue-Aufruf liefert `{"processed":0}`

---

#### Task 1-03-04: End-to-End pgmq Round-Trip Test
- **files_modified:** scripts/e2e-pgmq-smoke.sql
- **depends_on:** 1-03-03
- **type:** test
- **autonomous:** false
- **requirement:** FOUND-07, FOUND-08

**What to do:**

1. `scripts/e2e-pgmq-smoke.sql` — reproduzierbarer End-to-End-Test:
   ```sql
   -- Run: supabase db execute --file scripts/e2e-pgmq-smoke.sql --linked -v user_a='<uuid>'
   -- Schritt 1: Test-Job einfügen
   insert into public.ai_jobs(user_id, job_type, payload)
     values (:'user_a'::uuid, 'photo_analysis', '{"smoke":true}'::jsonb)
     returning id as smoke_job_id \gset

   -- Schritt 2: pgmq-Message senden (als service_role / admin)
   select pgmq_public.send(
     'ai_jobs',
     jsonb_build_object('job_id', :'smoke_job_id', 'job_type', 'photo_analysis', 'payload', '{"smoke":true}'::jsonb),
     0
   );

   -- Schritt 3: Ausgabe — nach diesem Schritt:
   --   a) Edge Function manuell triggern: supabase functions invoke ai-job-consumer
   --   b) ai_results.insert erwartet für smoke_job_id
   select 'Invoke now: supabase functions invoke ai-job-consumer --project-ref $SUPABASE_PROJECT_REF' as next_step;
   ```
2. **Manuelle Schritte (Mensch):**
   - `supabase db execute --file scripts/e2e-pgmq-smoke.sql --linked -v user_a=<test-user-a-uuid>`
   - Output notieren (smoke_job_id)
   - `supabase functions invoke ai-job-consumer --project-ref $SUPABASE_PROJECT_REF`
   - Erwartung: Response `{"processed": 1, "ids": [<msg_id>]}`
   - Verifikation in SQL:
     ```sql
     select * from public.ai_results where job_id = '<smoke_job_id>';
     -- Muss genau 1 Zeile liefern mit raw_response._phase1_placeholder = true
     select status from public.ai_jobs where id = '<smoke_job_id>';
     -- Muss 'done' sein
     ```

**Resume-Signal:** Antworte mit "pgmq round-trip verified" sobald ai_results eine Zeile für den smoke_job_id enthält und ai_jobs.status = 'done' ist.

**Verify:**
```bash
test -f scripts/e2e-pgmq-smoke.sql
```

**Done:**
- Smoke-SQL-Datei existiert
- Manueller Round-Trip-Test: Job → pgmq → Edge Function → ai_results funktioniert
- ai_jobs.status = 'done' nach erfolgreichem Consumer-Run

---

#### Task 1-03-05: Checkpoint — CI grün + EAS Build erfolgreich + Bundle-Scan clean + Sentry sendet Event
- **files_modified:** (keine — reine Verifikation)
- **depends_on:** 1-03-02, 1-03-03, 1-03-04
- **type:** checkpoint:human-verify
- **autonomous:** false
- **requirement:** FOUND-05, FOUND-06, NFR-08

**What-built:** CI-Pipeline inkl. Bundle-Secret-Scan; EAS-Build-Workflow auf main; Edge Function Consumer; Sentry-Integration.

**How-to-verify (Mensch — exakte Schritte):**

1. **PR-Checks:**
   - Branch `test/ci-smoke` erstellen; triviale Änderung committen; PR öffnen.
   - GitHub Actions Tab → Workflow `CI — PR Checks` muss grün sein.
   - Logs prüfen: `Scan bundle for secrets` Step muss `✓ Keine Secret-Muster im Bundle.` ausgeben.
2. **EAS Build (main):**
   - PR mergen auf `main`.
   - Workflow `EAS Build (iOS + Web)` muss starten.
   - `pnpm install --frozen-lockfile` muss erfolgreich durchlaufen (Pitfall 1 check).
   - `expo export --platform web` muss `app/dist/` erzeugen.
   - `eas build --platform ios --profile production --non-interactive --no-wait` muss einen Build auf expo.dev anstoßen (Link im Log).
3. **Bundle-Scan lokal reproduzieren:**
   ```bash
   pnpm --filter app exec expo export --platform web
   bash scripts/check-claude-key-in-bundle.sh app/dist
   # Exit 0 erwartet
   grep -r "CLAUDE_API_KEY\|sk-ant" app/dist || echo "clean"
   # "clean" erwartet
   ```
4. **Sentry-Testevent:**
   - App mit DSN starten: `EXPO_PUBLIC_SENTRY_DSN=... pnpm --filter app start`
   - In `app/index.tsx` temporär `throw new Error('sentry-smoke-test-' + Date.now());` im Render — einmalig.
   - Sentry Dashboard (EU-Org) → Issues → Event muss innerhalb ~2 Minuten erscheinen.
   - Danach: Throw wieder entfernen (Commit nicht nötig — war nur lokal).
5. **FOUND-06 Dreifach-Gate:**
   - `grep -r "CLAUDE_API_KEY" app/` → nur in Kommentaren/`.env.example` erlaubt (nicht in `src/` oder `app/`-Route-Code).
   - `grep -r "sk-ant" app/dist` → leer.
   - CI-Job `Scan bundle for secrets` in letztem main-Run grün.

**Resume-Signal:** Antworte mit "phase 1 verified" sobald alle fünf Unter-Checks bestanden sind. Bei Fehlschlag: Welche Stufe schlug wie fehl.

**Verify:**
```bash
# Dokumentation des Gates; keine automatische CLI-Verifikation
echo "Manual gate — see instructions above"
```

**Done:**
- CI-PR-Workflow grün auf Test-PR
- EAS Build auf main startet Web-Export + iOS-Build ohne manuelle Eingriffe
- `check-claude-key-in-bundle.sh` exitet 0 gegen produzierten `dist/`
- Sentry zeigt Test-Event in EU-Dashboard
- `grep -r "CLAUDE_API_KEY" app/` liefert nur Kommentare / .env.example

---

## Verification (gesamter Plan)

```bash
# Lokale Smoke-Tests
pnpm -r run typecheck
pnpm -r run test --passWithNoTests
pnpm --filter app exec expo export --platform web
bash scripts/check-claude-key-in-bundle.sh app/dist

# Remote
supabase functions list --project-ref $SUPABASE_PROJECT_REF | grep ai-job-consumer
supabase functions invoke ai-job-consumer --project-ref $SUPABASE_PROJECT_REF
```

## Success Criteria

1. CI-PR-Workflow läuft für jeden PR (lint + typecheck + test + bundle-scan) — grün
2. Main-Workflow produziert iOS-Build auf EAS + Web-Export in `app/dist/` — ohne manuelle Eingriffe
3. Bundle-Scan findet weder `sk-ant-` noch `CLAUDE_API_KEY` im Web-Bundle
4. Edge Function `ai-job-consumer` deployed; `supabase functions invoke` antwortet 200; E2E-Round-Trip persistiert eine Zeile in `ai_results`
5. Sentry empfängt Test-Event in EU-Region
6. Alle Phase-1-Success-Criteria aus ROADMAP sind grün (FOUND-01..08, NFR-06, NFR-08)

## Output

Nach Abschluss erstelle: `.planning/phases/01-foundation/01-03-SUMMARY.md`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions ↔ Expo/EAS | CI authentifiziert sich per `EXPO_TOKEN`; Builds laufen in EAS-Infrastruktur |
| client Web-Bundle ↔ Internet | Bundle ist öffentlich auslesbar → keine Secrets darin erlaubt |
| Edge Function (Deno) ↔ Anthropic API | Claude-API-Key lebt ausschließlich in `Deno.env` |
| Edge Function (service_role) ↔ Postgres | Bypasst RLS bewusst — Attack Surface, muss minimal gehalten werden |
| pgmq-Queue ↔ Edge Function | Payload ist untrusted (client-generated) — Validation erforderlich |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Information Disclosure | Claude-API-Key in Web-Bundle | mitigate | `scripts/check-claude-key-in-bundle.sh` grep auf `sk-ant-` + `CLAUDE_API_KEY` — im CI zwingend grün; DreiFach-Gate (Env-Trennung + CI + Bundle-Scan) in Task 1-03-05 |
| T-3-02 | Information Disclosure | Supabase `SERVICE_ROLE_KEY` im Bundle | mitigate | Gleiches Grep-Muster deckt `SUPABASE_SERVICE_ROLE_KEY` ab; Key steht nur in Edge-Function-Secrets, nie in `EXPO_PUBLIC_*` |
| T-3-03 | Tampering | Böser pgmq-Payload erzeugt Prompt-Injection oder beliebige Tool-Calls an Claude | mitigate (deferred) | Phase 1 ruft Claude nicht auf (Mock-Response). Phase 4 muss Payload via Zod validieren BEVOR er in den Claude-Prompt fließt. Vermerkt in Edge-Function-Code-Kommentar |
| T-3-04 | Denial of Service | Konsument verarbeitet eine Nachricht endlos (Crash-Loop) | mitigate | `visibility_timeout = 30s`; `read_ct`-Feld in pgmq zählt Retries; Edge Function setzt `status='failed'` nach Exception und archiviert nicht — Alert-Logik kommt in Phase 4 |
| T-3-05 | Repudiation | Fehlgeschlagene Jobs hinterlassen keinen Audit-Trail | mitigate | `ai_jobs.last_error` + `ai_results` (nur bei Success) + pgmq-Archive bei Success = klarer Trail |
| T-3-06 | Elevation of Privilege | GitHub-Actions-Runner greift auf SERVICE_ROLE_KEY zu | mitigate | SERVICE_ROLE_KEY ist KEIN GitHub-Secret. Nur `EXPO_TOKEN`, `SUPABASE_ACCESS_TOKEN` (für Migrations), `SENTRY_AUTH_TOKEN`. Service-Role lebt nur in Supabase-Function-Secrets |
| T-3-07 | Information Disclosure | Sentry schickt PII aus Events | mitigate (partial) | `tracesSampleRate: 1.0` für MVP ok (geringe Eventzahl); Phase 2+ muss `beforeSend`-Filter ergänzen, wenn PII-Breadcrumbs entstehen |
| T-3-08 | Tampering | Bösartiger PR modifiziert CI-Workflow und leaked Secrets | mitigate | GitHub Actions default: `pull_request` aus Forks hat KEINEN Zugriff auf Repo-Secrets; EAS-Build läuft nur auf `push: main`, also nach Review-Merge |

## ASVS L1 Controls Addressed

- V2.10.2 (Credentials Management): Claude-API-Key nur in Deno.env; keine `EXPO_PUBLIC_*`-Variable für Server-Keys
- V6.2.1 (Cryptographic Key Management): Keine Key-Rotation in Phase 1 — MVP akzeptiert manuelle Rotation via Dashboard
- V10.3.1 (Deployment — Verified Source): Bundle-Scan als CI-Gate vor jedem Release
- V14.1.3 (Build — Supply Chain): `pnpm install --frozen-lockfile` im CI erzwingt Lockfile-Integrität
- V14.2.1 (Configuration — Disable Dev Features in Prod): `ENVIRONMENT=prod` via eas.json unterscheidbar
</threat_model>
