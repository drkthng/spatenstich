---
phase: 07-plan-editor-drafts-integration-m2-m07-5
plan: 06
type: execute
wave: 5
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - supabase/migrations/20260513000018_plan_elements_layer.sql
  - .planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-HUMAN-VERIFY.md
  - .planning/STATE.md
  - .planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-CONTEXT.md
autonomous: true
requirements: [EDIT-12]
tags: [migration-push, supabase, human-verify, manual-smoke, wave5]

must_haves:
  truths:
    - "Migration file 20260513000018_plan_elements_layer.sql is committed to git (created in Plan 02) — verified via `git log -1 -- supabase/migrations/20260513000018_plan_elements_layer.sql` returning a non-empty commit hash before the push gate runs"
    - "Pre-flight Gate 1 (`supabase migration list --linked`) exits 0; output shows 20260513000018 in Local column only (Remote column empty/absent for it). If exit != 0 → checkpoint:human-action (auth required or unlinked project)"
    - "Pre-flight Gate 2 (`supabase db push --dry-run --linked --yes`) exits 0; output mentions migration 20260513000018 in the planned-to-apply list. If exit != 0 → checkpoint:human-action (migration SQL invariant or remote schema state divergence)"
    - "Real push (`supabase db push --linked --yes`) exits 0 with output indicating migration 20260513000018 applied; AND a follow-up `supabase migration list --linked` shows 20260513000018 in BOTH Local and Remote columns"
    - "07-HUMAN-VERIFY.md exists at .planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-HUMAN-VERIFY.md containing EXACTLY 4 manual checklist sections matching 07-VALIDATION.md §Manual-Only Verifications: (1) 60fps@200 elements EDIT-12, (2) Auto-save survives force-quit EDIT-09, (3) Bed-draft drag-to-canvas places element at finger position DRAFT-02, (4) Stale-import filter visual sanity DRAFT-03"
    - "STATE.md decisions section appended with a new line: '[Phase 07 P06] Migration 20260513000018 (plan_elements.layer) pushed to live Supabase project (ref vitrqkzxkiqvadqfzrcx, Frankfurt) — verified via `supabase migration list --linked` Local+Remote columns on YYYY-MM-DD'"
    - "07-CONTEXT.md decisions section appended with a small note confirming D-16 migration is now LIVE (status: applied, with the same project ref + date stamp)"
  artifacts:
    - path: ".planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-HUMAN-VERIFY.md"
      provides: "Deferred manual smoke checklist for Dirk (4 sections: 60fps, autosave-crash, bed-drop, stale-filter)"
      contains: "Performance Smoke — 60fps @ 200 Elementen"
    - path: "supabase/migrations/20260513000018_plan_elements_layer.sql"
      provides: "Migration 018 — already committed in Plan 02; this plan's push gate confirms it lands on the live database"
      contains: "plan_elements_layer_check"
    - path: ".planning/STATE.md"
      provides: "Decision log entry recording the live push of Migration 018"
      contains: "Phase 07 P06"
    - path: ".planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-CONTEXT.md"
      provides: "D-16 status update (migration is live)"
      contains: "Migration 018"
  key_links:
    - from: "supabase/migrations/20260513000018_plan_elements_layer.sql"
      to: "live Supabase project (ref vitrqkzxkiqvadqfzrcx, Frankfurt)"
      via: "supabase db push --linked --yes"
      pattern: "20260513000018"
    - from: ".planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-HUMAN-VERIFY.md"
      to: "07-VALIDATION.md §Manual-Only Verifications"
      via: "checklist sections 1:1 mirror VALIDATION manual table"
      pattern: "EDIT-12|EDIT-09|DRAFT-02|DRAFT-03"
    - from: ".planning/STATE.md"
      to: "Phase 7 phase tracking"
      via: "decisions section append"
      pattern: "Phase 07 P06"
---

<objective>
Close Phase 7 by **pushing Migration 018 to the live Supabase project** (the only schema change in Phase 7) and **writing the deferred manual smoke checklist (07-HUMAN-VERIFY.md)** for Dirk's iPhone-side verifications that cannot be automated in CI. This plan mirrors the 6.5 P05 pattern: a single autonomous push gate (with checkpoint:human-action fallbacks when gates fail), followed by markdown writes that surface the remaining EDIT-12 + EDIT-09 + DRAFT-02 + DRAFT-03 manual checks for the user to execute on a real device after the phase ships.

Purpose: Plans 01-05 build inert pieces — the migration SQL is committed but not applied, the editor compiles and renders in jest but has never been driven on an iPhone. Plan 06 turns the schema key (so SyncWorker can write `layer` column server-side without "column does not exist" errors on the next push of any plant element) and hands Dirk a clean, focused manual-checklist for the four behaviors that CI cannot prove: 60fps @ 200 elements (EDIT-12), autosave-survives-force-quit (EDIT-09), bed-draft-drag-drops-at-finger-position (DRAFT-02), stale-filter visual sanity (DRAFT-03).

Output: Live migration on Supabase (ref vitrqkzxkiqvadqfzrcx) + 1 new HUMAN-VERIFY.md + 2 markdown frontmatter/decisions updates in STATE.md + 07-CONTEXT.md. After this plan, `supabase migration list --linked` shows 20260513000018 in both Local and Remote columns; Dirk has a check-off list to run when he's next on the iPhone.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-CONTEXT.md
@.planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-VALIDATION.md
@.planning/phases/06.5-draft-sichtung-promotion/06.5-05-wire-and-push-PLAN.md
@supabase/migrations/20260513000018_plan_elements_layer.sql
@supabase/migrations/20260512000017_plan_elements_provenance.sql

<interfaces>
<!-- Supabase CLI command shapes (established by Phase 6.5 P05). -->

```bash
# Project ref: vitrqkzxkiqvadqfzrcx (Frankfurt) — from MEMORY: reference_supabase.md
# All commands run from repo root.

# Gate 1 — Pre-flight: confirm local has the migration, remote doesn't yet
supabase migration list --linked

# Gate 2 — Dry-run: simulate the push without changing remote state
supabase db push --dry-run --linked --yes

# Real push
supabase db push --linked --yes

# Post-push verification
supabase migration list --linked
```

The `--yes` flag suppresses the y/n confirmation prompt — critical for the autonomous gate. The `--linked` flag tells the CLI to use the currently linked project (Frankfurt ref `vitrqkzxkiqvadqfzrcx`); if the project is not linked (e.g., after `supabase link` was never run), Gate 1 exits non-zero with an error like `Cannot find project ref. Have you run supabase link?`.

For non-TTY environments where interactive auth prompts would otherwise block:
```bash
SUPABASE_ACCESS_TOKEN=<token-from-dashboard> supabase db push --linked --yes
```
</interfaces>

<gate-failure-handling>
**If Gate 1 (migration list) fails:**
- Cause is almost always: project not linked, or CLI not authenticated.
- The executor switches to `checkpoint:human-action` automatically (Task 2 below has the human-action variant inline).
- Resume signal: user runs `supabase link --project-ref vitrqkzxkiqvadqfzrcx` and `supabase login`, then replies "ready".

**If Gate 2 (dry-run) fails:**
- Cause is almost always: the migration's DO-block invariants detected an unexpected remote state (e.g., the `layer` column already exists in remote from a previous failed partial push).
- Read the error output — it states exactly which invariant raised.
- The executor switches to `checkpoint:human-action` so the user can inspect via Supabase Studio SQL editor before proceeding.
- Resume signal: user diagnoses + either fixes manually (drop the partial column) or reports back.

**If real push fails AFTER Gate 1 + Gate 2 both passed:**
- Network/transient issue. Re-run once. If still failing, switch to checkpoint:human-action.
- Resume signal: user retries manually and reports the outcome.
</gate-failure-handling>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Verify Migration 018 file is committed to git (pre-push sanity check)</name>
  <files>
    supabase/migrations/20260513000018_plan_elements_layer.sql
  </files>
  <read_first>
    - supabase/migrations/20260513000018_plan_elements_layer.sql (created in Plan 02 — confirm it exists and is committed)
    - .planning/STATE.md (check that Phase 07 P02 SUMMARY is logged as completed before pushing)
  </read_first>
  <action>
    **Sanity check — DO NOT push if any of these fail:**

    1. **File exists:**
       ```bash
       ls -la supabase/migrations/20260513000018_plan_elements_layer.sql
       ```
       Exit 0 expected.

    2. **File is committed to git:**
       ```bash
       git log -1 --pretty=format:"%h %s" -- supabase/migrations/20260513000018_plan_elements_layer.sql
       ```
       Must print a commit hash + message. If output is empty, the migration is uncommitted — STOP and direct the user to commit Plan 02's deliverables first.

    3. **File contains the expected invariants** (per Plan 02 must_haves):
       ```bash
       grep -c "plan_elements_layer_check" supabase/migrations/20260513000018_plan_elements_layer.sql
       ```
       Must print a number ≥ 1 (the CHECK constraint name).

    4. **No staged-but-uncommitted changes to the migration file:**
       ```bash
       git status --porcelain supabase/migrations/20260513000018_plan_elements_layer.sql
       ```
       Empty output expected. If non-empty, STOP — uncommitted local changes to a migration about to be pushed are a high-risk scenario (the file on disk may differ from what git has, and `supabase db push` reads from disk).

    All four checks must pass before proceeding to Task 2. If any fails, the executor halts the plan and reports the failure to the user with a clear remediation step (commit the file, drop staged changes, re-run Plan 02, etc.) — do NOT attempt to push a non-committed or modified migration.
  </action>
  <verify>
    <automated>cd /d/AiProjects/garden-app && test -f supabase/migrations/20260513000018_plan_elements_layer.sql && [ -n "$(git log -1 --pretty=format:%h -- supabase/migrations/20260513000018_plan_elements_layer.sql)" ] && grep -q plan_elements_layer_check supabase/migrations/20260513000018_plan_elements_layer.sql && [ -z "$(git status --porcelain supabase/migrations/20260513000018_plan_elements_layer.sql)" ] && echo "PRE_PUSH_SANE"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/20260513000018_plan_elements_layer.sql` exists on disk
    - `git log -1` for the migration file returns a non-empty commit hash (file is committed)
    - `grep plan_elements_layer_check` in the migration file returns ≥1 match
    - `git status --porcelain` for the migration file is empty (no uncommitted modifications)
    - Verify command prints `PRE_PUSH_SANE`
  </acceptance_criteria>
  <done>Migration 018 file is on disk, committed to git, unmodified, and contains the expected CHECK constraint marker — safe to push.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2 [BLOCKING]: Pre-flight Gate 1 — supabase migration list --linked (exit 0 expected)</name>
  <files>
    supabase/migrations/20260513000018_plan_elements_layer.sql
  </files>
  <read_first>
    - MEMORY reference_supabase.md (project ref `vitrqkzxkiqvadqfzrcx`, Frankfurt)
    - .planning/phases/06.5-draft-sichtung-promotion/06.5-05-wire-and-push-PLAN.md §Task 2 (analog push gate; same CLI shape, same gate-failure protocol)
  </read_first>
  <action>
    **Run Gate 1 — pre-flight migration list:**

    ```bash
    supabase migration list --linked
    ```

    Expected exit code: `0`.
    Expected output: a table with columns `Local | Remote | Time (UTC)` — the row `20260513000018 plan_elements_layer` should appear in the LOCAL column only (Remote column for that row should be empty / "--"). All earlier migrations (017 and below) should appear in BOTH columns.

    **Parse the output for the new migration:**

    The executor confirms by piping through grep:
    ```bash
    supabase migration list --linked | grep 20260513000018
    ```
    Must print a line containing `20260513000018` and the migration label.

    **Gate failure handling:**
    - If `supabase migration list --linked` exits non-zero (e.g., "project not linked", "not authenticated"):
      - This task converts to `checkpoint:human-action` (see Task 2-fallback below for the human-action variant).
      - DO NOT proceed to Task 3.

    **Gate success criteria:** exit 0 + grep finds the new migration ID in the local column.

    **Project ref:** `vitrqkzxkiqvadqfzrcx` (Frankfurt) — display this in the gate output for user trace per MEMORY feedback_show_project_id.md.
  </action>
  <verify>
    <automated>cd /d/AiProjects/garden-app && supabase migration list --linked 2>&1 | tee /tmp/gsd-p7p6-gate1.log && supabase migration list --linked 2>/dev/null | grep -q 20260513000018 && echo "GATE1_PASS project=vitrqkzxkiqvadqfzrcx"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase migration list --linked` exits 0
    - Output contains the literal `20260513000018` (the new migration ID) in the local column
    - Verify command prints `GATE1_PASS project=vitrqkzxkiqvadqfzrcx`
    - Earlier migration `20260512000017` (Phase 6.5 P05) is shown as applied in BOTH local and remote columns (sanity check that the project is correctly linked and previously-pushed migrations are visible)
  </acceptance_criteria>
  <done>Gate 1 green: project is linked, CLI authenticated, Migration 018 is locally pending and not yet on remote.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2-fallback (only if Task 2 failed): Manually authenticate Supabase CLI + re-link project</name>
  <what-built>
    Plan 06 Task 2 attempted `supabase migration list --linked` but exited non-zero — the CLI is either not authenticated, the project is not linked, or both.
  </what-built>
  <files>n/a (CLI configuration)</files>
  <read_first>
    - MEMORY reference_supabase.md (project ref `vitrqkzxkiqvadqfzrcx`, Frankfurt)
    - The error output from Task 2's failed command (executor includes it in the resume prompt)
  </read_first>
  <action>
    **This task ONLY runs if Task 2 exited non-zero. Otherwise, skip directly to Task 3.**

    The user needs to:

    1. Confirm the Supabase CLI is installed:
       ```bash
       supabase --version
       ```
       Should print v1.x or v2.x. If not installed, install via `brew install supabase/tap/supabase` or download from https://supabase.com/docs/guides/local-development/cli/getting-started.

    2. Authenticate the CLI:
       ```bash
       supabase login
       ```
       Opens a browser tab to confirm; the CLI then stores a session token. After login, `supabase projects list` should show the user's projects including `Kleingarten-App` (ref `vitrqkzxkiqvadqfzrcx`).

    3. Re-link the local repo to the project:
       ```bash
       supabase link --project-ref vitrqkzxkiqvadqfzrcx
       ```
       May prompt for the database password — see Supabase Dashboard → Settings → Database → Connection string → Password (or reset it if forgotten).

    4. Verify the link:
       ```bash
       supabase migration list --linked
       ```
       Should now exit 0 and print the migration table.

    **Alternative non-interactive path (CI-like):**

    ```bash
    export SUPABASE_ACCESS_TOKEN=<personal-access-token from Supabase Dashboard → Account → Access Tokens>
    supabase link --project-ref vitrqkzxkiqvadqfzrcx
    supabase migration list --linked
    ```

    **Why this is checkpoint:human-action:**
    - `supabase login` opens a browser, which Claude cannot drive.
    - `supabase link` may prompt for a database password that only the user knows.
    - A personal access token is a sensitive credential that should be set by the user, not Claude.

    **Resume signal:** Reply "linked" with the output of `supabase migration list --linked` showing the migration table. Then the executor re-runs Task 2's verify command and proceeds to Task 3.

    If the user cannot link (e.g., access lost, project deleted), reply "blocked: <reason>" — the executor halts the plan and surfaces the blocker to the user.
  </action>
  <verify>
    Manual: user reports "linked" with `supabase migration list --linked` output showing migration table.
  </verify>
  <acceptance_criteria>
    - User confirms `supabase migration list --linked` now exits 0
    - User confirms the output shows migration `20260513000018` in the local column
    - User confirms project ref `vitrqkzxkiqvadqfzrcx` is the linked project
    - User reply contains the literal string "linked" (or equivalent positive confirmation) OR "blocked: <reason>"
  </acceptance_criteria>
  <done>Supabase CLI authenticated + project linked; Task 2's gate can now pass.</done>
  <resume-signal>Reply "linked" with the supabase migration list output, OR "blocked: <reason>" if linking failed (e.g., access lost).</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3 [BLOCKING]: Pre-flight Gate 2 — supabase db push --dry-run --linked --yes (exit 0 expected)</name>
  <files>
    supabase/migrations/20260513000018_plan_elements_layer.sql
  </files>
  <read_first>
    - The Gate 1 output from Task 2 (confirming 018 is local-only)
  </read_first>
  <action>
    **Run Gate 2 — dry-run push:**

    ```bash
    supabase db push --dry-run --linked --yes
    ```

    Expected exit code: `0`.
    Expected output: lists Migration 20260513000018 as planned-to-apply; does NOT make any remote change; does NOT raise the DO-block invariants (because the dry-run plans against a snapshot of remote, not against actually executing the DO blocks).

    **Parse the output:**

    ```bash
    supabase db push --dry-run --linked --yes 2>&1 | tee /tmp/gsd-p7p6-gate2.log
    grep -E "(20260513000018|plan_elements_layer)" /tmp/gsd-p7p6-gate2.log
    ```
    Must print a line containing the migration ID or label.

    **Gate failure handling:**
    - If `supabase db push --dry-run --linked --yes` exits non-zero:
      - This usually means the migration SQL itself has a syntax error (caught only on dry-run, not at file-write time) OR a DO-block invariant raised when checked against remote schema state.
      - The executor switches to `checkpoint:human-action` (see Task 3-fallback below).
      - DO NOT proceed to Task 4.

    **Gate success criteria:** exit 0 + grep finds `20260513000018` in the dry-run output.
  </action>
  <verify>
    <automated>cd /d/AiProjects/garden-app && supabase db push --dry-run --linked --yes 2>&1 | tee /tmp/gsd-p7p6-gate2.log && grep -E "(20260513000018|plan_elements_layer)" /tmp/gsd-p7p6-gate2.log && echo "GATE2_PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push --dry-run --linked --yes` exits 0
    - Output contains the literal `20260513000018` OR `plan_elements_layer`
    - Verify command prints `GATE2_PASS`
    - Output does NOT contain `ERROR`, `EXCEPTION`, or `migration_018_invariant`
  </acceptance_criteria>
  <done>Gate 2 green: dry-run accepts the migration; safe to perform the real push in Task 4.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3-fallback (only if Task 3 failed): Diagnose dry-run failure</name>
  <what-built>
    Plan 06 Task 3 attempted `supabase db push --dry-run --linked --yes` but exited non-zero — the migration cannot be applied because either its SQL is invalid or the remote schema state already partially contains the change.
  </what-built>
  <files>supabase/migrations/20260513000018_plan_elements_layer.sql</files>
  <read_first>
    - The dry-run error output from Task 3's failed command
    - supabase/migrations/20260513000018_plan_elements_layer.sql (full file — to inspect the DO blocks)
    - Supabase Studio → SQL editor (browser-side; user-only)
  </read_first>
  <action>
    **This task ONLY runs if Task 3 exited non-zero. Otherwise, skip directly to Task 4.**

    Common causes + remediation:

    1. **SQL syntax error in the migration** (rare — Plan 02 should have caught this, but possible if a hand-edit slipped in):
       - Read the error from `/tmp/gsd-p7p6-gate2.log`.
       - Open `supabase/migrations/20260513000018_plan_elements_layer.sql` and fix the syntax.
       - Re-commit the fix.
       - Re-run Task 3's verify command.

    2. **DO-block invariant raised** (the migration self-asserts the expected pre/post state; output will print the assertion name, e.g., `migration_018_invariant_layer_already_exists`):
       - Likely cause: a previous partial migration push left `plan_elements.layer` already in remote, OR another phase added the column ahead of schedule.
       - Inspect via Supabase Studio SQL editor:
         ```sql
         SELECT column_name FROM information_schema.columns
           WHERE table_schema='public' AND table_name='plan_elements'
           ORDER BY ordinal_position;
         ```
       - If `layer` already exists with the expected type + CHECK constraint, the migration's job is already done — mark Migration 018 as applied in the migration table (manual upsert) OR revise the DO block to short-circuit when the column already exists.
       - If `layer` exists with the WRONG type or no CHECK constraint, this is a recovery scenario — user diagnoses + drops + re-creates manually, then marks Migration 018 as applied.

    3. **Auth / link drift** (re-occurrence of Task 2-fallback):
       - Re-run `supabase migration list --linked` to confirm the link still holds.
       - If it doesn't, perform Task 2-fallback steps again.

    **Why this is checkpoint:human-action:**
    - Diagnosing a DO-block invariant raise requires inspecting live remote schema via Supabase Studio (browser-only access).
    - Recovery scenarios (manual column drop, manual migration-table upsert) are destructive on production data — user-confirmation gate.
    - Claude does not have the user's database password to run psql commands directly against the production DB.

    **Resume signal:** Reply "resolved" with a short explanation of what was wrong + what was done. Then the executor re-runs Task 3's verify and proceeds to Task 4. Reply "blocked: <reason>" if recovery is not possible — the executor halts.
  </action>
  <verify>
    Manual: user reports "resolved" with diagnosis + remediation summary AND `supabase db push --dry-run --linked --yes` now exits 0.
  </verify>
  <acceptance_criteria>
    - User confirms `supabase db push --dry-run --linked --yes` now exits 0
    - User confirms the migration is now in a state where the real push will succeed (either the column was already correctly applied, or the migration table was manually upserted, or the SQL was corrected and re-committed)
    - User reply contains the literal string "resolved" or "blocked: <reason>"
  </acceptance_criteria>
  <done>Dry-run gate now passes; Task 4 real push can proceed.</done>
  <resume-signal>Reply "resolved" with a brief diagnosis + remediation summary, OR "blocked: <reason>" if recovery failed.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 4 [BLOCKING]: Real push — supabase db push --linked --yes + post-verify</name>
  <files>
    supabase/migrations/20260513000018_plan_elements_layer.sql
  </files>
  <read_first>
    - The Gate 1 + Gate 2 outputs (both passed)
  </read_first>
  <action>
    **Run the real push (Gates 1 + 2 both green):**

    ```bash
    supabase db push --linked --yes 2>&1 | tee /tmp/gsd-p7p6-push.log
    ```

    Expected exit code: `0`.
    Expected output: a "Pushing migrations..." progress + final confirmation like `Applied migration 20260513000018_plan_elements_layer.sql` (exact wording varies by CLI version, but the migration ID + "Applied" or "Pushed" or similar success indicator is present).

    **Post-push verification — confirm Migration 018 is in BOTH columns:**

    ```bash
    supabase migration list --linked 2>&1 | tee /tmp/gsd-p7p6-postcheck.log
    grep 20260513000018 /tmp/gsd-p7p6-postcheck.log
    ```
    Must print a single line where `20260513000018` appears with a non-empty Remote column value (timestamp or "Applied" marker).

    Additionally, run a quick column-presence smoke (uses the same dry-run trick to check schema without writing):
    ```bash
    supabase db push --dry-run --linked --yes 2>&1 | grep -i "Local database is up to date" && echo "POST_PUSH_SCHEMA_SYNCED"
    ```
    Expected: prints `POST_PUSH_SCHEMA_SYNCED` (meaning no further migrations pending; remote = local).

    **If the real push fails AFTER both gates passed:**
    - This is the rarest case — usually a network blip or a Supabase API transient.
    - Re-run the real push command ONCE.
    - If it still fails, switch to checkpoint:human-action (the executor surfaces the error and asks the user to investigate via Supabase Studio).

    **Project ref reminder:** `vitrqkzxkiqvadqfzrcx` (Frankfurt) — displayed in the push gate output trace.
  </action>
  <verify>
    <automated>cd /d/AiProjects/garden-app && supabase db push --linked --yes 2>&1 | tee /tmp/gsd-p7p6-push.log && supabase migration list --linked 2>&1 | grep 20260513000018 && supabase db push --dry-run --linked --yes 2>&1 | grep -qi "up to date" && echo "PUSH_VERIFIED project=vitrqkzxkiqvadqfzrcx migration=20260513000018"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push --linked --yes` exits 0
    - `/tmp/gsd-p7p6-push.log` indicates Migration 20260513000018 was applied
    - `supabase migration list --linked` shows 20260513000018 with a Remote-column timestamp (or non-empty Remote marker)
    - A follow-up dry-run reports "Local database is up to date" (no further pending migrations)
    - Verify command prints `PUSH_VERIFIED project=vitrqkzxkiqvadqfzrcx migration=20260513000018`
  </acceptance_criteria>
  <done>Migration 018 is now LIVE on the Supabase project (vitrqkzxkiqvadqfzrcx, Frankfurt); `plan_elements.layer` column exists with NOT NULL DEFAULT 'infrastructure' + CHECK constraint.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Write 07-HUMAN-VERIFY.md (4-section manual smoke checklist)</name>
  <files>
    .planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-HUMAN-VERIFY.md
  </files>
  <read_first>
    - .planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-VALIDATION.md §"Manual-Only Verifications" (lines 89-97) — the canonical source for the 4 manual checks: EDIT-12 60fps, EDIT-09 autosave-crash, DRAFT-02 bed-drop, DRAFT-03 stale-filter
    - .planning/phases/06.5-draft-sichtung-promotion/06.5-05-wire-and-push-PLAN.md §Task 3 (analog manual smoke pattern with EXPECTED + FAIL signal lines)
    - .planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-CONTEXT.md §D-04 (200-element budget, real iPhone)
  </read_first>
  <action>
    **Write `.planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-HUMAN-VERIFY.md`** using the Write tool. The file's content is the four manual checks below — 1:1 mirror of 07-VALIDATION.md §Manual-Only Verifications, but expanded with step-by-step instructions, EXPECTED outcomes, FAIL signals, and reply format. German UTF-8 in any visible-to-user strings; checklist language is English (planning artifact, not shipped UI).

    ```markdown
    ---
    phase: 07-plan-editor-drafts-integration-m2-m07-5
    plan: 06
    type: human-verify
    requirements_covered: [EDIT-09, EDIT-12, DRAFT-02, DRAFT-03]
    status: pending
    created: 2026-05-12
    ---

    # Phase 7 — Human-Verify Manual Smoke Checklist

    > Four behaviors that CI cannot prove. To be run by Dirk on his iPhone (or in the iOS Simulator if good enough). Each section ends with a pass/fail reply line for `/gsd-verify-work` to log.
    >
    > Project Supabase ref: `vitrqkzxkiqvadqfzrcx` (Frankfurt) — visible in the bottom of the iOS app's debug overlay if enabled.

    ---

    ## Section 1 — Performance Smoke — 60fps @ 200 Elementen (EDIT-12)

    **What this verifies:** The Skia canvas + composed gestures hold 60 fps median while panning/pinching a 200-element garden on a real iPhone.

    **Setup:**
    1. Build a dev client on iPhone:
       - Option A (preferred): `pnpm --filter app exec eas build --profile development --platform ios` and install via TestFlight.
       - Option B (faster, dev-only): `pnpm --filter app exec expo run:ios --device` if the user has a Mac + Xcode set up.
    2. Seed the garden with 200 mixed elements. Two routes:
       - **Route A (manual seed script — preferred):** Run `pnpm --filter app exec ts-node scripts/seed-200.ts` (script TBD; place in `app/scripts/seed-200.ts` if not yet present — Phase 8 may formalize). Generates: 5 beds (Beet), 180 plants (Pflanze) inside the beds, 15 infrastructure items (3 Weg, 2 Laube, 2 Zaun, 4 Baum, 2 Kompost, 1 Wasserstelle, 1 Sitzplatz).
       - **Route B (manual UI placement — fallback):** Place ~200 items by hand via the Element-Palette. Tedious but works if the seed script is not yet implemented.
    3. Open the editor (Home → "Plan öffnen").
    4. (Optional) Enable in-app FPS overlay if implemented; alternatively connect Xcode Instruments → Time Profiler trace.

    **Steps:**
    1. Pan the canvas continuously for 30 seconds (swipe left/right/up/down without lifting finger).
    2. Pinch-to-zoom in and out for another 30 seconds.
    3. Tap an element to select it, drag to move (gestureActive bypass should prevent autosave during drag).

    **Expected:**
    - Median frame rate ≥ 58 fps across the 60-second pan/pinch session (target: 60 fps; ≥ 58 is "no visible jank" per Apple HIG).
    - No frame longer than 33 ms (= no skipped frames at 60 Hz).
    - Skia worklet does not show JS-thread blocking warnings in console.

    **Fail signals:**
    - FPS drops below 50 during pan/pinch → investigate Pitfall-6 (inner-group transforms instead of single outer Group transform) or Pitfall-12 (Pan onUpdate writes to JS state instead of shared values).
    - Console shows "JS thread blocked > 100 ms" → setState slipped into a worklet (Pattern 9 anti-pattern).

    **Trace artifact:** Save the Xcode Instruments trace as `.planning/phases/07-plan-editor-drafts-integration-m2-m07-5/instruments-trace-200el.trace` (or `.zip` if the directory is unwieldy). Optional but strongly recommended for retrospective.

    **Reply format:**
    - Pass: `approved — median FPS XX, trace at <path>.`
    - Fail: `fail — median FPS XX, jank observed at <action>, console errors: <list>.`

    ---

    ## Section 2 — Auto-Save Crash Recovery (EDIT-09)

    **What this verifies:** The 5-second debounced auto-save persists element mutations BEFORE a force-quit when ≥ 5 seconds have elapsed, and does NOT persist when the force-quit happens within 4 seconds (proving the debounce is real, not just a noop).

    **Setup:**
    1. Same dev client + seeded garden from Section 1 (or skip seeding; this test works on any non-empty garden).
    2. Make sure the device is in airplane mode OR the SyncWorker is observably quiet (so the only writer is the local autosave debounce, not a server roundtrip).

    **Steps — round 1 (debounce respects window):**
    1. Open the editor.
    2. Place a new element from the palette (long-press Beet card → drop on canvas).
    3. **Within 4 seconds** of placing, force-quit the app (swipe up + flick away the app card).
    4. Re-open the app and navigate to the editor.

    **Expected (round 1):** The element placed in step 2 is NOT present (the 5-second debounce did not yet fire; write was discarded with the app).

    **Steps — round 2 (debounce fires after window):**
    5. Place another new element (long-press a different palette item → drop).
    6. **Wait 6 seconds** without touching the app.
    7. Force-quit the app.
    8. Re-open and navigate to the editor.

    **Expected (round 2):** The element placed in step 5 IS present (the 5-second debounce fired at second 5; write was persisted to expo-sqlite before the quit).

    **Steps — round 3 (manual save bypasses debounce):**
    9. Place a new element.
    10. **Immediately tap the Save button** in the toolbar (testID `editor-save-button` shows "Wird gespeichert …" briefly).
    11. Force-quit within 1 second of seeing "Gespeichert" badge.
    12. Re-open and navigate to the editor.

    **Expected (round 3):** The element placed in step 9 IS present (manual save flushed the debounce synchronously).

    **Fail signals:**
    - Round 1 shows the element persisted → debounce timer is too short OR auto-save fires synchronously.
    - Round 2 shows the element absent → debounce timer is too long OR autosave is dropping writes.
    - Round 3 shows the element absent → manual save button does NOT call `flushAllPendingSaves` correctly.

    **Reply format:**
    - Pass: `approved — round 1/2/3 all match expected. Debounce works.`
    - Fail: `fail — round N: <observed vs expected>.`

    ---

    ## Section 3 — Bed-Draft Drag-to-Canvas Places Element at Finger Position (DRAFT-02)

    **What this verifies:** Dragging a bed-draft from the Drafts-Tray onto the canvas creates a real Beet element at the exact finger position (within ±0.5 m tolerance, accounting for finger contact-point centering).

    **Setup:**
    1. Import a sample bed-draft via the Phase 6.5 flow:
       - Use a JSON payload with at least one entry in `beds[]` — e.g., `supabase/spatenstich-import-v1/examples/full.json`.
       - Trigger import via the existing entry point (`Aus Claude.ai importieren` from Home).
       - On the Sichtungs-Screen, do NOT promote the bed yet — leave it in the `pending` state.
    2. Navigate to the editor via the new `Plan öffnen` CTA on Home (added in Plan 05 Task 4).
    3. Open the Drafts-Tray by tapping the chip "Letzte Importe (1)" at the bottom-left.

    **Steps:**
    1. Confirm the DraftsTrayBottomSheet expands and shows the bed-draft card under "Beete (1)".
    2. Long-press the bed-draft card (≥ 300 ms hold).
    3. While holding, drag your finger to a specific position on the canvas — choose a visually distinctive location (e.g., 2 meters from the left edge, 3 meters from the top edge in garden coordinates; use the 1×1 m grid as a reference).
    4. Release the finger.

    **Expected:**
    - A new Beet element appears on the canvas centered at (approximately) the release-finger position.
    - The center coordinate of the element, measured in garden meters (toolbar → tap element → read tooltip or x/y in inspector), is within ±0.5 m of the intended location.
    - The DraftsTrayBottomSheet re-loads; the just-dragged draft is no longer in the list (its status flipped to `promoted`).
    - The new Beet has `provenance.kind === 'import'` and `importedFrom === <draft.id>` (verifiable via expo-sqlite inspector OR Supabase Studio after the next sync).

    **Fail signals:**
    - Element appears at the garden origin (0, 0) → `finalCoords` was not passed through to `promoteBedDraft`; the 6-arg form was ignored. Verify by `grep -n "promoteBedDraft(" app/app/(app)/plan/index.tsx` and confirm the 6th argument is `{xM, yM}`.
    - Element appears but draft is NOT removed from the tray → `dismissDraft` or status update did not fire.
    - Element appears off by >2 m from finger → coord system bug; check `screenToGarden` in `viewMatrix.ts` (Plan 03), specifically the tx/ty signs.

    **Reply format:**
    - Pass: `approved — bed dropped at (X.X, Y.Y) m, target was (X.X, Y.Y) m, delta < 0.5m. Draft promoted, provenance.kind=import.`
    - Fail: `fail — <observed behavior>; expected <target>.`

    ---

    ## Section 4 — Stale-Import Filter Visual Sanity (DRAFT-03)

    **What this verifies:** Drafts older than 30 days render with a "Stale" amber TrafficLightBadge in the Drafts-Tray, and the filter chips correctly partition the visible drafts.

    **Setup:**
    1. Insert (or have existing) at least 2 drafts with different `imports.imported_at` timestamps:
       - One fresh: imported_at = today (or within the last few days).
       - One stale: imported_at = 31+ days ago.
    2. Easiest seeding path (manual via Supabase Studio SQL editor — user-only operation):
       ```sql
       INSERT INTO public.imports (id, garden_id, source, imported_at, version, ...)
         VALUES (gen_random_uuid(), '<active-garden-id>', 'claude-ai-project', now() - interval '31 days', 'spatenstich-import.v1', ...);
       INSERT INTO public.import_items (id, import_id, garden_id, kind, ...) VALUES (...);
       INSERT INTO public.bed_drafts (id, import_item_id, garden_id, label, status, ...)
         VALUES (gen_random_uuid(), '<import-item-id>', '<garden-id>', 'Altes Beet', 'pending', ...);
       ```
       Replace `<active-garden-id>` etc. as appropriate. After inserts, the SyncWorker pull will surface the rows in the next round, OR manually trigger a sync from the dev menu.

    **Steps:**
    1. Open the editor and expand the Drafts-Tray.
    2. Filter is `Alle` by default — confirm both drafts are visible (one with Stale badge, one without).
    3. Tap the `Stale` filter chip.
       - **Expected:** Only the stale draft is visible. The fresh draft is hidden.
       - **Visual check:** The stale draft card shows an amber TrafficLightBadge with label "Stale". Tooltip / hint text reads "Älter als 30 Tage — prüfe vor dem Übernehmen." (UTF-8 Umlaute — verify "Älter" and "prüfe" render correctly).
    4. Tap the `Aktuell` filter chip.
       - **Expected:** Only the fresh draft is visible. The stale draft is hidden.
    5. Tap the `Alle` filter chip.
       - **Expected:** Both drafts visible again.
    6. Long-press the stale draft → drag onto canvas (same path as Section 3).
       - **Expected:** Promotion succeeds. The draft is removed from the tray. Confirm DRAFT-03 invariant "nie auto-gelöscht" by checking that the stale draft was REMOVED only because the user promoted it (not auto-cleaned).
    7. Verify no auto-delete: Wait 30+ seconds idle. The remaining stale drafts (if any) MUST still be present in the tray. The app must not silently dismiss them.

    **Expected (overall):**
    - Amber Stale badge appears on >30-day drafts.
    - Filter chips Alle / Aktuell / Stale show the correct partition at each tap.
    - No drafts are auto-deleted by the app (D-15 invariant).
    - German UI text uses literal Umlaute (Älter, prüfe, Übernehmen — NOT ASCII replacements).

    **Fail signals:**
    - Stale badge missing on the >30-day draft → check `isStale(importedAt)` math in `DraftsTrayBottomSheet.tsx`; ensure `STALE_MS = 30 * 24 * 60 * 60 * 1000` and `Date.now() - new Date(importedAt).getTime() > STALE_MS`.
    - Filter chip "Stale" shows fresh drafts too → filter handler is inverted; check the `passes(d)` predicate.
    - German text shows "Aelter" or "pruefe" → ASCII replacement bug; check `packages/shared/src/i18n/de.json` for the editor.tray.staleHint key.
    - Drafts disappear after some idle time → auto-delete logic was introduced somewhere; this violates D-15 — find and remove.

    **Reply format:**
    - Pass: `approved — stale badge correct, filter chips correct, no auto-delete observed, UTF-8 Umlaute literal.`
    - Fail: `fail — <observed>.`

    ---

    ## Summary checkbox

    After all four sections pass, mark this file's frontmatter `status: passed`. Until then, leave `status: pending`.

    - [ ] Section 1 — 60fps @ 200 elements (EDIT-12)
    - [ ] Section 2 — Auto-save crash recovery (EDIT-09)
    - [ ] Section 3 — Bed-draft drag-to-canvas drop accuracy (DRAFT-02)
    - [ ] Section 4 — Stale-import filter + no-auto-delete (DRAFT-03)

    ---

    *Phase: 07-plan-editor-drafts-integration-m2-m07-5*
    *Created by: gsd-plan-phase (Plan 06)*
    *To be run by: Dirk on iPhone (or simulator) after Plan 06 schema push completes.*
    ```
  </action>
  <verify>
    <automated>cd /d/AiProjects/garden-app && test -f .planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-HUMAN-VERIFY.md && grep -q "Section 1 — Performance Smoke" .planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-HUMAN-VERIFY.md && grep -q "Section 2 — Auto-Save" .planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-HUMAN-VERIFY.md && grep -q "Section 3 — Bed-Draft Drag" .planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-HUMAN-VERIFY.md && grep -q "Section 4 — Stale-Import" .planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-HUMAN-VERIFY.md && grep -q "EDIT-12" .planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-HUMAN-VERIFY.md && grep -q "DRAFT-02" .planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-HUMAN-VERIFY.md && grep -q "vitrqkzxkiqvadqfzrcx" .planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-HUMAN-VERIFY.md && grep -Fq "Älter" .planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-HUMAN-VERIFY.md && echo "HUMAN_VERIFY_WRITTEN"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-HUMAN-VERIFY.md` exists
    - Contains exactly the 4 section headers: "Section 1 — Performance Smoke", "Section 2 — Auto-Save", "Section 3 — Bed-Draft Drag", "Section 4 — Stale-Import"
    - Contains the requirement IDs EDIT-09, EDIT-12, DRAFT-02, DRAFT-03 (all four explicitly named in section bodies)
    - Contains the project ref `vitrqkzxkiqvadqfzrcx` for trace
    - Contains literal UTF-8 Umlaut `Älter` (sanity check that the file was written without ASCII-replacement)
    - Contains a summary checkbox section with 4 unchecked checkboxes
    - Frontmatter has `status: pending` (user updates to `passed` after running)
    - Verify command prints `HUMAN_VERIFY_WRITTEN`
  </acceptance_criteria>
  <done>07-HUMAN-VERIFY.md is in place with the 4 deferred checks; user can run when convenient on a real iPhone.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Append decisions to STATE.md + 07-CONTEXT.md recording the live migration push</name>
  <files>
    .planning/STATE.md,
    .planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-CONTEXT.md
  </files>
  <read_first>
    - .planning/STATE.md (locate the decisions section / append at the end with a timestamped entry)
    - .planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-CONTEXT.md (decisions section — D-16 mentions Migration 018 will be pushed; add a status note)
  </read_first>
  <action>
    **Edit 1 — append to STATE.md decisions section** (or at the end of the file if no decisions section exists yet; the executor reads the file to determine the best insertion point):

    Add a line in the format established by prior phases (06.5 P05 used a similar pattern):

    ```markdown
    - [Phase 07 P06] Migration 20260513000018 (plan_elements.layer) pushed to live Supabase project (ref `vitrqkzxkiqvadqfzrcx`, Frankfurt) — verified via `supabase migration list --linked` showing Local + Remote columns populated. Date: 2026-05-12.
    ```

    Use today's actual date (`date +%Y-%m-%d` if the executor wants the live date — or the date the push actually ran).

    **Edit 2 — append a status note to 07-CONTEXT.md D-16:**

    Locate the D-16 paragraph in 07-CONTEXT.md (around line 44, in the `### Data Model` block). The current text describes Migration 018 as planned. Append a single line at the END of D-16's text:

    ```markdown
    **Status (Plan 06):** APPLIED. Live on Supabase project `vitrqkzxkiqvadqfzrcx` as of 2026-05-12.
    ```

    Keep the original D-16 wording intact; this is an additive marker. Use the Edit tool with a precise old-string / new-string to ensure no other content shifts.

    **Why these markdown updates matter:**
    - STATE.md is the canonical state-of-the-project log read by every future planner/checker — recording the live push prevents future plans from attempting to push 018 a second time.
    - 07-CONTEXT.md D-16 status flip provides a quick lookup signal during checker review: "Is Migration 018 live?" → check D-16 line.
  </action>
  <verify>
    <automated>cd /d/AiProjects/garden-app && grep -q "Phase 07 P06" .planning/STATE.md && grep -q "20260513000018" .planning/STATE.md && grep -q "vitrqkzxkiqvadqfzrcx" .planning/STATE.md && grep -q "Status (Plan 06): APPLIED" .planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-CONTEXT.md && echo "DECISIONS_LOGGED"</automated>
  </verify>
  <acceptance_criteria>
    - .planning/STATE.md contains literal `Phase 07 P06`
    - .planning/STATE.md contains literal `20260513000018`
    - .planning/STATE.md contains literal `vitrqkzxkiqvadqfzrcx`
    - .planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-CONTEXT.md contains literal `Status (Plan 06): APPLIED`
    - .planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-CONTEXT.md still contains the original D-16 text (additive change — verify by grepping for `Migration 018` count >= 2: the original mention + the new status note)
    - Verify command prints `DECISIONS_LOGGED`
  </acceptance_criteria>
  <done>STATE.md + 07-CONTEXT.md decisions updated; future planners/checkers will see Migration 018 as live without re-attempting the push.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local CLI session → live Supabase project (`vitrqkzxkiqvadqfzrcx`, Frankfurt) | Push command writes to production schema; user-authenticated session via `supabase login` |
| Migration file (on disk) → applied SQL on remote | What `db push` reads is the file content at push time, not the git HEAD — pre-flight Task 1 enforces `git status --porcelain` empty |
| HUMAN-VERIFY checklist | Documentation artifact; no runtime trust surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-25 | Tampering | Local migration file diverges from committed git version (push reads file content, not git) | mitigate | Task 1 enforces `git status --porcelain supabase/migrations/20260513000018_plan_elements_layer.sql` is empty before any push command runs. If non-empty, the plan halts. |
| T-07-26 | Denial of Service | Real push runs during active user session | accept | 2-user MVP; Migration 018 is an `ALTER TABLE ADD COLUMN NOT NULL DEFAULT 'infrastructure'` + `CHECK` constraint. Postgres handles ADD COLUMN with DEFAULT in a non-blocking manner since v11. CHECK constraint is `NOT VALID`-friendly if needed (but Plan 02's invariants will surface any issue at dry-run, not at real push). |
| T-07-27 | Tampering | DO-block invariants raised on real push (after dry-run passed) | accept (low) | If a dry-run passes and a real push raises, the remote state diverged between dry-run and push (e.g., another admin ran SQL in Studio). For a solo-developer 2-user MVP, this race is essentially impossible. If observed, Task 4's exception-handling path switches to checkpoint:human-action. |
| T-07-28 | Information Disclosure | `SUPABASE_ACCESS_TOKEN` accidentally committed to git in Task 2-fallback / Task 3-fallback | mitigate | The fallback tasks show the token as `<token-from-dashboard>` placeholder ONLY; explicit instruction NOT to commit. Personal access tokens are sensitive but the failure mode is severable (revoke + reissue from Dashboard). |
| T-07-29 | Repudiation | Push happens but no audit log entry | mitigate | Task 6 appends to STATE.md AND 07-CONTEXT.md with the project ref + date. Combined with Supabase's own migration table (`supabase_migrations.schema_migrations`), there are 3 audit trails. |
| T-07-30 | Spoofing | HUMAN-VERIFY.md is edited by a non-user before Dirk runs it | accept | File lives in `.planning/` checked into git; tampering would show in git diff. Trust boundary is "Dirk reads his own planning docs". |
</threat_model>

<verification>
- Sanity: `git log -1 -- supabase/migrations/20260513000018_plan_elements_layer.sql` returns a non-empty hash.
- Gate 1: `supabase migration list --linked` exits 0 and shows 20260513000018 in the Local column.
- Gate 2: `supabase db push --dry-run --linked --yes` exits 0 and includes 20260513000018 in the planned-to-apply list.
- Real push: `supabase db push --linked --yes` exits 0; follow-up `migration list --linked` shows 20260513000018 in BOTH columns.
- File: `.planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-HUMAN-VERIFY.md` exists with 4 section headers + project ref + UTF-8 Älter.
- STATE.md + 07-CONTEXT.md contain the decision-log markers.
- Project ref `vitrqkzxkiqvadqfzrcx` (Frankfurt) is displayed in the push gate output trace and recorded in all decision-log markers.
</verification>

<success_criteria>
- Migration 20260513000018 is live on Supabase project `vitrqkzxkiqvadqfzrcx` (Frankfurt) — confirmed by `migration list --linked` showing Local + Remote.
- 07-HUMAN-VERIFY.md exists with the 4 deferred manual checks (EDIT-12 60fps, EDIT-09 autosave-crash, DRAFT-02 bed-drop, DRAFT-03 stale-filter).
- STATE.md decisions section has a new entry for Phase 07 P06 with the migration ID + project ref + date.
- 07-CONTEXT.md D-16 has a `Status (Plan 06): APPLIED` note.
- All push gate pre-flights green; no checkpoint:human-action paths were activated (or, if they were, they were resolved successfully before the real push).
- No regressions: `pnpm --filter app test` still green (no schema-related test failures triggered by the live column add).
</success_criteria>

<output>
After completion, create `.planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-06-SUMMARY.md` documenting:
- The exact output of `supabase migration list --linked` (post-push) for the Local + Remote columns showing 20260513000018.
- The exact output of `supabase db push --linked --yes` (real push) success line.
- The project ref + date stamp logged in STATE.md and 07-CONTEXT.md.
- A pointer to 07-HUMAN-VERIFY.md with the note that the 4 deferred checks remain pending until Dirk runs them on a real iPhone.
- Phase 7 completion status: all 14 requirement IDs (EDIT-01..09, EDIT-11, EDIT-12, DRAFT-01..03) have either passing automated tests OR a corresponding entry in HUMAN-VERIFY.md.

Additionally, the SUMMARY.md may flag: "Phase 7 ready for `/gsd-verify-work`. Manual smoke (07-HUMAN-VERIFY.md) is deferred to user; verify-work will note EDIT-12 + EDIT-09 + DRAFT-02 + DRAFT-03 manual-pass as outstanding until the user reports back."
</output>
