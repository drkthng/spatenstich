---
phase: 10-aussaatkalender-v1
plan: "08"
type: execute
wave: 2
depends_on: ["10-06"]
files_modified:
  - app/src/hooks/useKalenderData.ts
  - app/src/hooks/__tests__/useKalenderData.test.ts
autonomous: true
gap_closure: true
requirements: [CAL-04, CAL-05]
must_haves:
  truths:
    - "addPlantToPlan platziert die neue Pflanze INNERHALB eines bestehenden Beets, sodass findBeeteForPlant sie danach findet (CAL-05 löst CAL-04 ein)"
    - "Bei Gartenwechsel überschreiben verspätete loadData-Antworten des alten Gartens NICHT die Daten des neuen Gartens (cancelled-Flag)"
    - "Wird activeGardenId null, werden elements und dimensions zurückgesetzt (keine stale Daten)"
    - "addPlantToPlan validiert den Account-Modus explizit statt mode! zu erzwingen"
  artifacts:
    - path: "app/src/hooks/useKalenderData.ts"
      provides: "In-Bed-Placement, cancelled-Flag im Lade-Effekt, expliziter mode-Guard"
      contains: "cancelled"
    - path: "app/src/hooks/__tests__/useKalenderData.test.ts"
      provides: "In-Bed-Placement-Test + Reset-bei-null-Test"
      contains: "im Beet"
  key_links:
    - from: "app/src/hooks/useKalenderData.ts"
      to: "app/src/lib/kalenderBeete.ts"
      via: "Platzierung via Beet-Geometrie, von findBeeteForPlant auffindbar"
      pattern: "parentBedId|findBeeteForPlant|beetToPolygon"
    - from: "app/src/hooks/useKalenderData.ts"
      to: "loadAcceptedElements"
      via: "cancelled-Flag im useEffect verhindert Race"
      pattern: "cancelled"
---

<objective>
Schließt WR-06 (`addPlantToPlan` platziert via `nextFreeBedSlot` NEBEN/außerhalb der Beete → "Auf welchem Beet?" zeigt danach weiterhin "Noch nicht im Plan"), WR-07 (Lade-`useEffect` ohne cancelled-Flag → Race bei Gartenwechsel + stale Elemente bei `activeGardenId = null`) und IN-04 (`mode!` Non-null-Assertion) im Daten-Layer-Hook `app/src/hooks/useKalenderData.ts`.

WR-06 ist der Kern des CAL-05→CAL-04-Versprechens: Der CTA "Zu Plan hinzufügen" verspricht, die Pflanze dem Plan hinzuzufügen, aber `nextFreeBedSlot` ist ein Beet-Stacking-Layouter (platziert NEBEN bestehenden Beeten mit 50 cm Lücke, vermeidet explizit Überlappung). Für eine 0,3×0,3-Pflanze landet sie außerhalb jedes Beets → `findBeeteForPlant` findet sie nicht → "Auf welchem Beet?" bleibt leer trotz Erfolgs-Banner. WR-07: Bei Gartenwechsel können out-of-order auflösende Antworten des alten Gartens die Daten des neuen überschreiben; bei `activeGardenId = null` bleiben Alt-Daten im State.

Wave 2, hängt von 10-06 ab: Die In-Bed-Platzierung nutzt die dort korrigierte Beet-Geometrie-Konvention (center ± half), damit die platzierte Pflanze tatsächlich im Beet-Polygon liegt und von `findBeeteForPlant` gefunden wird. Kein File-Overlap mit dem parallelen Wave-2-Plan 10-07.

Purpose: CAL-05 löst CAL-04 tatsächlich ein (Pflanze landet in einem Beet, "Auf welchem Beet?" zeigt es); kein Race/Stale bei Gartenwechsel; Typsicherheit im Write-Pfad.
Output: In-Bed-Placement in `addPlantToPlan`, cancelled-Flag + Reset im Lade-Effekt, expliziter mode-Guard, neue Tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-aussaatkalender-v1/10-REVIEW.md
@.planning/phases/10-aussaatkalender-v1/10-02-SUMMARY.md
@app/src/lib/kalenderBeete.ts
@app/src/hooks/useCompanionDetection.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WR-06 In-Bed-Placement + IN-04 expliziter mode-Guard in addPlantToPlan</name>
  <files>app/src/hooks/useKalenderData.ts, app/src/hooks/__tests__/useKalenderData.test.ts</files>
  <read_first>
    - app/src/hooks/useKalenderData.ts (zu ändernde Datei — `addPlantToPlan` Z. 153-189, speziell `nextFreeBedSlot` Z. 159 und `mode!` Z. 184)
    - app/src/lib/kalenderBeete.ts (nach Plan 10-06 — `beetToPolygon`/center-Konvention; `getPlantSlug`; ggf. `findPflanzenInBeet`)
    - app/src/hooks/useCompanionDetection.ts (Source-of-Truth: `findBedForPlant` nutzt `provenance.parentBedId` Fast-Path Z. 86-88 + Pflanzen-Center `{x: xM, y: yM}`; `getBedPolygon` center ± half)
    - app/src/lib/draftPromotionRepo.ts (`nextFreeBedSlot` Z. 58-80 — beweist: platziert NEBEN Beeten, nicht IN; nur als Negativ-Referenz lesen)
    - .planning/phases/10-aussaatkalender-v1/10-REVIEW.md (WR-06 Z. 157-161 mit Fix-Empfehlung; IN-04 Z. 210-214)
  </read_first>
  <behavior>
    - WR-06 (RED→GREEN): Nach `addPlantToPlan(plant)` liegt der Mittelpunkt `{x: xM, y: yM}` des neuen Pflanze-Elements INNERHALB des ersten nicht-gelöschten Beets (per `pointInPolygon` gegen `beetToPolygon` des Beets). Test: Plan mit einem Beet (Center xM=2,yM=2,w=2,h=2 → Polygon [1,1]→[3,3]); nach addPlantToPlan erfüllt das geschriebene Element `pointInPolygon({x: el.xM, y: el.yM}, beetPolygon)` === true. Schlägt mit dem alten `nextFreeBedSlot` fehl (außerhalb), grün nach Fix.
    - WR-06 parentBedId: Das geschriebene Element trägt `provenance.parentBedId === <id des Ziel-Beets>` (D-03 Fast-Path, den findBedForPlant konsumiert) zusätzlich zu `plantSlug`.
    - IN-04 (RED→GREEN): Wird `addPlantToPlan` im lokalen Modus aufgerufen (`mode='local'`), wirft es einen aussagekräftigen Fehler (`account_erforderlich` o. ä.) VOR dem Write, statt `mode!` blind an writePlanElement zu übergeben. Test: mode='local' → addPlantToPlan rejects mit erkennbarem Fehler; writePlanElement NICHT aufgerufen.
    - Regression: bestehender Test "addPlantToPlan wirft wenn kein Beet vorhanden" (hasBeetImPlan-Guard) bleibt grün; xM/yM/widthM/heightM=0.3/provenance.plantSlug/layer='seasonal' bleiben.
  </behavior>
  <action>
    Korrigiere `addPlantToPlan` in `useKalenderData.ts`:

    (1) WR-06 — In-Bed-Placement: Ersetze die `nextFreeBedSlot`-Platzierung (die NEBEN Beeten platziert) durch eine Platzierung INNERHALB eines bestehenden Beets. Wähle das erste nicht-gelöschte Beet-Element aus `elements`. Berechne eine freie Position innerhalb seines Polygons — pragmatisch der Beet-Mittelpunkt `{xM: beet.xM, yM: beet.yM}` (Center-Konvention nach Plan 10-06; garantiert innerhalb des Rechteck-Polygons). Setze `xM`/`yM` des neuen Elements auf diese Position und ergänze `provenance.parentBedId = beet.id` (D-03 Fast-Path, den `findBedForPlant`/`findBeeteForPlant` konsumieren) zusätzlich zu `plantSlug`. Entferne den `nextFreeBedSlot`-Import, falls er sonst nirgends im File genutzt wird. Behalte widthM/heightM=0.3, layer='seasonal', isAccepted=true.

    (2) IN-04 — expliziter mode-Guard: Füge vor dem Write `if (mode !== 'account') throw new Error('account_erforderlich');` ein und übergib danach `mode` OHNE `!`-Assertion an `writePlanElement`. Der bestehende `assertAccount`-Schutz in `writePlanElement` bleibt als zweite Verteidigungslinie; der explizite Guard drückt die Vorbedingung aus und liefert eine klare Fehlerursache (10-REVIEW.md IN-04).

    Erweitere `useKalenderData.test.ts`: (a) In-Bed-Test — nach addPlantToPlan liegt der geschriebene Mittelpunkt im Beet-Polygon (importiere `pointInPolygon` aus geometry/bedLayout oder rekonstruiere die Assertion über die bekannten Beet-Koordinaten); prüfe zusätzlich `provenance.parentBedId === beetId`. (b) IN-04-Test — mode='local' → addPlantToPlan rejects, writePlanElement nicht aufgerufen. Nutze die bestehende useAuthStore-Mock-Struktur (Object.assign hookFn + getState, siehe 10-02-SUMMARY Deviation 3).
  </action>
  <verify>
    <automated>pnpm --filter app exec jest --testPathPattern="useKalenderData"</automated>
  </verify>
  <acceptance_criteria>
    - Das in `addPlantToPlan` geschriebene Element setzt `provenance.parentBedId` (Grep: `grep -c "parentBedId" app/src/hooks/useKalenderData.ts` >= 1).
    - `nextFreeBedSlot` wird in `addPlantToPlan` nicht mehr für die Pflanzen-Position genutzt (Grep: `grep -c "nextFreeBedSlot" app/src/hooks/useKalenderData.ts` === 0, falls Import vollständig entfernt).
    - Kein `mode!` mehr im Write-Pfad (Grep: `grep -c "mode!" app/src/hooks/useKalenderData.ts` === 0); stattdessen `mode !== 'account'`-Guard vorhanden.
    - In-Bed-Test: geschriebener Pflanzen-Mittelpunkt liegt im Beet-Polygon + parentBedId gesetzt — grün.
    - IN-04-Test: mode='local' → reject, kein writePlanElement-Call — grün.
    - Bestehender "kein Beet → throw"-Test — grün.
    - `pnpm --filter app exec jest --testPathPattern="useKalenderData"` — 0 failures.
    - `pnpm --filter app exec tsc --noEmit` — exit 0.
  </acceptance_criteria>
  <done>addPlantToPlan platziert die Pflanze im Beet (parentBedId + Center), findBeeteForPlant findet sie; mode-Guard explizit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-07 cancelled-Flag + Elements-Reset im Lade-Effekt</name>
  <files>app/src/hooks/useKalenderData.ts, app/src/hooks/__tests__/useKalenderData.test.ts</files>
  <read_first>
    - app/src/hooks/useKalenderData.ts (nach Task 1 — `loadData` useCallback Z. 84-101, Lade-`useEffect` Z. 103-106; früher Return bei `!activeGardenId` Z. 85-88 ohne Reset)
    - app/app/(app)/index.tsx (Source-of-Truth für cancelled-Flag-Pattern, Z. 28-36 laut 10-REVIEW.md WR-07)
    - .planning/phases/10-aussaatkalender-v1/10-REVIEW.md (WR-07 Z. 163-188 mit vollständiger Fix-Skizze)
  </read_first>
  <behavior>
    - WR-07 Reset (RED→GREEN): Wird `activeGardenId` zu `null`, werden `elements` auf `[]` und `dimensions` auf `null` zurückgesetzt (loading → false). Test: Hook mit activeGardenId → Daten geladen; danach activeGardenId=null re-rendern → `elements.length === 0`, `meinePflanzenslugs.size === 0`. Schlägt mit der alten Early-Return-Logik (behält Alt-Daten) fehl.
    - WR-07 Cancellation: Der Lade-Effekt setzt ein lokales `cancelled`-Flag und ruft `setElements`/`setDimensions`/`setLoading` nur, wenn `!cancelled`. Beim Re-Run (activeGardenId-Wechsel) wird die Cleanup-Funktion `() => { cancelled = true; }` ausgeführt. (Verhaltens-Assertion über den Race ist schwer deterministisch zu testen; der Reset-Test + Code-Struktur-Assertion via Grep decken die Korrektur ab.)
    - Regression: Erfolgreicher Load mit gültiger activeGardenId füllt elements/dimensions weiterhin korrekt (bestehende Tests grün).
  </behavior>
  <action>
    Strukturiere den Lade-Pfad in `useKalenderData.ts` nach dem etablierten cancelled-Flag-Pattern (Vorbild: `app/app/(app)/index.tsx`, 10-REVIEW.md WR-07-Skizze):

    Verlagere die Ladelogik in den `useEffect` (oder behalte `loadData` als von `refresh` aufgerufene Funktion, aber führe das cancelled-Flag im Effekt ein). Im Effekt:
    - `let cancelled = false; setLoading(true);`
    - Wenn `!activeGardenId`: `setElements([]); setDimensions(null); setLoading(false); return;` (Reset statt nur loading=false — schließt die stale-data-Hälfte von WR-07).
    - Sonst async `Promise.all([loadDimensions, loadAcceptedElements])`; im `then`/`try` nur bei `!cancelled` `setDimensions`/`setElements`; im `finally` nur bei `!cancelled` `setLoading(false)`.
    - Cleanup `return () => { cancelled = true; };`.

    Stelle sicher, dass `refresh` weiterhin funktioniert (ein manueller Reload nach addPlantToPlan). Falls `refresh` `loadData` aufruft, kann `loadData` ohne cancelled-Flag bleiben (manueller Reload ist nicht race-anfällig gegenüber Garten-Wechsel), ODER `refresh` ruft denselben Lade-Code; wähle die Variante mit der kleinsten Änderungsfläche, die beide Tests grün macht.

    Erweitere `useKalenderData.test.ts`: Reset-Test — Hook rendert mit activeGardenId (Daten vorhanden), dann mit activeGardenId=null re-rendern; assertiere `elements` leer und `meinePflanzenslugs.size === 0`. Nutze die bestehende useAuthStore-Mock-Struktur, sodass `activeGardenId` zwischen Renders umgeschaltet werden kann (z. B. module-level let-Variable, die der Mock per getState/Selector liest — siehe 10-02-SUMMARY).
  </action>
  <verify>
    <automated>pnpm --filter app exec jest --testPathPattern="useKalenderData"</automated>
  </verify>
  <acceptance_criteria>
    - Lade-Effekt enthält ein `cancelled`-Flag mit Cleanup-Return (Grep: `grep -c "cancelled" app/src/hooks/useKalenderData.ts` >= 2).
    - Bei `!activeGardenId` werden `setElements([])` und `setDimensions(null)` aufgerufen (Grep: `grep -c "setElements(\\[\\])" app/src/hooks/useKalenderData.ts` >= 1).
    - Reset-Test: activeGardenId → null → elements leer + meinePflanzenslugs leer — grün.
    - Bestehende useKalenderData-Tests (hasBeetImPlan, meinePflanzenslugs, klimazone null, nurMeinePflanzen) — grün.
    - `pnpm --filter app exec jest --testPathPattern="useKalenderData"` — 0 failures.
    - `pnpm --filter app exec tsc --noEmit` — exit 0.
  </acceptance_criteria>
  <done>Lade-Effekt mit cancelled-Flag + Reset bei null activeGardenId; Reset-Test grün; keine stale Daten beim Gartenwechsel.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Geänderte/neue Symbole dieses Plans:
- `addPlantToPlan` (`app/src/hooks/useKalenderData.ts`) — In-Bed-Placement (Beet-Center + `provenance.parentBedId`); expliziter `mode !== 'account'`-Guard statt `mode!`.
- Lade-`useEffect` in `useKalenderData` — cancelled-Flag + Cleanup; Reset von `elements`/`dimensions` bei `activeGardenId = null`.
- `nextFreeBedSlot`-Import in useKalenderData entfernt (falls sonst ungenutzt).
- Neue Tests in `useKalenderData.test.ts`: In-Bed-Placement (+ parentBedId), IN-04 local-mode-reject, WR-07 Reset.
Keine neuen öffentlichen Exporte; Hook-Signatur unverändert.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| addPlantToPlan → writePlanElement | Schreibt PlanElementRow; account-/RLS-geschützt |
| activeGardenId-Wechsel → State | Async Laden; out-of-order-Antworten könnten falschen Garten anzeigen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Elevation of Privilege | addPlantToPlan write | mitigate | Expliziter `mode !== 'account'`-Guard VOR dem Write + bestehender `assertAccount` in writePlanElement (Defense-in-Depth); RLS member-check serverseitig |
| T-10-08-RACE | Tampering | Garten-Daten bei Wechsel | mitigate | cancelled-Flag verhindert, dass verspätete Antworten des alten Gartens die Daten des neuen überschreiben; Reset bei null verhindert Cross-Garden-Datenleck in der Anzeige |
| T-10-06-DATA | Tampering | parentBedId Provenance | accept | parentBedId wird aus dem real existierenden, nicht-gelöschten Beet gesetzt; findBedForPlant fällt bei fehlendem/gelöschtem Beet auf PiP zurück (T-09-03) |
| T-10-08-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Packages — reine Hook-Korrektur |
</threat_model>

<verification>
- `pnpm --filter app exec jest --testPathPattern="useKalenderData"` — alle Tests grün (In-Bed, IN-04, WR-07 Reset)
- `pnpm --filter app exec tsc --noEmit` — exit 0
</verification>

<success_criteria>
- WR-06 geschlossen: addPlantToPlan platziert im Beet (parentBedId + Center); findBeeteForPlant findet die Pflanze
- WR-07 geschlossen: cancelled-Flag + Reset bei null activeGardenId
- IN-04 geschlossen: expliziter mode-Guard statt mode!
</success_criteria>

<output>
Create `.planning/phases/10-aussaatkalender-v1/10-08-SUMMARY.md` when done
</output>
