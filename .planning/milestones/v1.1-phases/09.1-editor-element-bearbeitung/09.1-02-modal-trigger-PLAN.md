---
phase: 09.1-editor-element-bearbeitung
plan: 02
type: execute
wave: 2
depends_on:
  - 09.1-01
files_modified:
  - app/src/components/editor/ElementEditorModal.tsx
  - app/src/components/editor/ElementEditorMoreFields.tsx
  - app/src/components/editor/ZOrderButtons.tsx
  - app/src/components/editor/CrossPlatformDatePicker.tsx
  - app/src/components/editor/CrossPlatformColorPicker.tsx
  - app/src/components/editor/EditorCanvas.tsx
  - app/src/components/editor/web/WebPlanEditor.tsx
  - app/app/(app)/plan/index.tsx
  - app/src/lib/editor/hitTest.ts
  - app/src/components/editor/__tests__/ElementEditorModal.test.tsx
  - app/src/components/editor/__tests__/ZOrderButtons.test.tsx
  - app/src/components/editor/__tests__/WebPlanEditor.dblclick.test.tsx
  - app/src/components/editor/__tests__/EditorCanvas.longPress.test.tsx
  - app/src/lib/editor/__tests__/hitTest.test.ts
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-09
  - D-10
  - D-11
  - D-12
  - D-16
  - D-17
  - D-18
  - D-21
  - D-23

must_haves:
  truths:
    - "Web: Doppelklick auf SVG-Element öffnet ElementEditorModal (D-01)"
    - "Skia: Long-Press 500ms ohne Bewegung öffnet ElementEditorModal (D-01/D-02)"
    - "Modal zeigt Kern-Felder (name/widthCm/heightCm/rotationDeg) + ZOrder Buttons (D-09/D-12)"
    - "Modal hat 3-Button Footer: destructive Löschen / ghost Abbrechen / default Speichern (D-17)"
    - "ESC schließt Modal auf Web; Hardware-Back schließt auf Android via onRequestClose (D-18)"
    - "Mehr-Felder Section default-collapsed mit (3)-Counter wenn Werte gesetzt (D-11)"
    - "Cancel/ESC verwirft pending changes ohne Confirm — Undo (Ctrl+Z) fängt ab (D-18)"
    - "Save dispatched updateElement mit gespreadter provenance (Pitfall 5) — plantSlug bleibt erhalten"
    - "Save schreibt 'name' Form-Field auf dedizierte label-Spalte (D-21), NICHT in provenance"
    - "Drag-vs-Dblclick: mousedown setzt drag erst nach 5px Movement-Threshold (RESEARCH §Open Q 2)"
    - "Long-Press / Doppelklick Hit-Test importiert findElementAtPoint aus @/src/lib/editor/hitTest (kein Inline-Code)"
  artifacts:
    - path: "app/src/components/editor/ElementEditorModal.tsx"
      provides: "Modal-Shell mit Kern-Felder + Mehr-Felder + 3-Button Footer + ESC-listener (Web)"
      contains: "element-editor-modal"
      min_lines: 120
    - path: "app/src/components/editor/ZOrderButtons.tsx"
      provides: "4-Button Row mit lucide ChevronsUp/ChevronUp/ChevronDown/ChevronsDown + provenance-spread Update"
      min_lines: 40
    - path: "app/src/components/editor/ElementEditorMoreFields.tsx"
      provides: "Collapsible Section mit Notiz/Pflanzdatum/Akzentfarbe + counter"
      min_lines: 40
    - path: "app/src/components/editor/CrossPlatformDatePicker.tsx"
      provides: "Web <input type=date> / Mobile lazy-require datetimepicker (Pattern 9)"
    - path: "app/src/components/editor/CrossPlatformColorPicker.tsx"
      provides: "Web <input type=color> / Mobile reanimated-color-picker mit Hex-Validation-Guard"
      contains: "/^#[0-9a-fA-F]{6}$/"
    - path: "app/src/lib/editor/hitTest.ts"
      provides: "GREEN implementation of findElementAtPoint (bbox hit-test + sortByZOrder top-most)"
      min_lines: 15
  key_links:
    - from: "app/app/(app)/plan/index.tsx"
      to: "app/src/components/editor/ElementEditorModal.tsx"
      via: "conditional mount on editingElementId"
      pattern: "editingElementId.*ElementEditorModal"
    - from: "app/src/components/editor/web/WebPlanEditor.tsx"
      to: "editorStore.setEditingElementId"
      via: "onDoubleClick handler"
      pattern: "onDoubleClick.*setEditingElementId"
    - from: "app/src/components/editor/EditorCanvas.tsx"
      to: "editorStore.setEditingElementId"
      via: "Gesture.LongPress().onStart → runOnJS hit-test"
      pattern: "LongPress.*setEditingElementId"
    - from: "app/src/components/editor/EditorCanvas.tsx"
      to: "app/src/lib/editor/hitTest.ts"
      via: "import findElementAtPoint for longPress hit-test"
      pattern: "from.*'@/src/lib/editor/hitTest'|findElementAtPoint"
    - from: "app/src/components/editor/ZOrderButtons.tsx"
      to: "app/src/lib/editor/zOrder.ts"
      via: "import bringToFront/bringForward/sendBackward/sendToBack"
      pattern: "from.*'@/src/lib/editor/zOrder'"
---

<objective>
Wave-2 Modal-Shell + Trigger-Gesten: Erstelle das ElementEditorModal mit Kern- und Mehr-Feldern, die ZOrderButtons-Komponente, plattform-spezifische Date/Color-Picker, integriere Doppelklick (Web) + Long-Press (Skia) als Öffnungs-Trigger, fülle `app/src/lib/editor/hitTest.ts` (Wave-0-Stub) GREEN als gemeinsame Hit-Test-Logik, und mounte das Modal in `plan/index.tsx`.

**Supabase Project (immer anzeigen):** vitrqkzxkiqvadqfzrcx (Frankfurt, EU)

Purpose: Erstes user-sichtbares Feature — User kann jetzt Editor öffnen und Properties via Modal bearbeiten. Plan 03 ergänzt parallel die Canvas-Handles für direkte Manipulation.

Output: 5 neue Komponenten + 3 modifizierte Renderer/Screens + 1 GREEN Pure-Function-Modul (hitTest.ts) + 5 Test-Dateien GREEN.

**Revision Note (Checker WARNING #3 + NIT #8):**
- D-21 (label dedizierte Spalte) wurde zu `requirements:` ergänzt, da `handleSave` die `label`-Spalte schreibt (Task 1 action).
- `app/src/lib/editor/hitTest.ts` ist jetzt explizit Teil der `files_modified` — der Wave-0-Stub aus Plan 00 wird in Task 2 GREEN gefüllt. Plan 03 importiert die Funktion ohne sie zu duplizieren.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09.1-editor-element-bearbeitung/09.1-CONTEXT.md
@.planning/phases/09.1-editor-element-bearbeitung/09.1-PATTERNS.md
@.planning/phases/09.1-editor-element-bearbeitung/09.1-VALIDATION.md
@.planning/phases/09.1-editor-element-bearbeitung/09.1-01-SUMMARY.md

# Reference patterns (verbatim shells)
@app/src/components/editor/BedPickerModal.tsx
@app/src/components/editor/EditorToolbar.tsx
@app/src/components/editor/EditorCanvas.tsx
@app/src/components/editor/web/WebPlanEditor.tsx
@app/app/(app)/plan/index.tsx
@app/src/components/ui/button.tsx
@app/src/components/ui/input.tsx
@app/src/components/ui/card.tsx
@packages/shared/src/i18n/de.json

# Wave-0 Stubs to fill GREEN
@app/src/lib/editor/hitTest.ts
@app/src/lib/editor/__tests__/hitTest.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ElementEditorModal + ZOrderButtons + MoreFields + Cross-Platform Pickers (per D-21 label-Spalten-Discipline)</name>
  <files>
    app/src/components/editor/ElementEditorModal.tsx,
    app/src/components/editor/ElementEditorMoreFields.tsx,
    app/src/components/editor/ZOrderButtons.tsx,
    app/src/components/editor/CrossPlatformDatePicker.tsx,
    app/src/components/editor/CrossPlatformColorPicker.tsx,
    app/src/components/editor/__tests__/ElementEditorModal.test.tsx,
    app/src/components/editor/__tests__/ZOrderButtons.test.tsx
  </files>
  <behavior>
    - Test 1 (Modal): renders with testID `element-editor-modal` when visible=true
    - Test 2 (Modal): renders i18n title key editor.elementEditor.title
    - Test 3 (Modal): renders 4 kern-input testIDs (name, widthCm, heightCm, rotationDeg)
    - Test 4 (Modal): renders 3-button footer with testIDs element-editor-delete (destructive), element-editor-cancel (ghost), element-editor-save (default)
    - Test 5 (Modal): Save press → calls useEditorStore.getState().updateElement with patch including dedicated `label` column (D-21) + spreads previous provenance + clears editingElementId
    - Test 6 (Modal): Cancel press → clears editingElementId without calling updateElement
    - Test 7 (Modal): Delete press → calls deleteElement + clears editingElementId
    - Test 8 (Modal): More-Fields section default-collapsed
    - Test 9 (Modal): More-Fields counter renders `(3)` when note + plantedAt + accentColor all set
    - Test 10 (ZOrderButtons): renders 4 button testIDs zorder-front/forward/backward/back
    - Test 11 (ZOrderButtons): front press computes via bringToFront + updateElement spreads provenance preserving plantSlug
    - Test 12 (ZOrderButtons): back press computes via sendToBack
    - Test 13 (ZOrderButtons): Pitfall-5 assertion: original provenance.plantSlug survives after zOrder mutation
    - Test 14 (Modal — D-21 explicit): Save patch shape has `label` as top-level key (NOT inside provenance); `provenance` patch does NOT contain a `label` key
  </behavior>
  <read_first>
    - app/src/components/editor/BedPickerModal.tsx (Pattern S4 Modal-Shell + Footer 2-Button-Pattern; D-16 reference)
    - app/src/components/editor/EditorToolbar.tsx (lucide icon + Pressable cluster pattern for ZOrderButtons)
    - app/src/components/ui/button.tsx (variants 'default' | 'destructive' | 'outline' | 'ghost' — verbatim für D-17)
    - app/src/components/ui/input.tsx (Input mit min-h-[44px] + brand focus border)
    - app/src/components/ui/card.tsx (Card / CardHeader / CardContent — optional für Modal-Innenlayout)
    - app/src/components/editor/CompanionToast.tsx (Platform.OS branching reference für Pickers)
    - app/src/lib/editor/zOrder.ts (bringToFront/Forward/Backward/Back imports für ZOrderButtons)
    - app/src/components/editor/__tests__/CompanionToast.test.tsx (RTL render + getByTestId + fireEvent.press)
    - app/src/components/editor/__tests__/EditorToolbar.test.tsx (Store-Mock pattern for editorStore in component tests)
    - .planning/phases/09.1-editor-element-bearbeitung/09.1-PATTERNS.md (§ElementEditorModal.tsx, §ZOrderButtons.tsx, §ElementEditorMoreFields.tsx, §CrossPlatformDatePicker.tsx, §CrossPlatformColorPicker.tsx)
    - .planning/phases/09.1-editor-element-bearbeitung/09.1-RESEARCH.md §Pattern 9 (Date+Color-Picker Cross-Platform — lazy-require Sicherheits-Pattern)
    - .planning/phases/09.1-editor-element-bearbeitung/09.1-RESEARCH.md §Pattern 8 (Modal Shell BedPickerModal Reference)
    - .planning/phases/09.1-editor-element-bearbeitung/09.1-RESEARCH.md §Security Domain (hex regex `/^#[0-9a-fA-F]{6}$/` + maxLength 500 für note)
    - .planning/phases/09.1-editor-element-bearbeitung/09.1-CONTEXT.md §D-21 (label exklusiv auf dedizierter Spalte, niemals in provenance)
  </read_first>
  <action>
    Schritt 1 — ElementEditorModal.tsx (per PATTERNS.md §ElementEditorModal.tsx):

    a) Imports: `* as React`, `View, Text, Modal, Pressable, ScrollView, TextInput, Platform` aus react-native; `PlanElementRow` type aus `@spatenstich/shared`; Button + Input + Card aus `@/src/components/ui/*`; `useEditorStore` aus `@/src/stores/editorStore`; `ZOrderButtons` und `ElementEditorMoreFields` aus Geschwistern; `t` utility (Pattern S1) aus `@spatenstich/shared/i18n/de`.

    b) Props interface: `{ visible: boolean; element: PlanElementRow; onSave: (patch: Partial<PlanElementRow>) => void; onCancel: () => void; onDelete: () => void; }`.

    c) Local form-state (React.useState) für `{ label, widthCm, heightCm, rotationDeg, note, plantedAt, accentColor }`. Initialize per useEffect aus `element` prop wenn visible toggles true→false vermieden, true→true updated. Das Form-Feld heißt `name` im UI (i18n `editor.elementEditor.name`), wird aber intern als `label` getrackt, da D-21 die Spalte so benennt.

    d) Modal-Shell verbatim aus BedPickerModal.tsx:38-48 mit testID="element-editor-modal" und Title-Key `editor.elementEditor.title`. Platform-Branch:
       - Web: centered `w-11/12 max-w-md` (BedPickerModal-Pattern).
       - Mobile: bottom-sheet `w-full self-end mb-0 rounded-t-2xl` als Alternative (D-16 discretion: bottom-sheet auf Mobile wenn passend).

    e) Kern-Felder (D-09): 4 `<Input>` Felder (name/widthCm/heightCm/rotationDeg) mit testIDs `kern-name`, `kern-width`, `kern-height`, `kern-rotation`. Numeric Inputs nutzen `keyboardType="numeric"` und parsen via `Number(value)` mit `Number.isFinite` validation. Validierung: widthCm/heightCm clamped to [5, 500] (Claude's discretion per CONTEXT.md), rotationDeg clamped to [0, 359].

    f) `<ZOrderButtons elementId={element.id} />` einsetzen.

    g) `<ElementEditorMoreFields ... expanded onToggle ... />` einsetzen.

    h) Footer (D-17): 3 Buttons in `<View className="flex-row justify-between mt-4">`:
       - Links: `<Button variant="destructive" testID="element-editor-delete" onPress={onDelete}>` (Trash icon optional).
       - Rechts (group): `<Button variant="ghost" testID="element-editor-cancel" onPress={onCancel}>` + `<Button variant="default" testID="element-editor-save" onPress={handleSave}>`.

    i) handleSave computed patch via Pattern S2 + D-21 Column-Discipline:
       - widthM = widthCm / 100 (CONTEXT §"widthM und heightM" dedicated columns per D-20).
       - heightM = heightCm / 100.
       - **label = labelFormField (per D-21: label ist dedizierte top-level Spalte; NICHT in provenance)**.
       - prev = element.provenance ?? {}. Pitfall 5: `nextProvenance = { ...prev, rotateDeg: rotationDeg, note, plantedAt, accentColor }`. Note: zOrder bleibt unverändert hier — wird von ZOrderButtons separately handled. Niemals `label`, `widthM` oder `heightM` in `nextProvenance` schreiben.
       - `onSave({ widthM, heightM, label, provenance: nextProvenance })` — `label` als top-level Schlüssel, parallel zu widthM/heightM (D-20/D-21).

    j) ESC-Listener für Web (Pattern S5 + per D-18):
       - `React.useEffect` wenn `Platform.OS === 'web'` und visible: addEventListener('keydown', onKey) mit INPUT/TEXTAREA guard; onKey === 'Escape' → onCancel().
       - Native: `onRequestClose={onCancel}` auf `<Modal>` handlet Hardware-Back (D-18).

    k) NO confirm-dialog bei pending changes — D-18 sagt "immer verwerfen ohne Confirm" weil Undo Ctrl+Z greift.

    Schritt 2 — ZOrderButtons.tsx (verbatim aus PATTERNS.md §ZOrderButtons.tsx):

    a) Imports: `lucide-react-native` icons `ChevronsUp, ChevronUp, ChevronDown, ChevronsDown`; `zOrder.ts` algorithms; `useEditorStore`.

    b) Props: `{ elementId: string }`.

    c) Pro Button: testID `zorder-front|zorder-forward|zorder-backward|zorder-back`, lucide Icon, Pattern S2 spread:
       - apply(compute) helper liest aktuelle elements, berechnet neuen zOrder via compute(elements, elementId), spreadt prev provenance, dispatched `updateElement(elementId, { provenance: { ...prev, zOrder: newZ } })`.
       - Pattern S2 ist Pflicht — Pitfall-5-Test (Test 13) prüft das.

    Schritt 3 — ElementEditorMoreFields.tsx:

    a) Props: `{ expanded: boolean; onToggle: () => void; note: string; setNote: (s: string) => void; plantedAt: string | null; setPlantedAt: (s: string | null) => void; accentColor: string | null; setAccentColor: (s: string | null) => void; }`.

    b) Toggle Pressable mit testID "more-fields-toggle" und Label aus `t('editor.elementEditor.moreFields')` / `t('editor.elementEditor.lessFields')`.

    c) Counter: `const extraCount = [note, plantedAt, accentColor].filter(Boolean).length;` — render `({extraCount})` an Toggle-Label wenn > 0 (D-11).

    d) `{expanded && (...)}` block:
       - Notiz TextInput multi-line mit `maxLength={500}` (Security V5 mitigation, RESEARCH §Security Domain).
       - `<CrossPlatformDatePicker value={plantedAt} onChange={setPlantedAt} testID="more-plantedAt" />`.
       - `<CrossPlatformColorPicker value={accentColor} onChange={setAccentColor} testID="more-accentColor" />`.

    Schritt 4 — CrossPlatformDatePicker.tsx (verbatim aus RESEARCH §Pattern 9 Date):
       - Web: `Platform.OS === 'web'` → render raw `<input type="date" value={value ?? ''} onChange={e => onChange(e.currentTarget.value || null)} />` via React DOM.
       - Mobile: `const DateTimePicker = require('@react-native-community/datetimepicker').default;` **inside** the function body (lazy-require, never at module top — Pitfall im RESEARCH).
       - Props: `{ value: string | null; onChange: (s: string | null) => void; testID?: string }`.

    Schritt 5 — CrossPlatformColorPicker.tsx (verbatim aus RESEARCH §Pattern 9 Color + Hex-Guard):
       - `const HEX_RE = /^#[0-9a-fA-F]{6}$/;`
       - `safeOnChange(hex): if (hex !== null && !HEX_RE.test(hex)) return; onChange(hex);`
       - Web: `<input type="color" value={value ?? '#000000'} onChange={e => safeOnChange(e.currentTarget.value)} />`.
       - Mobile: lazy `require('reanimated-color-picker')` wrapped in `<Modal>` (RESEARCH §Pattern 9 Color). On confirm/save, hex from picker → safeOnChange.

    Schritt 6 — Tests GREEN-fillen (ElementEditorModal.test.tsx + ZOrderButtons.test.tsx):

    a) ElementEditorModal.test.tsx (10 todos → 10 it + 1 neuer Test für D-21 Column-Discipline = 11 it()):
       - Store-Mock Pattern aus EditorToolbar.test.tsx: `editorState: any = { elements: [makeEl('e1')], ... }; jest.mock('@/src/stores/editorStore', () => ...);` + `mockUpdateElement: jest.fn()`.
       - Render mit `<ElementEditorModal visible element={makeEl('e1', { provenance: { source: 'manual', plantSlug: 'tomate' } })} ... />`.
       - Assertions wie in behavior list. Test 5 prüft Pitfall-5 spread: nach Save call args zu mockUpdateElement haben `provenance.plantSlug === 'tomate'`.
       - **Test 14 (D-21 explicit):** Setze Initial-Element mit `label: 'Beet A'`, ändere Name-Form-Field zu 'Beet B', drücke Save. Assert: `mockUpdateElement.mock.calls[0][1].label === 'Beet B'` UND `mockUpdateElement.mock.calls[0][1].provenance.label === undefined`. Header-Kommentar im Test: `// D-21: label lebt auf dedizierter Spalte, niemals in provenance.`

    b) ZOrderButtons.test.tsx (4 todos → 4 it):
       - Render `<ZOrderButtons elementId="e1" />` mit storeState.elements containing 3 elements with zOrder 0, 5, 10 (e1 hat zOrder 5).
       - Test 2: fireEvent.press(getByTestId('zorder-front')) → mockUpdateElement called with provenance.zOrder === 11.
       - Test 3: zorder-back → -1 (min others ist 0, also 0-1 = -1).
       - Test 4: Spread-Discipline — Test mit element { provenance: { source: 'manual', plantSlug: 'tomate', zOrder: 5 } } → nach press: plantSlug muss in der call args provenance bleiben.

    KEINE fenced code blocks im action — Verbatim Component-Bodies leben in PATTERNS.md.
  </action>
  <verify>
    <automated>cd app && pnpm exec vitest run ElementEditorModal ZOrderButtons --reporter=dot 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - 5 neue Komponenten-Dateien existieren (ls aller 5 Pfade returns success).
    - `ElementEditorModal.tsx` contains literal `testID="element-editor-modal"` (grep returns 1 hit).
    - `ElementEditorModal.tsx` contains 3 footer testID strings: element-editor-delete, element-editor-cancel, element-editor-save (grep `element-editor-(delete|cancel|save)` returns 3 hits).
    - `ElementEditorModal.tsx` handleSave dispatches `label` as a top-level patch field, NOT inside provenance (grep `label:\s*[a-zA-Z]` near handleSave returns 1+ hit AND grep within nextProvenance build does NOT include `label:`).
    - `ZOrderButtons.tsx` imports the 4 z-order functions: `bringToFront`, `bringForward`, `sendBackward`, `sendToBack` (grep `bringToFront|bringForward|sendBackward|sendToBack` returns >= 4 hits in import line).
    - `ZOrderButtons.tsx` contains literal `...prev` for provenance spread (grep `\.\.\.prev` returns 1+ hit) — Pattern S2 evidence.
    - `CrossPlatformColorPicker.tsx` contains literal `/^#[0-9a-fA-F]{6}$/` hex regex (grep returns 1 hit) — Security V5.
    - `ElementEditorMoreFields.tsx` contains `maxLength={500}` for note input (grep `maxLength=\{500\}` returns 1 hit) — Security V5 DoS-mitigation.
    - `CrossPlatformDatePicker.tsx` has NO top-level `import .* from '@react-native-community/datetimepicker'` (grep `^import.*datetimepicker` returns 0) — lazy-require Pitfall in RESEARCH §Pattern 9.
    - `ElementEditorModal.test.tsx` has zero `it.todo` (grep returns 0); >= 11 `it(` blocks (10 ursprüngliche + 1 D-21 Test).
    - `ZOrderButtons.test.tsx` has zero `it.todo`; 4 `it(` blocks.
    - `pnpm --filter app exec vitest run ElementEditorModal ZOrderButtons` exits 0.
  </acceptance_criteria>
  <done>
    Modal + alle Sub-Komponenten sind contract-conform per D-09/D-11/D-12/D-16/D-17/D-21, Hex-Validation + Note-Length-Cap aktiv, 15 Tests GREEN. `label` wird ausschließlich auf dedizierter Spalte geschrieben.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: hitTest.ts GREEN-Fill + Trigger-Gesten (Doppelklick Web + Long-Press Skia) + Modal-Mount in plan/index.tsx</name>
  <files>
    app/src/lib/editor/hitTest.ts,
    app/src/lib/editor/__tests__/hitTest.test.ts,
    app/src/components/editor/EditorCanvas.tsx,
    app/src/components/editor/web/WebPlanEditor.tsx,
    app/app/(app)/plan/index.tsx,
    app/src/components/editor/__tests__/WebPlanEditor.dblclick.test.tsx,
    app/src/components/editor/__tests__/EditorCanvas.longPress.test.tsx
  </files>
  <behavior>
    - Test 1 (hitTest): findElementAtPoint returns the element whose bbox contains (xM, yM)
    - Test 2 (hitTest): returns undefined when no element covers the point
    - Test 3 (hitTest): returns top-most by zOrder when multiple overlap (sortByZOrder ascending → top-most is last in sorted order)
    - Test 4 (hitTest): ignores elements with deletedAt !== null
    - Test 5 (hitTest): uses widthM/heightM dedicated columns (D-20), not provenance.widthM
    - Test 6 (dblclick): SVG <G> onDoubleClick sets editingElementId via setEditingElementId(el.id)
    - Test 7 (dblclick): onDoubleClick e.stopPropagation called
    - Test 8 (dblclick): mousedown does NOT setDrag immediately; only after 5px movement threshold exceeded
    - Test 9 (longPress): Gesture.LongPress minDuration is 500ms (D-02)
    - Test 10 (longPress): Gesture.LongPress maxDistance is 10px (D-02)
    - Test 11 (longPress): handleLongPressAt invokes findElementAtPoint from @/src/lib/editor/hitTest and calls setEditingElementId(hit.id) when element underneath
  </behavior>
  <read_first>
    - app/src/lib/editor/hitTest.ts (Wave-0 Stub aus Plan 00 — throws TODO Error)
    - app/src/lib/editor/__tests__/hitTest.test.ts (Wave-0 it.todo Slots)
    - app/src/lib/editor/zOrder.ts (sortByZOrder GREEN aus Plan 01 — wird in hitTest.ts importiert)
    - app/src/components/editor/EditorCanvas.tsx (lines 65-85 handleCanvasTap hit-test reference; line 150 existing longPress placeholder; lines 117-127 Pan gesture pattern)
    - app/src/components/editor/web/WebPlanEditor.tsx (lines 99-114 keydown effect with INPUT/TEXTAREA guard; lines 119-152 window mousemove drag effect; lines 199-215 handleElementMouseDown; lines 288-343 element <G> render)
    - app/app/(app)/plan/index.tsx (lines 226-233 + 273-280 CompanionToast conditional mount — analog für Modal-Mount; line ~70 selection store read pattern)
    - .planning/phases/09.1-editor-element-bearbeitung/09.1-PATTERNS.md §MODIFIED EditorCanvas.tsx + §MODIFIED WebPlanEditor.tsx + §MODIFIED plan/index.tsx
    - .planning/phases/09.1-editor-element-bearbeitung/09.1-RESEARCH.md §Pattern 1 (Long-Press Disambiguation) + §Pattern 2 (Web onDoubleClick + Drag-vs-Dblclick) + §Open Q 2 (5px threshold)
    - .planning/phases/09.1-editor-element-bearbeitung/09.1-RESEARCH.md §Pitfall 7 (Shift key INPUT/TEXTAREA guard) — applies here too
    - app/src/components/editor/__tests__/PlanEditor.smoke.test.tsx (Skia mock infrastructure reference)
    - app/src/components/editor/__tests__/setup.ts (Global jest setup für Skia/lucide)
    - packages/shared/src/types/entities.ts (PlanElementRow Felder — widthM/heightM/deletedAt für hit-test bbox)
  </read_first>
  <action>
    Schritt 0 — hitTest.ts GREEN-Fill (per Checker NIT #8 zentralisiert):

    a) Datei `app/src/lib/editor/hitTest.ts` öffnen (Wave-0 Stub aus Plan 00).
    - Imports: `import type { PlanElementRow } from '@spatenstich/shared';` + `import { sortByZOrder } from './zOrder';` (sortByZOrder ist GREEN aus Plan 01).
    - Signatur: `export function findElementAtPoint(elements: PlanElementRow[], xM: number, yM: number): PlanElementRow | undefined`.
    - Body:
      - Filter `live = elements.filter(e => e.deletedAt === null);`
      - Sortiere `sorted = sortByZOrder(live);` — ascending zOrder, top-most ist letztes Element.
      - Iteriere `sorted` und sammle alle Treffer wo Punkt in bbox liegt: `const hits = sorted.filter(e => { const halfW = e.widthM / 2; const halfH = e.heightM / 2; return xM >= e.xM - halfW && xM <= e.xM + halfW && yM >= e.yM - halfH && yM <= e.yM + halfH; });`
      - Return `hits.length === 0 ? undefined : hits[hits.length - 1];` (top-most = letzter Treffer in sort-order ascending).
    - JSDoc: "MVP carve-out: axis-aligned bbox (RESEARCH A1). Rotated-element hit-test deferred to v1.1 — User-tap auf rotated element greift solange Punkt in der achsenparallelen bbox liegt."

    b) Datei `app/src/lib/editor/__tests__/hitTest.test.ts` öffnen — alle `it.todo` zu `it()` mit echten Assertions konvertieren:
    - Imports + makeEl Factory (Pattern S7 aus plantSpacing.test.ts).
    - Test 1: 1 Element bei xM=2 yM=3 widthM=2 heightM=2; findElementAtPoint(elements, 2.5, 3.5) === element.
    - Test 2: 1 Element bei xM=2 yM=3 widthM=1 heightM=1; findElementAtPoint(elements, 10, 10) === undefined.
    - Test 3: 2 überlappende Elemente {id:'a', zOrder:0} und {id:'b', zOrder:5} beide bei xM=2 yM=2 widthM=2 heightM=2; findElementAtPoint(elements, 2, 2).id === 'b' (top-most by zOrder).
    - Test 4: 1 Element mit deletedAt='2026-01-01T00:00:00Z' bei xM=2 yM=2 widthM=2 heightM=2; findElementAtPoint(elements, 2, 2) === undefined.
    - Test 5: Element mit widthM=2.0 (dedizierte Spalte) und provenance.widthM=99 (irrelevant). findElementAtPoint(elements, 2.9, 2) === element (hit-test nutzt 2.0, nicht 99 — sonst würde 2.9 weit außerhalb fallen wenn 99 falsch interpretiert würde).

    c) `pnpm --filter app exec vitest run hitTest --reporter=dot` muss exit 0.

    Schritt 1 — WebPlanEditor.tsx Doppelklick (per PATTERNS.md §MODIFIED WebPlanEditor.tsx):

    a) Add to element `<G>` render (line ~294) ein neues `onDoubleClick` Handler-Attribut:
       - `onDoubleClick={((e: React.MouseEvent) => { e.stopPropagation(); useEditorStore.getState().setSelection(el.id); useEditorStore.getState().setEditingElementId(el.id); }) as any}`.

    b) Drag-vs-Dblclick Threshold (RESEARCH §Open Q 2):
       - Add `const MOUSE_DRAG_THRESHOLD_PX = 5;` constant at top of component.
       - Modify `handleElementMouseDown` (line 199): KEIN sofortiges `setDrag({...})` und KEIN `setGestureActive(true)`. Stattdessen: capture {id, startMouseX, startMouseY, elStartXM, elStartYM} in einer Ref (z.B. `pendingDragRef`).
       - Hinzufügen: window-level mousemove listener while pendingDragRef is set; on first move where `dx >= 5 || dy >= 5`: promote pendingDragRef → setDrag(...) + setGestureActive(true).
       - On mouseup before threshold: clear pendingDragRef ohne drag-start (Browser handelt dblclick natürlich).
       - Cleanup window listeners on unmount.

    Schritt 2 — EditorCanvas.tsx Long-Press (per PATTERNS.md §MODIFIED EditorCanvas.tsx, mit findElementAtPoint-Import):

    a) Imports erweitern: `import { findElementAtPoint } from '@/src/lib/editor/hitTest';`.

    b) Replace `Gesture.LongPress().minDuration(500).onStart(() => {})` (line 150) mit:
       - `Gesture.LongPress().minDuration(500).maxDistance(10).onStart((e) => { runOnJS(handleLongPressAt)(e.x, e.y); });`

    c) Add `handleLongPressAt` React.useCallback nach existierendem `handleCanvasTap` (analog zu lines 65-85):
       - Body: viewport-untransform (x, y) → (xM, yM) gemäß bestehender Pattern in handleCanvasTap.
       - Aufruf `const hit = findElementAtPoint(useEditorStore.getState().elements, xM, yM);`
       - Wenn `!hit`: `return;` (kein Modal öffnen — Pitfall 1).
       - Andernfalls: `useEditorStore.getState().setSelection(hit.id); useEditorStore.getState().setEditingElementId(hit.id);`.
       - WICHTIG: KEIN Inline-bbox-Filter mehr in handleLongPressAt — die gesamte Hit-Logik lebt jetzt zentral in `findElementAtPoint`. Plan 03 importiert dasselbe Modul (kein Duplikat).

    d) Composed-Gesture bleibt unverändert weil `Gesture.Exclusive(longPress, tap)` schon korrekt aus Phase 7 P04.

    Schritt 3 — plan/index.tsx Modal-Mount (per PATTERNS.md §MODIFIED plan/index.tsx):

    a) Add `const editingElementId = useEditorStore((s) => s.editingElementId);` neben der bestehenden `selection` selection (~line 70).

    b) Add `const elements = useEditorStore((s) => s.elements);` falls noch nicht direkt verfügbar (wahrscheinlich existiert bereits).

    c) Add conditional mount in BEIDEN shells (Mobile + Web) — analog zu CompanionToast mount lines 226 + 273:
       - `{editingElementId && (<ElementEditorModal visible element={elements.find(e => e.id === editingElementId)!} onSave={...} onCancel={...} onDelete={...} />)}`
       - onSave: `(patch) => { useEditorStore.getState().updateElement(editingElementId, patch); useEditorStore.getState().setEditingElementId(null); }`
       - onCancel: `() => useEditorStore.getState().setEditingElementId(null)`
       - onDelete: `() => { useEditorStore.getState().deleteElement(editingElementId); useEditorStore.getState().setEditingElementId(null); }`

    Schritt 4 — Tests GREEN-fillen:

    a) WebPlanEditor.dblclick.test.tsx (3 todos → 3 it):
       - Mock editorStore wie EditorToolbar.test.tsx Pattern, mit mockSetEditingElementId.
       - Test 6: render `<WebPlanEditor />` mit 1 element; `fireEvent.doubleClick(getByTestId(or queryAllByRole svg element))` → assert mockSetEditingElementId called with el.id.
       - Test 7: stopPropagation assertion — wrap in parent mousedown spy; nach dblclick wird parent NICHT triggered.
       - Test 8: mousedown without movement → setDrag NICHT aufgerufen. Anschließend simulate window mousemove +6px → setDrag wird aufgerufen.

    b) EditorCanvas.longPress.test.tsx (3 todos → 3 it):
       - Mock react-native-gesture-handler wie in editor jest setup.ts (Phase 7 P04 etablierte Pattern). Skia Group/Rect bereits gemockt.
       - Test 9: Inspect Gesture.LongPress() call — assert .minDuration(500) chained.
       - Test 10: assert .maxDistance(10) chained.
       - Test 11: invoke captured .onStart(handler) directly with synthetic event {x: 100, y: 100} where element is at xM=2 yM=2; assert findElementAtPoint wurde aufgerufen UND mockSetEditingElementId('e-target') called. Mock von '@/src/lib/editor/hitTest' mit `vi.spyOn` möglich, oder reale Funktion + Element-Setup.
  </action>
  <verify>
    <automated>cd app && pnpm exec vitest run hitTest WebPlanEditor.dblclick EditorCanvas.longPress --reporter=dot 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `app/src/lib/editor/hitTest.ts` no longer contains `throw new Error('TODO` (grep returns 0).
    - `hitTest.ts` exports `findElementAtPoint` with a real body (grep `^export function findElementAtPoint` returns 1 hit; file has >= 15 lines per artifact spec).
    - `hitTest.ts` imports `sortByZOrder` from './zOrder' (grep `from\s*['"]\./zOrder['"]` returns 1 hit).
    - `hitTest.test.ts` has zero `it.todo` (grep returns 0); 5 `it(` blocks.
    - WebPlanEditor.tsx contains literal `onDoubleClick` handler (grep `onDoubleClick` returns 1+ hit).
    - WebPlanEditor.tsx contains literal `MOUSE_DRAG_THRESHOLD_PX` (or similar 5px-threshold constant; grep `MOUSE_DRAG_THRESHOLD_PX|threshold.*5|5.*threshold` returns 1+ hit).
    - WebPlanEditor.tsx onDoubleClick handler invokes `setEditingElementId` (grep within file: `onDoubleClick[\s\S]{0,200}setEditingElementId` returns 1 hit with multiline mode).
    - EditorCanvas.tsx contains `.minDuration(500)` and `.maxDistance(10)` on LongPress (grep `minDuration\(500\).*maxDistance\(10\)|maxDistance\(10\).*minDuration\(500\)` multiline returns 1 hit).
    - EditorCanvas.tsx imports `findElementAtPoint` from `@/src/lib/editor/hitTest` (grep `from\s*['"]@/src/lib/editor/hitTest['"]` returns 1 hit).
    - EditorCanvas.tsx `handleLongPressAt` invokes `findElementAtPoint(` (grep `handleLongPressAt[\s\S]{0,500}findElementAtPoint\(` multiline returns 1 hit).
    - EditorCanvas.tsx `handleLongPressAt` invokes `setEditingElementId` (grep `handleLongPressAt[\s\S]{0,500}setEditingElementId` multiline returns 1 hit).
    - plan/index.tsx mounts `ElementEditorModal` conditionally on `editingElementId` (grep `editingElementId\s*&&\s*<ElementEditorModal|editingElementId\s*\?[\s\S]*ElementEditorModal` multiline returns 1+ hit).
    - WebPlanEditor.dblclick.test.tsx zero `it.todo`; 3 `it(` blocks.
    - EditorCanvas.longPress.test.tsx zero `it.todo`; 3 `it(` blocks.
    - `pnpm --filter app exec vitest run hitTest WebPlanEditor.dblclick EditorCanvas.longPress` exits 0.
  </acceptance_criteria>
  <done>
    hitTest.ts ist GREEN als zentrale Hit-Test-Funktion; Trigger-Gesten Web (Doppelklick) + Skia (Long-Press) öffnen das Modal über `findElementAtPoint`; Drag-vs-Dblclick Konflikt durch 5px Threshold gelöst; Modal-Mount im Screen erfolgt für Mobile- und Web-Shell. Plan 03 kann `findElementAtPoint` importieren statt zu duplizieren.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Touch/Mouse → Trigger-Gesten | Long-Press / Double-Click sind UI-Events; Hit-Test läuft client-side mit gerendertem Element-Set; kein Server-Roundtrip. |
| User Input (Notiz, Akzentfarbe) → provenance jsonb | XSS-Risiko bei Color (CSS injection) und DoS via large note. |
| Modal-Form-State → updateElement | Pattern S2 (provenance spread) ist die einzige Verteidigung gegen plantSlug-Verlust (Phase 9 dependency). |
| Modal-Form-State `name` → label-Spalte | D-21: name (UI-Form-Field) wird auf dedizierte `label`-Spalte geschrieben, NIEMALS in provenance dupliziert. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-XSS-COLOR | Tampering / XSS | CrossPlatformColorPicker accentColor input | mitigate | Hex regex `/^#[0-9a-fA-F]{6}$/` validates ALL color inputs before write to provenance. Web `<input type=color>` already constrains by browser; native color-picker output passes through safeOnChange guard. ASVS V5.1.3. |
| T-09.1-DOS-NOTE | Denial of Service | Modal Notiz TextInput | mitigate | `maxLength={500}` client-side cap. Server-side check deferred to v1.2 (RESEARCH §Security Domain — accept residual risk because provenance is already RLS-scoped to garden members). ASVS V5.1.4. |
| T-09.1-MODAL-ESC | Repudiation | ESC / Hardware-Back handling | mitigate | Web: keydown listener with INPUT/TEXTAREA guard (Pattern S5 prevents Shift-key hijacking while typing); Mobile: `Modal.onRequestClose` handles Android Back. Both paths converge on onCancel which clears editingElementId without writing — Undo (Ctrl+Z) is the recovery mechanism (D-18). ASVS V4. |
| T-09.1-PROVENANCE-LOSS | Tampering | ZOrderButtons + Modal Save provenance write | mitigate | Pattern S2 (spread prevProvenance) enforced by acceptance criteria (grep `\.\.\.prev`) and Pitfall-5 test (ZOrderButtons.test #4). Failure mode: Phase 9 companion detection breaks silently. ASVS V8.3. |
| T-09.1-LABEL-DUP | Tampering | Modal Save label write | mitigate | D-21 enforcement: `label` ist im Save-Patch nur als top-level Schlüssel präsent; `nextProvenance` enthält nie `label`. Test 14 in ElementEditorModal.test.tsx asserted das. ASVS V5. |
| T-09.1-DBLCLICK-RACE | Tampering | Drag-vs-Dblclick conflict | mitigate | 5px movement threshold (RESEARCH §Open Q 2) — mousedown stays passive until 5px exceeded, leaving native dblclick window intact. Test 8 in dblclick.test.tsx enforces. ASVS V5. |
| T-09.1-HITTEST-DUP | Tampering | Long-Press / Double-Click Hit-Logic | mitigate | Zentrales Modul `hitTest.ts` mit `findElementAtPoint` — Plan 02 (longPress) und Plan 03 (Handle-Mount-Logik wenn nötig) importieren dasselbe pure helper. Verhindert Drift zwischen Skia- und Web-Pfad. Tests 1-5 (hitTest.test.ts) sichern Algorithm-Invarianten. ASVS V5. |
</threat_model>

<verification>
- `pnpm --filter app exec vitest run ElementEditorModal ZOrderButtons WebPlanEditor.dblclick EditorCanvas.longPress hitTest` exits 0 (alle ~26 Tests GREEN)
- `pnpm --filter app exec tsc --noEmit` exits 0 (kein Typenkonflikt aus neuen Komponenten)
- Vollständige Suite kein Regression: `pnpm --filter app exec vitest run` exits 0
- Plan-Index render-test (PlanEditor.smoke) bleibt grün
</verification>

<success_criteria>
1. User auf Web kann Element doppelklicken → Modal öffnet sich, zeigt Felder, Save updated.
2. User auf Skia kann Long-Press → Modal öffnet sich (manuell auf Device verifiziert in Plan 05).
3. Pattern S2 (provenance spread) ist überprüfbar in ZOrderButtons + Modal Save.
4. D-21 Column-Discipline ist überprüfbar in ElementEditorModal.test Test 14 (label NICHT in provenance).
5. Hex-Color Validation + 500-char Notiz-Cap aktiv.
6. ESC + Hardware-Back schließen Modal verlustfrei (Undo fängt ab).
7. hitTest.ts GREEN als zentrale Hit-Test-Funktion — Plan 03 importiert ohne Code-Duplikat.
8. Plans 03 (Handles) + 05 (i18n) hängen direkt von D-09/D-23 ab → können nach Plan 02 starten (Plan 03 ist Wave 3 mit depends_on: [09.1-02]; Plan 05 ist Wave 3 mit depends_on: [09.1-02]).
</success_criteria>

<output>
Create `.planning/phases/09.1-editor-element-bearbeitung/09.1-02-SUMMARY.md` when done — summarize:
- Anzahl neuer Komponenten + LOC
- Tests GREEN/total für ElementEditorModal/ZOrderButtons/WebPlanEditor.dblclick/EditorCanvas.longPress/hitTest
- Modal-Mount-Strategy in plan/index.tsx (zwei Shells)
- Pitfall-5 Spread-Evidence (grep results)
- D-21 Column-Discipline Evidence (label nicht in nextProvenance)
- hitTest.ts GREEN-Status (Plan 03 darf importieren)
</output>
