---
phase: 05-ai-removal-import-schema
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - app/src/lib/sync/SyncWorker.ts
  - app/src/lib/sync/SyncTriggers.ts
  - app/src/lib/gardenPlanRepo.ts
  - app/src/lib/migrateLocalToAccount.ts
  - app/src/lib/photos/PhotoUploader.ts (DELETE)
  - app/src/lib/photos/photoQueueRepo.ts (DELETE)
  - app/src/lib/photoResizer.ts (DELETE)
  - app/src/lib/extractVereinsregeln.ts (DELETE)
  - app/src/lib/enqueueAiJob.ts (DELETE)
  - app/src/lib/uploadVereinsregelPdf.ts (DELETE)
  - app/src/lib/mappers/rowMappers.ts
  - app/src/storage/SqliteAdapter.ts
  - app/src/storage/IndexedDbAdapter.ts
  - app/src/components/AnalysisLoader.tsx (DELETE)
  - app/src/components/BudgetWarningBanner.tsx (DELETE)
  - app/src/components/CaptureStepCard.tsx (DELETE)
  - app/src/components/ConfidenceBadge.tsx (DELETE)
  - app/src/components/DimensionInput.tsx (DELETE)
  - app/src/components/ExtractionLoader.tsx (DELETE)
  - app/src/components/PhotoThumbnail.tsx (DELETE)
  - app/src/components/ShapeSelector.tsx (DELETE)
  - app/app/(app)/capture/ (DELETE entire directory)
  - app/app/(app)/index.tsx
  - app/app/(app)/profile/vereinsregeln/upload.tsx
  - packages/shared/src/i18n/de.json
  - README.md
  - app/.env.example
autonomous: true
requirements:
  - REMOVE-01
  - REMOVE-02
  - REMOVE-03

must_haves:
  truths:
    - "App baut ohne TypeScript-Fehler (npx tsc --noEmit)"
    - "grep -ri anthropic|plantnet|vision app/src/ app/app/ liefert keine funktionalen Code-Treffer"
    - "Home-Screen zeigt keinen Capture-Button mehr"
    - "vereinsregeln/upload.tsx zeigt Platzhalter statt PDF-Upload"
    - "SyncWorker hat keinen photo_queue case mehr"
    - "Keine Camera-Imports oder Capture-Navigation in index.tsx"
  artifacts:
    - path: "app/app/(app)/index.tsx"
      provides: "Home-Screen ohne Capture-Buttons"
    - path: "app/app/(app)/profile/vereinsregeln/upload.tsx"
      provides: "Platzhalter-Screen ohne AI-Extraktion"
    - path: "packages/shared/src/i18n/de.json"
      provides: "i18n ohne capture-Block"
  key_links:
    - from: "app/src/lib/sync/SyncWorker.ts"
      to: "packages/shared/src/types/entities.ts"
      via: "EntityName import"
      pattern: "import.*EntityName"
    - from: "app/src/storage/SqliteAdapter.ts"
      to: "packages/shared/src/types/entities.ts"
      via: "EntityName import"
      pattern: "import.*EntityName"
---

<objective>
App-Level AI-Code-Bereinigung: Alle Client-Libs, Screens, Komponenten, Sync-Referenzen, Storage-Adapter-Eintraege, i18n-Keys und Dokumentation von AI-Relikten befreien.

Purpose: Nach Plan 01 (Types bereinigt) koennen nun alle App-Dateien bereinigt werden, ohne TypeScript-Inkonsistenzen.
Output: Saubere App-Codebase ohne AI-Code, build-faehig, zero AI-Referenzen im funktionalen Code.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ai-removal-import-schema/05-CONTEXT.md
@.planning/phases/05-ai-removal-import-schema/05-RESEARCH.md
@.planning/phases/05-ai-removal-import-schema/05-PATTERNS.md
@.planning/phases/05-ai-removal-import-schema/05-01-SUMMARY.md

<interfaces>
<!-- After Plan 01, entities.ts no longer has photo_queue, PhotoQueueRow, PlanElementCandidate, aiResultId -->
<!-- All app files importing these must be updated accordingly -->

Expected EntityName after Plan 01:
```typescript
export type EntityName =
  | 'gardens'
  | 'garden_members'
  | 'profiles'
  | 'vereinsregeln'
  | 'invite_codes'
  | 'garden_dimensions'
  | 'plan_elements';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Client-Libs + Komponenten loeschen, Sync + Storage + Repos bereinigen</name>
  <files>
    app/src/lib/photos/PhotoUploader.ts (DELETE)
    app/src/lib/photos/photoQueueRepo.ts (DELETE)
    app/src/lib/photoResizer.ts (DELETE)
    app/src/lib/extractVereinsregeln.ts (DELETE)
    app/src/lib/enqueueAiJob.ts (DELETE)
    app/src/lib/uploadVereinsregelPdf.ts (DELETE)
    app/src/components/AnalysisLoader.tsx (DELETE)
    app/src/components/BudgetWarningBanner.tsx (DELETE)
    app/src/components/CaptureStepCard.tsx (DELETE)
    app/src/components/ConfidenceBadge.tsx (DELETE)
    app/src/components/DimensionInput.tsx (DELETE)
    app/src/components/ExtractionLoader.tsx (DELETE)
    app/src/components/PhotoThumbnail.tsx (DELETE)
    app/src/components/ShapeSelector.tsx (DELETE)
    app/src/lib/sync/SyncWorker.ts
    app/src/lib/sync/SyncTriggers.ts
    app/src/lib/gardenPlanRepo.ts
    app/src/lib/migrateLocalToAccount.ts
    app/src/lib/mappers/rowMappers.ts
    app/src/storage/SqliteAdapter.ts
    app/src/storage/IndexedDbAdapter.ts
  </files>
  <read_first>
    app/src/lib/sync/SyncWorker.ts (photo_queue case in dispatchPush, pushPhotoQueue method, PhotoQueueRow import)
    app/src/lib/sync/SyncTriggers.ts (uploadPending import + 2 Aufrufe)
    app/src/lib/gardenPlanRepo.ts (saveElements function, PlanElementCandidate import, AI-Kommentar)
    app/src/lib/migrateLocalToAccount.ts (photo_queue sync_state Zeilen)
    app/src/lib/mappers/rowMappers.ts (photoQueueFromDb Mapper)
    app/src/storage/SqliteAdapter.ts (photo_queue in ROW_ENTITIES, GARDEN_ID_COLUMN, GARDEN_ID_FIELD)
    app/src/storage/IndexedDbAdapter.ts (photo_queue in ROW_ENTITIES, GARDEN_ID_COLUMN/FIELD, v3Entities)
    app/src/lib/photos/PhotoUploader.ts (bestaetigen vor Loeschung)
    app/src/lib/photos/photoQueueRepo.ts (bestaetigen vor Loeschung)
  </read_first>
  <action>
**Reihenfolge ist kritisch -- Pitfall 2 und 5 aus RESEARCH.md vermeiden.**

1. **Client-Libs komplett loeschen** (per D-06 + implizite AI-only Dateien):
   ```bash
   rm app/src/lib/photos/PhotoUploader.ts
   rm app/src/lib/photos/photoQueueRepo.ts
   rm app/src/lib/photoResizer.ts
   rm app/src/lib/extractVereinsregeln.ts
   rm app/src/lib/enqueueAiJob.ts
   rm app/src/lib/uploadVereinsregelPdf.ts
   ```
   Wenn `app/src/lib/photos/` danach leer ist, Verzeichnis loeschen.

2. **Komponenten komplett loeschen** (per D-06 + implizite AI-only):
   ```bash
   rm app/src/components/AnalysisLoader.tsx
   rm app/src/components/BudgetWarningBanner.tsx
   rm app/src/components/CaptureStepCard.tsx
   rm app/src/components/ConfidenceBadge.tsx
   rm app/src/components/DimensionInput.tsx
   rm app/src/components/ExtractionLoader.tsx
   rm app/src/components/PhotoThumbnail.tsx
   rm app/src/components/ShapeSelector.tsx
   ```

3. **SyncTriggers.ts bereinigen** (Pitfall 5):
   - Import entfernen: `import { uploadPending } from '../photos/PhotoUploader';`
   - Block 1 entfernen (reconnect-Handler): `uploadPending().catch(...)` Block (ca. Zeilen 54-57)
   - Block 2 entfernen (AppState-Handler): `uploadPending().catch(...)` Block (ca. Zeilen 67-70)
   - Beide Event-Handler rufen danach nur noch `getSyncWorker().syncAll()` auf

4. **SyncWorker.ts bereinigen** (Pitfall 2):
   - Import entfernen: `PhotoQueueRow` aus dem named-import Block
   - Kommentar anpassen: "Entities, die gepullt werden (photo_queue ist upload-driven...)" -> "Entities, die gepullt werden"
   - switch-case entfernen: `case 'photo_queue': return this.pushPhotoQueue(entry);` aus `dispatchPush()`
   - Methode komplett entfernen: `private pushPhotoQueue(entry: OutboxEntry)` (ca. Zeilen 329-364)

5. **gardenPlanRepo.ts bereinigen** (per D-08):
   - Import entfernen: `PlanElementCandidate` aus dem named-import Block
   - Kommentar-Update: "Account-only (Claude Vision = server-side API call requires account mode)." -> "Account-only."
   - Funktion komplett entfernen: `saveElements()` (ca. Zeilen 96-144)
   - BEHALTEN: `saveDimensions()`, `loadDimensions()`, `loadAcceptedElements()`, `deleteAllElements()`

6. **migrateLocalToAccount.ts bereinigen** (per D-04):
   - Entferne die 2 Zeilen fuer photo_queue sync_state Reset (ca. Zeilen 291-292):
     ```typescript
     // photo_queue — nach Migration: 0 Einträge (keine Rows zu pullen)
     await storage.setSyncState({ entity: 'photo_queue', lastPullAt: serverNow, lastPushAt: null });
     ```

7. **rowMappers.ts bereinigen:**
   - `photoQueueFromDb` Mapper-Funktion entfernen (ca. Zeilen 337-353)
   - Zugehoerige Imports (PhotoQueueRow) entfernen falls nur dort genutzt

8. **SqliteAdapter.ts bereinigen** (Pitfall 2):
   - `'photo_queue'` aus ROW_ENTITIES Array entfernen
   - `photo_queue: 'garden_id'` aus GARDEN_ID_COLUMN Record entfernen
   - `photo_queue: 'gardenId'` aus GARDEN_ID_FIELD Record entfernen

9. **IndexedDbAdapter.ts bereinigen** (Pitfall 2, spiegelt SqliteAdapter):
   - `'photo_queue'` aus ROW_ENTITIES Array entfernen
   - `photo_queue: 'garden_id'` aus GARDEN_ID_COLUMN Record entfernen
   - `'photo_queue'` aus v3Entities inline-Array im openDB upgrade-Hook entfernen
  </action>
  <verify>
    <automated>bash -c "cd app && npx tsc --noEmit 2>&1 | head -20; echo '---'; test ! -f src/lib/photos/PhotoUploader.ts && test ! -f src/lib/photos/photoQueueRepo.ts && test ! -f src/lib/photoResizer.ts && test ! -f src/lib/extractVereinsregeln.ts && ! grep -q 'photo_queue' src/storage/SqliteAdapter.ts && ! grep -q 'photo_queue' src/storage/IndexedDbAdapter.ts && ! grep -q 'uploadPending' src/lib/sync/SyncTriggers.ts && ! grep -q 'pushPhotoQueue' src/lib/sync/SyncWorker.ts && echo 'FILES PASS' || echo 'FILES FAIL'"</automated>
  </verify>
  <acceptance_criteria>
    - app/src/lib/photos/PhotoUploader.ts does not exist
    - app/src/lib/photos/photoQueueRepo.ts does not exist
    - app/src/lib/photoResizer.ts does not exist
    - app/src/lib/extractVereinsregeln.ts does not exist
    - app/src/lib/enqueueAiJob.ts does not exist
    - app/src/lib/uploadVereinsregelPdf.ts does not exist
    - app/src/components/AnalysisLoader.tsx does not exist
    - SyncTriggers.ts does NOT contain 'uploadPending'
    - SyncWorker.ts does NOT contain 'pushPhotoQueue'
    - SyncWorker.ts does NOT contain 'photo_queue'
    - gardenPlanRepo.ts does NOT contain 'saveElements'
    - gardenPlanRepo.ts does NOT contain 'PlanElementCandidate'
    - gardenPlanRepo.ts does NOT contain 'Claude Vision'
    - migrateLocalToAccount.ts does NOT contain 'photo_queue'
    - SqliteAdapter.ts does NOT contain 'photo_queue'
    - IndexedDbAdapter.ts does NOT contain 'photo_queue'
    - rowMappers.ts does NOT contain 'photoQueueFromDb'
    - `cd app && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Alle AI-Client-Libs und Komponenten geloescht. SyncWorker, SyncTriggers, gardenPlanRepo, migrateLocalToAccount, rowMappers, beide StorageAdapter bereinigt. TypeScript-Build ist gruen.</done>
</task>

<task type="auto">
  <name>Task 2: Capture-Screens loeschen, Home-Screen + vereinsregeln/upload.tsx + i18n + README bereinigen</name>
  <files>
    app/app/(app)/capture/ (DELETE entire directory)
    app/app/(app)/index.tsx
    app/app/(app)/profile/vereinsregeln/upload.tsx
    packages/shared/src/i18n/de.json
    README.md
    app/.env.example
  </files>
  <read_first>
    app/app/(app)/index.tsx (Camera import, 2 Capture-Navigations-Aufrufe, Empty-State-Block)
    app/app/(app)/profile/vereinsregeln/upload.tsx (extractVereinsregeln + uploadVereinsregelPdf imports, ExtractionLoader)
    packages/shared/src/i18n/de.json (capture-Block ca. Zeilen 176-251)
    README.md (Zeilen mit "Claude API", "Pl@ntNet API", "Claude Vision")
    app/.env.example (CLAUDE_API_KEY Kommentar)
  </read_first>
  <action>
1. **Capture-Verzeichnis komplett loeschen** (per D-06, D-10):
   ```bash
   rm -rf app/app/(app)/capture/
   ```
   Expo Router file-based routing: Verzeichnis-Loeschung entfernt alle Capture-Routen automatisch.

2. **Home-Screen (`app/app/(app)/index.tsx`) bereinigen** (per D-10, Pitfall 3):
   - Import entfernen: `import { Camera } from 'lucide-react-native';`
   - Entferne den gesamten Footer CTAs-Block mit den zwei Capture-Pressables:
     - "Garten bearbeiten" -> `router.push('/(app)/capture/plan')`
     - "Erneut erfassen" -> `router.push('/(app)/capture/step-overview')`
   - Entferne im Empty-State-Block: Camera-Icon + "Garten erfassen"-Pressable + zugehoerigen Text
   - BEHALTEN: `loadAcceptedElements` / `loadDimensions` Imports, `GardenPlanView`-Render, `useRouter`
   - Ersetze den Empty-State-Block durch einen einfachen Placeholder:
     ```tsx
     <View className="flex-1 items-center justify-center p-6">
       <Text className="text-lg font-semibold text-stone-700 dark:text-stone-200 mb-2">
         {de.home.emptyTitle ?? 'Noch kein Gartenplan'}
       </Text>
       <Text className="text-sm text-stone-500 dark:text-stone-400 text-center">
         {de.home.emptySubtitle ?? 'Import-Funktion kommt bald.'}
       </Text>
     </View>
     ```

3. **vereinsregeln/upload.tsx komplett neu schreiben** (Pitfall 4):
   Ersetze den gesamten Inhalt durch einen Platzhalter-Screen:
   ```tsx
   import * as React from 'react';
   import { View, Text, Pressable, ScrollView } from 'react-native';
   import { useRouter } from 'expo-router';

   export default function VereinsregelnUploadScreen(): React.JSX.Element {
     const router = useRouter();
     return (
       <View className="flex-1 bg-stone-50 dark:bg-stone-900">
         <ScrollView contentContainerClassName="p-6 gap-6">
           <Text className="text-2xl font-semibold text-stone-900 dark:text-stone-100">
             Vereinssatzung
           </Text>
           <Text className="text-sm text-stone-600 dark:text-stone-300">
             Vereinsregeln-Import wird in einem zukuenftigen Update verfuegbar.
             Alternativ koennen Regeln manuell eingegeben werden.
           </Text>
           <Pressable
             accessibilityRole="button"
             onPress={() => router.push('/(app)/profile/vereinsregeln/confirm' as any)}
             className="mt-4 bg-[#4A7C59] dark:bg-[#6BAA7E] rounded-lg px-6 py-3 min-h-[44px] items-center justify-center active:opacity-80"
           >
             <Text className="text-white font-semibold">Manuell eingeben</Text>
           </Pressable>
         </ScrollView>
       </View>
     );
   }
   ```

4. **de.json bereinigen:**
   - Gesamten `"capture": { ... }` Block entfernen (ca. Zeilen 176-251, ~76 Zeilen)
   - Sicherstellen dass JSON syntaktisch korrekt bleibt (kein trailing comma)
   - Pruefe ob `"home"` Sektion existiert; falls ja, fuege `"emptyTitle": "Noch kein Gartenplan"` und `"emptySubtitle": "Import-Funktion kommt bald."` hinzu

5. **README.md bereinigen** (per REMOVE-03):
   - Ersetze "Claude API (Vision + Text)" durch "Strukturierter Import aus Claude.ai-Projekt"
   - Entferne "Pl@ntNet API" Referenz komplett
   - Ersetze "Claude Vision analysiert" durch "Manueller Plan-Editor"
   - Entferne jegliche Sprache die suggeriert die App mache AI-API-Aufrufe

6. **app/.env.example bereinigen** (per REMOVE-02):
   - Entferne den CLAUDE_API_KEY Kommentar/Verweis (ist schon kommentiert, aber Kommentar referenziert FOUND-06 das superseded ist)
   - Entferne ANTHROPIC_API_KEY / PLANTNET_API_KEY Referenzen falls vorhanden
  </action>
  <verify>
    <automated>bash -c "test ! -d app/app/\(app\)/capture && ! grep -q 'Camera' app/app/\(app\)/index.tsx && ! grep -q 'capture' app/app/\(app\)/index.tsx && ! grep -q 'extractVereinsregeln' app/app/\(app\)/profile/vereinsregeln/upload.tsx && ! grep -qi 'anthropic\|plantnet\|vision' README.md && ! grep -q '\"capture\"' packages/shared/src/i18n/de.json && cd app && npx tsc --noEmit 2>&1 | tail -5; echo 'VERIFY DONE'"</automated>
  </verify>
  <acceptance_criteria>
    - app/app/(app)/capture/ directory does not exist
    - index.tsx does NOT contain 'Camera' (from lucide-react-native)
    - index.tsx does NOT contain 'capture'
    - index.tsx does NOT contain 'step-overview'
    - vereinsregeln/upload.tsx does NOT contain 'extractVereinsregeln'
    - vereinsregeln/upload.tsx does NOT contain 'uploadVereinsregelPdf'
    - vereinsregeln/upload.tsx does NOT contain 'ExtractionLoader'
    - vereinsregeln/upload.tsx contains 'Manuell eingeben'
    - de.json does NOT contain '"capture"' as a top-level key
    - README.md does NOT contain 'Pl@ntNet'
    - README.md does NOT contain 'Claude Vision'
    - .env.example does NOT contain 'CLAUDE_API_KEY'
    - `grep -ri "anthropic\|plantnet" app/src/ app/app/` returns no functional code matches
    - `cd app && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Capture-Verzeichnis geloescht, Home-Screen und vereinsregeln/upload.tsx bereinigt, i18n capture-Block entfernt, README und .env.example von AI-Sprache bereinigt. App baut fehlerfrei.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-facing screens | Home-Screen und vereinsregeln/upload duerfen keine toten Navigation-Links mehr haben |
| i18n strings | Keine capture-Keys die auf geloeschte Screens referenzieren |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Denial of Service | Home-Screen capture navigation | mitigate | Capture-Buttons komplett entfernt statt Redirect (D-10) |
| T-05-05 | Information Disclosure | README AI-Sprache | mitigate | README bereinigt, keine Suggestions dass App AI-Calls macht |
| T-05-06 | Denial of Service | vereinsregeln/upload.tsx broken imports | mitigate | Screen komplett neu geschrieben als Platzhalter |
</threat_model>

<verification>
- `cd app && npx tsc --noEmit` -- TypeScript baut fehlerfrei
- `grep -ri "anthropic\|plantnet\|vision" app/src/ app/app/` -- keine funktionalen Code-Treffer
- `grep -ri "capture" app/app/\(app\)/index.tsx` -- keine Capture-Referenzen
- `test ! -d app/app/\(app\)/capture` -- Capture-Verzeichnis existiert nicht
</verification>

<success_criteria>
- Alle AI-Client-Libs und Komponenten geloescht (16 Dateien)
- SyncWorker, SyncTriggers, gardenPlanRepo, migrateLocalToAccount, rowMappers, SqliteAdapter, IndexedDbAdapter bereinigt
- Capture-Verzeichnis komplett geloescht (9 Screens + Layout)
- Home-Screen ohne Capture-Buttons
- vereinsregeln/upload.tsx als Platzhalter neu geschrieben
- de.json ohne capture-Block
- README ohne AI-Call-Sprache
- .env.example ohne AI-Key-Referenzen
- TypeScript-Build ist gruen
</success_criteria>

<output>
After completion, create `.planning/phases/05-ai-removal-import-schema/05-02-SUMMARY.md`
</output>
