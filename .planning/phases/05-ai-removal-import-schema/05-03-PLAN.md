---
phase: 05-ai-removal-import-schema
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - schemas/spatenstich-import.v1.json
  - schemas/examples/full.json
  - schemas/examples/minimal.json
  - schemas/examples/edge-cases.json
  - scripts/validate-import-schema.js
autonomous: false
requirements:
  - IMPORT-01
  - IMPORT-02

user_setup:
  - service: supabase
    why: "Database schema push for Migration 015"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard -> Account -> Access Tokens"
    dashboard_config: []

must_haves:
  truths:
    - "schemas/spatenstich-import.v1.json ist valides JSON Schema draft 2020-12"
    - "Alle 3 Referenz-Payloads (full, minimal, edge-cases) validieren gegen das Schema"
    - "Schema enthaelt schemaVersion, capture, beds, plants, observations, complianceFlags, freeFormNotes"
    - "Supabase-Migration 015 ist auf die Datenbank gepusht"
  artifacts:
    - path: "schemas/spatenstich-import.v1.json"
      provides: "JSON Schema draft 2020-12 fuer spatenstich-import.v1"
      contains: "json-schema.org/draft/2020-12/schema"
    - path: "schemas/examples/full.json"
      provides: "Vollstaendiges Referenz-Payload mit allen Feldern"
      contains: "spatenstich-import.v1"
    - path: "schemas/examples/minimal.json"
      provides: "Minimales Referenz-Payload mit nur Pflichtfeldern"
      contains: "spatenstich-import.v1"
    - path: "schemas/examples/edge-cases.json"
      provides: "Edge-Case-Payload mit niedriger Confidence und fehlenden optionalen Feldern"
      contains: "spatenstich-import.v1"
    - path: "scripts/validate-import-schema.js"
      provides: "AJV-Validierungsscript fuer Schema + Payloads"
      contains: "ajv/dist/2020"
  key_links:
    - from: "scripts/validate-import-schema.js"
      to: "schemas/spatenstich-import.v1.json"
      via: "require/readFileSync"
      pattern: "spatenstich-import.v1.json"
    - from: "schemas/examples/full.json"
      to: "schemas/spatenstich-import.v1.json"
      via: "schema validation"
      pattern: "schemaVersion.*spatenstich-import.v1"
---

<objective>
Import-Schema `spatenstich-import.v1` als JSON Schema (draft 2020-12) definieren, drei Referenz-Payloads erstellen, Validierungsscript bauen, und Migration 015 auf Supabase pushen.

Purpose: Datenkontrakt zwischen Claude.ai-Projekt und Spatenstich-App. Grundlage fuer Phase 6 (Import-Flow).
Output: Schema-Datei, 3 Referenz-Payloads, Validierungsscript, gepushte DB-Migration.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ai-removal-import-schema/05-CONTEXT.md
@.planning/phases/05-ai-removal-import-schema/05-RESEARCH.md
@docs/specs/M07-claude-ai-bridge.md

<interfaces>
<!-- From M07-claude-ai-bridge.md — the data contract this schema must formalize -->
<!-- Schema version: "spatenstich-import.v1" -->
<!-- Required: schemaVersion, capture.timestamp, at least one of beds|plants|observations -->
<!-- Enums:
  sunExposure: "full" | "half" | "shade" | "mixed"
  stageEstimate: "seedling" | "vegetative" | "flowering" | "fruiting" | "senescent"
  kind (observation): "pest" | "disease" | "weather" | "soil" | "structural" | "other"
  status (compliance): "compliant" | "warn" | "violation"
  confidence: number 0.0-1.0
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: JSON Schema + Referenz-Payloads + Validierungsscript erstellen</name>
  <files>
    schemas/spatenstich-import.v1.json
    schemas/examples/full.json
    schemas/examples/minimal.json
    schemas/examples/edge-cases.json
    scripts/validate-import-schema.js
  </files>
  <read_first>
    docs/specs/M07-claude-ai-bridge.md (Datenkontrakt-Beispiel, Enum-Werte, Required-Felder)
    .planning/phases/05-ai-removal-import-schema/05-RESEARCH.md (Schema-Grundstruktur Zeilen 270-636, AJV-Script Zeilen 643-680)
  </read_first>
  <action>
1. **Verzeichnisse anlegen:**
   ```bash
   mkdir -p schemas/examples
   mkdir -p scripts  # existiert bereits, aber sicherheitshalber
   ```

2. **`schemas/spatenstich-import.v1.json` erstellen** (per D-11, D-12, D-13, D-14, IMPORT-01):

   Vollstaendiges JSON Schema draft 2020-12. Exakte Struktur:
   ```json
   {
     "$schema": "https://json-schema.org/draft/2020-12/schema",
     "$id": "https://spatenstich.app/schemas/spatenstich-import.v1.json",
     "title": "Spatenstich Import Payload v1",
     "description": "Datenkontrakt zwischen Claude.ai-Projekt und Spatenstich-App. Emittiert am Ende jeder Foto-Analyse-Session.",
     "type": "object",
     "required": ["schemaVersion", "capture"],
     "additionalProperties": false,
     "properties": {
       "schemaVersion": {
         "const": "spatenstich-import.v1",
         "description": "Versionierung: neue Version = neues Schema-File (D-13). v1 bleibt stabil."
       },
       "capture": {
         "type": "object",
         "required": ["timestamp"],
         "additionalProperties": false,
         "properties": {
           "timestamp": { "type": "string", "format": "date-time" },
           "location": {
             "type": "object",
             "required": ["lat", "lon"],
             "additionalProperties": false,
             "properties": {
               "lat": { "type": "number", "minimum": -90, "maximum": 90 },
               "lon": { "type": "number", "minimum": -180, "maximum": 180 }
             }
           },
           "photoRefs": {
             "type": "array",
             "items": { "type": "string" },
             "description": "Dateiname-Hints, kein Payload"
           },
           "chatReference": { "type": "string", "format": "uri" }
         }
       },
       "beds": {
         "type": "array",
         "items": {
           "type": "object",
           "required": ["localId", "label"],
           "additionalProperties": false,
           "properties": {
             "localId": { "type": "string" },
             "label": { "type": "string" },
             "approxDimensions": {
               "type": "object",
               "additionalProperties": false,
               "properties": {
                 "lengthCm": { "type": "number", "minimum": 0 },
                 "widthCm": { "type": "number", "minimum": 0 }
               }
             },
             "sunExposure": { "type": "string", "enum": ["full", "half", "shade", "mixed"] },
             "soilNotes": { "type": "string" },
             "confidence": { "type": "number", "minimum": 0, "maximum": 1 }
           }
         }
       },
       "plants": {
         "type": "array",
         "items": {
           "type": "object",
           "required": ["localId"],
           "additionalProperties": false,
           "properties": {
             "localId": { "type": "string" },
             "bedRef": { "type": "string" },
             "scientificName": { "type": "string" },
             "commonNameDe": { "type": "string" },
             "stageEstimate": { "type": "string", "enum": ["seedling", "vegetative", "flowering", "fruiting", "senescent"] },
             "healthNotes": { "type": "string" },
             "confidence": { "type": "number", "minimum": 0, "maximum": 1 }
           }
         }
       },
       "observations": {
         "type": "array",
         "items": {
           "type": "object",
           "required": ["localId", "kind", "summary"],
           "additionalProperties": false,
           "properties": {
             "localId": { "type": "string" },
             "bedRef": { "type": "string" },
             "kind": { "type": "string", "enum": ["pest", "disease", "weather", "soil", "structural", "other"] },
             "summary": { "type": "string" },
             "suggestedActions": { "type": "array", "items": { "type": "string" } }
           }
         }
       },
       "complianceFlags": {
         "type": "array",
         "description": "Optional (D-11) — wird in Phase 10 angezeigt. v1 akzeptiert, ignoriert in UI.",
         "items": {
           "type": "object",
           "required": ["regulation", "status"],
           "additionalProperties": false,
           "properties": {
             "regulation": { "type": "string" },
             "status": { "type": "string", "enum": ["compliant", "warn", "violation"] },
             "note": { "type": "string" }
           }
         }
       },
       "freeFormNotes": {
         "type": "string",
         "description": "Markdown erlaubt, nicht validiert (D-12)."
       }
     }
   }
   ```

3. **`schemas/examples/full.json` erstellen** (IMPORT-02 -- alle Felder belegt):
   ```json
   {
     "schemaVersion": "spatenstich-import.v1",
     "capture": {
       "timestamp": "2026-05-08T14:32:00+02:00",
       "location": { "lat": 51.34, "lon": 12.37 },
       "photoRefs": ["IMG_2415.jpg", "IMG_2416.jpg"],
       "chatReference": "https://claude.ai/chat/abc123"
     },
     "beds": [
       {
         "localId": "bed-a",
         "label": "Hochbeet Nordseite",
         "approxDimensions": { "lengthCm": 200, "widthCm": 80 },
         "sunExposure": "half",
         "soilNotes": "lehmig, mulchig",
         "confidence": 0.85
       },
       {
         "localId": "bed-b",
         "label": "Erdbeerbeet Suedseite",
         "approxDimensions": { "lengthCm": 300, "widthCm": 120 },
         "sunExposure": "full",
         "soilNotes": "sandig-humos",
         "confidence": 0.92
       }
     ],
     "plants": [
       {
         "localId": "plant-1",
         "bedRef": "bed-a",
         "scientificName": "Beta vulgaris subsp. cicla",
         "commonNameDe": "Mangold",
         "stageEstimate": "vegetative",
         "healthNotes": "leichter Schneckenfraß",
         "confidence": 0.72
       },
       {
         "localId": "plant-2",
         "bedRef": "bed-b",
         "scientificName": "Fragaria × ananassa",
         "commonNameDe": "Erdbeere",
         "stageEstimate": "flowering",
         "healthNotes": "gesund, kraeftig",
         "confidence": 0.95
       }
     ],
     "observations": [
       {
         "localId": "obs-1",
         "bedRef": "bed-a",
         "kind": "pest",
         "summary": "Schnecken aktiv, Nachtschaeden",
         "suggestedActions": ["Schneckenkorn Eisen-III", "Bierfalle"]
       }
     ],
     "complianceFlags": [
       {
         "regulation": "BKleingG §1 Abs. 1",
         "status": "compliant",
         "note": "Gartennutzung gaertnerisch, kein dominanter Rasen"
       }
     ],
     "freeFormNotes": "Insgesamt guter Zustand. Kompost-Ecke im Nordwesten beachten."
   }
   ```

4. **`schemas/examples/minimal.json` erstellen** (IMPORT-02 -- nur Pflichtfelder + 1 Entity):
   ```json
   {
     "schemaVersion": "spatenstich-import.v1",
     "capture": {
       "timestamp": "2026-05-09T10:00:00Z"
     },
     "beds": [
       {
         "localId": "bed-001",
         "label": "Hochbeet Nord"
       }
     ]
   }
   ```

5. **`schemas/examples/edge-cases.json` erstellen** (IMPORT-02 -- niedrige Confidence, fehlende optionale Felder, multilingual):
   ```json
   {
     "schemaVersion": "spatenstich-import.v1",
     "capture": {
       "timestamp": "2026-05-09T16:45:00+02:00",
       "photoRefs": ["foto_unscharfes_beet.jpg"]
     },
     "beds": [
       {
         "localId": "bed-x",
         "label": "Unklares Beet (Schatten)",
         "sunExposure": "shade",
         "confidence": 0.35
       }
     ],
     "plants": [
       {
         "localId": "plant-x1",
         "bedRef": "bed-x",
         "commonNameDe": "Vermutlich Petersilie",
         "confidence": 0.28
       },
       {
         "localId": "plant-x2",
         "scientificName": "Solanum lycopersicum",
         "commonNameDe": "Tomate",
         "stageEstimate": "senescent",
         "healthNotes": "Braunfaeule-Verdacht, Blaetter verfaerbt",
         "confidence": 0.55
       }
     ],
     "observations": [
       {
         "localId": "obs-x1",
         "kind": "disease",
         "summary": "Moegliche Braunfaeule an Tomaten im Schattenbeet",
         "suggestedActions": ["Befallene Blaetter entfernen", "Kupferspritzmittel erwaegen"]
       },
       {
         "localId": "obs-x2",
         "kind": "structural",
         "summary": "Zaunpfosten schief, Sturmschaden"
       }
     ],
     "complianceFlags": [
       {
         "regulation": "Leipziger Gartenordnung §4",
         "status": "warn",
         "note": "Heckenhoehe unklar, moeglicherweise ueber 1.20m"
       }
     ],
     "freeFormNotes": "Fotos bei schlechtem Licht gemacht. Confidence-Werte entsprechend niedrig.\n\n## Offene Fragen\n- Beet im Schatten: was waechst da genau?\n- Zaunreparatur priorisieren?"
   }
   ```

6. **`scripts/validate-import-schema.js` erstellen** (Validierungsscript mit AJV draft 2020-12):
   ```javascript
   #!/usr/bin/env node
   // Validates all reference payloads against the spatenstich-import v1 schema.
   // Usage: node scripts/validate-import-schema.js

   const Ajv2020 = require('ajv/dist/2020');
   const addFormats = require('ajv-formats');
   const path = require('path');
   const fs = require('fs');

   const ajv = new Ajv2020({ strict: false, allErrors: true });
   addFormats(ajv);

   const schemaPath = path.join(__dirname, '..', 'schemas', 'spatenstich-import.v1.json');
   const schema = JSON.parse(fs.readFileSync(schemaPath, 'utf8'));
   const validate = ajv.compile(schema);

   const payloads = ['full', 'minimal', 'edge-cases'];
   let allValid = true;

   for (const name of payloads) {
     const filePath = path.join(__dirname, '..', 'schemas', 'examples', `${name}.json`);
     const payload = JSON.parse(fs.readFileSync(filePath, 'utf8'));
     const valid = validate(payload);
     if (!valid) {
       console.error(`INVALID: ${name}.json`);
       console.error(JSON.stringify(validate.errors, null, 2));
       allValid = false;
     } else {
       console.log(`VALID: ${name}.json`);
     }
   }

   if (!allValid) {
     process.exit(1);
   } else {
     console.log('\nAll payloads valid against spatenstich-import.v1 schema.');
   }
   ```
  </action>
  <verify>
    <automated>node scripts/validate-import-schema.js</automated>
  </verify>
  <acceptance_criteria>
    - schemas/spatenstich-import.v1.json exists and contains "$schema": "https://json-schema.org/draft/2020-12/schema"
    - schemas/spatenstich-import.v1.json contains "const": "spatenstich-import.v1"
    - schemas/spatenstich-import.v1.json contains "beds", "plants", "observations", "complianceFlags", "freeFormNotes" as properties
    - schemas/examples/full.json exists and contains "schemaVersion": "spatenstich-import.v1"
    - schemas/examples/minimal.json exists and contains "schemaVersion": "spatenstich-import.v1"
    - schemas/examples/edge-cases.json exists and contains "schemaVersion": "spatenstich-import.v1"
    - scripts/validate-import-schema.js exists and contains "ajv/dist/2020"
    - `node scripts/validate-import-schema.js` outputs "VALID: full.json", "VALID: minimal.json", "VALID: edge-cases.json" and exits 0
  </acceptance_criteria>
  <done>JSON Schema v1 definiert, 3 Referenz-Payloads erstellt, alle validieren gegen Schema via AJV.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Supabase DB Push (Migration 015)</name>
  <what-built>Migration 015 (DROP ai_results, ai_jobs, pgmq queue) wurde in Plan 01 erstellt. Schema muss auf die Live-Datenbank gepusht werden.</what-built>
  <how-to-verify>
1. Setze SUPABASE_ACCESS_TOKEN als Umgebungsvariable (falls nicht bereits gesetzt):
   ```bash
   export SUPABASE_ACCESS_TOKEN=sbp_... # aus Supabase Dashboard -> Account -> Access Tokens
   ```
2. Fuehre den DB-Push aus:
   ```bash
   supabase db push --linked
   ```
3. Erwartete Ausgabe: Migration 015 angewendet, keine Fehler.
4. Verifiziere in Supabase Dashboard -> Table Editor: Tabellen `ai_jobs` und `ai_results` existieren nicht mehr.
  </how-to-verify>
  <resume-signal>Tippe "pushed" wenn Migration erfolgreich, oder beschreibe Fehler</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema validation | Validierungsscript prueft Payloads gegen Schema -- kein Laufzeit-Risk |
| DB Push | Migration 015 DROP TABLE ist destruktiv -- aber Phase 4 war nie produktiv |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Tampering | Schema file | accept | Schema ist read-only Artefakt, keine Runtime-Abhaengigkeit in Phase 5 |
| T-05-08 | Denial of Service | DB Push failure | mitigate | pgmq.drop_queue mit IF EXISTS guard; DROP TABLE IF EXISTS CASCADE |
</threat_model>

<verification>
- `node scripts/validate-import-schema.js` -- alle 3 Payloads VALID
- `cat schemas/spatenstich-import.v1.json | python -m json.tool` -- valides JSON
- `supabase db push --linked` -- Migration 015 erfolgreich (human-verified)
</verification>

<success_criteria>
- Schema-Datei `schemas/spatenstich-import.v1.json` existiert als valides JSON Schema draft 2020-12
- Drei Referenz-Payloads existieren und validieren gegen Schema
- Validierungsscript `scripts/validate-import-schema.js` laeuft fehlerfrei
- Migration 015 auf Supabase-DB gepusht
</success_criteria>

<output>
After completion, create `.planning/phases/05-ai-removal-import-schema/05-03-SUMMARY.md`
</output>
