---
phase: 05-ai-removal-import-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260509000015_remove_ai_tables.sql
  - supabase/functions/ai-job-consumer/ (DELETE)
  - supabase/functions/extract-vereinsregeln/ (DELETE)
  - supabase/config.toml
  - supabase/tests/enqueue_photo_analysis.sql (DELETE)
  - supabase/tests/photo-queue-rls.test.sql (DELETE)
  - packages/shared/src/types/entities.ts
  - packages/shared/src/types/database.ts
  - packages/shared/src/types/supabase.ts
autonomous: true
requirements:
  - REMOVE-01
  - REMOVE-02
  - REMOVE-03

must_haves:
  truths:
    - "ai_jobs und ai_results Tabellen existieren nicht mehr in Supabase"
    - "pgmq ai_jobs Queue ist geloescht"
    - "Edge Functions ai-job-consumer und extract-vereinsregeln existieren nicht mehr"
    - "photo_queue ist aus EntityName Union entfernt"
    - "PlanElementCandidate Interface existiert nicht mehr"
    - "supabase config.toml referenziert keine AI Functions mehr"
  artifacts:
    - path: "supabase/migrations/20260509000015_remove_ai_tables.sql"
      provides: "DROP TABLE ai_results, ai_jobs, pgmq queue"
      contains: "DROP TABLE IF EXISTS public.ai_results"
    - path: "packages/shared/src/types/entities.ts"
      provides: "Bereinigte Type-Definitionen ohne AI-Typen"
  key_links:
    - from: "supabase/migrations/20260509000015_remove_ai_tables.sql"
      to: "supabase/config.toml"
      via: "Edge Function entries removed matches deleted function dirs"
      pattern: "ai-job-consumer|extract-vereinsregeln"
---

<objective>
Backend-Bereinigung: AI-Tabellen, Edge Functions, Supabase-Tests und Shared Types von allen AI-Relikten befreien.

Purpose: Fundament fuer Wave 2 -- alle Backend- und Type-Aenderungen muessen zuerst stehen, bevor App-Code bereinigt wird (TypeScript-Abhaengigkeiten).
Output: Supabase-Migration 015 (DROP ai_results, ai_jobs, pgmq queue), geloeschte Edge Functions, bereinigte shared types.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ai-removal-import-schema/05-CONTEXT.md
@.planning/phases/05-ai-removal-import-schema/05-RESEARCH.md
@.planning/phases/05-ai-removal-import-schema/05-PATTERNS.md
@docs/specs/M07-claude-ai-bridge.md

<interfaces>
<!-- Key types the executor needs from packages/shared/src/types/entities.ts -->
<!-- Lines to REMOVE are marked; everything else stays -->

From packages/shared/src/types/entities.ts:
```typescript
// REMOVE from EntityName union (line 11):
  | 'photo_queue'

// REMOVE entire interface (lines 54-62):
export interface PhotoQueueRow extends RowBase { ... }

// REMOVE from PlanElementRow (line 74):
  aiResultId: string | null;

// REMOVE entire interface (lines 86-94):
export interface PlanElementCandidate { ... }

// REMOVE from AnyRow union (line 101):
  | PhotoQueueRow
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Supabase-Migration 015 + Edge Functions loeschen + config.toml + Tests</name>
  <files>
    supabase/migrations/20260509000015_remove_ai_tables.sql
    supabase/functions/ai-job-consumer/ (DELETE entire directory)
    supabase/functions/extract-vereinsregeln/ (DELETE entire directory)
    supabase/config.toml
    supabase/tests/enqueue_photo_analysis.sql (DELETE)
    supabase/tests/photo-queue-rls.test.sql (DELETE)
  </files>
  <read_first>
    supabase/migrations/20260504000014_garden_plan.sql (Migration-Pattern: Header-Kommentar, Sektionen, DO $$ Invariant-Block)
    supabase/config.toml (aktuelle Eintraege fuer ai-job-consumer und extract-vereinsregeln finden)
    supabase/functions/ai-job-consumer/index.ts (bestaetigen dass Verzeichnis existiert)
    supabase/functions/extract-vereinsregeln/index.ts (bestaetigen dass Verzeichnis existiert)
    supabase/tests/enqueue_photo_analysis.sql (bestaetigen dass Datei existiert)
    supabase/tests/photo-queue-rls.test.sql (bestaetigen dass Datei existiert)
  </read_first>
  <action>
1. **Neue Migration erstellen:** `supabase/migrations/20260509000015_remove_ai_tables.sql`

   Inhalt (exakt -- per D-03, D-05):
   ```sql
   -- Phase 5 Plan 01 Task 1: AI-Tabellen und pgmq-Queue entfernen
   -- Provides: DROP ai_results, ai_jobs, pgmq.ai_jobs-Queue
   -- Follows: Migration 014 pattern (Kommentar, Sektionen, DO $$ Invariant-Block)
   --
   -- Atomicity: Supabase wraps file in implicit transaction. DO NOT add BEGIN/COMMIT.

   -- ──────────────────────────────────────────────────────────────
   -- Section 1 — RLS-Policies entfernen
   -- ──────────────────────────────────────────────────────────────
   DROP POLICY IF EXISTS "ai_results_creator_read" ON public.ai_results;
   DROP POLICY IF EXISTS "ai_results_insert_service" ON public.ai_results;
   DROP POLICY IF EXISTS "ai_jobs_creator_insert" ON public.ai_jobs;
   DROP POLICY IF EXISTS "ai_jobs_creator_read" ON public.ai_jobs;
   DROP POLICY IF EXISTS "ai_jobs_update_service" ON public.ai_jobs;
   DROP POLICY IF EXISTS "ai_jobs_member_insert" ON public.ai_jobs;
   DROP POLICY IF EXISTS "ai_jobs_member_read" ON public.ai_jobs;
   DROP POLICY IF EXISTS "ai_results_member_read" ON public.ai_results;

   -- ──────────────────────────────────────────────────────────────
   -- Section 2 — Tabellen droppen (ai_results zuerst wegen FK auf ai_jobs)
   -- ──────────────────────────────────────────────────────────────
   DROP TABLE IF EXISTS public.ai_results CASCADE;
   DROP TABLE IF EXISTS public.ai_jobs CASCADE;

   -- ──────────────────────────────────────────────────────────────
   -- Section 3 — pgmq-Queue loeschen (D-05: Extension kann bleiben)
   -- ──────────────────────────────────────────────────────────────
   DO $$
   BEGIN
     IF EXISTS (SELECT 1 FROM information_schema.schemata WHERE schema_name = 'pgmq') THEN
       PERFORM pgmq.drop_queue('ai_jobs', true);
     END IF;
   END $$;

   -- ──────────────────────────────────────────────────────────────
   -- Section 4 — Post-migration Invariant-Assertions
   -- ──────────────────────────────────────────────────────────────
   DO $$
   BEGIN
     IF EXISTS (SELECT 1 FROM information_schema.tables
                WHERE table_schema='public' AND table_name IN ('ai_jobs','ai_results')) THEN
       RAISE EXCEPTION 'phase5_invariant: ai tables still exist after drop';
     END IF;
     RAISE NOTICE 'phase5 migration ok: ai_jobs + ai_results + pgmq queue removed';
   END $$;
   ```

2. **Edge Functions komplett loeschen** (per D-06):
   - `rm -rf supabase/functions/ai-job-consumer/` (gesamtes Verzeichnis)
   - `rm -rf supabase/functions/extract-vereinsregeln/` (gesamtes Verzeichnis)

3. **supabase/config.toml bereinigen** (per D-07):
   Entferne diese beiden Bloecke (ungefaehr Zeilen 384-388):
   ```toml
   [functions.ai-job-consumer]
   verify_jwt = true

   [functions.extract-vereinsregeln]
   verify_jwt = true
   ```

4. **Supabase-Tests loeschen:**
   - `rm supabase/tests/enqueue_photo_analysis.sql`
   - `rm supabase/tests/photo-queue-rls.test.sql`
  </action>
  <verify>
    <automated>bash -c "test ! -d supabase/functions/ai-job-consumer && test ! -d supabase/functions/extract-vereinsregeln && test -f supabase/migrations/20260509000015_remove_ai_tables.sql && ! grep -q 'ai-job-consumer' supabase/config.toml && ! grep -q 'extract-vereinsregeln' supabase/config.toml && echo 'PASS' || echo 'FAIL'"</automated>
  </verify>
  <acceptance_criteria>
    - supabase/functions/ai-job-consumer/ directory does not exist
    - supabase/functions/extract-vereinsregeln/ directory does not exist
    - supabase/migrations/20260509000015_remove_ai_tables.sql contains "DROP TABLE IF EXISTS public.ai_results CASCADE"
    - supabase/migrations/20260509000015_remove_ai_tables.sql contains "DROP TABLE IF EXISTS public.ai_jobs CASCADE"
    - supabase/migrations/20260509000015_remove_ai_tables.sql contains "pgmq.drop_queue"
    - supabase/config.toml does NOT contain "ai-job-consumer"
    - supabase/config.toml does NOT contain "extract-vereinsregeln"
    - supabase/tests/enqueue_photo_analysis.sql does not exist
    - supabase/tests/photo-queue-rls.test.sql does not exist
  </acceptance_criteria>
  <done>Migration 015 erstellt, beide Edge Functions geloescht, config.toml bereinigt, AI-bezogene Supabase-Tests geloescht.</done>
</task>

<task type="auto">
  <name>Task 2: Shared Types bereinigen (entities.ts, database.ts, supabase.ts)</name>
  <files>
    packages/shared/src/types/entities.ts
    packages/shared/src/types/database.ts
    packages/shared/src/types/supabase.ts
  </files>
  <read_first>
    packages/shared/src/types/entities.ts (vollstaendig lesen -- EntityName, PhotoQueueRow, PlanElementCandidate, PlanElementRow.aiResultId, AnyRow)
    packages/shared/src/types/database.ts (ai_jobs, ai_results Definitionen finden)
    packages/shared/src/types/supabase.ts (ai_jobs, ai_results, photo_queue Definitionen finden)
  </read_first>
  <action>
1. **entities.ts** bereinigen (per D-04, D-09):
   - `EntityName` Union: `| 'photo_queue'` Zeile entfernen (Zeile 11)
   - `PhotoQueueRow` Interface komplett entfernen (Zeilen 54-62)
   - `PlanElementRow`: Feld `aiResultId: string | null;` entfernen (Zeile 74)
   - `PlanElementCandidate` Interface komplett entfernen (Zeilen 86-94, inkl. Kommentar "Pre-confirmation shape from Claude Vision")
   - `AnyRow` Union: `| PhotoQueueRow` entfernen (Zeile 101)

   Resultierende EntityName:
   ```typescript
   export type EntityName =
     | 'gardens'
     | 'garden_members'
     | 'profiles'
     | 'vereinsregeln'
     | 'invite_codes'
     | 'garden_dimensions'
     | 'plan_elements';
   ```

   Resultierende AnyRow:
   ```typescript
   export type AnyRow =
     | GardenRow
     | GardenMemberRow
     | ProfileRow
     | VereinsregelnRow
     | InviteCodeRow
     | GardenDimensionsRow
     | PlanElementRow;
   ```

   Resultierende PlanElementRow (ohne aiResultId):
   ```typescript
   export interface PlanElementRow extends RowBase {
     gardenId: string;
     elementType: string;
     label: string;
     xM: number;
     yM: number;
     widthM: number;
     heightM: number;
     confidence: 'high' | 'medium' | 'low' | null;
     isAccepted: boolean;
   }
   ```

2. **database.ts** bereinigen:
   - Alle `ai_jobs`-Table-Definitionen entfernen
   - Alle `ai_results`-Table-Definitionen entfernen

3. **supabase.ts** bereinigen:
   - Alle `ai_jobs`-Table-Definitionen entfernen
   - Alle `ai_results`-Table-Definitionen entfernen
   - Alle `photo_queue`-Table-Definitionen entfernen
  </action>
  <verify>
    <automated>bash -c "! grep -q 'photo_queue' packages/shared/src/types/entities.ts && ! grep -q 'PhotoQueueRow' packages/shared/src/types/entities.ts && ! grep -q 'PlanElementCandidate' packages/shared/src/types/entities.ts && ! grep -q 'aiResultId' packages/shared/src/types/entities.ts && ! grep -q 'ai_jobs' packages/shared/src/types/database.ts && ! grep -q 'ai_results' packages/shared/src/types/database.ts && echo 'PASS' || echo 'FAIL'"</automated>
  </verify>
  <acceptance_criteria>
    - entities.ts does NOT contain 'photo_queue'
    - entities.ts does NOT contain 'PhotoQueueRow'
    - entities.ts does NOT contain 'PlanElementCandidate'
    - entities.ts does NOT contain 'aiResultId'
    - entities.ts contains EntityName with exactly 7 members: gardens, garden_members, profiles, vereinsregeln, invite_codes, garden_dimensions, plan_elements
    - database.ts does NOT contain 'ai_jobs'
    - database.ts does NOT contain 'ai_results'
    - supabase.ts does NOT contain 'ai_jobs'
    - supabase.ts does NOT contain 'ai_results'
    - supabase.ts does NOT contain 'photo_queue'
  </acceptance_criteria>
  <done>Shared types bereinigt: photo_queue, PhotoQueueRow, PlanElementCandidate, aiResultId entfernt aus entities.ts. ai_jobs/ai_results aus database.ts und supabase.ts entfernt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase DB Migration | DROP TABLE removes data permanently -- acceptable since Phase 4 was never human-verified |
| Edge Function Secrets | CLAUDE_API_KEY in Supabase Dashboard muss manuell entfernt werden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | Supabase Secrets | accept | CLAUDE_API_KEY manuell ueber `supabase secrets unset` entfernen -- Plan 02 dokumentiert dies als Checkpoint |
| T-05-02 | Elevation of Privilege | RLS Policies on dropped tables | mitigate | Migration 015 explizit DROP POLICY vor DROP TABLE; CASCADE als Fallback |
| T-05-03 | Denial of Service | pgmq queue with pending messages | mitigate | pgmq.drop_queue mit purge=true loescht alle pending messages |
</threat_model>

<verification>
- `test -f supabase/migrations/20260509000015_remove_ai_tables.sql` -- Migration existiert
- `test ! -d supabase/functions/ai-job-consumer` -- Edge Function geloescht
- `test ! -d supabase/functions/extract-vereinsregeln` -- Edge Function geloescht
- `grep -c 'photo_queue' packages/shared/src/types/entities.ts` returns 0
- `grep -c 'PlanElementCandidate' packages/shared/src/types/entities.ts` returns 0
</verification>

<success_criteria>
- Migration 015 erstellt mit korrekter DROP-Reihenfolge (ai_results vor ai_jobs)
- Beide Edge Function Verzeichnisse geloescht
- config.toml ohne AI Function-Eintraege
- entities.ts ohne photo_queue, PhotoQueueRow, PlanElementCandidate, aiResultId
- database.ts und supabase.ts ohne ai_jobs/ai_results
</success_criteria>

<output>
After completion, create `.planning/phases/05-ai-removal-import-schema/05-01-SUMMARY.md`
</output>
