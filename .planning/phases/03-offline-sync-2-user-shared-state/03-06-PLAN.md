---
phase: 03-offline-sync-2-user-shared-state
plan: 06
type: execute
wave: 4
depends_on: ["03-01", "03-02", "03-03", "03-04", "03-05"]
files_modified:
  - app/src/components/SyncStatusBadge.tsx (neu)
  - app/src/components/SyncStatusBadge.styles.ts (neu — NativeWind classnames)
  - app/src/hooks/useSyncStatus.ts (neu)
  - app/src/i18n/de.ts (erweitert um sync.*-Keys)
  - app/app/(app)/_layout.tsx (Header-Right SyncStatusBadge einhängen)
  - app/app/(app)/settings/sync.tsx (neu — Detail-Screen)
  - app/app/(app)/settings.tsx (Link zu /settings/sync)
  - app/src/lib/sync/SyncWorker.ts (erweitert — public retryOp/discardOp Methoden)
  - app/src/lib/sync/__tests__/SyncStatusBadge.test.tsx (neu)
  - app/src/lib/sync/__tests__/useSyncStatus.test.ts (neu)
  - app/src/lib/sync/__tests__/reconnect-30s.integration.test.ts (neu — SC-5 30s-window)
  - app/src/lib/sync/__tests__/syncStatus.retry-discard.test.ts (neu)
autonomous: true
requirements: [SYNC-04, NFR-01]
must_haves:
  truths:
    - "Global Header-Badge zeigt 3 Zustände: ✓ idle (synced) / ⇄ syncing (N pending) / ⚠ degraded (N failed)"
    - "Badge wird auf ALLEN Authenticated-Routen angezeigt (app/(app)/_layout.tsx headerRight)"
    - "Badge tap → navigiert zu /settings/sync Detail-Screen"
    - "Detail-Screen listet Pending-Ops (FIFO nach created_at) und Failed-Ops getrennt"
    - "Failed-Ops zeigen Retry + Verwerfen-Buttons (inline-confirm-expansion-Pattern, UI-SPEC Zeile 234)"
    - "Retry → setzt attempts=0, nextAttemptAt=now, triggert SyncWorker.syncAll()"
    - "Verwerfen → deleteOutboxEntry() + scheduleWriteDebounced → Delta-Pull korrigiert lokale Row"
    - "useSyncStatus subscribed auf syncEvents + storage.listOutboxEntries, debounced 300ms"
    - "Alle sichtbaren Strings aus i18n/de.ts via t('sync.*'), KEINE Hardcoded-Texte"
    - "Offline-Indikator (NetInfo isConnected=false) überlagert Badge als ⊘ offline mit gedimmter Farbe"
    - "2-User-Reconnect-Test: Gerät A offline-edit, reconnect, Gerät B pullt binnen 30s (SC-5)"
  artifacts:
    - path: "app/src/components/SyncStatusBadge.tsx"
      provides: "Global Header-Badge mit 3 Zuständen + Offline-Overlay"
      contains: "SyncStatusBadge"
    - path: "app/src/hooks/useSyncStatus.ts"
      provides: "Hook subscribed syncEvents + Outbox-Counts"
      contains: "useSyncStatus"
    - path: "app/app/(app)/settings/sync.tsx"
      provides: "Detail-Screen Pending/Failed mit Retry/Discard"
      contains: "SyncDetailScreen"
    - path: "app/src/i18n/de.ts"
      provides: "i18n-Keys sync.badge.synced/syncing/degraded/offline, sync.detail.*"
      contains: "sync"
  key_links:
    - from: "app/src/hooks/useSyncStatus.ts"
      to: "app/src/lib/sync/events.ts"
      via: "syncEvents.on('status_change')"
      pattern: "syncEvents\\.on"
    - from: "app/src/components/SyncStatusBadge.tsx"
      to: "expo-router.Link zu /settings/sync"
      via: "router.push"
      pattern: "router\\.push.*settings/sync"
    - from: "app/app/(app)/settings/sync.tsx"
      to: "storage.listOutboxEntries"
      via: "Adapter listet pending/failed"
      pattern: "listOutboxEntries"
    - from: "app/app/(app)/settings/sync.tsx"
      to: "SyncWorker.retryOp / discardOp"
      via: "Retry/Verwerfen-Buttons"
      pattern: "(retryOp|discardOp)"
---

<objective>
Sync-Status-UI (D-21, D-22): globaler Header-Badge mit 3 Zuständen (✓ idle / ⇄ syncing / ⚠ degraded) auf allen Authenticated-Routen, plus Settings > Sync Detail-Screen mit Pending/Failed-Liste und Retry/Verwerfen-Pattern. Erfüllt SYNC-04 sowie das SC-5 "zuletzt bearbeitet von"-Feedback-Window (2-User-Reconnect binnen 30s).

Purpose: Dirk und seine Frau brauchen jederzeit Feedback, ob Änderungen bereits beim Partner angekommen sind. Optimistisches UI (D-22) macht Writes unmittelbar sichtbar, aber bei flaky Netz/LWW-Reject muss der Badge den aktuellen Zustand transparent kommunizieren. Failed-Ops müssen manuell retry-bar oder verwerfbar sein, damit ein dauerhaft kaputter Write die Queue nicht blockiert.

Output:
- `SyncStatusBadge` Component im Header (headerRight in `_layout.tsx`) mit Tap → Detail-Screen
- `useSyncStatus` Hook — subscribed `syncEvents` + `storage.listOutboxEntries`, rechnet Badge-Status aus
- Settings > Sync Detail-Screen `/settings/sync` — Listen + Retry/Verwerfen mit inline-confirm-expansion
- i18n-Keys `sync.badge.*` + `sync.detail.*` in `de.ts`
- `SyncWorker.retryOp(id)` + `SyncWorker.discardOp(id)` Public-Methoden
- 2-User-Reconnect-Integration-Test für SC-5 30s-Window
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-offline-sync-2-user-shared-state/03-CONTEXT.md
@.planning/phases/03-offline-sync-2-user-shared-state/03-RESEARCH.md
@.planning/phases/03-offline-sync-2-user-shared-state/03-VALIDATION.md
@.planning/phases/03-offline-sync-2-user-shared-state/03-01-PLAN.md
@.planning/phases/03-offline-sync-2-user-shared-state/03-02-PLAN.md
@.planning/phases/03-offline-sync-2-user-shared-state/03-03-PLAN.md
@.planning/phases/03-offline-sync-2-user-shared-state/03-04-PLAN.md
@.planning/phases/03-offline-sync-2-user-shared-state/03-05-PLAN.md

<interfaces>
<!-- Typen + Exports aus Plan 03-02/03-04/03-05, die diese Tasks konsumieren. -->
<!-- Executor soll diese Signaturen direkt benutzen, KEIN Codebase-Scouting. -->

Aus `packages/shared/src/types/storage.ts` (erweitert in Plan 03-02):
```ts
export interface StorageAdapter {
  // ... KV + Row-Methoden ...
  listOutboxEntries(opts?: { entity?: EntityName; rowId?: string }): Promise<OutboxEntry[]>;
  deleteOutboxEntry(id: string): Promise<void>;
  updateOutboxEntry(id: string, patch: Partial<OutboxEntry>): Promise<void>;
}

export type OutboxEntry = {
  id: string;
  entity: EntityName;
  rowId: string;
  op: 'upsert' | 'delete';
  payloadJson: string;
  createdAt: string;
  attempts: number;
  nextAttemptAt: string | null;
  lastError: string | null;
  lastAttemptedAt: string | null;
  status: 'pending' | 'in_flight' | 'failed';
};
```

Aus `app/src/lib/sync/events.ts` (Plan 03-04):
```ts
export type SyncEvent =
  | { type: 'status_change'; status: 'idle' | 'syncing' | 'degraded' }
  | { type: 'push_start'; entity: EntityName; rowId: string; opId: string }
  | { type: 'push_success'; entity: EntityName; rowId: string; opId: string }
  | { type: 'push_retry'; entity: EntityName; rowId: string; opId: string; attempts: number; lastError: string }
  | { type: 'push_conflict'; entity: EntityName; rowId: string; opId: string }
  | { type: 'push_permanent_failure'; entity: EntityName; rowId: string; opId: string; lastError: string }
  | { type: 'pull_start'; entity: EntityName }
  | { type: 'pull_success'; entity: EntityName; count: number }
  | { type: 'pull_failure'; entity: EntityName; error: string };

export const syncEvents: {
  emit(evt: SyncEvent): void;
  on(listener: (evt: SyncEvent) => void): () => void;
  _reset(): void;  // nur für Tests
};
```

Aus `app/src/lib/sync/SyncWorker.ts` (Plan 03-04 — wird in Task 03-06-02 erweitert):
```ts
export class SyncWorker {
  constructor(deps: { storage: StorageAdapter; supabase: SupabaseClient });
  async push(): Promise<void>;
  async pull(): Promise<void>;
  async syncAll(): Promise<void>;
  // NEU in Task 03-06-02:
  async retryOp(opId: string): Promise<void>;
  async discardOp(opId: string): Promise<void>;
}
```

Aus `app/src/i18n/de.ts` (erweitert):
```ts
export const de = {
  // ... existing keys ...
  sync: {
    badge: {
      synced: 'Synchronisiert',
      syncing: 'Synchronisiere …',
      degraded: 'Probleme beim Synchronisieren',
      offline: 'Offline',
      pendingCount: (n: number) => `${n} ausstehend`,
      failedCount: (n: number) => `${n} fehlgeschlagen`,
    },
    detail: {
      title: 'Sync-Status',
      pendingHeader: 'Ausstehend',
      failedHeader: 'Fehlgeschlagen',
      empty: 'Alle Änderungen sind synchronisiert.',
      entryLabel: (entity: string, op: string) => `${entity} · ${op}`,
      retryButton: 'Erneut versuchen',
      discardButton: 'Verwerfen',
      discardConfirm: 'Änderung wirklich verwerfen? Dein lokaler Wert wird durch den Server-Stand überschrieben.',
      discardConfirmYes: 'Verwerfen',
      discardConfirmNo: 'Abbrechen',
      lastError: 'Letzter Fehler',
      attemptsLabel: (n: number) => `Versuche: ${n}`,
      conflictLabel: 'Konflikt — älterer Stand, wurde verworfen.',
    },
  },
};
```

Aus `app/src/lib/sync/SyncTriggers.ts` (Plan 03-04):
```ts
export function registerSyncTriggers(deps: { worker: SyncWorker }): () => void;
export function scheduleWriteDebounced(): void;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 03-06-01: useSyncStatus Hook + Badge-State-Machine</name>
  <files>
    - app/src/hooks/useSyncStatus.ts (neu)
    - app/src/lib/sync/__tests__/useSyncStatus.test.ts (neu)
  </files>
  <read_first>
    - `app/src/lib/sync/events.ts` (aus Plan 03-04) — syncEvents-API
    - `app/src/lib/storage.ts` (Platform.select StorageAdapter)
    - `app/src/hooks/useNetInfo.ts` oder direkt `@react-native-community/netinfo` Pattern aus `app/src/lib/sync/SyncTriggers.ts`
    - `packages/shared/src/types/storage.ts` (nach Plan 03-02-01) — listOutboxEntries-Signatur
  </read_first>
  <behavior>
    - Test 1: Initial-Render mit leerer Outbox → status = 'synced', pendingCount = 0, failedCount = 0
    - Test 2: Nach `syncEvents.emit({ type: 'status_change', status: 'syncing' })` → status = 'syncing'
    - Test 3: Outbox enthält 3 pending + 2 failed → pendingCount = 3, failedCount = 2, status = 'degraded' (failed überschreibt syncing)
    - Test 4: `useNetInfo.isConnected = false` → status = 'offline' (überschreibt alle anderen)
    - Test 5: Nach `syncEvents.emit({ type: 'push_success', ... })` → Hook re-fetcht Outbox-Counts (debounced 300ms)
    - Test 6: Unmount ruft cleanup von syncEvents.on() + NetInfo.addEventListener
    - Test 7: Debouncing verifiziert — 5 schnelle Events innerhalb 300ms triggern nur 1 listOutboxEntries-Call
  </behavior>
  <action>
    Erstelle `app/src/hooks/useSyncStatus.ts`:

    ```ts
    import { useEffect, useRef, useState } from 'react';
    import NetInfo from '@react-native-community/netinfo';
    import { getStorage } from '@/lib/storage';
    import { syncEvents, type SyncEvent } from '@/lib/sync/events';
    import type { OutboxEntry } from '@shared/types/storage';

    export type SyncStatusValue = 'synced' | 'syncing' | 'degraded' | 'offline';

    export interface SyncStatus {
      status: SyncStatusValue;
      pendingCount: number;
      failedCount: number;
      /** Interne Flag, ob gerade aktiv gepusht/gepullt wird (aus status_change-Event). */
      activelySyncing: boolean;
    }

    const DEBOUNCE_MS = 300;

    export function useSyncStatus(): SyncStatus {
      const [pendingCount, setPendingCount] = useState(0);
      const [failedCount, setFailedCount] = useState(0);
      const [activelySyncing, setActivelySyncing] = useState(false);
      const [isConnected, setIsConnected] = useState<boolean>(true);
      const debounceTimerRef = useRef<ReturnType<typeof setTimeout> | null>(null);

      // NetInfo subscription
      useEffect(() => {
        const unsub = NetInfo.addEventListener((state) => {
          setIsConnected(state.isConnected === true);
        });
        // Initial-Fetch
        NetInfo.fetch().then((state) => setIsConnected(state.isConnected === true));
        return () => unsub();
      }, []);

      const refreshCounts = () => {
        if (debounceTimerRef.current) clearTimeout(debounceTimerRef.current);
        debounceTimerRef.current = setTimeout(async () => {
          try {
            const entries = await getStorage().listOutboxEntries();
            const pending = entries.filter((e: OutboxEntry) => e.status !== 'failed').length;
            const failed = entries.filter((e: OutboxEntry) => e.status === 'failed').length;
            setPendingCount(pending);
            setFailedCount(failed);
          } catch {
            // Adapter-Fehler still schlucken — UI zeigt dann nur status_change
          }
        }, DEBOUNCE_MS);
      };

      useEffect(() => {
        // Initial-Fetch ohne Debounce
        (async () => {
          try {
            const entries = await getStorage().listOutboxEntries();
            setPendingCount(entries.filter((e: OutboxEntry) => e.status !== 'failed').length);
            setFailedCount(entries.filter((e: OutboxEntry) => e.status === 'failed').length);
          } catch {
            /* noop */
          }
        })();

        const unsub = syncEvents.on((evt: SyncEvent) => {
          if (evt.type === 'status_change') {
            setActivelySyncing(evt.status !== 'idle');
          }
          // Jedes Event triggert Re-Count (debounced)
          refreshCounts();
        });
        return () => {
          unsub();
          if (debounceTimerRef.current) clearTimeout(debounceTimerRef.current);
        };
      }, []);

      let status: SyncStatusValue;
      if (!isConnected) {
        status = 'offline';
      } else if (failedCount > 0) {
        status = 'degraded';
      } else if (activelySyncing || pendingCount > 0) {
        status = 'syncing';
      } else {
        status = 'synced';
      }

      return { status, pendingCount, failedCount, activelySyncing };
    }
    ```

    Erstelle `app/src/lib/sync/__tests__/useSyncStatus.test.ts`:
    - Mocke `@react-native-community/netinfo` mit `addEventListener` + `fetch`
    - Mocke `@/lib/storage` mit `getStorage()` → InMemory-Adapter mit configurable listOutboxEntries
    - Nutze `@testing-library/react-native` mit `renderHook` + `act`
    - Test die 7 Behavior-Fälle oben
    - jest.useFakeTimers() für Debounce-Test (Test 7)

    Verwende NICHT `jest.useFakeTimers('modern')` auf RN-0.76 — stick to default fake timers.
  </action>
  <verify>
    <automated>pnpm --filter app test -- src/lib/sync/__tests__/useSyncStatus.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `useSyncStatus` exportiert aus `app/src/hooks/useSyncStatus.ts`
    - [ ] 7 Jest-Tests grün
    - [ ] Hook priorisiert `offline > degraded > syncing > synced` korrekt
    - [ ] Debouncing auf 300ms verifiziert
    - [ ] Kein Memory-Leak: cleanup entfernt syncEvents + NetInfo listener
  </acceptance_criteria>
  <done>
    Hook rendert korrekten Status in allen 4 Zuständen; 7/7 Tests grün.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-06-02: SyncWorker.retryOp + SyncWorker.discardOp Public-Methoden</name>
  <files>
    - app/src/lib/sync/SyncWorker.ts (erweitert)
    - app/src/lib/sync/__tests__/syncStatus.retry-discard.test.ts (neu)
  </files>
  <read_first>
    - `app/src/lib/sync/SyncWorker.ts` (aus Plan 03-04) — private handlers + updateOutboxEntry-Usage
    - `packages/shared/src/types/storage.ts` (Plan 03-02) — updateOutboxEntry + deleteOutboxEntry
    - `app/src/lib/sync/SyncTriggers.ts` (Plan 03-04) — scheduleWriteDebounced-Pattern für Delta-Pull-Trigger
  </read_first>
  <behavior>
    - Test 1: `retryOp(opId)` setzt attempts=0, nextAttemptAt=now, lastError=null, status='pending' und triggert `syncAll()`
    - Test 2: `retryOp(nichtExistierendeId)` throws Error('outbox_entry_not_found')
    - Test 3: `discardOp(opId)` ruft `storage.deleteOutboxEntry(opId)`
    - Test 4: `discardOp(opId)` triggert anschließend `pullEntity(entity)` für Delta-Pull → lokale Row wird mit Server-Stand überschrieben (D-22)
    - Test 5: `discardOp(nichtExistierendeId)` ist idempotent — keine Exception, deleteOutboxEntry wird no-op auf fehlender ID
    - Test 6: retryOp emittiert `status_change: syncing` vor dem Worker-Run
  </behavior>
  <action>
    Erweitere `app/src/lib/sync/SyncWorker.ts` um zwei neue public Methoden, NACH der existierenden `syncAll`:

    ```ts
    // In der Klasse SyncWorker:

    /**
     * Retry eines failed/stuck Outbox-Eintrags (D-21 Detail-Screen Retry-Button).
     * Setzt attempts=0, clear lastError, triggert syncAll().
     * Throws 'outbox_entry_not_found' wenn opId nicht existiert.
     */
    async retryOp(opId: string): Promise<void> {
      const entries = await this.storage.listOutboxEntries();
      const entry = entries.find((e) => e.id === opId);
      if (!entry) {
        throw new Error('outbox_entry_not_found');
      }
      await this.storage.updateOutboxEntry(opId, {
        attempts: 0,
        nextAttemptAt: new Date().toISOString(),
        lastError: null,
        status: 'pending',
      });
      syncEvents.emit({ type: 'status_change', status: 'syncing' });
      await this.syncAll();
    }

    /**
     * Verwerfen eines Outbox-Eintrags (D-21 Detail-Screen Verwerfen-Button).
     * Löscht die Op, triggert Delta-Pull für das Entity — Server-Stand überschreibt lokalen Wert (D-22).
     * Idempotent: fehlende IDs führen NICHT zu Exception.
     */
    async discardOp(opId: string): Promise<void> {
      const entries = await this.storage.listOutboxEntries();
      const entry = entries.find((e) => e.id === opId);
      await this.storage.deleteOutboxEntry(opId);
      if (entry) {
        // Delta-Pull für Entity, damit lokale Row auf Server-Stand zurückfällt
        try {
          await this.pullEntity(entry.entity);
        } catch {
          // Pull-Fehler still — nächster Trigger wird versuchen
        }
      }
    }
    ```

    HINWEIS: `pullEntity` ist in Plan 03-04 als private Methode definiert. Falls noch private, auf `public`/`internal` promoten (keine TypeScript-Sichtbarkeits-Nebenwirkung, nur Test-Zugänglichkeit). Alternativ: `async syncAll({ onlyEntity })`-Parameter einführen — aber für MVP reicht direkter pullEntity-Call.

    Erstelle `app/src/lib/sync/__tests__/syncStatus.retry-discard.test.ts`:
    - Setup: `new SyncWorker({ storage: InMemoryAdapter, supabase: supabaseShim })` mit vorbefülltem OutboxEntry (id='op-1', entity='gardens', rowId='g-1', status='failed', attempts=10, lastError='500 Server Error')
    - Test 1: `retryOp('op-1')` → `storage.listOutboxEntries()` liefert Entry mit attempts=0, lastError=null, status='pending'
    - Test 2: `retryOp('does-not-exist')` → rejects mit 'outbox_entry_not_found'
    - Test 3: `discardOp('op-1')` → `storage.listOutboxEntries()` liefert leeres Array
    - Test 4: Nach `discardOp('op-1')`: supabaseShim.from('gardens').select() wurde aufgerufen (Delta-Pull-Verifikation)
    - Test 5: `discardOp('does-not-exist')` resolves ohne throw; storage.deleteOutboxEntry wurde aufgerufen
    - Test 6: spy auf syncEvents.on() → nach retryOp wurde status_change:syncing emittiert

    Verwende den existing InMemoryStorageAdapter aus Plan 03-02-01 (Contract-Test-Harness) + den supabaseShim aus Plan 03-04-03.
  </action>
  <verify>
    <automated>pnpm --filter app test -- src/lib/sync/__tests__/syncStatus.retry-discard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `SyncWorker.retryOp` + `SyncWorker.discardOp` public exportiert
    - [ ] 6 Tests grün
    - [ ] retryOp nicht-existent → typed Error 'outbox_entry_not_found'
    - [ ] discardOp idempotent (keine Exception bei fehlender ID)
    - [ ] discardOp triggert pullEntity für betroffenes Entity
  </acceptance_criteria>
  <done>
    Beide Methoden exportiert; Detail-Screen kann sie aufrufen; 6/6 Tests grün.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-06-03: SyncStatusBadge Component + Header-Integration + i18n</name>
  <files>
    - app/src/components/SyncStatusBadge.tsx (neu)
    - app/src/components/SyncStatusBadge.styles.ts (neu — NativeWind classnames)
    - app/src/i18n/de.ts (erweitert um sync.badge.*)
    - app/app/(app)/_layout.tsx (headerRight mit SyncStatusBadge)
    - app/src/lib/sync/__tests__/SyncStatusBadge.test.tsx (neu)
  </files>
  <read_first>
    - `app/app/(app)/_layout.tsx` — Stack-Config + existierender headerRight-Content
    - `app/src/hooks/useSyncStatus.ts` (Task 03-06-01) — Hook-Signatur
    - `app/src/i18n/de.ts` — existierender Export für Extension-Pattern
    - `app/src/components/*.tsx` — existierende Components für NativeWind-Pattern + accessibility-Usage
  </read_first>
  <behavior>
    - Test 1: status='synced' → rendert ✓-Icon + Label "Synchronisiert", keine Zahl
    - Test 2: status='syncing' + pendingCount=3 → rendert ⇄-Icon + Label "Synchronisiere …" + "3 ausstehend"
    - Test 3: status='degraded' + failedCount=2 → rendert ⚠-Icon + Label "Probleme beim Synchronisieren" + "2 fehlgeschlagen"
    - Test 4: status='offline' → rendert ⊘-Icon + Label "Offline", gedimmte Farbe
    - Test 5: Tap auf Badge ruft router.push('/settings/sync') auf
    - Test 6: accessibilityLabel enthält Status + Counts für Screenreader
    - Test 7: testID='sync-status-badge' gesetzt
  </behavior>
  <action>
    Erweitere `app/src/i18n/de.ts` — hänge `sync`-Key an den existierenden `de`-Export. Falls Struktur verschachtelt (`de.sync = { badge: {...}, detail: {...} }`), erstelle es genau wie im `<interfaces>`-Block oben spezifiziert.

    Erstelle `app/src/components/SyncStatusBadge.styles.ts`:
    ```ts
    export const syncBadgeClasses = {
      synced: 'text-green-600 dark:text-green-400',
      syncing: 'text-blue-600 dark:text-blue-400',
      degraded: 'text-amber-600 dark:text-amber-400',
      offline: 'text-gray-500 dark:text-gray-400 opacity-70',
    } as const;
    ```

    Erstelle `app/src/components/SyncStatusBadge.tsx`:
    ```tsx
    import { Pressable, Text, View } from 'react-native';
    import { useRouter } from 'expo-router';
    import { useSyncStatus, type SyncStatusValue } from '@/hooks/useSyncStatus';
    import { de } from '@/i18n/de';
    import { syncBadgeClasses } from './SyncStatusBadge.styles';

    const ICON: Record<SyncStatusValue, string> = {
      synced: '✓',
      syncing: '⇄',
      degraded: '⚠',
      offline: '⊘',
    };

    const LABEL: Record<SyncStatusValue, string> = {
      synced: de.sync.badge.synced,
      syncing: de.sync.badge.syncing,
      degraded: de.sync.badge.degraded,
      offline: de.sync.badge.offline,
    };

    export function SyncStatusBadge() {
      const router = useRouter();
      const { status, pendingCount, failedCount } = useSyncStatus();

      const suffix =
        status === 'degraded' && failedCount > 0
          ? de.sync.badge.failedCount(failedCount)
          : status === 'syncing' && pendingCount > 0
          ? de.sync.badge.pendingCount(pendingCount)
          : null;

      const fullLabel = suffix ? `${LABEL[status]} — ${suffix}` : LABEL[status];

      return (
        <Pressable
          testID="sync-status-badge"
          accessibilityRole="button"
          accessibilityLabel={fullLabel}
          onPress={() => router.push('/settings/sync')}
          className="flex-row items-center px-2 py-1"
        >
          <Text className={`text-sm mr-1 ${syncBadgeClasses[status]}`}>{ICON[status]}</Text>
          {suffix && (
            <Text className={`text-xs ${syncBadgeClasses[status]}`}>{suffix}</Text>
          )}
        </Pressable>
      );
    }
    ```

    Erweitere `app/app/(app)/_layout.tsx` — in der Stack-Konfiguration `screenOptions` oder per `Stack.Screen options={{ headerRight: () => <SyncStatusBadge /> }}`:
    - Import `SyncStatusBadge` aus `@/components/SyncStatusBadge`
    - Füge `headerRight: () => <SyncStatusBadge />` zu den `screenOptions` des äußeren Stack hinzu (gilt damit für alle Authenticated-Routen)
    - Bestehendes headerRight (falls vorhanden) NICHT überschreiben — in einen Container packen: `headerRight: () => (<View className="flex-row gap-2"><ExistingButton /><SyncStatusBadge /></View>)`

    Erstelle `app/src/lib/sync/__tests__/SyncStatusBadge.test.tsx`:
    - Mocke `@/hooks/useSyncStatus` mit `jest.fn().mockReturnValue({ status, pendingCount, failedCount, activelySyncing: false })`
    - Mocke `expo-router.useRouter` mit `{ push: jest.fn() }`
    - Nutze `@testing-library/react-native` mit `render` + `fireEvent.press`
    - Test die 7 Behavior-Fälle oben
    - Assertion für Test 4: `queryByText(de.sync.badge.offline)` + getByTestId hat style-tokens für 'offline' (via testID-accessible text-container)
  </action>
  <verify>
    <automated>pnpm --filter app test -- src/lib/sync/__tests__/SyncStatusBadge.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `SyncStatusBadge` exportiert und in _layout.tsx headerRight integriert
    - [ ] 7 Tests grün
    - [ ] i18n-Keys sync.badge.* vollständig in de.ts
    - [ ] Tap → router.push('/settings/sync') verifiziert
    - [ ] Accessibility-Label enthält Status + Count-Suffix
    - [ ] testID='sync-status-badge' für E2E-Hook gesetzt
  </acceptance_criteria>
  <done>
    Badge rendert in allen 4 Zuständen; wird im Header aller Authenticated-Routen sichtbar; 7/7 Tests grün.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-06-04: Settings > Sync Detail-Screen mit Retry/Verwerfen</name>
  <files>
    - app/app/(app)/settings/sync.tsx (neu)
    - app/app/(app)/settings.tsx (Link zu /settings/sync ergänzt)
    - app/src/i18n/de.ts (erweitert um sync.detail.*)
  </files>
  <read_first>
    - `app/app/(app)/settings.tsx` — existierendes Layout-Pattern für Settings-Entries
    - `app/app/(app)/settings/privacy.tsx` (aus Plan 03-05-03) — Screen-Pattern als Vorlage
    - `app/src/lib/sync/SyncWorker.ts` (nach Task 03-06-02) — retryOp/discardOp
    - `packages/shared/src/types/storage.ts` — OutboxEntry shape
    - UI-SPEC-Pattern aus 03-RESEARCH.md §"Validation Architecture" Zeile 234 — inline-confirm-expansion (nicht Modal)
  </read_first>
  <behavior>
    - Test 1: Leere Outbox → rendert `de.sync.detail.empty` Text
    - Test 2: 2 Pending-Entries → Sektion "Ausstehend" listet beide mit `entity · op`-Label
    - Test 3: 1 Failed-Entry → Sektion "Fehlgeschlagen" mit Retry + Verwerfen-Buttons + Fehlermeldung sichtbar
    - Test 4: Tap "Erneut versuchen" → ruft `syncWorker.retryOp(id)`; Row verschwindet aus Failed-Sektion nach re-fetch
    - Test 5: Tap "Verwerfen" → inline-Confirm-Expansion ausgeklappt (kein Modal!) mit 2 Buttons "Verwerfen" / "Abbrechen"
    - Test 6: Tap "Verwerfen" (Bestätigung) → ruft `syncWorker.discardOp(id)`; Row verschwindet
    - Test 7: Tap "Abbrechen" → Expansion collabiert, keine API-Calls
    - Test 8: PermanentFailure-Event via `syncEvents.emit` triggert re-fetch der Liste
  </behavior>
  <action>
    Erweitere `app/src/i18n/de.ts` um `de.sync.detail.*` gemäß `<interfaces>`-Block oben.

    Erstelle `app/app/(app)/settings/sync.tsx`:
    ```tsx
    import { useCallback, useEffect, useState } from 'react';
    import { ScrollView, Text, View, Pressable, ActivityIndicator } from 'react-native';
    import { Stack } from 'expo-router';
    import { getStorage } from '@/lib/storage';
    import { getSyncWorker } from '@/lib/sync/SyncWorker';  // angenommen: Singleton-Accessor oder via context
    import { syncEvents } from '@/lib/sync/events';
    import { de } from '@/i18n/de';
    import type { OutboxEntry } from '@shared/types/storage';

    export default function SyncDetailScreen() {
      const [entries, setEntries] = useState<OutboxEntry[]>([]);
      const [loading, setLoading] = useState(true);
      const [confirmDiscardId, setConfirmDiscardId] = useState<string | null>(null);
      const [busyId, setBusyId] = useState<string | null>(null);

      const refresh = useCallback(async () => {
        try {
          const list = await getStorage().listOutboxEntries();
          setEntries(list);
        } catch {
          setEntries([]);
        } finally {
          setLoading(false);
        }
      }, []);

      useEffect(() => {
        refresh();
        const unsub = syncEvents.on(() => {
          void refresh();
        });
        return () => unsub();
      }, [refresh]);

      const pending = entries.filter((e) => e.status !== 'failed');
      const failed = entries.filter((e) => e.status === 'failed');

      const handleRetry = async (id: string) => {
        setBusyId(id);
        try {
          await getSyncWorker().retryOp(id);
        } finally {
          setBusyId(null);
          void refresh();
        }
      };

      const handleDiscard = async (id: string) => {
        setBusyId(id);
        try {
          await getSyncWorker().discardOp(id);
        } finally {
          setBusyId(null);
          setConfirmDiscardId(null);
          void refresh();
        }
      };

      if (loading) {
        return (
          <View className="flex-1 items-center justify-center">
            <ActivityIndicator />
          </View>
        );
      }

      return (
        <ScrollView className="flex-1 bg-white dark:bg-gray-900" testID="sync-detail-screen">
          <Stack.Screen options={{ title: de.sync.detail.title }} />

          {entries.length === 0 && (
            <Text className="p-4 text-gray-600 dark:text-gray-400" testID="sync-detail-empty">
              {de.sync.detail.empty}
            </Text>
          )}

          {pending.length > 0 && (
            <View testID="sync-detail-pending-section">
              <Text className="px-4 pt-4 pb-2 text-xs uppercase text-gray-500">
                {de.sync.detail.pendingHeader}
              </Text>
              {pending.map((entry) => (
                <View
                  key={entry.id}
                  testID={`sync-entry-${entry.id}`}
                  className="px-4 py-3 border-b border-gray-200 dark:border-gray-700"
                >
                  <Text className="text-sm">
                    {de.sync.detail.entryLabel(entry.entity, entry.op)}
                  </Text>
                  <Text className="text-xs text-gray-500">
                    {de.sync.detail.attemptsLabel(entry.attempts)}
                  </Text>
                </View>
              ))}
            </View>
          )}

          {failed.length > 0 && (
            <View testID="sync-detail-failed-section">
              <Text className="px-4 pt-4 pb-2 text-xs uppercase text-amber-600">
                {de.sync.detail.failedHeader}
              </Text>
              {failed.map((entry) => (
                <View
                  key={entry.id}
                  testID={`sync-entry-${entry.id}`}
                  className="px-4 py-3 border-b border-gray-200 dark:border-gray-700"
                >
                  <Text className="text-sm">
                    {de.sync.detail.entryLabel(entry.entity, entry.op)}
                  </Text>
                  {entry.lastError && (
                    <Text className="text-xs text-amber-700 mt-1">
                      {de.sync.detail.lastError}: {entry.lastError}
                    </Text>
                  )}
                  <Text className="text-xs text-gray-500 mt-1">
                    {de.sync.detail.attemptsLabel(entry.attempts)}
                  </Text>

                  <View className="flex-row gap-2 mt-2">
                    <Pressable
                      testID={`retry-${entry.id}`}
                      disabled={busyId === entry.id}
                      onPress={() => handleRetry(entry.id)}
                      className="px-3 py-1 bg-blue-600 rounded"
                    >
                      <Text className="text-white text-xs">{de.sync.detail.retryButton}</Text>
                    </Pressable>
                    <Pressable
                      testID={`discard-${entry.id}`}
                      disabled={busyId === entry.id}
                      onPress={() =>
                        setConfirmDiscardId(confirmDiscardId === entry.id ? null : entry.id)
                      }
                      className="px-3 py-1 bg-gray-200 dark:bg-gray-700 rounded"
                    >
                      <Text className="text-xs">{de.sync.detail.discardButton}</Text>
                    </Pressable>
                  </View>

                  {confirmDiscardId === entry.id && (
                    <View
                      testID={`discard-confirm-${entry.id}`}
                      className="mt-3 p-3 bg-amber-50 dark:bg-amber-900/30 rounded"
                    >
                      <Text className="text-xs mb-2">{de.sync.detail.discardConfirm}</Text>
                      <View className="flex-row gap-2">
                        <Pressable
                          testID={`discard-confirm-yes-${entry.id}`}
                          onPress={() => handleDiscard(entry.id)}
                          className="px-3 py-1 bg-red-600 rounded"
                        >
                          <Text className="text-white text-xs">
                            {de.sync.detail.discardConfirmYes}
                          </Text>
                        </Pressable>
                        <Pressable
                          testID={`discard-confirm-no-${entry.id}`}
                          onPress={() => setConfirmDiscardId(null)}
                          className="px-3 py-1 bg-gray-200 dark:bg-gray-700 rounded"
                        >
                          <Text className="text-xs">{de.sync.detail.discardConfirmNo}</Text>
                        </Pressable>
                      </View>
                    </View>
                  )}
                </View>
              ))}
            </View>
          )}
        </ScrollView>
      );
    }
    ```

    HINWEIS zu `getSyncWorker()`: Plan 03-04 hat den Worker in SyncTriggers registriert. Falls kein globaler Accessor existiert, ergänze einen minimalen Singleton in `app/src/lib/sync/SyncWorker.ts`:
    ```ts
    let _instance: SyncWorker | null = null;
    export function setSyncWorker(w: SyncWorker) { _instance = w; }
    export function getSyncWorker(): SyncWorker {
      if (!_instance) throw new Error('SyncWorker not initialized');
      return _instance;
    }
    ```
    Und in `_layout.tsx` nach `registerSyncTriggers()` den `setSyncWorker(worker)` aufrufen. Siehe Plan 03-04 root-layout-Setup.

    Erweitere `app/app/(app)/settings.tsx` — ergänze eine Entry-Zeile:
    ```tsx
    <Link href="/settings/sync" asChild>
      <Pressable testID="settings-link-sync" className="px-4 py-3 border-b border-gray-200">
        <Text>{de.sync.detail.title}</Text>
      </Pressable>
    </Link>
    ```
    Füge den Eintrag NACH dem "Datenschutz"-Link aus Plan 03-05-03 hinzu (Reihenfolge: Profil → Datenschutz → Sync-Status → Abmelden).

    ERZEUGE KEINEN separaten Test hier — dieser Task wird durch Task 03-06-05 (reconnect-30s-integration-test) mit-abgedeckt. Inline-Discard-Expansion wird aber zusätzlich durch Task 03-06-05 Unit-Case Test 3/4 geprüft.
  </action>
  <verify>
    <automated>pnpm --filter app test -- src/lib/sync/__tests__/syncStatus.retry-discard.test.ts && pnpm --filter app typecheck</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `/settings/sync` rendert ohne Crash
    - [ ] Settings-Main zeigt neuen Link zu Sync-Status
    - [ ] Retry/Verwerfen-Buttons rufen SyncWorker.retryOp/discardOp korrekt auf
    - [ ] Inline-Confirm-Expansion (KEIN Modal) beim Verwerfen
    - [ ] i18n-Keys sync.detail.* vollständig
    - [ ] `pnpm --filter app typecheck` grün (no any-Fehler)
  </acceptance_criteria>
  <done>
    Detail-Screen voll funktional; Failed-Ops können manuell retry-bar oder verwerfbar sein; typecheck grün.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-06-05: 2-User-Reconnect-Integration-Test (SC-5 30s-Window)</name>
  <files>
    - app/src/lib/sync/__tests__/reconnect-30s.integration.test.ts (neu)
  </files>
  <read_first>
    - `app/src/lib/sync/__tests__/reconnect-2user.integration.test.ts` (aus Plan 03-04-03) — supabaseShim mit LWW-Simulation
    - `app/src/lib/sync/SyncWorker.ts` (nach Task 03-06-02) — push/pull/syncAll/retryOp/discardOp
    - `app/src/lib/sync/events.ts` — syncEvents für Beobachtung
    - `app/src/hooks/useSyncStatus.ts` (Task 03-06-01) — für Status-Transitions-Assertion
  </read_first>
  <behavior>
    - Test 1 (SC-5 Happy Path): Gerät A editiert gardens.name offline → OutboxEntry. Reconnect-Simulation: NetInfo→online, push läuft, SyncEvents `push_start → push_success` emittiert, Server (supabaseShim) hat neuen Wert. Gerät B (separater Storage + Worker gegen selben Shim) pullt; assertion: binnen simuliertem 30s-Fenster (mocked clock, 30 ticks à 1s) ist der Wert in Gerät B lokal sichtbar via `storage.getRow('gardens', gardenId)`.
    - Test 2 (LWW-Konflikt): Gerät A offline-edit mit updated_at = t0, Gerät B offline-edit mit updated_at = t0+1s auf derselben Row. Beide reconnecten gleichzeitig. Server-Shim simuliert LWW-Trigger: A bekommt P9011 (älterer Write rejected), B succeeds. Assertion: Gerät A emittiert `push_conflict`, Outbox-Entry wird gelöscht, Delta-Pull aktualisiert lokale Row auf B-Wert. `useSyncStatus` auf Gerät A transitioniert: syncing → degraded? (NEIN — conflict ist nicht `failed`, es ist `discard+pull`, also Übergang zu synced)
    - Test 3 (Permanent-Failure → Retry via Detail-Screen): Gerät A hat 10 retries hinter sich (simuliere alle 500-Fehler in supabaseShim), Outbox-Entry in status='failed'. User tapped Retry → `syncWorker.retryOp(id)`. supabaseShim liefert jetzt success. Assertion: nach retryOp() ist Outbox leer, `useSyncStatus` = synced.
    - Test 4 (Verwerfen via Detail-Screen → Delta-Pull korrigiert): Gerät A hat Failed-Entry (500er), Server hat aber bereits neuere Daten von Gerät B. User tapped Verwerfen → `syncWorker.discardOp(id)`. Assertion: Outbox-Entry gelöscht + Delta-Pull wurde getriggert + lokale Row in Gerät A zeigt jetzt Server-Wert (nicht den lokal-failed-Wert).
  </behavior>
  <action>
    Erstelle `app/src/lib/sync/__tests__/reconnect-30s.integration.test.ts`:

    ```ts
    import { SyncWorker } from '../SyncWorker';
    import { syncEvents, type SyncEvent } from '../events';
    import { createInMemoryAdapter } from '@shared/testing/inMemoryStorageAdapter';
    // angenommene Harness aus Plan 03-04-03:
    import { createSupabaseShim } from './helpers/supabaseShim';

    jest.useFakeTimers();

    describe('SC-5: 2-User Reconnect 30s-Window', () => {
      afterEach(() => {
        syncEvents._reset();
        jest.clearAllTimers();
      });

      test('Gerät A Write → Gerät B sieht Wert binnen 30s', async () => {
        const serverShim = createSupabaseShim();
        // Seed: gardens-Row existiert auf Server
        await serverShim.seed('gardens', {
          id: 'g-1',
          name: 'Alter Name',
          updated_at: '2026-01-01T00:00:00.000Z',
          owner_user_id: 'u-dirk',
          deleted_at: null,
        });

        const storageA = createInMemoryAdapter();
        const storageB = createInMemoryAdapter();
        const workerA = new SyncWorker({ storage: storageA, supabase: serverShim.client });
        const workerB = new SyncWorker({ storage: storageB, supabase: serverShim.client });

        // Gerät B bootstrap pull
        await workerB.pull();
        expect(await storageB.getRow('gardens', 'g-1')).toMatchObject({ name: 'Alter Name' });

        // Gerät A offline-edit (lokal schreiben + Outbox)
        await storageA.writeWithOutbox('gardens', {
          id: 'g-1',
          name: 'Neuer Name von A',
          updated_at: '2026-01-01T00:05:00.000Z',
          owner_user_id: 'u-dirk',
          deleted_at: null,
        });

        // Reconnect-Simulation: Gerät A push
        const events: SyncEvent[] = [];
        const unsub = syncEvents.on((e) => events.push(e));
        await workerA.syncAll();
        expect(events.some((e) => e.type === 'push_success' && e.entity === 'gardens')).toBe(true);

        // Binnen 30s-Fenster — Gerät B pullt
        jest.advanceTimersByTime(29_000);
        await workerB.pull();
        const gardenOnB = await storageB.getRow('gardens', 'g-1');
        expect(gardenOnB).toMatchObject({ name: 'Neuer Name von A' });

        unsub();
      });

      test('LWW-Konflikt: älterer Write rejected, Gerät pullt Server-Stand', async () => {
        const serverShim = createSupabaseShim({ enforceLww: true });
        await serverShim.seed('gardens', {
          id: 'g-1',
          name: 'Original',
          updated_at: '2026-01-01T00:00:00.000Z',
          owner_user_id: 'u-dirk',
          deleted_at: null,
        });

        const storageA = createInMemoryAdapter();
        const storageB = createInMemoryAdapter();
        const workerA = new SyncWorker({ storage: storageA, supabase: serverShim.client });
        const workerB = new SyncWorker({ storage: storageB, supabase: serverShim.client });

        await workerA.pull();
        await workerB.pull();

        // Beide offline-edit auf derselben Row
        await storageA.writeWithOutbox('gardens', {
          id: 'g-1',
          name: 'Von A (älter)',
          updated_at: '2026-01-01T00:01:00.000Z',
          owner_user_id: 'u-dirk',
          deleted_at: null,
        });
        await storageB.writeWithOutbox('gardens', {
          id: 'g-1',
          name: 'Von B (neuer)',
          updated_at: '2026-01-01T00:02:00.000Z',
          owner_user_id: 'u-dirk',
          deleted_at: null,
        });

        // B pusht zuerst — Server akzeptiert
        await workerB.syncAll();
        // A pusht danach — Server lehnt mit P9011 ab
        const events: SyncEvent[] = [];
        const unsub = syncEvents.on((e) => events.push(e));
        await workerA.syncAll();
        expect(events.some((e) => e.type === 'push_conflict')).toBe(true);

        // A's Delta-Pull (nach discard) sollte jetzt B-Wert zeigen
        const aGardenAfter = await storageA.getRow('gardens', 'g-1');
        expect(aGardenAfter).toMatchObject({ name: 'Von B (neuer)' });

        // Outbox auf A ist leer
        const outboxA = await storageA.listOutboxEntries();
        expect(outboxA).toHaveLength(0);
        unsub();
      });

      test('Permanent-Failure → manueller Retry aus Detail-Screen erfolgreich', async () => {
        const serverShim = createSupabaseShim({ failNextN: 10 });
        await serverShim.seed('gardens', {
          id: 'g-1',
          name: 'Original',
          updated_at: '2026-01-01T00:00:00.000Z',
          owner_user_id: 'u-dirk',
          deleted_at: null,
        });

        const storage = createInMemoryAdapter();
        const worker = new SyncWorker({ storage, supabase: serverShim.client });

        await storage.writeWithOutbox('gardens', {
          id: 'g-1',
          name: 'Edit',
          updated_at: '2026-01-01T00:01:00.000Z',
          owner_user_id: 'u-dirk',
          deleted_at: null,
        });

        // 10 fail-Versuche → permanent_failure
        for (let i = 0; i < 10; i++) {
          await worker.syncAll();
        }
        const outboxFailed = await storage.listOutboxEntries();
        expect(outboxFailed).toHaveLength(1);
        expect(outboxFailed[0].status).toBe('failed');

        // User tapped Retry — Server nicht mehr failing
        serverShim.clearFailures();
        await worker.retryOp(outboxFailed[0].id);

        const outboxAfter = await storage.listOutboxEntries();
        expect(outboxAfter).toHaveLength(0);
      });

      test('Verwerfen → Delta-Pull überschreibt lokale Row mit Server-Stand', async () => {
        const serverShim = createSupabaseShim({ failNextN: 10 });
        await serverShim.seed('gardens', {
          id: 'g-1',
          name: 'Server-Stand',
          updated_at: '2026-01-01T00:10:00.000Z',
          owner_user_id: 'u-dirk',
          deleted_at: null,
        });

        const storage = createInMemoryAdapter();
        const worker = new SyncWorker({ storage, supabase: serverShim.client });

        // Gerät hat alten Wert + lokalen Failed-Edit
        await storage.writeWithOutbox('gardens', {
          id: 'g-1',
          name: 'Lokaler Failed-Edit',
          updated_at: '2026-01-01T00:05:00.000Z',
          owner_user_id: 'u-dirk',
          deleted_at: null,
        });
        for (let i = 0; i < 10; i++) await worker.syncAll();
        const [failed] = await storage.listOutboxEntries();
        expect(failed.status).toBe('failed');

        // User tapped Verwerfen — Server-Stand wird via Delta-Pull gezogen
        serverShim.clearFailures();
        await worker.discardOp(failed.id);

        const finalRow = await storage.getRow('gardens', 'g-1');
        expect(finalRow).toMatchObject({ name: 'Server-Stand' });
        expect(await storage.listOutboxEntries()).toHaveLength(0);
      });
    });
    ```

    HINWEIS: Falls `helpers/supabaseShim` aus Plan 03-04-03 noch nicht die Methoden `seed()`, `clearFailures()`, oder den `enforceLww`/`failNextN`-Options-Constructor hat, erweitere ihn entsprechend in diesem Test-File selbst (inline Mock), ohne den Plan-03-04-Shim zu brechen.
  </action>
  <verify>
    <automated>pnpm --filter app test -- src/lib/sync/__tests__/reconnect-30s.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - [ ] 4 Tests grün (Happy-Path + LWW-Konflikt + Permanent-Failure-Retry + Verwerfen-PullOverride)
    - [ ] SC-5 "binnen 30s sichtbar" via jest.advanceTimersByTime(29_000) verifiziert
    - [ ] LWW-Konflikt emittiert `push_conflict` Event
    - [ ] Verwerfen triggert Delta-Pull und lokale Row zeigt Server-Stand
  </acceptance_criteria>
  <done>
    Alle 4 SC-5-Szenarien per Integration-Test abgedeckt; 4/4 grün.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client-UI → local storage | User-Input (Retry/Discard-Buttons) löst direkte Storage-Operationen aus |
| Client-UI → SyncWorker | retryOp/discardOp schreiben in Outbox + triggern Push/Pull |
| Storage → Render | OutboxEntry lastError kann fremden String enthalten (Server-Error-Message) |
| SyncWorker → Supabase RPC/REST | Push-Payloads werden ungeprüft weitergegeben (RLS auf Server-Seite enforcement) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-06-01 | Tampering | User verwirft legitime Op durch Missverständnis | mitigate | Inline-Confirm-Expansion mit explizitem Hinweistext `de.sync.detail.discardConfirm` — User muss 2× tappen. Kein one-tap-destroy. |
| T-3-06-02 | Information Disclosure | `lastError`-String aus Server-Response könnte sensitive Info leaken (z.B. Stack-Trace, Internal-ID) | mitigate | SyncWorker Plan 03-04 setzt lastError auf `error.message` NICHT `error.stack` — hier nur Pass-Through. Kein weiteres Handling nötig. |
| T-3-06-03 | Denial of Service | Benutzer drückt wiederholt Retry auf derselben Op → Server-Last | mitigate | `busyId`-State disabled Button während Retry läuft (Task 03-06-04). Worker-Push in-flight-Lock (Plan 03-04) verhindert parallele Pushes. |
| T-3-06-04 | Spoofing | Offline-Mode-Spoof: NetInfo meldet online obwohl kein Netz → Retry failed sofort | accept | Kein Schaden — User sieht Failed-Status wieder + kann erneut retry-en. NetInfo-False-Positive ist OS-Bug, nicht Client-Security-Problem. |
| T-3-06-05 | Repudiation | User behauptet, nicht auf Verwerfen getapped zu haben | accept | Solo-User-App (Dirk + Frau Shared Garden), kein Audit-Log-Requirement im MVP. |
| T-3-06-06 | Elevation of Privilege | useSyncStatus zeigt Outbox-Entries fremder Gärten an (falls mehrere Gärten lokal) | accept | MVP = single activeGardenId. `listOutboxEntries` filtered implizit durch `garden_id`-scope der Row-Tables (siehe Plan 03-02). Kein Multi-Garden-Leak möglich. |
| T-3-06-07 | Tampering | Malicious-App-on-device manipulates IndexedDB-Outbox → falsche Retry-Trigger | accept | Selbes Risiko wie für alle Offline-First-Apps. Device-Level-Security ist außerhalb des App-Scopes; Server RLS + LWW-Trigger machen Server-Seite robust. |
</threat_model>

<verification>
## Phase-Level-Verifikation nach Plan 03-06

1. **Unit-Tests grün** (`pnpm --filter app test`):
   - `useSyncStatus.test.ts` 7/7
   - `syncStatus.retry-discard.test.ts` 6/6
   - `SyncStatusBadge.test.tsx` 7/7
   - `reconnect-30s.integration.test.ts` 4/4

2. **TypeScript**: `pnpm --filter app typecheck` grün — insbesondere neue Exports in de.ts + SyncWorker.ts + useSyncStatus.ts

3. **Runtime-Sanity** (dev-Runtime):
   - `pnpm --filter app start` — App bootet, Header zeigt Badge
   - Settings → Sync-Status navigiert zu /settings/sync
   - Manueller Write + Offline-Mode: Badge zeigt `⇄ 1 ausstehend`
   - Reconnect: Badge → `✓ Synchronisiert`

4. **i18n-Vollständigkeit**: `de.sync.badge.*` + `de.sync.detail.*` Keys alle benutzt (keine Hardcoded-Texte im Component-Render)

5. **Accessibility**: VoiceOver/TalkBack liest Badge-Label inkl. Count; Screenreader findet Retry/Discard-Buttons mit accessibilityRole="button"
</verification>

<success_criteria>
- [ ] `SyncStatusBadge` im Header aller Authenticated-Routen sichtbar (`app/(app)/_layout.tsx` headerRight)
- [ ] Badge zeigt 4 Zustände korrekt: ✓ synced / ⇄ syncing + pending-count / ⚠ degraded + failed-count / ⊘ offline
- [ ] Tap auf Badge → `/settings/sync` Detail-Screen
- [ ] Detail-Screen listet Pending (FIFO) + Failed getrennt
- [ ] Failed-Entries: Retry-Button → SyncWorker.retryOp; Verwerfen → inline-Confirm-Expansion → SyncWorker.discardOp
- [ ] Verwerfen triggert automatisch Delta-Pull (D-22 Server-Stand-Korrektur)
- [ ] SC-5 Integration-Test grün: 2-User-Reconnect binnen 30s propagiert Änderung
- [ ] LWW-Konflikt-Integration-Test grün: `push_conflict` Event + lokale Row auf Server-Stand
- [ ] i18n-Keys sync.badge.* + sync.detail.* vollständig auf Deutsch
- [ ] Alle 4 Test-Suites grün (useSyncStatus, retry-discard, SyncStatusBadge, reconnect-30s)
- [ ] `pnpm --filter app typecheck` grün
</success_criteria>

<output>
Nach Abschluss erstelle `.planning/phases/03-offline-sync-2-user-shared-state/03-06-SUMMARY.md` mit:
- Liste der erstellten Components + Hooks + Screens
- Test-Resultate (Unit + Integration inkl. SC-5)
- i18n-Keys-Liste
- Screenshot-Referenz (falls Runtime-Verify durchgeführt)
- Offene Punkte für Phase-Abschluss (human-verify SYNC-04 Sub-Checkpoints)
</output>
