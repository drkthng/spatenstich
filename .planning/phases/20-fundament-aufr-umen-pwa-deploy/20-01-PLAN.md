---
phase: 20-fundament-aufr-umen-pwa-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/eslint.config.js
  - app/app/(app)/settings.tsx
  - app/src/lib/sync/SyncWorker.ts
  - app/app/(app)/plan/index.tsx
  - app/app/(app)/import/preview.tsx
  - app/src/components/SyncStatusBadge.tsx
  - app/src/components/editor/web/WebPaletteBar.tsx
  - app/src/components/editor/web/WebPlanEditor.tsx
  - app/src/storage/IndexedDbAdapter.ts
  - app/app/(app)/_layout.tsx
  - app/app/(app)/import/index.tsx
  - app/src/components/editor/EditorCanvas.tsx
  - app/src/components/__tests__/create-garden-entrypoints.test.tsx
  - app/src/lib/sync/__tests__/reconnect-2user.integration.test.ts
  - app/src/lib/sync/__tests__/reconnect-30s.integration.test.ts
  - .github/workflows/ci.yml
  - .github/workflows/eas-build.yml
  - CLAUDE.md
  - README.md
  - pnpm-workspace.yaml
autonomous: true
requirements: [DEPLOY-01]
user_setup:
  - service: github
    why: "CI braucht die Supabase-Verbindungsdaten als Repository-Variablen, sonst laeuft der Web-Export im Workflow gegen leere Env-Werte (Masterplan Kap. 5, Schritt M2)."
    env_vars:
      - name: EXPO_PUBLIC_SUPABASE_URL
        source: "GitHub -> Repo -> Settings -> Secrets and variables -> Actions -> Variables: https://vitrqkzxkiqvadqfzrcx.supabase.co"
      - name: EXPO_PUBLIC_SUPABASE_ANON_KEY
        source: "GitHub -> Settings -> Variables (Anon-Key aus Supabase Dashboard -> Settings -> API)"
      - name: EXPO_PUBLIC_SENTRY_DSN
        source: "GitHub -> Settings -> Secrets: EU-DSN (…ingest.de.sentry.io…)"
    dashboard_config:
      - task: "M2 aus Masterplan Kap. 5 erledigen (Variables + Secret anlegen)"
        location: "GitHub Repo Settings -> Secrets and variables -> Actions"

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "pnpm -r run lint beendet sich mit exit 0 (DEPLOY-01, Masterplan WP 20.1 Akzeptanz)."
    - "Kein eslint-disable-Kommentar wurde als Loesung fuer react-hooks/exhaustive-deps eingefuegt; die drei Fundstellen sind fachlich behoben (Masterplan WP 20.1)."
    - "Der Jest-Lauf von create-garden-entrypoints.test.tsx erzeugt keinen console.error-Block mehr (DEPLOY-01)."
    - "Ein Jest-Lauf mit --detectOpenHandles ueber die reconnect-Integrationstests meldet keine offenen Handles mehr (DEPLOY-01)."
    - "ci.yml setzt EXPO_PUBLIC_SUPABASE_URL und EXPO_PUBLIC_SUPABASE_ANON_KEY aus GitHub-Variablen als env (DEPLOY-01)."
    - "eas-build.yml wird nur noch per workflow_dispatch ausgeloest, nicht mehr per push (D-01: kein nativer Build in v2.0)."
    - "CLAUDE.md, README.md und pnpm-workspace.yaml beschreiben den Ist-Stack (Expo 53.0.27 / RN 0.76.7 / React 18.3.1 / expo-router 4.0.22) und Web-first PWA."
    - statement: "Der CI-Workflow laeuft auf dem Phasen-PR (gsd/phase-20-fundament-aufr-umen-pwa-deploy) gruen durch."
      verification: backstop
  artifacts:
    - "app/eslint.config.js mit Override-Block fuer **/__tests__/** und **/__mocks__/**"
    - ".github/workflows/ci.yml mit env-Block aus vars.*"
    - ".github/workflows/eas-build.yml mit on: workflow_dispatch"
    - "CLAUDE.md / README.md / pnpm-workspace.yaml mit korrigierter Stack-Angabe"
  key_links:
    - "eslint.config.js Array-Reihenfolge -> Override greift nur, wenn der Block NACH expoConfig steht (RESEARCH Pitfall 6)"
    - "ci.yml env-Block -> app/src/lib/supabase.ts liest process.env.EXPO_PUBLIC_SUPABASE_URL zur Build-Zeit"
    - "create-garden-entrypoints.test.tsx Mock -> useAuthStore wird sowohl als Hook als auch via getState() benutzt"
  prohibitions:
    - "Keine neuen AI-/API-Clients (R1)."
    - "Keine Dependency-Upgrades oder -Installationen in diesem Plan (D-13)."
    - "Keine Loeschung von Lokal-Modus- oder Vereinsregeln-Code in diesem Plan (D-04/D-05, gehoert zu 20-02)."
    - "Kein eslint-disable / eslint-disable-next-line als Loesung fuer die drei exhaustive-deps-Fundstellen (Masterplan WP 20.1)."
    - "Keine ASCII-Ersatzschreibungen (ae/oe/ue/ss) in packages/shared/src/i18n/de.json."
    - "Kein supabase db push in diesem Plan."
---

<objective>
WP 20.1 aus MASTERPLAN-v2.md Kap. 4: Das Repo wird gruen. `pnpm -r run lint` liefert exit 0, das Test-Rauschen aus zwei bekannten Quellen verschwindet, CI bekommt die Supabase-Env-Variablen und der native EAS-Build-Workflow wird auf `workflow_dispatch` gestellt (D-01: kein nativer Build in v2.0). Zum Schluss sagen CLAUDE.md, README.md und pnpm-workspace.yaml die Wahrheit ueber den tatsaechlich installierten Stack.

Purpose: Ohne gruenen Lint- und Test-Gate kann keiner der folgenden Plaene (20-02 Cleanup, 20-03 PWA, 20-04 Deploy) sein eigenes Akzeptanzkriterium beweisen — jedes davon endet mit denselben Verifikationskommandos aus Masterplan Kap. 0.4.
Output: Gruenes Lint, rauschfreie Tests, CI-Workflow mit Env-Wiring, korrigierte Doku.

Hinweis zum manuellen Schritt M2: Die GitHub-Variablen/Secrets legt Dirk an (siehe `user_setup`). Die Workflow-YAML wird unabhaengig davon geschrieben und committet; fehlen die Variablen noch, laeuft CI mit leeren Werten weiter wie heute — das ist kein Blocker fuer diesen Plan.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/MASTERPLAN-v2.md
@.planning/phases/20-fundament-aufr-umen-pwa-deploy/20-CONTEXT.md
@.planning/phases/20-fundament-aufr-umen-pwa-deploy/20-RESEARCH.md
@.planning/phases/20-fundament-aufr-umen-pwa-deploy/20-PATTERNS.md
@.planning/phases/20-fundament-aufr-umen-pwa-deploy/20-VALIDATION.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Lint-Gate end-to-end — ESLint-Overrides fuer Tests und Mocks</name>
  <files>app/eslint.config.js</files>
  <read_first>
    - app/eslint.config.js (aktueller Inhalt: defineConfig([expoConfig, { ignores: [...] }]))
    - .planning/phases/20-fundament-aufr-umen-pwa-deploy/20-RESEARCH.md — Code Examples §2 und Pitfall 6 (Flat-Config-Reihenfolge)
    - .planning/phases/20-fundament-aufr-umen-pwa-deploy/20-PATTERNS.md — Abschnitt "app/eslint.config.js (config, transform)"
  </read_first>
  <action>
    Duenne Scheibe durch den kompletten Lint-Pfad: Konfiguration aendern, Gate laufen lassen, Ergebnis pruefen — bevor irgendein Quellcode angefasst wird.

    Fuege dem Array, das an `defineConfig` uebergeben wird, ein zusaetzliches Konfigurationsobjekt hinzu mit `files: ['**/__tests__/**', '**/__mocks__/**']` und `rules`, in denen `react/display-name`, `import/first` und `@typescript-eslint/no-require-imports` je auf `'off'` stehen. Das Objekt MUSS im Array nach `expoConfig` stehen — ESLint 9 Flat Config mergt in Array-Reihenfolge, ein Block vor `expoConfig` verliert (RESEARCH Pitfall 6). Das bestehende `{ ignores: [...] }`-Objekt bleibt unveraendert erhalten.

    Fasse `expoConfig` selbst nicht an und fuege keine zeilenweise Deaktivierung in Quelldateien ein — die Regelklassen werden ausschliesslich ueber diesen Glob-Override abgeschaltet.

    Fuehre danach `pnpm --filter app run lint` einmal aus und halte in der SUMMARY fest, wie viele Verstoesse vorher und nachher gemeldet wurden. Dass der Lauf an dieser Stelle noch nicht exit 0 liefert, ist erwartet — die echten Quellcode-Verstoesse folgen in Task 2.
  </action>
  <acceptance_criteria>
    - app/eslint.config.js enthaelt genau einen zusaetzlichen Konfigurationsblock mit dem Glob-Paar `**/__tests__/**` und `**/__mocks__/**`.
    - Die drei Regelnamen stehen dort auf `'off'`.
    - Der neue Block steht im Array hinter `expoConfig`.
    - Ein Lauf von `pnpm --filter app run lint` meldet keinen einzigen Verstoss mehr, dessen Regel-ID `react/display-name`, `import/first` oder `@typescript-eslint/no-require-imports` ist.
    - In keiner Datei unter app/ wurde ein zeilenweiser ESLint-Deaktivierungskommentar neu eingefuegt.
  </acceptance_criteria>
  <verify>
    <automated>OUT=$(pnpm --filter app run lint 2>&1); ! printf '%s' "$OUT" | grep -qE 'react/display-name|import/first|@typescript-eslint/no-require-imports'</automated>
    <fails_when>Die Lint-Ausgabe enthaelt weiterhin mindestens eine Zeile mit einer der drei Regel-IDs — dann greift der Glob-Override nicht (falsche Array-Position oder falscher Glob).</fails_when>
  </verify>
  <reversibility rating="reversible">Reine Konfigurationsergaenzung, ein Commit-Revert stellt den Vorzustand her.</reversibility>
  <done>Die drei Regelklassen erscheinen nicht mehr in der Lint-Ausgabe; app/eslint.config.js ist committet.</done>
</task>

<task type="auto">
  <name>Task 2: Echte Lint-Verstoesse im Quellcode beheben</name>
  <files>app/app/(app)/settings.tsx, app/src/lib/sync/SyncWorker.ts, app/app/(app)/plan/index.tsx, app/app/(app)/import/preview.tsx, app/src/components/SyncStatusBadge.tsx, app/src/components/editor/web/WebPaletteBar.tsx, app/src/components/editor/web/WebPlanEditor.tsx, app/src/storage/IndexedDbAdapter.ts, app/app/(app)/_layout.tsx, app/app/(app)/import/index.tsx, app/src/components/editor/EditorCanvas.tsx</files>
  <read_first>
    - Die Ausgabe von `pnpm -r run lint` aus Task 1 (sie ist die Faktenquelle fuer Datei und Zeile — Masterplan Kap. 0.2: "Code gewinnt bei Fakten")
    - .planning/MASTERPLAN-v2.md — Kap. 4, WP 20.1 (Liste der Fundstellen mit Zeilennummern)
    - app/app/(app)/_layout.tsx (Zeilen 12-21: der share-intent-Effekt)
    - app/app/(app)/import/index.tsx (Zeilen 25-46: fileUri-Effekt und handleValidate)
    - app/src/components/editor/EditorCanvas.tsx (Umgebung der gemeldeten Zeile)
  </read_first>
  <action>
    Behebe die verbleibenden Verstoesse in drei Klassen, jeweils fachlich, nie per Deaktivierungskommentar (Masterplan WP 20.1 fordert das ausdruecklich).

    1. Doppelter Import in app/app/(app)/settings.tsx (Regel `import/no-duplicates`, gemeldet um Zeile 14-15): die beiden Import-Anweisungen aus demselben Modul zu einer zusammenfassen.

    2. Ungenutzte Importe entfernen in: app/src/lib/sync/SyncWorker.ts (Importblock Zeilen 14-24), app/app/(app)/plan/index.tsx (Zeile 25), app/app/(app)/import/preview.tsx (Zeile 17), app/src/components/SyncStatusBadge.tsx (Zeile 7), app/src/components/editor/web/WebPaletteBar.tsx (Zeile 11), app/src/components/editor/web/WebPlanEditor.tsx (Zeile 12), app/src/storage/IndexedDbAdapter.ts (Zeile 16). Nur den Import-Bezeichner loeschen — nie den dahinterliegenden Code oder einen Typ, der noch verwendet wird. Wenn ein Bezeichner nur in einem Typ-Kontext gebraucht wird, ist `import type` die richtige Loesung statt Loeschen.

    3. `react-hooks/exhaustive-deps` an drei Stellen fachlich beheben:
       - app/app/(app)/_layout.tsx, Effekt ab Zeile 16: die Abhaengigkeitsliste um die im Effekt tatsaechlich benutzten Werte (die Reset-Funktion des Share-Intent-Kontexts und das Router-Objekt) ergaenzen. Hinweis fuer den Ausfuehrenden: Plan 20-02 entfernt diesen Effekt vollstaendig; hier reicht die minimale, korrekte Ergaenzung, damit der Lint-Gate dieses Plans ehrlich gruen wird.
       - app/app/(app)/import/index.tsx, Effekt mit Abhaengigkeit `[fileUri]` (Zeile 46): `handleValidate` in ein `React.useCallback` mit den eigenen Abhaengigkeiten heben und in die Abhaengigkeitsliste des Effekts aufnehmen. Das Validierungsverhalten darf sich nicht aendern — dieselbe Fehlerbehandlung, dieselben State-Setter.
       - app/src/components/editor/EditorCanvas.tsx an der gemeldeten Stelle: fehlende Abhaengigkeiten ergaenzen oder instabile Callbacks stabilisieren (useCallback/useMemo), so dass kein zusaetzlicher Renderzyklus und keine Endlosschleife entsteht.

    Fuehre nach jeder Teilgruppe die Editor- und Sync-Testprojekte aus, damit eine Regression sofort auffaellt statt erst am Ende.
  </action>
  <acceptance_criteria>
    - `pnpm -r run lint` endet mit exit 0.
    - `pnpm -r run typecheck` endet mit exit 0.
    - `pnpm --filter app exec jest --ci` und `pnpm --filter @spatenstich/shared exec jest --ci` enden mit exit 0.
    - Ein `git diff` dieses Tasks enthaelt keine neu hinzugefuegte Zeile mit einem ESLint-Deaktivierungskommentar.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -r run typecheck && pnpm -r run lint</automated>
    <fails_when>non-zero exit, oder die Lint-Ausgabe enthaelt eine Zeile, die auf `problems` oder `error` endet mit einer Zahl groesser 0</fails_when>
  </verify>
  <verify>
    <automated>pnpm --filter app exec jest --ci</automated>
    <fails_when>non-zero exit, oder die Zusammenfassungszeile beginnend mit `Tests:` nennt einen failed-Anteil groesser 0</fails_when>
  </verify>
  <reversibility rating="reversible">Kleine, lokal begrenzte Quellcode-Aenderungen; per Commit-Revert zuruecknehmbar.</reversibility>
  <done>Lint exit 0, Typecheck exit 0, beide Jest-Suiten gruen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Test-Rauschen beseitigen — getState-Mock und Worker-Leak</name>
  <files>app/src/components/__tests__/create-garden-entrypoints.test.tsx, app/src/lib/sync/__tests__/reconnect-2user.integration.test.ts, app/src/lib/sync/__tests__/reconnect-30s.integration.test.ts</files>
  <read_first>
    - app/src/components/__tests__/create-garden-entrypoints.test.tsx (Zeilen 55-70: der aktuelle useAuthStore-Mock)
    - app/src/hooks/__tests__/useKalenderData.test.ts (Zeilen 36-50: das Zielmuster mit hookFn.getState)
    - app/src/lib/sync/__tests__/reconnect-2user.integration.test.ts und reconnect-30s.integration.test.ts (Timer-/Intervall-Setup und teardown)
    - .planning/phases/20-fundament-aufr-umen-pwa-deploy/20-PATTERNS.md — Abschnitt "create-garden-entrypoints.test.tsx (test, CRUD)"
  </read_first>
  <behavior>
    - Der Mock von useAuthStore verhaelt sich als Funktion (Selector-Aufruf) UND besitzt eine getState-Eigenschaft, die ein Objekt mit userId liefert; der Test laeuft ohne console.error durch.
    - Nach Abschluss der beiden reconnect-Integrationstests bleibt kein Timer und kein Intervall offen; ein Lauf mit --detectOpenHandles meldet nichts.
  </behavior>
  <action>
    Erst rot, dann gruen (Repo-Konvention RED/GREEN-Commitpaar):

    1. Uebernimm in create-garden-entrypoints.test.tsx exakt das Muster aus useKalenderData.test.ts: eine benannte Funktion, die den Selector mit dem Mock-State aufruft, und eine daran gehaengte getState-Eigenschaft, die die imperativ gelesenen Felder liefert; das Modul-Mock gibt diese Funktion als useAuthStore zurueck. Der Modulpfad bleibt der in dieser Datei bereits verwendete Alias-Stil. Welche Felder getState liefern muss, ergibt sich aus dem Produktionscode, der im Test durchlaufen wird — nicht raten, im Quellcode nachsehen.

    2. Lokalisiere den Worker-Leak: einmal `pnpm --filter app exec jest --ci --detectOpenHandles --testPathPattern=reconnect-` laufen lassen und die gemeldete Quelle lesen. Schliesse den Verursacher in den beiden reconnect-Tests: gefakte Timer verwenden und in einem afterEach alle laufenden Timer und den erzeugten Worker/Intervall abraeumen. Kein `--forceExit`, kein globales Hochsetzen von Timeouts als Ersatz fuer das Aufraeumen.

    3. Nimm die reparierten Erwartungen in die SUMMARY auf: Anzahl der Suiten/Tests vorher und nachher.
  </action>
  <acceptance_criteria>
    - Ein Lauf von create-garden-entrypoints.test.tsx endet mit exit 0 und die Ausgabe enthaelt keine Zeile mit einer Konsolen-Fehlerausgabe von Jest.
    - Ein Lauf der beiden reconnect-Tests mit --detectOpenHandles endet mit exit 0 und ohne den Jest-Hinweis auf erkannte offene Handles.
    - Die Gesamtsuite `pnpm --filter app exec jest --ci` bleibt gruen; die Testanzahl sinkt nicht.
  </acceptance_criteria>
  <verify>
    <automated>OUT=$(pnpm --filter app exec jest --ci --testPathPattern=create-garden-entrypoints 2>&1) &amp;&amp; ! printf '%s' "$OUT" | grep -q 'console\.error'</automated>
    <fails_when>Jest endet non-zero, oder die Ausgabe enthaelt weiterhin einen von Jest gerahmten Konsolen-Fehlerblock</fails_when>
  </verify>
  <verify>
    <automated>OUT=$(pnpm --filter app exec jest --ci --detectOpenHandles --testPathPattern=reconnect- 2>&1) &amp;&amp; ! printf '%s' "$OUT" | grep -q 'Jest has detected the following'</automated>
    <fails_when>Jest endet non-zero, oder die Ausgabe enthaelt den Hinweis auf offene Handles nach Testende</fails_when>
  </verify>
  <reversibility rating="reversible">Nur Testdateien betroffen.</reversibility>
  <done>Beide Rausch-Quellen sind weg, Gesamtsuite gruen.</done>
</task>

<task type="auto">
  <name>Task 4: CI-Env-Wiring, EAS-Trigger und Doku auf Ist-Stand</name>
  <files>.github/workflows/ci.yml, .github/workflows/eas-build.yml, CLAUDE.md, README.md, pnpm-workspace.yaml</files>
  <read_first>
    - .github/workflows/ci.yml (vollstaendig — der Job heisst checks und hat heute keinen env-Block)
    - .github/workflows/eas-build.yml (Trigger-Block am Dateianfang)
    - .planning/phases/20-fundament-aufr-umen-pwa-deploy/20-PATTERNS.md — Abschnitt "Env-Var-Pattern"
    - .planning/MASTERPLAN-v2.md — Kap. 5 (M2) und Kap. 4 WP 20.1 (Doku-Punkte)
    - CLAUDE.md (Stack-Tabelle), README.md (Status-Tabelle), pnpm-workspace.yaml (Kopfkommentar)
    - app/package.json (Ist-Versionen von expo, react-native, react, expo-router — Faktenquelle fuer die Tabelle)
  </read_first>
  <precondition>Die GitHub-Repository-Variablen EXPO_PUBLIC_SUPABASE_URL und EXPO_PUBLIC_SUPABASE_ANON_KEY sowie das Secret EXPO_PUBLIC_SENTRY_DSN existieren (manueller Schritt M2). Fehlen sie, wird die YAML trotzdem geschrieben und committet; der erste gruene CI-Lauf mit echten Werten verschiebt sich dann bis M2 erledigt ist — in der SUMMARY vermerken.</precondition>
  <action>
    1. ci.yml: Auf Job-Ebene einen env-Block ergaenzen, der EXPO_PUBLIC_SUPABASE_URL und EXPO_PUBLIC_SUPABASE_ANON_KEY aus den GitHub-Repository-Variablen liest (Kontext `vars`), sowie EXPO_PUBLIC_SENTRY_DSN aus den Secrets. Alle bestehenden Steps und deren Reihenfolge bleiben unveraendert; insbesondere bleibt der Secret-Scan-Step mit scripts/check-claude-key-in-bundle.sh am Ende stehen (R7).

    2. eas-build.yml: Den Trigger-Block so aendern, dass ausschliesslich `workflow_dispatch` uebrig bleibt — der push-Trigger auf master/main verschwindet. Direkt darueber eine Kommentarzeile, die festhaelt, dass der native Build erst nach Phase 29 wieder automatisch laeuft (D-01). Job-Inhalt unveraendert lassen.

    3. CLAUDE.md: Die Stack-Tabelle auf die tatsaechlich installierten Versionen aus app/package.json korrigieren (Expo 53.0.27, React Native 0.76.7, React 18.3.1, expo-router 4.0.22) und den Hinweis ergaenzen, dass v2.0 Web-first als PWA ausgeliefert wird und der native Build bis Phase 29 deaktiviert ist. Die in der Tabelle stehenden Wunsch-Versionen (SDK 55 etc.) als Zielzustand fuer Phase 29 kennzeichnen statt sie als Ist auszugeben.

    4. pnpm-workspace.yaml: Den Kopfkommentar entsprechend korrigieren.

    5. README.md: Status-Tabelle auf den aktuellen Meilenstein v2.0 aktualisieren und dieselbe Web-first-Aussage aufnehmen. Die drei Anleitungsabschnitte fuer Handy-Installation, Teilen-Import und Claude-Projekt kommen erst in Plan 20-04 dazu — hier nicht vorwegnehmen.

    Deutsche Texte in Doku-Dateien mit echten Umlauten schreiben.
  </action>
  <acceptance_criteria>
    - Der Trigger-Abschnitt von .github/workflows/eas-build.yml enthaelt workflow_dispatch und keinen push-Trigger mehr.
    - .github/workflows/ci.yml enthaelt einen env-Block, der beide EXPO_PUBLIC_SUPABASE_*-Namen aus dem vars-Kontext bezieht.
    - CLAUDE.md nennt die Zeichenfolgen 53.0.27, 0.76.7, 18.3.1 und 4.0.22.
    - Beide YAML-Dateien sind syntaktisch gueltiges YAML.
    - `pnpm -r run lint` und `pnpm -r run typecheck` bleiben exit 0.
  </acceptance_criteria>
  <verify>
    <automated>sed -n '/^on:/,/^jobs:/p' .github/workflows/eas-build.yml | grep -v '^[[:space:]]*#' | grep -q 'workflow_dispatch' &amp;&amp; ! sed -n '/^on:/,/^jobs:/p' .github/workflows/eas-build.yml | grep -v '^[[:space:]]*#' | grep -q 'push:'</automated>
    <fails_when>Der Trigger-Abschnitt enthaelt kein workflow_dispatch, oder er enthaelt weiterhin eine push-Zeile</fails_when>
  </verify>
  <verify>
    <automated>grep -v '^[[:space:]]*#' .github/workflows/ci.yml | grep -q 'vars.EXPO_PUBLIC_SUPABASE_URL' &amp;&amp; grep -v '^[[:space:]]*#' .github/workflows/ci.yml | grep -q 'vars.EXPO_PUBLIC_SUPABASE_ANON_KEY'</automated>
    <fails_when>Eine der beiden Variablenreferenzen fehlt ausserhalb von Kommentarzeilen in ci.yml</fails_when>
  </verify>
  <verify>
    <automated>node -e "const fs=require('fs');for(const f of ['.github/workflows/ci.yml','.github/workflows/eas-build.yml']){const s=fs.readFileSync(f,'utf8');if(!/^on:/m.test(s)||!/^jobs:/m.test(s)){console.error('broken '+f);process.exit(1)}}" &amp;&amp; grep -q '53\.0\.27' CLAUDE.md &amp;&amp; grep -q '0\.76\.7' CLAUDE.md &amp;&amp; grep -q '18\.3\.1' CLAUDE.md &amp;&amp; grep -q '4\.0\.22' CLAUDE.md</automated>
    <fails_when>Eine der beiden Workflow-Dateien hat keinen on:- oder jobs:-Block mehr, oder eine der vier Ist-Versionen fehlt in CLAUDE.md</fails_when>
  </verify>
  <reversibility rating="reversible">Konfiguration und Dokumentation; jederzeit zuruecknehmbar.</reversibility>
  <done>CI hat das Env-Wiring, eas-build laeuft nur noch manuell, Doku stimmt mit app/package.json ueberein.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions Runner -> Repository-Secrets/Variables | Der Workflow bekommt Zugriff auf Anon-Key und Sentry-DSN; jeder Fork-PR-Lauf ist ein potenzieller Leser |
| Build-Artefakt app/dist -> oeffentliches Web-Bundle | Alles, was der Export enthaelt, ist spaeter oeffentlich lesbar |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01-01 | Information Disclosure | ci.yml env-Block (Anon-Key als Repository-Variable) | low | accept | Der Anon-Key ist erwartungsgemaess im Web-Bundle sichtbar; RLS ist die Grenze, nicht die Geheimhaltung (Masterplan R7). Deshalb Variable, nicht Secret. |
| T-20-01-02 | Information Disclosure | EXPO_PUBLIC_SENTRY_DSN | medium | mitigate | Als GitHub-Secret ablegen, nie als Variable; EU-Ingest-DSN verwenden (STATE.md Blocker). |
| T-20-01-03 | Tampering | Secret-Scan-Step in ci.yml | high | mitigate | Der Step mit scripts/check-claude-key-in-bundle.sh bleibt unveraendert im Workflow und wird nicht umsortiert oder uebersprungen; er ist die Kontrolle gegen versehentlich mitgebaute Service-Role- oder Claude-Keys. |
| T-20-01-04 | Elevation of Privilege | eas-build.yml mit push-Trigger und EXPO_TOKEN | medium | mitigate | Umstellung auf workflow_dispatch verkleinert die Angriffsflaeche: der Token wird nur noch bei bewusst gestartetem Lauf exponiert. |
| T-20-01-SC | Tampering | npm/pnpm-Installationen | high | mitigate | In diesem Plan werden keine Pakete installiert (D-13). Die Package-Legitimacy-Pruefung fuer sharp/workbox-cli liegt in Plan 20-03 als blockierender Human-Checkpoint. |
</threat_model>

## Artifacts this phase produces (Anteil dieses Plans)

| Artefakt | Art | Status nach diesem Plan |
|---|---|---|
| `app/eslint.config.js` — Override-Block fuer `**/__tests__/**`, `**/__mocks__/**` | geaendert | fertig |
| `.github/workflows/ci.yml` — job-level `env:` mit `vars.EXPO_PUBLIC_SUPABASE_URL`, `vars.EXPO_PUBLIC_SUPABASE_ANON_KEY`, `secrets.EXPO_PUBLIC_SENTRY_DSN` | geaendert | fertig |
| `.github/workflows/eas-build.yml` — Trigger nur `workflow_dispatch` | geaendert | fertig |
| `CLAUDE.md` / `README.md` / `pnpm-workspace.yaml` — Ist-Stack, Web-first-PWA-Hinweis | geaendert | fertig (README-Anleitungsabschnitte folgen in 20-04) |
| `app/src/components/__tests__/create-garden-entrypoints.test.tsx` — `hookFn.getState`-Mock | geaendert | fertig |
| `app/src/lib/sync/__tests__/reconnect-2user.integration.test.ts`, `reconnect-30s.integration.test.ts` — Timer-Teardown | geaendert | fertig |

## Validation

Verifikationsstrategie und die Zuordnung Task -> Kommando stehen in
`.planning/phases/20-fundament-aufr-umen-pwa-deploy/20-VALIDATION.md`
(Zeilen 20-01-01 bis 20-01-04). Sampling: nach jedem Task das betroffene Jest-Projekt,
nach dem Plan die volle Gate-Reihe aus Masterplan Kap. 0.4.

<verification>
```bash
pnpm -r run typecheck
pnpm -r run lint
pnpm --filter app exec jest --ci
pnpm --filter @spatenstich/shared exec jest --ci
```
Alle vier muessen exit 0 liefern. Zusaetzlich (Backstop, nicht automatisierbar in diesem Plan):
Der CI-Workflow muss auf dem Draft-PR des Phasen-Branches gruen durchlaufen — Nachweis als
Link auf den Actions-Run in der SUMMARY.
</verification>

<success_criteria>
- `pnpm -r run lint` exit 0 (DEPLOY-01)
- Kein `console.error`-Block aus create-garden-entrypoints im Jest-Lauf (DEPLOY-01)
- `--detectOpenHandles` ueber die reconnect-Tests meldet nichts (DEPLOY-01)
- ci.yml setzt beide EXPO_PUBLIC_SUPABASE_*-Werte aus `vars` (DEPLOY-01)
- eas-build.yml nur noch `workflow_dispatch` (D-01)
- CLAUDE.md / README.md / pnpm-workspace.yaml nennen den Ist-Stack (DEPLOY-01)
</success_criteria>

<output>
Create `.planning/phases/20-fundament-aufr-umen-pwa-deploy/20-01-SUMMARY.md` when done.
Darin festhalten: Lint-Verstoesse vorher/nachher, Jest-Suiten/Tests vorher/nachher,
Quelle des Worker-Leaks, Status von M2 (GitHub-Variablen vorhanden ja/nein).
</output>
