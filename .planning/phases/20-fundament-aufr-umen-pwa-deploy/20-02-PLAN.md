---
phase: 20-fundament-aufr-umen-pwa-deploy
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - packages/shared/src/constants/flags.ts
  - packages/shared/src/index.ts
  - packages/shared/src/i18n/de.json
  - app/src/hooks/useFlag.ts
  - app/src/hooks/__tests__/useFlag.test.ts
  - app/app/(app)/profile/index.tsx
  - app/app/(app)/profile/vereinsregeln/_layout.tsx
  - app/app/(app)/profile/vereinsregeln/index.tsx
  - app/app/(app)/profile/vereinsregeln/upload.tsx
  - app/src/lib/sync/SyncWorker.ts
  - app/src/lib/photos/exifStrip.ts
  - app/src/lib/photos/exifStrip.native.ts
  - app/src/lib/photos/exifStrip.web.ts
  - app/src/lib/photos/__tests__/exifStrip.test.ts
  - app/src/lib/photos/__tests__/setup.ts
  - app/src/lib/photos/__tests__/__fixtures__/exif-test.jpg
  - app/src/stores/captureStore.ts
  - app/src/stores/settingsStore.ts
  - app/src/stores/__tests__/settingsStore.test.ts
  - app/src/stores/importStore.ts
  - app/app/(app)/settings/privacy.tsx
  - app/app/(app)/settings.tsx
  - app/app/(app)/_layout.tsx
  - app/app/(app)/index.tsx
  - app/app.config.ts
  - app/jest.config.ts
  - app/package.json
  - pnpm-lock.yaml
  - supabase/seed.sql
  - supabase/migrations/20260910000020_cleanup_legacy.sql
  - supabase/tests/garden_plan_rls.sql
  - supabase/tests/storage_photos_rls.sql
  - supabase/tests/trigger_ordering.sql
  - supabase/tests/migration_003_atomic.sql
  - supabase/tests/rls_foundation.sql
  - supabase/tests/rls_member_check.sql
autonomous: false
requirements: [DEPLOY-02]
user_setup:
  - service: supabase
    why: "Migration 020 ist destruktiv (Tabellen, Spalten, ggf. Buckets). Vor Gate 3 muss ein Backup existieren (Masterplan R9, Kap. 5 Schritt M5)."
    dashboard_config:
      - task: "M5: scripts/backup-supabase.ps1 einmal ausfuehren oder ein Dashboard-Backup bestaetigen (DB-Passwort aus Dashboard -> Settings -> Database)"
        location: "Supabase Projekt vitrqkzxkiqvadqfzrcx (Frankfurt) -> Settings -> Database"

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 7
  confidence: low

must_haves:
  truths:
    - "git grep ueber app/src und app/app findet plantnet, anthropic, captureStore, exif und useFlag nur noch in Kommentarzeilen (DEPLOY-02, Masterplan WP 20.2 Akzeptanz)."
    - "git grep nach rules.upload und pdf_extraction ueber app und packages liefert nichts (D-05)."
    - "packages/shared exportiert eine Compile-Time-Konstante FEATURES mit vereinsregeln: false; useFlag und die feature_flags-Anbindung existieren nicht mehr (D-05/D-06)."
    - "Im Profil-Screen ist ohne aktives Flag kein Vereinsregeln-Einstieg sichtbar, und der Sync-Push fuer vereinsregeln laeuft nicht mehr an (D-05)."
    - "Der Lokal-Modus-Code ist vollstaendig erhalten; die Home-Buttons Plan oeffnen, Importieren und Kalender zeigen im Lokal-Modus den Hinweis common.accountRequired statt eine Exception auszuloesen (D-04)."
    - "Die sieben in Anhang A genannten Dependencies sind aus app/package.json entfernt und pnpm-lock.yaml ist konsistent (D-06)."
    - "Das Jest-Projekt photos existiert nicht mehr; die verbleibenden Projekte laufen gruen (D-06)."
    - "supabase/migrations/20260910000020_cleanup_legacy.sql existiert, ist idempotent formuliert und definiert transfer_ownership ohne die Audit-Regression aus Migration 013 neu."
    - "Migration 020 wurde erst nach ausdruecklicher Backup-Bestaetigung des Users gepusht (Masterplan R9 / Kap. 0.5)."
    - statement: "supabase migration list --linked zeigt 20260910000020 in der lokalen UND der Remote-Spalte."
      verification: backstop
    - statement: "Die Buckets photos und vereinsregeln sind entweder geloescht oder die Migration hat eine notice ausgegeben, dass sie nicht leer waren — beides ist ein gueltiges Ergebnis und wird in der SUMMARY festgehalten (RESEARCH Open Question 2)."
      verification: backstop
  artifacts:
    - "packages/shared/src/constants/flags.ts mit der FEATURES-Konstante"
    - "supabase/migrations/20260910000020_cleanup_legacy.sql"
    - "packages/shared/src/i18n/de.json mit dem Schluessel common.accountRequired"
    - "app/jest.config.ts ohne das Projekt photos"
    - "app/package.json ohne die sieben entfernten Dependencies"
  key_links:
    - "FEATURES.vereinsregeln -> Profil-Banner, Routen unter profile/vereinsregeln/ und pushVereinsregeln im SyncWorker (drei Konsumenten, alle muessen gegated sein, sonst entstehen weiter 22P02-Outbox-Eintraege)"
    - "Migration 020 drop von feature_flags -> setzt voraus, dass useFlag und die Supabase-Abfrage vorher aus dem Client raus sind"
    - "app/src/stores/importStore.ts referenziert captureStore/settingsStore -> muss beim Loeschen mit angepasst werden, sonst bricht der Typecheck"
    - "Loeschen des photos-Jest-Projekts -> app/jest.config.ts und die Testanzahl in der SUMMARY"
  prohibitions:
    - "Kein Loeschen von Lokal-Modus-Code: migrateLocalToAccount.ts, die Local-UUID in auth.ts, die KV-Zweige in profileRepo/vereinsregelnRepo und alle mode === 'local'-Verzweigungen bleiben unveraendert (D-04)."
    - "Kein Loeschen von Vereinsregeln-Screens, -Store, -Repo oder -Tests ueber die zwei benannten Stellen hinaus (D-05)."
    - "Keine Entfernung von @shopify/react-native-skia, reanimated-color-picker oder @react-native-community/datetimepicker — die gehoeren zu WP 22.4 (Anhang A)."
    - "Keine Dependency-Upgrades, keine neuen Laufzeit-Dependencies (D-13, R10)."
    - "Kein supabase db push ohne die vorherige Backup-Bestaetigung des Users (R9, Kap. 0.5)."
    - "Keine neuen AI-/API-Clients (R1)."
    - "Keine ASCII-Ersatzschreibungen (ae/oe/ue/ss) in de.json (R2)."
    - "Kein BEGIN/COMMIT in der Migrationsdatei — Supabase klammert die Datei bereits in eine Transaktion (Repo-Konvention aus 20260509000015_remove_ai_tables.sql)."
---

<!-- planner-discipline-allow: pdf_extraction, rules.upload, useFlag, captureStore, exif, plantnet, anthropic, created_by_user_id -->

<objective>
WP 20.2 aus MASTERPLAN-v2.md Kap. 4: Der tote Code aus der Foto-/KI-Aera verschwindet aus Client und Datenbank, die Vereinsregeln verschwinden hinter einer Compile-Time-Konstante, der Lokal-Modus wird crashfrei — und zwar ohne dass eine einzige Zeile Lokal-Modus- oder Vereinsregeln-Logik geloescht wird (D-04, D-05).

Purpose: Solange `useFlag` gegen die Tabelle `feature_flags` abfragt und der SyncWorker `vereinsregeln` pusht, produziert jede Sitzung `22P02`-Eintraege in der Outbox und der Umbau in Phase 21 startet auf verschmutztem Grund. Und solange die Foto-Pipeline im Bundle liegt, ist das Web-Bundle groesser als noetig fuer die PWA in 20-03.
Output: Sauberer Client, Migration 020 (geschrieben, gepruefte Freigabe, dann gepusht), Bundle-Ziel unter 5,2 MB.

Dieser Plan ist nicht autonom: vor `supabase db push` steht ein blockierender Human-Checkpoint (Backup M5, Masterplan R9).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/MASTERPLAN-v2.md
@.planning/phases/20-fundament-aufr-umen-pwa-deploy/20-CONTEXT.md
@.planning/phases/20-fundament-aufr-umen-pwa-deploy/20-RESEARCH.md
@.planning/phases/20-fundament-aufr-umen-pwa-deploy/20-PATTERNS.md
@.planning/phases/20-fundament-aufr-umen-pwa-deploy/20-VALIDATION.md
@.planning/phases/20-fundament-aufr-umen-pwa-deploy/20-01-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: FEATURES-Konstante end-to-end — shared, UI, Sync, Tests</name>
  <files>packages/shared/src/constants/flags.ts, packages/shared/src/index.ts, app/src/hooks/useFlag.ts, app/src/hooks/__tests__/useFlag.test.ts, app/app/(app)/profile/index.tsx, app/app/(app)/profile/vereinsregeln/_layout.tsx, app/app/(app)/profile/vereinsregeln/index.tsx, app/app/(app)/profile/vereinsregeln/upload.tsx, app/src/lib/sync/SyncWorker.ts, packages/shared/src/i18n/de.json</files>
  <read_first>
    - packages/shared/src/constants/flags.ts (aktuell: FLAGS-Objekt mit EXAMPLE)
    - packages/shared/src/index.ts (was exportiert das Paket heute)
    - app/src/hooks/useFlag.ts (die zu loeschende Supabase-Abfrage)
    - app/app/(app)/profile/index.tsx (Vereinsregeln-Banner)
    - app/app/(app)/profile/vereinsregeln/index.tsx (Zeilen 50-70, die PDF-Karte)
    - app/app/(app)/profile/vereinsregeln/upload.tsx (der Stub, der geloescht wird)
    - app/src/lib/sync/SyncWorker.ts (die Push-Verzweigung fuer vereinsregeln)
    - packages/shared/src/i18n/de.json (Block `rules`)
    - .planning/phases/20-fundament-aufr-umen-pwa-deploy/20-PATTERNS.md — Abschnitt "packages/shared/src/constants/flags.ts"
    - .planning/phases/20-fundament-aufr-umen-pwa-deploy/20-CONTEXT.md — D-05 im Wortlaut
  </read_first>
  <behavior>
    - Bei FEATURES.vereinsregeln === false rendert der Profil-Screen keinen Vereinsregeln-Einstieg.
    - Bei FEATURES.vereinsregeln === false erzeugt ein Sync-Durchlauf keinen Push-Versuch fuer die Entitaet vereinsregeln.
    - Die bestehenden Vereinsregeln-Tests laufen weiterhin, indem sie das shared-Modul partiell mocken und die Konstante auf true setzen.
  </behavior>
  <action>
    Duenne, vollstaendige Scheibe durch alle Client-Schichten dieses Plans (shared-Paket -> UI -> Sync-Worker -> Tests). Die Datenbank-Schicht ist bewusst nicht Teil dieser Scheibe — sie haengt am Human-Checkpoint in Task 5.

    1. packages/shared/src/constants/flags.ts vollstaendig ersetzen: statt des FLAGS-Objekts und des FlagKey-Typs eine Compile-Time-Konstante `FEATURES` als `as const` exportieren, mit genau einem Feld `vereinsregeln` auf `false` (D-05). Keine Abfrage, keine Supabase-Abhaengigkeit, kein React. Sicherstellen, dass packages/shared/src/index.ts die neue Konstante exportiert und den alten Export nicht mehr nennt.

    2. app/src/hooks/useFlag.ts und app/src/hooks/__tests__/useFlag.test.ts loeschen (D-06). Danach alle Aufrufstellen im Client suchen und auf die Konstante umstellen bzw. entfernen.

    3. app/app/(app)/profile/index.tsx: das Vereinsregeln-Banner hinter die Konstante legen, so dass es bei false gar nicht gerendert wird. Analog in app/app/(app)/profile/vereinsregeln/_layout.tsx die Routengruppe so gaten, dass sie ohne aktives Feature nicht erreichbar ist (Redirect oder Nicht-Registrierung der Screens — die konkrete Form ist Ermessen, das beobachtbare Verhalten steht in `behavior`).

    4. app/app/(app)/profile/vereinsregeln/index.tsx: die Karte fuer den Dokument-Upload (heute etwa Zeilen 56-62) entfernen. Zugehoerige i18n-Schluessel unterhalb von `rules` mit dem Praefix fuer den Upload aus de.json loeschen. app/app/(app)/profile/vereinsregeln/upload.tsx komplett loeschen (Stub mit ASCII-Ersatzschreibungen). Den Quellwert fuer PDF-Extraktion aus dem VereinsregelSource-Typ und aus allen Stellen entfernen, die ihn nur wegen dieses Wertes behandeln — den Typ selbst und die uebrigen Quellwerte behalten.

    5. app/src/lib/sync/SyncWorker.ts: die Push-Verzweigung fuer die Entitaet vereinsregeln hinter die Konstante legen, so dass bei false kein Push-Versuch mehr entsteht. Der Pull-Pfad und alle anderen Entitaeten bleiben unangetastet.

    6. Tests: die vorhandenen Vereinsregeln-Tests bleiben erhalten und laufen mit aktiviertem Feature. Setze das pro Testdatei ueber ein partielles Modul-Mock des shared-Pakets (die tatsaechlichen Exporte uebernehmen und nur die Konstante ueberschreiben) — dasselbe jest.mock-Idiom, das die Tests im Repo bereits verwenden. Betroffene Dateien vorher per grep bestimmen; mindestens die Store-, Repo- und packages/shared-Tests zum Thema Vereinsregeln.

    Reihenfolge: erst die Tests rot machen (RED-Commit), dann die Umstellung (GREEN-Commit) — Repo-Konvention.
  </action>
  <acceptance_criteria>
    - `git grep -n "rules\.upload\|pdf_extraction" -- app packages` liefert keine Zeile.
    - app/src/hooks/useFlag.ts und app/src/hooks/__tests__/useFlag.test.ts sind nicht mehr im Git-Index.
    - app/app/(app)/profile/vereinsregeln/upload.tsx ist nicht mehr im Git-Index.
    - packages/shared/src/constants/flags.ts enthaelt die Zeichenfolge `FEATURES` und `vereinsregeln`.
    - `pnpm -r run typecheck`, `pnpm -r run lint`, `pnpm --filter app exec jest --ci`, `pnpm --filter @spatenstich/shared exec jest --ci` enden alle mit exit 0.
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(git grep -n 'rules\.upload\|pdf_extraction' -- app packages)" &amp;&amp; ! git ls-files --error-unmatch app/src/hooks/useFlag.ts 2>/dev/null &amp;&amp; ! git ls-files --error-unmatch 'app/app/(app)/profile/vereinsregeln/upload.tsx' 2>/dev/null &amp;&amp; grep -q 'FEATURES' packages/shared/src/constants/flags.ts</automated>
    <fails_when>git grep gibt mindestens eine Trefferzeile aus, oder eine der drei zu loeschenden Dateien ist noch im Git-Index, oder flags.ts nennt die neue Konstante nicht</fails_when>
  </verify>
  <verify>
    <automated>pnpm -r run typecheck &amp;&amp; pnpm --filter @spatenstich/shared exec jest --ci &amp;&amp; pnpm --filter app exec jest --ci</automated>
    <fails_when>non-zero exit, oder eine Zusammenfassungszeile beginnend mit `Tests:` nennt einen failed-Anteil groesser 0</fails_when>
  </verify>
  <reversibility rating="reversible">Client-seitige Umstellung, per Commit-Revert zuruecknehmbar; es werden keine Daten geloescht.</reversibility>
  <done>Die Konstante ist die einzige Quelle fuer den Vereinsregeln-Schalter, alle drei Konsumenten sind gegated, beide Testsuiten gruen.</done>
</task>

<task type="auto">
  <name>Task 2: Foto-Pipeline, Share-Intent und sieben Dependencies entfernen</name>
  <files>app/src/lib/photos/exifStrip.ts, app/src/lib/photos/exifStrip.native.ts, app/src/lib/photos/exifStrip.web.ts, app/src/lib/photos/__tests__/exifStrip.test.ts, app/src/lib/photos/__tests__/setup.ts, app/src/lib/photos/__tests__/__fixtures__/exif-test.jpg, app/src/stores/captureStore.ts, app/src/stores/settingsStore.ts, app/src/stores/__tests__/settingsStore.test.ts, app/src/stores/importStore.ts, app/app/(app)/settings/privacy.tsx, app/app/(app)/settings.tsx, app/app/(app)/_layout.tsx, app/app.config.ts, app/jest.config.ts, app/package.json, pnpm-lock.yaml, supabase/seed.sql</files>
  <read_first>
    - app/src/stores/importStore.ts (es referenziert die zu loeschenden Stores — hier entscheidet sich, ob eine Anpassung noetig ist)
    - app/app/(app)/settings.tsx (Zeile 175: der Link auf den Datenschutz-Screen)
    - app/app/(app)/_layout.tsx (der Share-Intent-Provider und der Effekt, vollstaendig)
    - app/app.config.ts (der Plugin-Eintrag fuer das Teilen-Modul)
    - app/jest.config.ts (Zeilen 62-84: das Projekt fuer die Foto-Tests)
    - app/package.json (Dependency-Listen)
    - supabase/seed.sql (die Flag-Seeds)
    - .planning/MASTERPLAN-v2.md — Anhang A (Loesch-Inventar) und Kap. 4 WP 20.2
  </read_first>
  <action>
    Loeschen nach Anhang A, streng auf die dort genannte Liste begrenzt (D-06).

    1. Verzeichnis app/src/lib/photos/ mit allen Dateien und Fixtures loeschen. In app/jest.config.ts das zugehoerige Projekt aus dem projects-Array entfernen; die uebrigen Projekte bleiben unveraendert.

    2. app/src/stores/captureStore.ts, app/src/stores/settingsStore.ts und app/src/stores/__tests__/settingsStore.test.ts loeschen. app/src/stores/importStore.ts liest heute aus diesen Stores — dort die Referenzen entfernen und das verbleibende Verhalten so lassen, wie es ohne die geloeschten Einstellungen ist (kein Ersatz-Feature bauen, kein Platzhalter). Wenn ein Wert dadurch fehlt, den bisherigen Default direkt einsetzen.

    3. app/app/(app)/settings/privacy.tsx loeschen und in app/app/(app)/settings.tsx den Link auf diese Route entfernen (Zeile 175 herum). Der Screen wird in WP 24.5 als Datenschutz/Impressum neu gebaut — hier keinen Ersatz-Screen und keinen toten Link stehen lassen.

    4. app/app/(app)/_layout.tsx: den Provider des Teilen-Moduls und den kompletten Effekt, der auf ein OS-Teilen-Ereignis reagiert, entfernen. Die Stack-Konfiguration mit headerRight und dem Sync-Status-Badge bleibt woertlich erhalten. Danach exportiert die Datei nur noch eine Komponente, die den Stack rendert. Den Kommentarkopf entsprechend kuerzen.

    5. app/app.config.ts: den Plugin-Eintrag fuer das Teilen-Modul aus dem plugins-Array entfernen. Alle anderen Eintraege (expo-router, Sentry, experiments) bleiben unveraendert — die uebrigen Anpassungen an dieser Datei macht Plan 20-03.

    6. app/package.json: die sieben Dependencies aus Anhang A entfernen (@lodev09/react-native-exify, expo-image-manipulator, expo-image-picker, piexifjs, @types/piexifjs, expo-share-intent, jest-expo). Danach `pnpm install` laufen lassen, damit pnpm-lock.yaml konsistent ist. Die drei Editor-Pakete (Skia, Color-Picker, DateTimePicker) bleiben stehen, sie gehoeren zu WP 22.4.

    7. supabase/seed.sql: den Seed-Block fuer die Flag-Tabelle entfernen (die Tabelle faellt in Task 4).

    8. Bundle-Groesse vor und nach dem Aufraeumen messen (`pnpm --filter app run build` und die Groesse von app/dist) und beide Werte in der SUMMARY notieren; Zielmarke laut CONTEXT ist unter 5,2 MB.
  </action>
  <acceptance_criteria>
    - Ein git grep ueber app/src und app/app nach den fuenf Legacy-Bezeichnern liefert ausschliesslich Kommentarzeilen.
    - Keine der sieben entfernten Dependencies steht noch in app/package.json.
    - app/jest.config.ts enthaelt kein Projekt mehr, dessen testMatch auf das geloeschte Foto-Verzeichnis zeigt.
    - `pnpm -r run typecheck`, `pnpm -r run lint` und beide Jest-Suiten enden mit exit 0.
    - `pnpm --filter app run build` erzeugt app/dist ohne Fehler.
    - Die Bundle-Groesse vorher/nachher steht in der SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>test "$(git grep -n -i 'plantnet\|anthropic\|captureStore\|exif\|useFlag' -- app/src app/app | grep -vE ':[0-9]+:[[:space:]]*(//|\*|/\*)' | wc -l)" -eq 0</automated>
    <fails_when>Die Zaehlung ist groesser 0 — es gibt noch mindestens eine Nicht-Kommentar-Zeile in app/src oder app/app, die einen der fuenf Legacy-Bezeichner nennt</fails_when>
  </verify>
  <verify>
    <automated>node -e "const p=require('./app/package.json');const bad=['@lodev09/react-native-exify','expo-image-manipulator','expo-image-picker','piexifjs','@types/piexifjs','expo-share-intent','jest-expo'].filter(n=>(p.dependencies&amp;&amp;p.dependencies[n])||(p.devDependencies&amp;&amp;p.devDependencies[n]));if(bad.length){console.error('still present: '+bad.join(', '));process.exit(1)}"</automated>
    <fails_when>Das Skript gibt eine Zeile aus, die mit "still present:" beginnt, und endet non-zero — mindestens eine der sieben Dependencies steht noch in app/package.json</fails_when>
  </verify>
  <verify>
    <automated>pnpm -r run typecheck && pnpm -r run lint && pnpm --filter app exec jest --ci && pnpm --filter @spatenstich/shared exec jest --ci</automated>
    <fails_when>non-zero exit, oder eine Zusammenfassungszeile beginnend mit `Tests:` nennt einen failed-Anteil groesser 0</fails_when>
  </verify>
  <reversibility rating="costly">Loeschungen im Client sind per Git wiederherstellbar, aber das Entfernen der Dependencies aendert pnpm-lock.yaml; ein Zurueckdrehen erfordert einen erneuten Install-Lauf.</reversibility>
  <done>Legacy-Grep sauber, sieben Dependencies raus, Jest-Projekt fuer Foto-Tests weg, Build und beide Suiten gruen, Bundle-Groesse dokumentiert.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lokal-Modus crashfrei — Home-Buttons zeigen den Konto-Hinweis</name>
  <files>app/app/(app)/index.tsx, packages/shared/src/i18n/de.json</files>
  <read_first>
    - app/app/(app)/index.tsx (Zeilen 175-225: die vier Buttons im leeren Zustand; `mode === 'account'` wird dort bereits als Bedingung benutzt)
    - packages/shared/src/i18n/de.json (Block `common`)
    - .planning/phases/20-fundament-aufr-umen-pwa-deploy/20-CONTEXT.md — D-04 im Wortlaut
    - .planning/phases/20-fundament-aufr-umen-pwa-deploy/20-PATTERNS.md — Abschnitt "i18n key lookup helper"
  </read_first>
  <behavior>
    - Im Lokal-Modus fuehrt ein Tap auf "Plan oeffnen", "Importieren" oder "Kalender" nicht mehr in eine Exception, sondern zeigt den Hinweistext aus common.accountRequired.
    - Im Konto-Modus verhalten sich dieselben Buttons unveraendert und navigieren wie bisher.
  </behavior>
  <action>
    Einzige erlaubte Aenderung am Lokal-Modus (D-04): der Absturzpfad auf dem Home-Screen. Es wird kein Lokal-Modus-Code geloescht, keine Verzweigung entfernt und keine Route umgebaut.

    1. In packages/shared/src/i18n/de.json unter `common` einen Schluessel `accountRequired` ergaenzen, Text sinngemaess "Dafuer brauchst du ein Konto" — mit echten Umlauten geschrieben, Du-Form (R2).

    2. In app/app/(app)/index.tsx die drei Buttons, die heute im Lokal-Modus in eine Exception laufen ("Plan oeffnen", "Importieren", "Kalender", Bereich Zeilen 183-224), so umbauen, dass sie bei lokalem Modus den Hinweis anzeigen statt zu navigieren. Die Darstellung ist Ermessen — der bestehende InlineBanner oder ein Hinweistext direkt unter den Buttons; wichtig ist nur das beobachtbare Verhalten aus `behavior`. Die Buttons bleiben sichtbar und behalten ihre testID.

    3. Test zuerst: eine Testdatei nach dem Muster von app/src/components/__tests__/create-garden-entrypoints.test.tsx anlegen oder die vorhandene erweitern, die den Store im lokalen Modus mockt, die drei Buttons per testID antippt und prueft, dass kein Navigationsaufruf erfolgt und der Hinweistext erscheint. RED-Commit vor GREEN-Commit.
  </action>
  <acceptance_criteria>
    - packages/shared/src/i18n/de.json enthaelt den Pfad common.accountRequired.
    - Der Wert enthaelt echte UTF-8-Umlaute und keine ASCII-Ersatzschreibung.
    - Ein Test deckt alle drei Buttons im lokalen Modus ab und ist gruen.
    - Ein git diff dieses Tasks zeigt keine geaenderte Zeile in migrateLocalToAccount.ts oder auth.ts.
    - Beide Jest-Suiten und Typecheck/Lint bleiben exit 0.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const d=require('./packages/shared/src/i18n/de.json');const v=d.common&&d.common.accountRequired;if(!v){console.error('key missing');process.exit(1)};if(/\b(ae|oe|ue)\b/.test(v)){console.error('ascii substitution');process.exit(1)}"</automated>
    <fails_when>Das Skript endet non-zero mit "key missing" (Schluessel fehlt) oder "ascii substitution" (ASCII-Ersatzschreibung im Wert)</fails_when>
  </verify>
  <verify>
    <automated>pnpm --filter app exec jest --ci --selectProjects components && pnpm -r run typecheck</automated>
    <fails_when>non-zero exit, oder die Zusammenfassungszeile beginnend mit `Tests:` nennt einen failed-Anteil groesser 0</fails_when>
  </verify>
  <verify>
    <automated>test -z "$(git diff --name-only -- app/src/lib/migrateLocalToAccount.ts app/src/lib/auth.ts)"</automated>
    <fails_when>git diff nennt migrateLocalToAccount.ts oder auth.ts — D-04 verbietet jede Aenderung am Lokal-Modus-Kern</fails_when>
  </verify>
  <reversibility rating="reversible">Eine UI-Verzweigung und ein i18n-Schluessel.</reversibility>
  <done>Lokal-Modus stuerzt auf dem Home-Screen nicht mehr ab; der Lokal-Modus-Kern ist unveraendert.</done>
</task>

<task type="auto">
  <name>Task 4: Migration 020 schreiben und committen (noch nicht pushen)</name>
  <files>supabase/migrations/20260910000020_cleanup_legacy.sql, supabase/tests/garden_plan_rls.sql, supabase/tests/storage_photos_rls.sql, supabase/tests/trigger_ordering.sql, supabase/tests/migration_003_atomic.sql, supabase/tests/rls_foundation.sql, supabase/tests/rls_member_check.sql</files>
  <read_first>
    - supabase/migrations/20260509000015_remove_ai_tables.sql (Analog: Kopfkommentar, Sektionsbanner, DO-Bloecke, Invarianten-Assertion am Ende — und die Notiz, dass Supabase die Datei bereits in eine Transaktion klammert)
    - supabase/migrations/20260423000009_transfer_ownership_audit_invariant.sql (die urspruengliche Korrektur)
    - supabase/migrations/20260423000010_custom_sqlstate_codes.sql (ab Zeile 112: die SQLSTATE-Codes P9004/P9005)
    - supabase/migrations/20260424000013_offline_sync_infrastructure.sql (Zeilen 260-330: aktuelle Fassung mit der Regression und der Zeitstempel-Zeile, die erhalten bleiben muss)
    - supabase/migrations/20260416000001_foundation.sql (Definition der Flag-Tabelle)
    - .planning/MASTERPLAN-v2.md — Anhang C.1 (Skizze) und Kap. 0.5 (3-Gate-Protokoll)
    - .planning/phases/20-fundament-aufr-umen-pwa-deploy/20-RESEARCH.md — Pitfalls 4 und 5, Runtime State Inventory
    - supabase/tests/ (die sechs zu pruefenden Dateien)
  </read_first>
  <action>
    Migration schreiben, committen — und an dieser Stelle bewusst NICHT pushen. Der Push ist Task 6 und haengt am Checkpoint in Task 5 (R9).

    Datei: supabase/migrations/20260910000020_cleanup_legacy.sql. Struktur und Kommentarkonvention vom Analog 20260509000015 uebernehmen: Kopfkommentar mit Zweck und Provides-Zeile, Sektionsbanner, DROP POLICY vor DROP TABLE, DO-Bloecke fuer bedingte Operationen, am Dateiende ein Assertion-Block, der prueft, dass die Objekte wirklich weg sind, und sonst eine Exception wirft. Keine eigenstaendige Transaktionsklammer in der Datei (das Analog haelt fest, dass Supabase die Datei bereits klammert). Alles idempotent formulieren (if exists / if not exists), R5.

    Inhalt in dieser Reihenfolge:
    1. Die Funktion enqueue_photo_analysis droppen.
    2. Die Tabelle photo_queue mitsamt ihren Policies und Triggern droppen.
    3. Die Tabelle feature_flags mitsamt Policies droppen.
    4. Storage: in einem DO-Block pruefen, ob storage.objects fuer die Bucket-Ids photos und vereinsregeln null Zeilen hat. Nur dann die zugehoerigen Storage-Policies und danach die Bucket-Zeilen entfernen. Sonst eine notice ausgeben und nichts loeschen — beide Ausgaenge sind gueltige Ergebnisse (RESEARCH Open Question 2). Policies und Bucket immer im selben Block, damit keine verwaisten Policies zurueckbleiben.
    5. Vor dem Spalten-Drop: die Migrationen nach Lesern der drei Profilspalten durchsuchen und jede Funktion, die sie liest, in dieser Migration neu definieren. Aufpassen: die gleichnamigen Spalten auf der Garten-Tabelle sind produktiv und bleiben unangetastet (RESEARCH Pitfall 4) — der Drop zielt ausschliesslich auf public.profiles.
    6. Die drei Spalten plz, klimazone und archetype auf public.profiles droppen.
    7. transfer_ownership neu definieren. Body aus drei Quellen zusammensetzen (RESEARCH Pitfall 5): die SQLSTATE-Codes aus Migration 010, die Zeitstempel-Aktualisierung aus Migration 013 — und ohne die Zuweisung der Erzeuger-Spalte an den neuen Besitzer, die Migration 013 wieder eingeschleppt hat. Alle vier historischen Fassungen lesen, nicht zwei diffen.

    Danach die veralteten SQL-Tests unter supabase/tests/ behandeln: garden_plan_rls.sql (Test 6), storage_photos_rls.sql, trigger_ordering.sql, migration_003_atomic.sql, rls_foundation.sql, rls_member_check.sql. Vorher pruefen, welche Aussagen dieser Dateien durch Migration 020 ungueltig werden — nur diese entfernen oder anpassen, den Rest stehen lassen. Welche Datei geloescht und welche angepasst wurde, in der SUMMARY auflisten.
  </action>
  <acceptance_criteria>
    - supabase/migrations/20260910000020_cleanup_legacy.sql existiert und nennt alle fuenf Zielobjekte: photo_queue, enqueue_photo_analysis, feature_flags, storage.objects, transfer_ownership.
    - Die Datei enthaelt keine eigenstaendige Transaktionsklammer.
    - Ausserhalb von Kommentarzeilen enthaelt die Datei keine Zuweisung der Erzeuger-Spalte im UPDATE auf die Garten-Tabelle.
    - Der Bucket-Drop steht in einem DO-Block hinter einer Zaehlpruefung auf storage.objects.
    - `pnpm -r run typecheck` und beide Jest-Suiten bleiben exit 0 (die Migration beruehrt keinen Client-Code).
    - Der Push ist an dieser Stelle nachweislich NICHT erfolgt.
  </acceptance_criteria>
  <verify>
    <automated>M=supabase/migrations/20260910000020_cleanup_legacy.sql; test -f "$M" && for s in photo_queue enqueue_photo_analysis feature_flags storage.objects transfer_ownership; do grep -q "$s" "$M" || { echo "missing $s"; exit 1; }; done && ! grep -iqE '^[[:space:]]*(begin|commit)[[:space:]]*;' "$M"</automated>
    <fails_when>Die Datei fehlt, eine Zeile "missing <objekt>" wird ausgegeben, oder die Datei enthaelt eine eigenstaendige Transaktionsklammer-Anweisung</fails_when>
  </verify>
  <verify>
    <automated>M=supabase/migrations/20260910000020_cleanup_legacy.sql; ! grep -v '^[[:space:]]*--' "$M" | grep -qiE 'set[[:space:]]+created_by_user_id|created_by_user_id[[:space:]]*=[[:space:]]*p_to_user_id'</automated>
    <fails_when>Eine Nicht-Kommentar-Zeile der Migration weist die Erzeuger-Spalte zu — die Regression aus Migration 013 waere damit erneut eingebaut</fails_when>
  </verify>
  <verify>
    <automated>M=supabase/migrations/20260910000020_cleanup_legacy.sql; grep -qi 'storage.objects' "$M" && grep -qiE 'count\(\*\)' "$M" && grep -qi 'do \$\$' "$M"</automated>
    <fails_when>Der Bucket-Drop steht nicht hinter einer Zaehlpruefung in einem DO-Block — dann wuerde er unbedingt loeschen</fails_when>
  </verify>
  <reversibility rating="reversible">Solange nur committet und nicht gepusht wird, ist nichts an der Live-Datenbank geschehen.</reversibility>
  <done>Migration 020 ist geschrieben, committet und ungepusht; die veralteten SQL-Tests sind bereinigt.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 5: ENTSCHEIDUNG — destruktive Bereinigung der Live-Datenbank durchfuehren?</name>
  <read_first>
    - supabase/migrations/20260910000020_cleanup_legacy.sql (das Ergebnis aus Task 4, vollstaendig)
    - .planning/MASTERPLAN-v2.md — Anhang C.1, Regel R9, Kap. 0.5
    - .planning/phases/20-fundament-aufr-umen-pwa-deploy/20-RESEARCH.md — Open Question 2 (Bucket-Inhalt unbekannt)
  </read_first>
  <decision>
    Migration 020 entfernt Objekte aus der Live-Datenbank vitrqkzxkiqvadqfzrcx endgueltig: die Foto-Warteschlangen-Tabelle mit ihrer Funktion, die Flag-Tabelle, drei Spalten auf der Profil-Tabelle und — nur falls leer — zwei Storage-Buckets samt Policies. Dazu wird transfer_ownership neu definiert. Ein Rueckweg existiert nur ueber das Backup aus Schritt M5. Soll diese Bereinigung jetzt in dieser Phase erfolgen?
  </decision>
  <options>
    A) Jetzt bereinigen (Empfehlung, entspricht CONTEXT und Masterplan WP 20.2): Migration 020 wird nach dem Backup gepusht. Phase 21 startet auf einem sauberen Schema; die Outbox erzeugt keine Eintraege mehr fuer geloeschte Objekte.
    B) Migration committen, Push auf Phase 21 verschieben und zusammen mit Migration 021 pushen (dort ist das Backup M5 ohnehin Pflicht). Kosten: DEPLOY-02 bleibt bis dahin unvollstaendig, die Legacy-Objekte bleiben in der Datenbank.
    C) Gar nicht bereinigen: die Legacy-Objekte bleiben dauerhaft stehen. Kosten: widerspricht D-06 und Masterplan Anhang A; der Client haette dann Code entfernt, dessen Serverseite weiterlebt.
  </options>
  <acceptance_criteria>
    - Der User hat die Migration im Wortlaut gesehen, bevor er entscheidet.
    - Es liegt eine Auswahl A, B oder C vor.
    - Die Auswahl und ihre Begruendung stehen in der SUMMARY.
  </acceptance_criteria>
  <reversibility rating="one-way">Bei Auswahl A werden Datenbankobjekte endgueltig entfernt; nur das Backup aus M5 fuehrt zurueck.</reversibility>
  <resume-signal>Der User hat A, B oder C gewaehlt. Bei A weiter mit Task 6. Bei B oder C: Tasks 6 und 7 entfallen, der Plan endet mit committeter, ungepushter Migration und einem entsprechenden Vermerk in der SUMMARY.</resume-signal>
  <done>Die Entscheidung ist getroffen und dokumentiert.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 6: CHECKPOINT — Backup nach M5 bestaetigen</name>
  <read_first>
    - .planning/MASTERPLAN-v2.md — Kap. 5 (M5), Kap. 7.4 (Backup-Verfahren), Regel R9
    - Das Ergebnis von Task 5 (Auswahl A, B oder C)
  </read_first>
  <action>
    Nur bei Auswahl A aus Task 5. Vor dem Push muss ein aktuelles Backup existieren (R9) — Dirks echter Gartenplan liegt in dieser Datenbank.
  </action>
  <instructions>
    1. Backup erstellen: `scripts/backup-supabase.ps1` ausfuehren (das Skript entsteht in Plan 20-04; bis dahin genuegt ein manueller pg_dump nach Masterplan Kap. 7.4 oder ein bestaetigtes Dashboard-Backup). Das Datenbank-Passwort steht im Supabase-Dashboard unter Settings und Database.
    2. Den Pfad der erzeugten Dump-Datei oder die Art der Dashboard-Bestaetigung nennen.
    3. Die Freigabe erteilen mit der Antwort auf die Frage: "Liegt ein aktuelles Backup vor, und soll ich Migration 020 jetzt pushen? (ja / nein)"
  </instructions>
  <verification>
    Der User hat eine Backup-Quelle genannt (Dateipfad oder Dashboard-Bestaetigung) und ausdruecklich mit ja oder nein geantwortet. Ohne ein "ja" mit genannter Quelle gilt die Freigabe als nicht erteilt.
  </verification>
  <acceptance_criteria>
    - Es liegt eine eindeutige Antwort ja oder nein vor.
    - Bei ja: die Backup-Quelle ist in der SUMMARY notiert.
    - Bei nein: Task 7 wurde nicht ausgefuehrt und der offene Punkt steht in der SUMMARY.
  </acceptance_criteria>
  <resume-signal>Freigabe erteilt (weiter mit Task 7) oder verweigert (Plan endet hier; in der SUMMARY steht, dass Migration 020 aussteht und DEPLOY-02 damit noch nicht vollstaendig erfuellt ist — das ist ein gueltiges Planende, kein Fehler).</resume-signal>
  <done>Backup-Freigabe erteilt oder verweigert, in beiden Faellen dokumentiert.</done>
</task>

<task type="auto">
  <name>Task 7: Migration 020 nach 3-Gate-Protokoll pushen</name>
  <files>supabase/migrations/20260910000020_cleanup_legacy.sql</files>
  <read_first>
    - .planning/MASTERPLAN-v2.md — Kap. 0.5 (die vier Kommandos in genau dieser Reihenfolge)
    - Das Ergebnis von Task 5 (Auswahl A/B/C) und Task 6 (Backup-Freigabe ja/nein)
  </read_first>
  <precondition>Der User hat in Task 5 Auswahl A getroffen und in Task 6 ausdruecklich "ja" geantwortet sowie eine Backup-Quelle genannt. Ohne diese Freigabe wird dieser Task uebersprungen und der Plan endet mit committeter, ungepushter Migration.</precondition>
  <action>
    Die vier Kommandos aus Masterplan Kap. 0.5 in genau dieser Reihenfolge ausfuehren und jede Ausgabe in die SUMMARY uebernehmen:

    1. Gate 1: `supabase migration list --linked` — Exit 0 und den Local/Remote-Diff lesen. Steht 20260910000020 bereits remote, ist nichts zu tun.
    2. Gate 2: `supabase db push --linked --dry-run --yes` — Exit 0. Meldet der Dry-Run einen Konflikt oder ein fehlendes Objekt, hier abbrechen und die Migration nachbessern (zurueck zu Task 4), nicht mit Gate 3 weitermachen.
    3. Gate 3: `supabase db push --linked --yes`.
    4. Nachweis: `supabase migration list --linked` erneut — 20260910000020 muss in der lokalen UND der Remote-Spalte stehen.

    Die Ausgabe der Migration auf Hinweise pruefen: erschien die notice zu nicht leeren Buckets, dann sind die Buckets absichtlich stehen geblieben. Das in der SUMMARY als offenen manuellen Aufraeumpunkt vermerken, nicht als Fehlschlag werten (RESEARCH Open Question 2).
  </action>
  <acceptance_criteria>
    - Alle vier Kommandos wurden ausgefuehrt, ihre Ausgaben stehen in der SUMMARY.
    - Gate 2 endete mit exit 0, bevor Gate 3 gestartet wurde.
    - Nach Gate 3 erscheint 20260910000020 in beiden Spalten der Migrationsliste.
    - Der Bucket-Ausgang (geloescht oder notice) ist dokumentiert.
  </acceptance_criteria>
  <verify>
    <automated>supabase migration list --linked</automated>
    <fails_when>non-zero exit, oder die Zeile mit 20260910000020 hat eine leere Remote-Spalte</fails_when>
  </verify>
  <reversibility rating="one-way">Der Push loescht Objekte in der Live-Datenbank; Wiederherstellung nur aus dem Backup von Schritt M5.</reversibility>
  <done>Migration 020 ist live und in beiden Spalten der Migrationsliste nachgewiesen — oder der Task wurde mangels Freigabe uebersprungen und das steht in der SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lokale Entwicklungsumgebung -> Live-Postgres (Supabase Frankfurt) | `supabase db push` fuehrt destruktives DDL gegen echte Nutzerdaten aus |
| Client-Bundle -> Storage-Buckets | Die Policies der zu loeschenden Buckets sind heute Teil der Zugriffsgrenze |
| Client -> feature_flags-Tabelle | Faellt mit dieser Migration weg; danach ist der Schalter eine Compile-Time-Konstante |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-02-01 | Denial of Service | Migration 020 gegen Live-DB (Datenverlust) | critical | mitigate | Blockierender Human-Checkpoint in Task 5 mit Backup-Nachweis (M5, R9); 3-Gate-Protokoll mit Dry-Run vor dem echten Push. |
| T-20-02-02 | Elevation of Privilege | Verwaiste Storage-Policies nach Bucket-Drop | high | mitigate | Policies und Bucket-Zeile werden im selben DO-Block und nur bei leerem Bucket entfernt; nie Policy ohne Bucket oder umgekehrt (ASVS V4). |
| T-20-02-03 | Tampering | transfer_ownership-Neudefinition | high | mitigate | Der neue Body uebernimmt die SQLSTATE-Codes aus Migration 010 und die Zeitstempel-Aktualisierung aus 013, laesst aber die Audit-Regression weg; ein Verify-Gate greppt genau darauf. |
| T-20-02-04 | Information Disclosure | Nicht leere Buckets mit echten Fotos | medium | accept | Die Zaehlpruefung verhindert das Loeschen; nicht geloeschte Buckets bleiben mit ihren bestehenden Policies erhalten und werden als offener manueller Punkt dokumentiert. |
| T-20-02-05 | Spoofing | Wegfall des serverseitigen Flag-Schalters | low | accept | Der Schalter war ohne Nutzer und ohne Sicherheitswirkung; die Compile-Time-Konstante ist nicht manipulierbar und damit haerter als vorher. |
| T-20-02-SC | Tampering | npm/pnpm-Installationen | high | mitigate | Dieser Plan entfernt nur Dependencies und installiert keine neuen. `pnpm install` laeuft ausschliesslich zur Lockfile-Konsistenz. Die Legitimacy-Pruefung fuer die zwei neuen devDependencies liegt als blockierender Human-Checkpoint in Plan 20-03. |
</threat_model>

## Artifacts this phase produces (Anteil dieses Plans)

| Artefakt | Art | Status nach diesem Plan |
|---|---|---|
| `packages/shared/src/constants/flags.ts` — Konstante `FEATURES` mit `vereinsregeln: false` | ersetzt | fertig |
| `packages/shared/src/i18n/de.json` — Schluessel `common.accountRequired` | erweitert | fertig |
| `supabase/migrations/20260910000020_cleanup_legacy.sql` | neu | geschrieben; Push nur nach Freigabe |
| `app/jest.config.ts` — Projekt fuer die Foto-Tests entfernt | geaendert | fertig |
| `app/package.json` / `pnpm-lock.yaml` — sieben Dependencies entfernt | geaendert | fertig |
| `app/app/(app)/_layout.tsx` — ohne Provider des Teilen-Moduls | geaendert | fertig (SW-Glue folgt in 20-03) |
| `app/app.config.ts` — Plugin-Eintrag des Teilen-Moduls entfernt | geaendert | teilweise (restliche Felder in 20-03) |
| geloescht: `app/src/lib/photos/**`, `app/src/stores/captureStore.ts`, `app/src/stores/settingsStore.ts` (+Test), `app/app/(app)/settings/privacy.tsx`, `app/src/hooks/useFlag.ts` (+Test), `app/app/(app)/profile/vereinsregeln/upload.tsx` | geloescht | fertig |

## Validation

Verifikationsstrategie und Task-Kommando-Matrix: `.planning/phases/20-fundament-aufr-umen-pwa-deploy/20-VALIDATION.md`
(Zeilen 20-02-01 bis 20-02-07). Die Migrations-Gates sind dort als Infrastrukturkommandos gefuehrt,
die Bucket-Frage als manueller Beobachtungspunkt.

<verification>
```bash
pnpm -r run typecheck
pnpm -r run lint
pnpm --filter app exec jest --ci
pnpm --filter @spatenstich/shared exec jest --ci
pnpm --filter app run build
supabase migration list --linked
```
Zusaetzlich die beiden Grep-Gates aus den Akzeptanzkriterien von Task 1 und Task 2.
</verification>

<success_criteria>
- Legacy-Grep ueber app/src und app/app liefert nur Kommentarzeilen (DEPLOY-02)
- Kein Treffer mehr fuer die Upload-Schluessel und den PDF-Quellwert (D-05)
- Vereinsregeln-Banner, -Routen und -Push haengen an der FEATURES-Konstante (D-05)
- Lokal-Modus-Kern unveraendert, Home-Buttons crashfrei (D-04)
- Sieben Dependencies entfernt, Bundle-Groesse dokumentiert (D-06)
- Migration 020 geschrieben; gepusht genau dann, wenn der User in Task 5 freigegeben hat (R9)
</success_criteria>

<output>
Create `.planning/phases/20-fundament-aufr-umen-pwa-deploy/20-02-SUMMARY.md` when done.
Darin festhalten: Testanzahl vorher/nachher, Bundle-Groesse vorher/nachher, Liste der geloeschten
und angepassten SQL-Tests, Ergebnis der Entscheidung aus Task 5 (A/B/C) und der Backup-Freigabe aus Task 6 (ja/nein + Quelle),
Ausgang der Bucket-Pruefung (geloescht oder notice), Status von Migration 020.
</output>
