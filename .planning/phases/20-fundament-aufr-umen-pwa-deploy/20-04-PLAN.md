---
phase: 20-fundament-aufr-umen-pwa-deploy
plan: 04
type: execute
wave: 4
depends_on: ["20-03"]
files_modified:
  - .github/workflows/deploy-web.yml
  - .github/workflows/supabase-keepalive.yml
  - scripts/keepalive.ps1
  - scripts/backup-supabase.ps1
  - README.md
  - CLAUDE.md
autonomous: false
requirements: [DEPLOY-05, DEPLOY-06]
user_setup:
  - service: cloudflare
    why: "Der Deploy laeuft per Direct Upload aus GitHub Actions; ohne Token und Account-ID kann der Workflow nicht hochladen (D-02, Masterplan M1)."
    env_vars:
      - name: CLOUDFLARE_API_TOKEN
        source: "Cloudflare Dashboard -> My Profile -> API Tokens -> Template 'Cloudflare Pages — Edit' (nicht kontoweit) -> als GitHub-Secret hinterlegen"
      - name: CLOUDFLARE_ACCOUNT_ID
        source: "Cloudflare Dashboard -> Workers & Pages -> rechte Spalte Account ID -> als GitHub-Secret hinterlegen"
    dashboard_config:
      - task: "M1: Cloudflare-Projekt 'spatenstich' als Direct Upload anlegen"
        location: "Cloudflare Dashboard -> Workers & Pages -> Create project -> Direct Upload"
      - task: "M3: Site URL und Redirect URLs auf https://spatenstich.pages.dev setzen (plus http://localhost:8095 fuer Dev)"
        location: "Supabase Projekt vitrqkzxkiqvadqfzrcx -> Authentication -> URL Configuration"
  - service: github
    why: "Der Build im Deploy-Workflow braucht dieselben EXPO_PUBLIC_-Werte wie CI (Masterplan M2)."
    env_vars:
      - name: EXPO_PUBLIC_SUPABASE_URL
        source: "GitHub -> Settings -> Variables (falls in Plan 20-01 noch nicht geschehen)"
      - name: EXPO_PUBLIC_SUPABASE_ANON_KEY
        source: "GitHub -> Settings -> Variables"
      - name: EXPO_PUBLIC_SENTRY_DSN
        source: "GitHub -> Settings -> Secrets (EU-Ingest)"

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 5
  confidence: low

must_haves:
  truths:
    - "deploy-web.yml wird von einem Push auf master und per workflow_dispatch ausgeloest (DEPLOY-05)."
    - "Der Deploy-Workflow installiert mit gefrorenem Lockfile, baut mit dem build:web-Script, prueft danach das Bundle mit scripts/check-claude-key-in-bundle.sh und laedt erst dann nach Cloudflare Pages hoch (DEPLOY-05, R7)."
    - "Der Secret-Scan-Schritt steht im Workflow vor dem Upload-Schritt — nicht danach (R7)."
    - "supabase-keepalive.yml laeuft per Zeitplan alle drei Tage und zusaetzlich per workflow_dispatch und ruft die REST-Schnittstelle mit dem Anon-Key auf (DEPLOY-06)."
    - "scripts/keepalive.ps1 ist ein lauffaehiges PowerShell-Skript fuer den Task Scheduler auf dem 24/7-PC (DEPLOY-06, Masterplan Kap. 7.3)."
    - "scripts/backup-supabase.ps1 erstellt einen komprimierten Dump und haelt 30 Tage Rotation; das Passwort kommt aus dem Windows Credential Manager und steht nirgends im Repo (DEPLOY-06, Kap. 7.4, R7)."
    - "README.md erklaert die Installation auf dem Android-Handy, den Import aus der Claude-App per Teilen und das Einrichten des Claude-Projekts (DEPLOY-05)."
    - "README.md weist darauf hin, dass GitHub den Zeitplan nach 60 Tagen Repo-Inaktivitaet pausiert und deshalb der Task-Scheduler-Ping zusaetzlich noetig ist (RESEARCH Pitfall 7)."
    - statement: "Ein Push auf master erzeugt innerhalb von 10 Minuten einen erreichbaren Stand unter https://spatenstich.pages.dev."
      verification: backstop
    - statement: "Ein manuell gestarteter Lauf von supabase-keepalive.yml endet erfolgreich."
      verification: backstop
    - statement: "Beide Android-Handys installieren die App aus Chrome, das Icon ist auch rund maskiert korrekt und der Start erfolgt ohne Browser-Leiste (M6, deckt den Backstop aus Plan 20-03 ab)."
      verification: backstop
    - statement: "Teilen aus der Claude-App auf eine .json-Datei und auf einen Text oeffnet die Import-Vorschau mit dem Payload (M6, deckt DEPLOY-04 ab)."
      verification: backstop
    - statement: "Der zweite Start im Flugmodus zeigt die App-Shell mit dem letzten Plan (deckt DEPLOY-07 ab)."
      verification: backstop
  artifacts:
    - ".github/workflows/deploy-web.yml"
    - ".github/workflows/supabase-keepalive.yml"
    - "scripts/keepalive.ps1"
    - "scripts/backup-supabase.ps1"
    - "README.md mit den drei neuen Anleitungsabschnitten"
  key_links:
    - "deploy-web.yml build:web -> haengt an dem Script, das Plan 20-03 in app/package.json angelegt hat"
    - "wrangler-action Upload-Verzeichnis app/dist -> haengt daran, dass der Workbox-Lauf sw.js bereits dort erzeugt hat"
    - "Cloudflare-Projektname spatenstich -> muss mit dem in M1 angelegten Projekt uebereinstimmen, sonst legt der Upload ein zweites Projekt an"
    - "Supabase Site URL (M3) -> ohne sie schlagen Auth-Redirects auf der pages.dev-Domain fehl"
    - "GitHub-Zeitplan pausiert nach 60 Tagen Inaktivitaet -> deshalb zusaetzlich der Task-Scheduler-Ping"
  prohibitions:
    - "Keine Cloudflare-Zugangsdaten, kein Datenbank-Passwort und kein Anon-Key im Repo — alles ueber Secrets bzw. den Windows Credential Manager (R7)."
    - "Kein kontoweites Cloudflare-Token; nur die Pages-Edit-Vorlage (T-20-04-02)."
    - "Kein Deploy-Versuch, bevor der Checkpoint fuer M1 bis M3 durchlaufen ist."
    - "Der Secret-Scan-Schritt darf nicht entfernt, uebersprungen oder hinter den Upload sortiert werden (R7)."
    - "Keine Dependency-Upgrades, keine neuen Laufzeit-Dependencies (D-13, R10)."
    - "Keine ASCII-Ersatzschreibungen in deutschen Doku-Texten (R2)."
    - "Kein automatischer nativer Build wieder einschalten (D-01)."
---

<objective>
WP 20.4 aus MASTERPLAN-v2.md Kap. 4: Der Weg von master nach `https://spatenstich.pages.dev` wird automatisch, und Supabase bleibt auf dem kostenlosen Plan wach — ueber zwei unabhaengige Wege (GitHub-Zeitplan und Task Scheduler auf dem 24/7-PC, D-02). Dazu das Backup-Skript, das Migration 021 in Phase 21 voraussetzt, und die Anleitungen im README, mit denen Dirk und seine Partnerin die App tatsaechlich auf die Handys bekommen.

Purpose: Ohne diesen Plan bleibt die PWA aus 20-03 ein lokales Build-Artefakt. Erst der Deploy macht die Geraete-Abnahme (M6) und damit die offenen Nachweise aus DEPLOY-03, DEPLOY-04 und DEPLOY-07 ueberhaupt moeglich.
Output: Zwei Workflows, zwei PowerShell-Skripte, README-Anleitungen — und ein erster echter Deploy nach ausdruecklicher Freigabe.

Reihenfolge mit Absicht: erst alles bauen und lokal bis zur Deploy-Grenze beweisen, dann der Checkpoint fuer die manuellen Schritte M1 bis M3, dann der echte Deploy mit den Geraete-Pruefungen.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/MASTERPLAN-v2.md
@.planning/phases/20-fundament-aufr-umen-pwa-deploy/20-CONTEXT.md
@.planning/phases/20-fundament-aufr-umen-pwa-deploy/20-RESEARCH.md
@.planning/phases/20-fundament-aufr-umen-pwa-deploy/20-PATTERNS.md
@.planning/phases/20-fundament-aufr-umen-pwa-deploy/20-VALIDATION.md
@.planning/phases/20-fundament-aufr-umen-pwa-deploy/20-03-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Deploy-Kette end-to-end bis zur Upload-Grenze</name>
  <files>.github/workflows/deploy-web.yml</files>
  <read_first>
    - .github/workflows/ci.yml (das Praeambel-Muster: checkout, pnpm-Setup mit fester Version, node-Setup mit pnpm-Cache, Install mit gefrorenem Lockfile — woertlich uebernehmen)
    - .github/workflows/bundle-secret-scan.yml (der wiederverwendbare Scan-Workflow; pruefen, ob er hier eingebunden werden kann oder der direkte Skriptaufruf sauberer ist)
    - scripts/check-claude-key-in-bundle.sh (was genau geprueft wird)
    - app/package.json (das build:web-Script aus Plan 20-03)
    - .planning/phases/20-fundament-aufr-umen-pwa-deploy/20-PATTERNS.md — Abschnitt ".github/workflows/deploy-web.yml"
    - .planning/phases/20-fundament-aufr-umen-pwa-deploy/20-RESEARCH.md — Standard Stack (wrangler-action), Assumption A5
  </read_first>
  <action>
    Duenne, vollstaendige Scheibe durch die Deploy-Kette: Auslöser -> Install -> Build -> Sicherheitsprüfung -> Upload. Alles ausser dem letzten Schritt wird in diesem Task lokal bewiesen; der Upload braucht die Zugangsdaten aus M1 und wird in Task 5 scharf geschaltet.

    .github/workflows/deploy-web.yml anlegen:
    - Ausloeser: Push auf master und zusaetzlich manuell per workflow_dispatch.
    - Praeambel woertlich aus ci.yml: checkout, pnpm-Setup mit derselben festen Version, node-Setup mit Version 22 und pnpm-Cache, Install mit gefrorenem Lockfile.
    - Ein env-Block auf Job-Ebene mit den beiden EXPO_PUBLIC_SUPABASE_-Werten aus den Repository-Variablen und dem Sentry-DSN aus den Secrets — dieselben Namen wie in ci.yml.
    - Build-Schritt: das build:web-Script des app-Pakets.
    - Sicherheitsschritt: scripts/check-claude-key-in-bundle.sh gegen app/dist. Dieser Schritt MUSS vor dem Upload stehen (R7).
    - Upload-Schritt: die Cloudflare-Wrangler-Action in der in CONTEXT festgelegten Hauptversion, mit dem Kommando fuer einen Pages-Deploy des Verzeichnisses app/dist unter dem Projektnamen spatenstich auf den Branch main, mit Token und Account-ID aus den Secrets.
    - Falls die festgelegte Hauptversion der Action zur Laufzeit nicht mehr funktioniert: die naechsthoehere Hauptversion nimmt dieselben Eingaben (RESEARCH A5) — ein solcher Wechsel ist eine bewusste Abweichung von der Festlegung und gehoert in die SUMMARY.

    Beweis der Kette bis zur Grenze: lokal genau die Kommandos ausfuehren, die der Workflow ausfuehrt — das build:web-Script und danach den Bundle-Scan gegen app/dist. Beide muessen mit exit 0 enden. Die Laufzeit des lokalen Builds notieren; sie ist der erste Anhaltspunkt fuer das 10-Minuten-Ziel aus DEPLOY-05.
  </action>
  <acceptance_criteria>
    - .github/workflows/deploy-web.yml existiert und ist gueltiges YAML mit on- und jobs-Block.
    - Der Trigger-Abschnitt nennt den Push auf master und workflow_dispatch.
    - Der Workflow nennt das build:web-Script, das Scan-Skript und die Cloudflare-Action.
    - Die Zeile mit dem Scan-Skript steht im Datei-Verlauf vor der Zeile mit der Cloudflare-Action.
    - Der Projektname spatenstich und das Upload-Verzeichnis app/dist stehen im Kommando der Action.
    - Zugangsdaten stehen ausschliesslich als Secret-Referenzen in der Datei, nie im Klartext.
    - Lokal enden `pnpm --filter app run build:web` und `bash scripts/check-claude-key-in-bundle.sh app/dist` mit exit 0.
  </acceptance_criteria>
  <verify>
    <automated>W=.github/workflows/deploy-web.yml; test -f "$W" && grep -qE '^on:' "$W" && grep -qE '^jobs:' "$W" && sed -n '/^on:/,/^jobs:/p' "$W" | grep -v '^[[:space:]]*#' | grep -q 'workflow_dispatch' && sed -n '/^on:/,/^jobs:/p' "$W" | grep -v '^[[:space:]]*#' | grep -q 'master'</automated>
    <fails_when>Die Datei fehlt, hat keinen on- oder jobs-Block, oder der Trigger-Abschnitt nennt master bzw. workflow_dispatch nicht</fails_when>
  </verify>
  <verify>
    <automated>awk '/check-claude-key-in-bundle\.sh/{s=NR} /wrangler-action/{w=NR} END{ if (s>0 && w>0 && s<w) exit 0; else exit 1 }' .github/workflows/deploy-web.yml</automated>
    <fails_when>Das Scan-Skript fehlt, die Cloudflare-Action fehlt, oder der Scan steht im Datei-Verlauf nach dem Upload — dann koennte ein Bundle mit einem Schluessel bereits veroeffentlicht sein, bevor der Scan anschlaegt</fails_when>
  </verify>
  <verify>
    <automated>grep -q 'run build:web' .github/workflows/deploy-web.yml && grep -q 'project-name spatenstich' .github/workflows/deploy-web.yml && grep -q 'app/dist' .github/workflows/deploy-web.yml</automated>
    <fails_when>Der Build-Aufruf, der Projektname spatenstich oder das Upload-Verzeichnis app/dist fehlt im Workflow</fails_when>
  </verify>
  <verify>
    <automated>pnpm --filter app run build:web && bash scripts/check-claude-key-in-bundle.sh app/dist</automated>
    <fails_when>non-zero exit — entweder der Build bricht ab oder der Scan findet ein Schluesselmuster im Bundle</fails_when>
  </verify>
  <reversibility rating="reversible">Eine neue Workflow-Datei; ohne Secrets laeuft sie ins Leere statt etwas zu veroeffentlichen.</reversibility>
  <done>Der Workflow existiert vollstaendig, und alle seine Schritte ausser dem Upload sind lokal gruen nachgewiesen.</done>
</task>

<task type="auto">
  <name>Task 2: Keep-alive-Workflow und die beiden PowerShell-Skripte</name>
  <files>.github/workflows/supabase-keepalive.yml, scripts/keepalive.ps1, scripts/backup-supabase.ps1</files>
  <read_first>
    - .planning/phases/20-fundament-aufr-umen-pwa-deploy/20-RESEARCH.md — Code Examples §3 (Keep-alive-Skelett), Pitfall 7 (Zeitplan pausiert nach 60 Tagen), Assumption A6 (Verbindungszeichenfolge)
    - .planning/MASTERPLAN-v2.md — Kap. 7.3 (Keep-alive) und Kap. 7.4 (Backup mit Rotation und Restore-Hinweis)
    - scripts/check-claude-key-in-bundle.sh (Kopfkommentar-Konvention fuer Skripte in diesem Verzeichnis)
    - .planning/STATE.md — Blocker (project-ref des Supabase-Projekts)
  </read_first>
  <action>
    1. .github/workflows/supabase-keepalive.yml anlegen: Zeitplan alle drei Tage um 6 Uhr plus workflow_dispatch, ein einzelner Job ohne Node-Toolchain, der mit curl still und fehlerhart die REST-Schnittstelle des Projekts auf die Pflanzentabelle abfragt (ein Feld, Limit 1) und dabei den Anon-Key sowohl als apikey-Header als auch als Bearer-Token mitschickt. Basis-URL aus der Repository-Variablen, Schluessel aus den Secrets. Eine leere Antwort ist ein Erfolg — es zaehlt nur, dass die Datenbank angesprochen wurde.

    2. scripts/keepalive.ps1 anlegen: dasselbe Abfrageverhalten fuer den Windows Task Scheduler auf dem 24/7-PC. URL und Anon-Key aus Umgebungsvariablen lesen, nicht im Skript hinterlegen. Bei fehlenden Werten mit klarer deutscher Meldung und Fehlercode enden. Erfolg und Fehlschlag mit Zeitstempel in eine Logdatei schreiben. Im Kopfkommentar die Registrierung im Task Scheduler beschreiben (taeglich).

    3. scripts/backup-supabase.ps1 anlegen: erzeugt mit pg_dump einen komprimierten Dump des public-Schemas ohne Eigentuemer-Anweisungen in ein konfigurierbares Zielverzeichnis, Dateiname mit Datum. Danach Rotation: Dumps aelter als 30 Tage loeschen. Das Datenbank-Passwort aus dem Windows Credential Manager lesen, niemals als Parameter im Klartext und niemals im Repo (R7). Fehlt pg_dump oder das Anmeldeobjekt, mit klarer deutscher Meldung abbrechen. Im Kopfkommentar den Restore-Weg und den Hinweis auf die Verbindungszeichenfolge des Poolers vermerken — RESEARCH A6 stuft deren genaue Form als ungeprueft ein, das Skript soll Host, Port und project-ref daher als Variablen am Dateianfang fuehren, damit eine Korrektur eine Zeile ist.

    4. Beide Skripte auf Syntax pruefen, ohne sie auszufuehren.
  </action>
  <acceptance_criteria>
    - .github/workflows/supabase-keepalive.yml enthaelt den Zeitplan-Ausdruck fuer alle drei Tage und workflow_dispatch.
    - Der Workflow ruft die REST-Schnittstelle mit apikey- und Bearer-Header auf.
    - Weder Anon-Key noch Datenbank-Passwort stehen im Klartext in einer der drei Dateien.
    - Beide PowerShell-Skripte sind syntaktisch gueltig.
    - scripts/backup-supabase.ps1 enthaelt eine Rotation ueber 30 Tage und liest das Passwort aus dem Credential Manager.
  </acceptance_criteria>
  <verify>
    <automated>W=.github/workflows/supabase-keepalive.yml; test -f "$W" && grep -q '0 6 \*/3 \* \*' "$W" && grep -q 'workflow_dispatch' "$W" && grep -q 'rest/v1/plants' "$W" && grep -qi 'apikey' "$W" && grep -qi 'Bearer' "$W"</automated>
    <fails_when>Die Datei fehlt, oder der Zeitplan-Ausdruck, workflow_dispatch, der REST-Pfad, der apikey-Header oder der Bearer-Header fehlt</fails_when>
  </verify>
  <verify>
    <automated>for f in scripts/keepalive.ps1 scripts/backup-supabase.ps1; do test -f "$f" || { echo "missing $f"; exit 1; }; powershell.exe -NoProfile -Command "\$e=\$null; [System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path '$f').Path,[ref]\$null,[ref]\$e) | Out-Null; if(\$e.Count -gt 0){ \$e | ForEach-Object { Write-Output \$_.Message }; exit 1 }" || { echo "syntax error in $f"; exit 1; }; done</automated>
    <fails_when>Eine Zeile "missing <datei>" oder "syntax error in <datei>" wird ausgegeben; ebenso wenn powershell.exe auf diesem Rechner nicht aufrufbar ist — dann ist die Syntaxpruefung nicht erbracht und muss vor dem Abschluss anders belegt werden</fails_when>
  </verify>
  <verify>
    <automated>! grep -rniE 'eyJ[A-Za-z0-9_-]{20,}|password[[:space:]]*=[[:space:]]*["'"'"'][^"'"'"']+' .github/workflows/supabase-keepalive.yml scripts/keepalive.ps1 scripts/backup-supabase.ps1</automated>
    <fails_when>Eine der drei Dateien enthaelt etwas, das wie ein JWT oder ein im Klartext zugewiesenes Passwort aussieht</fails_when>
  </verify>
  <verify>
    <automated>grep -qiE '30' scripts/backup-supabase.ps1 && grep -qiE 'CredentialManager|CredRead|Get-StoredCredential|cmdkey' scripts/backup-supabase.ps1</automated>
    <fails_when>Die Rotation ueber 30 Tage oder der Zugriff auf den Windows Credential Manager fehlt im Backup-Skript</fails_when>
  </verify>
  <reversibility rating="reversible">Neue Dateien; noch nicht im Task Scheduler registriert.</reversibility>
  <done>Beide Wege des Keep-alive sind angelegt und das Backup-Skript ist bereit fuer Schritt M5 vor Phase 21.</done>
</task>

<task type="auto">
  <name>Task 3: README-Anleitungen und CLAUDE.md-Betriebshinweis</name>
  <files>README.md, CLAUDE.md</files>
  <read_first>
    - README.md (aktueller Stand nach Plan 20-01)
    - CLAUDE.md (Stack-Abschnitt nach Plan 20-01)
    - .planning/MASTERPLAN-v2.md — Kap. 4 WP 20.4 (die drei README-Abschnitte), Kap. 5 (M6, M8), Kap. 7.1 bis 7.4 (Runbook)
    - .planning/phases/20-fundament-aufr-umen-pwa-deploy/20-RESEARCH.md — Pitfall 7
  </read_first>
  <action>
    Drei neue Abschnitte in README.md, auf Deutsch mit echten Umlauten, Du-Form, ohne Screenshots (Platzhalter genuegen):

    1. "Installation auf dem Android-Handy (Chrome)": fuenf Schritte von der URL bis zum Icon auf dem Startbildschirm, inklusive des Hinweises, dass das Installations-Banner in der App derselbe Weg ist, und dass man sich in der installierten App einmal anmelden muss.

    2. "Import aus der Claude-App per Teilen": was geteilt werden kann (eine .json-Datei oder ein kopierter Text), dass Spatenstich erst nach der Installation im Teilen-Menue auftaucht, und wo der Payload landet (Import-Vorschau).

    3. "Claude-Projekt einrichten": wie ein Claude-Projekt fuer die Analyse angelegt wird, wo Prompt und Schema in der App kopiert werden koennen, und der Hinweis, dass das sowohl mit einem bezahlten als auch mit einem kostenlosen Konto funktioniert (M8).

    Dazu einen kurzen Betriebsabschnitt: Deploy per Merge nach master, Rollback ueber das Cloudflare-Dashboard, und der ausdrueckliche Hinweis, dass GitHub den Zeitplan nach 60 Tagen ohne Repo-Aktivitaet pausiert — deshalb laeuft zusaetzlich der Task-Scheduler-Ping auf dem 24/7-PC (RESEARCH Pitfall 7). Backup-Hinweis auf scripts/backup-supabase.ps1 mit dem Verweis, dass es vor Migration 021 gebraucht wird.

    In CLAUDE.md den Betrieb in einem Satz ergaenzen: Deploy-Ziel, Hosting und Keep-alive — ohne die Stack-Tabelle aus Plan 20-01 erneut anzufassen.

    Keine Roadmap-Versprechen und keine Aussagen ueber noch nicht gebaute Funktionen aufnehmen.
  </action>
  <acceptance_criteria>
    - README.md enthaelt die drei Ueberschriften zu Handy-Installation, Teilen-Import und Claude-Projekt.
    - README.md nennt die Deploy-Adresse spatenstich.pages.dev.
    - README.md nennt die 60-Tage-Pause des GitHub-Zeitplans und den Task-Scheduler-Ping.
    - Kein Text in den neuen Abschnitten verwendet ASCII-Ersatzschreibungen fuer Umlaute.
    - CLAUDE.md nennt Cloudflare Pages als Deploy-Ziel.
  </acceptance_criteria>
  <verify>
    <automated>grep -qi 'Android' README.md && grep -qi 'Teilen' README.md && grep -qi 'Claude-Projekt' README.md && grep -q 'spatenstich.pages.dev' README.md && grep -qi '60 Tagen' README.md && grep -qi 'Cloudflare Pages' CLAUDE.md</automated>
    <fails_when>Einer der drei Abschnitte, die Deploy-Adresse, der 60-Tage-Hinweis oder die Cloudflare-Erwaehnung in CLAUDE.md fehlt</fails_when>
  </verify>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('README.md','utf8');if(!/[äöüß]/.test(s)){console.error('no real umlauts in README');process.exit(1)}"</automated>
    <fails_when>Das Skript endet non-zero mit "no real umlauts in README" — die neuen Abschnitte wurden dann mit ASCII-Ersatzschreibungen statt echter Umlaute geschrieben (R2)</fails_when>
  </verify>
  <reversibility rating="reversible">Reine Dokumentation.</reversibility>
  <done>Beide koennen die App anhand des README installieren und den Teilen-Weg benutzen; der Betrieb ist beschrieben.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 4: CHECKPOINT — manuelle Schritte M1 bis M3 vor dem ersten echten Deploy</name>
  <files>—</files>
  <read_first>
    - .planning/MASTERPLAN-v2.md — Kap. 5, Zeilen zu M1, M2 und M3
    - .github/workflows/deploy-web.yml (Ergebnis aus Task 1: welche Secrets und Variablen der Workflow erwartet)
    - .github/workflows/supabase-keepalive.yml (Ergebnis aus Task 2)
  </read_first>
  <action>
    Halte hier an. Der Workflow ist fertig, aber ohne die drei manuellen Schritte kann er nichts veroeffentlichen.

    Lege dem User die drei Punkte einzeln vor und lass sie einzeln bestaetigen:

    - M1: Cloudflare-Konto vorhanden, unter Workers und Pages ein Direct-Upload-Projekt mit dem Namen spatenstich angelegt, ein API-Token aus der Vorlage "Cloudflare Pages — Edit" erzeugt (ausdruecklich nicht kontoweit) und zusammen mit der Account-ID als GitHub-Secrets CLOUDFLARE_API_TOKEN und CLOUDFLARE_ACCOUNT_ID hinterlegt.
    - M2: GitHub-Repository-Variablen EXPO_PUBLIC_SUPABASE_URL und EXPO_PUBLIC_SUPABASE_ANON_KEY gesetzt sowie das Secret EXPO_PUBLIC_SENTRY_DSN mit dem EU-Ingest-Wert (falls in Plan 20-01 noch offen geblieben).
    - M3: Im Supabase-Projekt vitrqkzxkiqvadqfzrcx unter Authentication und URL Configuration sind Site URL und Redirect URLs auf https://spatenstich.pages.dev gesetzt, zusaetzlich http://localhost:8095 fuer die Entwicklung.

    Frage woertlich: "Sind M1 (Cloudflare-Projekt + Token/Account-ID als Secrets), M2 (GitHub-Variablen und Sentry-Secret) und M3 (Supabase Site- und Redirect-URLs) erledigt? Soll ich den ersten echten Deploy ausloesen? (ja / nein)"

    Bei "nein" oder unvollstaendigen Angaben: Task 5 NICHT ausfuehren. Den Plan mit dem committeten, aber nie scharf geschalteten Workflow beenden und in der SUMMARY festhalten, welche der drei Schritte offen sind und dass die Backstop-Nachweise fuer DEPLOY-03, DEPLOY-04, DEPLOY-05 und DEPLOY-07 damit ausstehen. Das ist ein gueltiges Planende.

    Diese Freigabe ist nicht automatisch erteilbar.
  </action>
  <acceptance_criteria>
    - Alle drei Punkte wurden einzeln vorgelegt und einzeln beantwortet.
    - Es liegt eine eindeutige Gesamtantwort ja oder nein vor.
    - Der Stand jedes einzelnen Schritts ist in der SUMMARY notiert.
    - Bei nein wurde Task 5 nicht ausgefuehrt.
  </acceptance_criteria>
  <instructions>
    1. M1 — Cloudflare: Konto anlegen bzw. oeffnen, unter "Workers & Pages" ein Projekt "Create project -> Direct Upload" mit dem Namen `spatenstich` anlegen. Unter "My Profile -> API Tokens" ein Token aus der Vorlage "Cloudflare Pages — Edit" erzeugen (ausdruecklich kein kontoweites Token). Token und die Account-ID aus der rechten Spalte von "Workers & Pages" als GitHub-Secrets `CLOUDFLARE_API_TOKEN` und `CLOUDFLARE_ACCOUNT_ID` hinterlegen.
    2. M2 — GitHub: unter Settings -> Secrets and variables -> Actions die Variablen `EXPO_PUBLIC_SUPABASE_URL` und `EXPO_PUBLIC_SUPABASE_ANON_KEY` setzen sowie das Secret `EXPO_PUBLIC_SENTRY_DSN` mit dem EU-Ingest-Wert (falls in Plan 20-01 offen geblieben).
    3. M3 — Supabase: im Projekt vitrqkzxkiqvadqfzrcx unter Authentication -> URL Configuration die Site URL und die Redirect URLs auf https://spatenstich.pages.dev setzen, zusaetzlich http://localhost:8095 fuer die Entwicklung.
    4. Antworten auf: "Sind M1 (Cloudflare-Projekt + Token/Account-ID als Secrets), M2 (GitHub-Variablen und Sentry-Secret) und M3 (Supabase Site- und Redirect-URLs) erledigt? Soll ich den ersten echten Deploy ausloesen? (ja / nein)"
  </instructions>
  <verification>
    Jeder der drei Punkte ist einzeln mit erledigt oder offen beantwortet, und es liegt eine eindeutige Gesamtantwort ja oder nein vor. Ein "ja" bei offenen Punkten gilt nicht als Freigabe.
  </verification>
  <reversibility rating="costly">Ein Fehl-Deploy laesst sich ueber das Cloudflare-Dashboard zuruecksetzen, ist aber oeffentlich sichtbar gewesen. Ein zu weit gefasstes API-Token muesste widerrufen und neu erzeugt werden.</reversibility>
  <resume-signal>Freigabe erteilt (weiter mit Task 5) oder verweigert (Plan endet hier; in der SUMMARY stehen die offenen Schritte und der Hinweis, dass die Backstop-Nachweise fuer DEPLOY-03, DEPLOY-04, DEPLOY-05 und DEPLOY-07 damit ausstehen — das ist ein gueltiges Planende).</resume-signal>
  <done>Freigabe erteilt (weiter mit Task 5) oder verweigert (Plan endet hier, offene Schritte dokumentiert).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 5: CHECKPOINT — erster echter Deploy, Keep-alive-Lauf und Geraete-Abnahme</name>
  <files>—</files>
  <read_first>
    - .planning/MASTERPLAN-v2.md — Kap. 5 (M6) und Kap. 6.1 (Abnahmepunkte 1 bis 3)
    - .planning/phases/20-fundament-aufr-umen-pwa-deploy/20-03-SUMMARY.md (was beim Service Worker und beim HTML-Template tatsaechlich gegriffen hat)
  </read_first>
  <precondition>Der User hat in Task 4 ausdruecklich "ja" geantwortet und M1 bis M3 als erledigt gemeldet. Ohne diese Freigabe wird dieser Task uebersprungen.</precondition>
  <action>
    Jetzt scharf schalten und gemeinsam abnehmen.

    1. Deploy ausloesen: den Workflow manuell per workflow_dispatch starten oder den Phasen-Branch als Draft-PR nach master mergen (PRs immer als Draft anlegen, R8). Laufzeit stoppen — DEPLOY-05 fordert unter 10 Minuten. Danach https://spatenstich.pages.dev im Desktop-Browser oeffnen und pruefen, dass die App laedt.

    2. Cache-Header stichprobenartig pruefen: die Antwortkopfzeilen fuer /sw.js und /index.html abrufen und bestaetigen, dass sie no-cache melden (RESEARCH Pitfall 3).

    3. Keep-alive: supabase-keepalive.yml einmal manuell starten und den erfolgreichen Abschluss bestaetigen.

    4. Geraete-Abnahme mit dem User (M6, Masterplan Kap. 6.1) — diese Punkte schliessen die offenen Nachweise aus Plan 20-03:
       - Chrome bietet auf beiden Android-Handys "App installieren" an; nach der Installation ist das Icon auf dem Startbildschirm korrekt, auch rund maskiert; der Start erfolgt ohne Browser-Leiste, die Statusleiste ist in Moos eingefaerbt.
       - Lighthouse meldet die App als installierbar (DevTools, PWA-Pruefung).
       - In der installierten App anmelden.
       - In der Claude-App einmal "Teilen" auf eine .json-Datei und einmal auf einen kopierten Text: Spatenstich erscheint im Teilen-Menue, die Import-Vorschau zeigt den Payload.
       - Flugmodus einschalten, App ein zweites Mal starten: die App-Shell erscheint mit dem letzten Plan.

    Jeden Punkt einzeln mit Pass oder Fail und einer Notiz festhalten. Befunde werden nicht in diesem Plan repariert — sie gehen als Quick-Tasks oder in die Geraete-Abnahme in Phase 25.

    Bleibt der Deploy ueber 10 Minuten oder scheitert die Cloudflare-Action: pruefen, ob die in CONTEXT festgelegte Hauptversion der Action das Problem ist (RESEARCH A5, identische Eingaben in der naechsthoeheren Version) und die Abweichung in der SUMMARY begruenden.
  </action>
  <acceptance_criteria>
    - Der Deploy-Lauf ist abgeschlossen; seine Dauer ist notiert und liegt unter 10 Minuten.
    - https://spatenstich.pages.dev ist erreichbar und laedt die App.
    - Die Antwortkopfzeilen fuer /sw.js und /index.html melden no-cache.
    - Ein manueller Lauf von supabase-keepalive.yml war erfolgreich.
    - Alle fuenf Geraete-Punkte aus M6 sind mit Pass oder Fail und Notiz erfasst.
    - Fails sind als Folgeaufgaben notiert, nicht stillschweigend uebergangen.
  </acceptance_criteria>
  <what-built>
    Gebaut sind: der Deploy-Workflow (Push auf master und manuell, Install -> build:web -> Bundle-Scan -> Upload nach Cloudflare Pages), der Keep-alive-Workflow (alle drei Tage und manuell), die beiden PowerShell-Skripte fuer Task Scheduler und Backup, sowie die README-Anleitungen. Aus Plan 20-03 liegen Manifest, Icons, HTML-Template, Service Worker mit Teilen-Handler, Installations-Banner, Update-Hinweis und die Cache-Regeln im Build-Artefakt.
  </what-built>
  <how-to-verify>
    1. Deploy ausloesen (workflow_dispatch oder Merge des Draft-PR nach master, R8) und die Laufzeit stoppen — DEPLOY-05 fordert unter 10 Minuten. Danach https://spatenstich.pages.dev im Desktop-Browser oeffnen: die App muss laden.
    2. Antwortkopfzeilen pruefen: `curl -I https://spatenstich.pages.dev/sw.js` und dasselbe fuer `/index.html` — beide muessen `no-cache` melden (RESEARCH Pitfall 3).
    3. supabase-keepalive.yml einmal per workflow_dispatch starten; der Lauf muss erfolgreich enden.
    4. Auf beiden Android-Handys in Chrome: Menue -> "App installieren". Danach pruefen: Icon auf dem Startbildschirm korrekt, auch rund maskiert; Start ohne Browser-Leiste; Statusleiste in Moos. In DevTools zusaetzlich die Lighthouse-PWA-Pruefung: "installable" muss gruen sein.
    5. In der installierten App anmelden. Dann in der Claude-App einmal "Teilen" auf eine .json-Datei und einmal auf einen kopierten Text: "Spatenstich" muss im Teilen-Menue erscheinen und die Import-Vorschau den Payload zeigen.
    6. Flugmodus einschalten und die App ein zweites Mal starten: die App-Shell mit dem letzten Plan muss erscheinen.
    Jeden Punkt einzeln mit Pass oder Fail und einer Notiz festhalten. Befunde werden nicht hier repariert, sondern als Quick-Task oder in Phase 25 aufgenommen.
  </how-to-verify>
  <reversibility rating="costly">Ein fehlerhafter Deploy ist ueber die Cloudflare-Deployments zurueckrollbar, war aber kurzzeitig oeffentlich.</reversibility>
  <resume-signal>Alle sechs Pruefschritte sind mit Pass oder Fail und Notiz protokolliert; Fails sind als Folgeaufgaben notiert. Danach ist Plan 20-04 und damit Phase 20 abgeschlossen.</resume-signal>
  <done>Die App ist live erreichbar, das Keep-alive laeuft, und die Geraete-Abnahme ist protokolliert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions -> Cloudflare-Konto | Der Workflow haelt ein Token, das Inhalte veroeffentlichen darf |
| Build-Artefakt app/dist -> oeffentliches Internet | Alles im Upload ist danach oeffentlich lesbar |
| Task Scheduler auf dem 24/7-PC -> Supabase REST | Ein Dauerlaeufer haelt einen Anon-Key in einer Umgebungsvariablen |
| Backup-Skript -> Live-Datenbank | pg_dump liest den gesamten public-Bereich mit einem Datenbank-Passwort |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-04-01 | Information Disclosure | Veroeffentlichtes Bundle mit einem Schluessel | high | mitigate | Der Scan mit scripts/check-claude-key-in-bundle.sh laeuft im Workflow zwingend vor dem Upload; ein Verify-Gate prueft genau diese Reihenfolge (R7). |
| T-20-04-02 | Elevation of Privilege | Zu weit gefasstes Cloudflare-Token | high | mitigate | Im Checkpoint wird ausdruecklich die Vorlage "Cloudflare Pages — Edit" verlangt, kein kontoweites Token (RESEARCH Security Domain). |
| T-20-04-03 | Information Disclosure | Datenbank-Passwort im Backup-Skript | high | mitigate | Das Passwort kommt aus dem Windows Credential Manager; ein Verify-Gate greppt die drei neuen Dateien auf JWT-artige Zeichenketten und Klartext-Passwortzuweisungen (R7). |
| T-20-04-04 | Denial of Service | Supabase pausiert das Projekt | medium | mitigate | Zwei unabhaengige Wege: GitHub-Zeitplan alle drei Tage und Task-Scheduler-Ping taeglich — genau weil der GitHub-Zeitplan nach 60 Tagen Repo-Inaktivitaet pausiert (RESEARCH Pitfall 7). |
| T-20-04-05 | Tampering | Veralteter Rand-Cache nach einem Deploy | high | mitigate | Die no-cache-Regeln aus Plan 20-03 werden nach dem ersten Deploy anhand der Antwortkopfzeilen von /sw.js und /index.html stichprobenartig geprueft. |
| T-20-04-06 | Spoofing | Auth-Redirect auf eine fremde Adresse | medium | mitigate | M3 legt Site URL und Redirect URLs auf die eigene Adresse fest; ohne diesen Schritt wird nicht deployt (Checkpoint Task 4). |
| T-20-04-SC | Tampering | npm/pnpm-Installationen | high | mitigate | Dieser Plan installiert keine Pakete. Die Cloudflare-Action ist eine GitHub-Action mit fester Hauptversion, kein npm-Paket; ein Wechsel der Hauptversion ist eine dokumentationspflichtige Abweichung. |
</threat_model>

## Artifacts this phase produces (Anteil dieses Plans)

| Artefakt | Art | Status nach diesem Plan |
|---|---|---|
| `.github/workflows/deploy-web.yml` | neu | fertig; scharf geschaltet nach dem Checkpoint |
| `.github/workflows/supabase-keepalive.yml` | neu | fertig |
| `scripts/keepalive.ps1` | neu | fertig (Registrierung im Task Scheduler durch den User) |
| `scripts/backup-supabase.ps1` | neu | fertig (wird fuer M5 vor Phase 21 gebraucht) |
| `README.md` — Abschnitte Handy-Installation, Teilen-Import, Claude-Projekt, Betrieb | erweitert | fertig |
| `CLAUDE.md` — Betriebshinweis Cloudflare Pages und Keep-alive | erweitert | fertig |

## Validation

Verifikationsstrategie und Task-Kommando-Matrix: `.planning/phases/20-fundament-aufr-umen-pwa-deploy/20-VALIDATION.md`
(Zeilen 20-04-01 bis 20-04-05). Die Deploy-Dauer, der Keep-alive-Lauf und die fuenf Geraete-Punkte aus M6
sind dort als manuelle Pruefungen gefuehrt; sie schliessen zugleich die offenen Backstops aus Plan 20-03.

<verification>
```bash
pnpm --filter app run build:web
bash scripts/check-claude-key-in-bundle.sh app/dist
awk '/check-claude-key-in-bundle\.sh/{s=NR} /wrangler-action/{w=NR} END{ exit !(s>0 && w>0 && s<w) }' .github/workflows/deploy-web.yml
grep -q '0 6 \*/3 \* \*' .github/workflows/supabase-keepalive.yml
```
Manuell nach dem Deploy: Erreichbarkeit von https://spatenstich.pages.dev, Antwortkopfzeilen fuer
/sw.js und /index.html, ein erfolgreicher Keep-alive-Lauf, die fuenf Geraete-Punkte aus M6.
</verification>

<success_criteria>
- Push auf master fuehrt in unter 10 Minuten zu einem erreichbaren Stand unter spatenstich.pages.dev (DEPLOY-05)
- Der Secret-Scan laeuft im Workflow vor dem Upload (DEPLOY-05, R7)
- eas-build.yml bleibt auf manuellem Auslöser (D-01, aus Plan 20-01)
- Keep-alive laeuft ueber GitHub-Zeitplan und Task Scheduler; das Backup-Skript existiert (DEPLOY-06)
- README erklaert Installation, Teilen-Import und Claude-Projekt (DEPLOY-05)
- Die Geraete-Abnahme nach M6 ist protokolliert und schliesst die offenen Nachweise aus Plan 20-03 (DEPLOY-03, DEPLOY-04, DEPLOY-07)
</success_criteria>

<output>
Create `.planning/phases/20-fundament-aufr-umen-pwa-deploy/20-04-SUMMARY.md` when done.
Darin festhalten: Stand von M1 bis M3, Dauer des ersten Deploys, Ergebnis der Header-Stichprobe,
Ergebnis des Keep-alive-Laufs, die fuenf Geraete-Punkte mit Pass/Fail und Notiz, und ob die
Hauptversion der Cloudflare-Action von der Festlegung abweichen musste.
</output>
