---
phase: 07-plan-editor-drafts-integration-m2-m07-5
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - app/package.json
  - app/jest.config.ts
  - app/src/components/editor/__tests__/setup.ts
  - app/src/lib/geometry/__tests__/viewMatrix.test.ts
  - app/src/lib/geometry/__tests__/plantSpacing.test.ts
  - app/src/lib/geometry/__tests__/bedLayout.test.ts
  - app/src/lib/editor/__tests__/editorSaveDebounce.test.ts
  - app/src/stores/__tests__/editorStore.transform.test.ts
  - app/src/stores/__tests__/editorStore.dragdrop.test.ts
  - app/src/stores/__tests__/editorStore.polygon.test.ts
  - app/src/stores/__tests__/editorStore.undoredo.test.ts
  - app/src/components/editor/__tests__/PlanEditor.smoke.test.tsx
  - app/src/components/editor/__tests__/ElementPalette.test.tsx
  - app/src/components/editor/__tests__/EditorToolbar.test.tsx
  - app/src/components/editor/__tests__/DraftsTray.test.tsx
  - app/src/lib/__tests__/rowMappers.layer.test.ts
  - packages/shared/src/i18n/de.json
autonomous: true
requirements: [EDIT-01, EDIT-02, EDIT-03, EDIT-04, EDIT-05, EDIT-06, EDIT-07, EDIT-08, EDIT-09, EDIT-11, EDIT-12, DRAFT-01, DRAFT-02, DRAFT-03]
tags: [tests, scaffold, jest, react-native, skia, dependencies]

must_haves:
  truths:
    - "All 15 Wave-0 test stub files exist with describe blocks + it.todo() shells matching the 14 Phase-7 requirements (EDIT-01..09, EDIT-11, EDIT-12, DRAFT-01..03)"
    - "Three new runtime deps installed and pinned in app/package.json: @shopify/react-native-skia@1.12.4, react-native-gesture-handler@2.31.2, zundo@2.3.0"
    - "Jest config has a new `editor` project with transformIgnorePatterns covering skia/gesture-handler/reanimated/zundo, mapped to the new editor test directories"
    - "Editor jest setup file mocks Skia (Canvas/Group/Rect/Path/Circle/Line/Skia), gesture-handler (Gesture/GestureDetector), and reanimated (useSharedValue/runOnJS) — alongside the existing NativeWind + lucide mocks"
    - "de.json contains an empty placeholder `editor: {}` block so future plans can fill keys without merge conflicts (concrete strings finalized in Wave 4)"
    - "Quick command `pnpm --filter app exec jest --testPathPattern='editor|geometry|stale-badge|rowMappers.layer'` exits 0 with all todos visible"
  artifacts:
    - path: "app/package.json"
      provides: "Pinned runtime deps for Phase 7"
      contains: "\"@shopify/react-native-skia\": \"1.12.4\""
    - path: "app/jest.config.ts"
      provides: "Editor jest project block"
      contains: "displayName: 'editor'"
    - path: "app/src/components/editor/__tests__/setup.ts"
      provides: "Editor-only jest setup with Skia + gesture mocks"
      contains: "jest.mock('@shopify/react-native-skia'"
    - path: "app/src/lib/geometry/__tests__/viewMatrix.test.ts"
      provides: "EDIT-06 stub"
      contains: "describe('geometry.viewMatrix"
    - path: "app/src/stores/__tests__/editorStore.undoredo.test.ts"
      provides: "EDIT-11 stub"
      contains: "20-step"
    - path: "app/src/components/editor/__tests__/DraftsTray.test.tsx"
      provides: "DRAFT-01..03 stubs"
      contains: "describe('DraftsTrayBottomSheet"
  key_links:
    - from: "app/jest.config.ts"
      to: "app/src/components/editor/__tests__/setup.ts (this plan)"
      via: "setupFiles entry on editor project"
      pattern: "src/components/editor/__tests__/setup"
    - from: "app/src/components/editor/__tests__/DraftsTray.test.tsx"
      to: "app/src/components/editor/DraftsTrayBottomSheet.tsx (Plan 04)"
      via: "import target — file does not yet exist, stubs use it.todo() only"
      pattern: "DraftsTrayBottomSheet"
---

<objective>
Scaffold the Wave-0 test contract for Phase 7 — 15 test stub files covering every Phase-7 requirement (EDIT-01..09, EDIT-11, EDIT-12, DRAFT-01..03), install the three pinned runtime deps (Skia 1.12.4, gesture-handler 2.31.2, zundo 2.3.0), wire a new `editor` jest project + setup file with Skia/gesture mocks, and seed an empty `editor.*` i18n block. Mirrors the 6.5 Plan 01 cadence (TDD-RED), establishing the Nyquist verification surface BEFORE any production code lands.

Purpose: Lock the verification contract before implementation drifts. Without these scaffolds Plans 02-05 would invent ad-hoc tests, breaking Nyquist sampling continuity (90s feedback budget per 07-VALIDATION.md). Pinning Skia 1.12.4 here prevents `expo install` from pulling v2 in any downstream plan (Pitfall-1).

Output: 15 test stub files + jest config extension + setup file + pinned dependencies + empty i18n placeholder. Quick command `pnpm --filter app exec jest --testPathPattern='editor|geometry|stale-badge|rowMappers.layer'` exits 0 with todos visible.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-CONTEXT.md
@.planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-RESEARCH.md
@.planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-PATTERNS.md
@.planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-VALIDATION.md
@.planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-UI-SPEC.md
@.planning/phases/06.5-draft-sichtung-promotion/06.5-01-wave0-test-scaffold-PLAN.md

@app/jest.config.ts
@app/src/components/__tests__/setup.ts
@packages/shared/src/i18n/de.json

<interfaces>
<!-- Production targets that Plans 02-05 will create. Stubs here use it.todo() only — they do NOT import production targets yet (those would fail TS compile). -->

Stubs reference these signatures only conceptually (it.todo descriptions):

From app/src/lib/geometry/viewMatrix.ts (Plan 02):
  export interface ViewMatrix { tx: number; ty: number; scale: number }
  export function mToPx(m: number, scale: number): number
  export function pxToM(px: number, scale: number): number
  export function screenToGarden(xPx: number, yPx: number, vm: ViewMatrix): { xM: number; yM: number }
  export function gardenToScreen(xM: number, yM: number, vm: ViewMatrix): { xPx: number; yPx: number }

From app/src/lib/geometry/plantSpacing.ts (Plan 02):
  export function hasOverlap(placed, spacingM, others): boolean

From app/src/lib/geometry/bedLayout.ts (Plan 02):
  export function polygonToBbox(points): { xM, yM, widthM, heightM }

From app/src/lib/editor/saveDebounce.ts (Plan 02):
  export function scheduleSaveElement(mode, el): void
  export function flushAllPendingSaves(mode, byId): Promise<void>

From app/src/stores/editorStore.ts (Plan 02):
  export const useEditorStore = create()(temporal(...))

From app/src/components/editor/* (Plans 03+04):
  EditorCanvas, EditorToolbar, ElementPalette, DraftsTrayBottomSheet, etc.

From app/src/lib/mappers/rowMappers.ts (Plan 01-Wave1, this phase Plan 02):
  PlanElementRow now carries `layer: 'infrastructure' | 'seasonal'`.
</interfaces>

<test-framework-notes>
- 5 existing jest projects (app/jest.config.ts): node, hooks, stores, photos, components. This plan adds a 6th project `editor`.
- Module aliases already wired: `@spatenstich/shared` → `packages/shared/src`, `@/src/*` → `app/src/*`, `@/(.*)` → `app/$1`.
- Existing component test setup (`app/src/components/__tests__/setup.ts`) mocks NativeWind + lucide globally — the new editor setup file MUST mirror those two mocks AND add Skia + gesture-handler + reanimated mocks (per 07-PATTERNS.md §setup.ts spec lines 1114-1188).
- pnpm `--` forwarding bug (Phase 6.5 P01 lesson): use `pnpm --filter app exec jest --testPathPattern=…` NOT `pnpm --filter app test -- --testPathPattern=…` (pnpm v9 swallows the second `--`).
- Use `it.todo('description')` for all stubs — no body required, less brittle than `it.skip()`.
- Stubs MUST NOT import production modules (would fail TS compile until Plan 02 creates them). `it.todo()` does not require imports.
- Test file naming convention: matches existing 6.5 P01 cadence — `*.test.ts` (node) or `*.test.tsx` (jsdom). Filenames must contain the requirement keyword (editorStore, editorSaveDebounce, geometry, ElementPalette, DraftsTray, stale-badge, rowMappers.layer, PlanEditor.smoke) so `--testPathPattern` filters work.
</test-framework-notes>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install pinned runtime deps + extend jest config + create editor setup file + empty i18n placeholder</name>
  <files>
    app/package.json,
    app/jest.config.ts,
    app/src/components/editor/__tests__/setup.ts,
    packages/shared/src/i18n/de.json
  </files>
  <read_first>
    - app/package.json (existing deps — verify react@18.3.1, react-native@0.76.7, reanimated@3.17.4, zustand@5.0.2 per 07-RESEARCH.md §Standard Stack)
    - app/jest.config.ts (existing 5 projects — lines 81-105 components block is the analog for new editor block)
    - app/src/components/__tests__/setup.ts (existing NativeWind + lucide mocks; mirror verbatim into new editor setup file then ADD Skia + gesture-handler + reanimated mocks)
    - .planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-RESEARCH.md §Standard Stack (lines 110-157: exact versions + verification notes) and §Validation Architecture (lines 1100-1122: Skia mock contract)
    - .planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-PATTERNS.md §"app/jest.config.ts (MOD — add `editor` jest project)" lines 1073-1108 and §"app/src/components/editor/__tests__/setup.ts (NEW — Skia + gesture mocks)" lines 1114-1188
    - packages/shared/src/i18n/de.json (find insertion point — after `import` block closes at line ~227, before next top-level block — add a top-level `editor: {}` empty placeholder)
  </read_first>
  <action>
    **Edit 1 — app/package.json: pin exact versions (per RESEARCH §Standard Stack):**

    Add to `dependencies` (use `pnpm --filter app add` with EXACT versions — DO NOT use `expo install` (Pitfall-1: it would resolve Skia v2)):

    ```bash
    pnpm --filter app add @shopify/react-native-skia@1.12.4
    pnpm --filter app add react-native-gesture-handler@2.31.2
    pnpm --filter app add zundo@2.3.0
    ```

    After install, verify the entries in `app/package.json` `dependencies` read literally:
    ```json
    "@shopify/react-native-skia": "1.12.4",
    "react-native-gesture-handler": "2.31.2",
    "zundo": "2.3.0"
    ```
    (NO `^` or `~` prefix — exact pins; document via inline comment if package.json supports JSON5/JSONC, otherwise note in commit message.)

    **Edit 2 — app/jest.config.ts: add `editor` project block.**

    Insert AFTER the existing `components` block (currently lines 81-105) and BEFORE the closing `]` of the `projects` array (currently line ~106). Use the exact block from 07-PATTERNS.md §1073-1108:

    ```typescript
    {
      // Phase 7 Plan 01: Editor component + geometry + store tests.
      displayName: 'editor',
      preset: 'ts-jest',
      testEnvironment: 'jsdom',
      testMatch: [
        '**/src/components/editor/__tests__/**/*.test.ts?(x)',
        '**/src/lib/geometry/__tests__/**/*.test.ts?(x)',
        '**/src/lib/editor/__tests__/**/*.test.ts?(x)',
        '**/src/stores/__tests__/editorStore.*.test.ts?(x)',
      ],
      setupFiles: ['<rootDir>/src/components/editor/__tests__/setup.ts'],
      moduleNameMapper: {
        '^@spatenstich/shared$': '<rootDir>/../packages/shared/src/index.ts',
        '^@spatenstich/shared/i18n/de$': '<rootDir>/../packages/shared/src/i18n/de.json',
        '^react-native-url-polyfill/auto$': '<rootDir>/src/__mocks__/react-native-url-polyfill.ts',
        '^react-native$': '<rootDir>/src/__mocks__/react-native.ts',
        '^react-native-css-interop(.*)$': '<rootDir>/src/__mocks__/react-native-css-interop.ts',
        '^nativewind(.*)$': '<rootDir>/src/__mocks__/react-native-css-interop.ts',
        '^expo-secure-store$': '<rootDir>/src/__mocks__/expo-secure-store.ts',
        '^@react-native-async-storage/async-storage$': '<rootDir>/src/__mocks__/async-storage.ts',
        '^@/src/(.*)$': '<rootDir>/src/$1',
        '^@/(.*)$': '<rootDir>/$1',
      },
      transform: {
        '^.+\\.tsx?$': ['ts-jest', { tsconfig: 'tsconfig.jest-components.json' }],
      },
      transformIgnorePatterns: ['node_modules/(?!(@testing-library|@shopify/react-native-skia|react-native-gesture-handler|react-native-reanimated|zundo)/)'],
    },
    ```

    **Edit 3 — create app/src/components/editor/__tests__/setup.ts** (NEW directory + file):

    Copy verbatim from 07-PATTERNS.md §1114-1188 (the 5-section mock: NativeWind, lucide, Skia, gesture-handler, reanimated). Key sections:

    ```typescript
    // Phase 7 Plan 01 Wave 0: jest setup for editor project.
    // Mocks Skia/gesture-handler/reanimated for jsdom env (no native modules).
    // Mirrors app/src/components/__tests__/setup.ts NativeWind + lucide mocks verbatim, adds 3 native libs.

    // 1. NativeWind (verbatim from components/__tests__/setup.ts:3-16)
    jest.mock('react-native-css-interop', () => ({
      cssInterop: (component: any) => component,
      remapProps: () => {},
      useColorScheme: () => ({ colorScheme: 'light', setColorScheme: () => {}, toggleColorScheme: () => {} }),
      useUnstableNativeVariable: () => '',
      vars: () => ({}),
    }));
    jest.mock('react-native-css-interop/jsx-runtime', () => ({
      jsx: require('react').createElement,
      jsxs: require('react').createElement,
      Fragment: require('react').Fragment,
    }));

    // 2. lucide (verbatim from components/__tests__/setup.ts:23-32)
    jest.mock('lucide-react-native', () => {
      const React = require('react');
      return new Proxy({}, { get: () => (_p: unknown) => React.createElement('Icon', null) });
    });

    // 3. NEW: Skia — return primitive components for Canvas/Group/Rect/Path/Circle/Line/Skia
    jest.mock('@shopify/react-native-skia', () => {
      const React = require('react');
      const stub = (name: string) => (props: any) => React.createElement(name, props, props.children);
      return {
        Canvas: stub('Canvas'),
        Group: stub('Group'),
        Rect: stub('Rect'),
        Path: stub('Path'),
        Circle: stub('Circle'),
        Line: stub('Line'),
        Skia: { Path: { Make: () => ({ moveTo: jest.fn(), lineTo: jest.fn(), close: jest.fn() }) } },
      };
    });

    // 4. NEW: gesture-handler — passthrough Gesture.* + GestureDetector passes children
    jest.mock('react-native-gesture-handler', () => {
      const React = require('react');
      const mkGesture = () => {
        const g: any = {
          onUpdate: () => g, onStart: () => g, onEnd: () => g, onTouchesMove: () => g,
          minDuration: () => g, manualActivation: () => g,
        };
        return g;
      };
      return {
        Gesture: {
          Pan: mkGesture, Pinch: mkGesture, Tap: mkGesture, LongPress: mkGesture, Rotation: mkGesture,
          Race: (..._args: any[]) => mkGesture(),
          Exclusive: (..._args: any[]) => mkGesture(),
          Simultaneous: (..._args: any[]) => mkGesture(),
        },
        GestureDetector: ({ children }: any) => children,
        GestureHandlerRootView: ({ children }: any) => children,
      };
    });

    // 5. NEW: reanimated — useSharedValue returns plain ref-like; runOnJS calls inline
    jest.mock('react-native-reanimated', () => ({
      useSharedValue: (v: any) => ({ value: v }),
      useDerivedValue: (fn: any) => ({ value: fn() }),
      runOnJS: (fn: any) => fn,
      useFrameCallback: () => {},
    }));
    ```

    **Edit 4 — packages/shared/src/i18n/de.json: seed empty `editor: {}` block.**

    Locate the closing `}` of the `import` top-level block (around line 227). Add a new top-level key AFTER `import` and BEFORE the next top-level block:
    ```json
    "editor": {
      "_placeholder": "Phase 7 Plan 04 fills concrete strings per 07-UI-SPEC §Copywriting Contract"
    },
    ```

    Single placeholder key is intentional — preserves valid JSON, signals that Plan 04 owns the actual content. Plan 04 deletes `_placeholder` when it adds real keys. UTF-8 Umlaute mandatory in Plan 04 (`ä/ö/ü/ß`), but the placeholder string has none, so it's safe to write in ASCII here.
  </action>
  <verify>
    <automated>cd /d/AiProjects/garden-app && grep -E '"@shopify/react-native-skia": "1.12.4"|"react-native-gesture-handler": "2.31.2"|"zundo": "2.3.0"' app/package.json | wc -l | grep -q "^3$" && grep -q "displayName: 'editor'" app/jest.config.ts && grep -q "jest.mock('@shopify/react-native-skia'" app/src/components/editor/__tests__/setup.ts && grep -q '"editor"' packages/shared/src/i18n/de.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - app/package.json contains literal `"@shopify/react-native-skia": "1.12.4"` (exact pin, no caret/tilde)
    - app/package.json contains literal `"react-native-gesture-handler": "2.31.2"`
    - app/package.json contains literal `"zundo": "2.3.0"`
    - app/jest.config.ts contains literal `displayName: 'editor'`
    - app/jest.config.ts contains literal `@shopify/react-native-skia|react-native-gesture-handler` inside transformIgnorePatterns (single regex line)
    - app/src/components/editor/__tests__/setup.ts exists and contains all 5 sections (search for: `react-native-css-interop`, `lucide-react-native`, `@shopify/react-native-skia`, `react-native-gesture-handler`, `react-native-reanimated`)
    - packages/shared/src/i18n/de.json contains a top-level `"editor"` key and is valid JSON (verify with `node -e "JSON.parse(require('fs').readFileSync('packages/shared/src/i18n/de.json','utf8'))"`)
    - Existing 5 jest projects (node, hooks, stores, photos, components) still listed in app/jest.config.ts unchanged
  </acceptance_criteria>
  <done>Deps pinned, jest editor project added, setup file with 5 mocks created, i18n placeholder seeded; valid JSON, valid TS, `pnpm --filter app typecheck` still green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Scaffold 4 pure-helper test stub files (geometry × 3 + editorSaveDebounce × 1)</name>
  <files>
    app/src/lib/geometry/__tests__/viewMatrix.test.ts,
    app/src/lib/geometry/__tests__/plantSpacing.test.ts,
    app/src/lib/geometry/__tests__/bedLayout.test.ts,
    app/src/lib/editor/__tests__/editorSaveDebounce.test.ts
  </files>
  <read_first>
    - .planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-VALIDATION.md (lines 41-86 — per-task verification map + Wave-0 file list)
    - .planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-RESEARCH.md §Validation Architecture lines 1071-1122 (test mapping per requirement)
    - .planning/phases/06.5-draft-sichtung-promotion/06.5-01-wave0-test-scaffold-PLAN.md (canonical it.todo() shell pattern from previous phase)
    - app/src/lib/__tests__/draftPromotionRepo.layout.test.ts (pure-helper unit test analog from 6.5 P03 — Pattern H)
  </read_first>
  <action>
    Create 4 NEW test files using `it.todo()` shells. NO production imports — those would fail TS compile until Plan 02 creates the modules. Each file must run cleanly through jest's `editor` project (added in Task 1).

    **File 1 — app/src/lib/geometry/__tests__/viewMatrix.test.ts (EDIT-06):**

    ```typescript
    // Phase 7 Plan 01 Wave 0: pure-geometry test scaffold for viewMatrix (EDIT-06).
    // Pattern: 6.5 P01 — it.todo() shells; no production imports until Plan 02 creates the module.
    // Pitfall-4: round-trip symmetry within 1e-9, no rounding at storage layer.

    describe('geometry.viewMatrix > mToPx / pxToM', () => {
      it.todo('mToPx is symmetric inverse of pxToM at scale=1');
      it.todo('mToPx multiplies by scale (m=2, scale=10 → 20)');
      it.todo('pxToM divides by scale (px=200, scale=10 → 20)');
    });

    describe('geometry.viewMatrix > screenToGarden / gardenToScreen round-trip (Pitfall-4)', () => {
      it.todo('round-trip screenToGarden(gardenToScreen(x, y, vm), vm) === {x, y} within 1e-9 for vm={tx:0, ty:0, scale:1}');
      it.todo('round-trip is symmetric for randomized vm with non-zero translation (tx=100, ty=50, scale=2.5)');
      it.todo('round-trip preserves negative coordinates (xM=-3, yM=-1)');
      it.todo('does NOT round at any step (no Math.round, Math.floor, Math.trunc)');
    });
    ```

    **File 2 — app/src/lib/geometry/__tests__/plantSpacing.test.ts (EDIT-07):**

    ```typescript
    // Phase 7 Plan 01 Wave 0: plant-spacing overlap test scaffold (EDIT-07).
    // Per RESEARCH §Pattern 5 + Code Examples §5: pure Euclidean distance; seasonal layer only.

    describe('geometry.plantSpacing > hasOverlap', () => {
      it.todo('returns true when a seasonal-layer neighbour is within spacingM (dx² + dy² < spacingM²)');
      it.todo('returns false when nearest seasonal neighbour is at distance >= spacingM');
      it.todo('ignores neighbours on infrastructure layer (only seasonal counted)');
      it.todo('ignores deletedAt !== null neighbours');
      it.todo('ignores placed.id from the others list (no self-overlap)');
      it.todo('returns false on empty others array');
      it.todo('handles spacingM = 0 (no overlap reported even at coincident coords — degenerate boundary)');
    });
    ```

    **File 3 — app/src/lib/geometry/__tests__/bedLayout.test.ts (EDIT-05):**

    ```typescript
    // Phase 7 Plan 01 Wave 0: polygon-to-bbox test scaffold (EDIT-05).
    // Per RESEARCH §Code Examples §4: ≥3 points required, centroid = geometric center of bbox.

    describe('geometry.bedLayout > polygonToBbox', () => {
      it.todo('throws when points.length < 3 with message "polygon needs at least 3 points"');
      it.todo('triangle (0,0)/(2,0)/(1,2) returns xM=1, yM=1, widthM=2, heightM=2');
      it.todo('square (0,0)/(2,0)/(2,2)/(0,2) returns xM=1, yM=1, widthM=2, heightM=2');
      it.todo('irregular pentagon: xM/yM = geometric center of axis-aligned bbox (not polygon centroid — MVP approximation)');
      it.todo('handles negative coordinates: points (-1,-1)/(1,-1)/(0,1) returns xM=0, yM=0, widthM=2, heightM=2');
    });
    ```

    **File 4 — app/src/lib/editor/__tests__/editorSaveDebounce.test.ts (EDIT-09):**

    ```typescript
    // Phase 7 Plan 01 Wave 0: editor 5s autosave debounce test scaffold (EDIT-09).
    // Per RESEARCH §Pattern 7 + Code Examples §5: per-element Map<id, Timeout>, manual flushAll cancels.
    // Pitfall-5: drag-in-flight bypass (interim coords NOT persisted during gesture).

    describe('editorSaveDebounce > scheduleSaveElement', () => {
      it.todo('does NOT fire writePlanElement before 5000ms');
      it.todo('fires writePlanElement exactly once after 5000ms with the latest element value');
      it.todo('rapid edits to same element id reset the timer (only one write after 5s of quiet)');
      it.todo('concurrent edits to different element ids each schedule independent timers');
      it.todo('throws when writePlanElement rejects: error is logged in __DEV__ but does not crash');
    });

    describe('editorSaveDebounce > flushAllPendingSaves (manual save)', () => {
      it.todo('cancels all pending timers and writes immediately for each scheduled element id');
      it.todo('uses byId callback to fetch current element snapshot (not the stale one from when timer was scheduled)');
      it.todo('returns resolved Promise even when no timers are pending');
    });

    describe('editorSaveDebounce > _resetEditorSaveTimers (test-only)', () => {
      it.todo('clears all pending timers and the internal Map (used for test isolation)');
    });
    ```

    All 4 files use ONLY `it.todo('description')` — no `describe(...)` body imports, no `expect(...)` calls, no production module imports. Plans 02 fill the assertions.
  </action>
  <verify>
    <automated>cd /d/AiProjects/garden-app && pnpm --filter app exec jest --testPathPattern='geometry|editorSaveDebounce' --selectProjects editor 2>&1 | grep -E '(Tests:|todo)' | head -10</automated>
  </verify>
  <acceptance_criteria>
    - File app/src/lib/geometry/__tests__/viewMatrix.test.ts exists and contains `describe('geometry.viewMatrix`
    - File app/src/lib/geometry/__tests__/viewMatrix.test.ts contains the literal `Pitfall-4`
    - File app/src/lib/geometry/__tests__/plantSpacing.test.ts exists and contains `seasonal-layer neighbour is within spacingM`
    - File app/src/lib/geometry/__tests__/bedLayout.test.ts exists and contains `throws when points.length < 3`
    - File app/src/lib/editor/__tests__/editorSaveDebounce.test.ts exists and contains both `scheduleSaveElement` and `flushAllPendingSaves` describe blocks
    - All 4 files use `it.todo(` (no `it.skip` or real `it()` bodies)
    - Running `pnpm --filter app exec jest --testPathPattern='geometry|editorSaveDebounce' --selectProjects editor` exits 0, reports >=20 todo entries
  </acceptance_criteria>
  <done>4 pure-helper test stub files committed; editor jest project runs them and reports todos; no syntax errors.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Scaffold 4 editorStore test stub files (transform, dragdrop, polygon, undoredo)</name>
  <files>
    app/src/stores/__tests__/editorStore.transform.test.ts,
    app/src/stores/__tests__/editorStore.dragdrop.test.ts,
    app/src/stores/__tests__/editorStore.polygon.test.ts,
    app/src/stores/__tests__/editorStore.undoredo.test.ts
  </files>
  <read_first>
    - .planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-VALIDATION.md (lines 41-86 — EDIT-03 / EDIT-04 / EDIT-05 / EDIT-11 test rows)
    - .planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-RESEARCH.md §Pattern 8 (zundo + partialize + limit, lines 446-494) and §Pitfall 3 + Pitfall 5
    - app/src/stores/__tests__/settingsStore.test.ts (existing Zustand store test analog from 07-PATTERNS.md §Wave-0 row)
  </read_first>
  <action>
    Create 4 NEW editorStore test stub files using `it.todo()` shells. Naming convention `editorStore.<feature>.test.ts` matches the jest project's testMatch glob (`editorStore.*.test.ts`).

    **File 1 — app/src/stores/__tests__/editorStore.transform.test.ts (EDIT-04):**

    ```typescript
    // Phase 7 Plan 01 Wave 0: editorStore rotation + scale action test scaffold (EDIT-04).
    // Per CONTEXT D-04: rotation stored as provenance.rotateDeg (no schema column — Open Q 3, A11).

    describe('editorStore > rotation (EDIT-04)', () => {
      it.todo('updateElement with patch.provenance.rotateDeg writes the new angle to that element');
      it.todo('rotation persists 0..359 degrees; values >=360 are normalized via modulo');
      it.todo('rotation snapshot is captured by zundo (undoable)');
    });

    describe('editorStore > scale (EDIT-04)', () => {
      it.todo('updateElement with patch.widthM/heightM writes new dimensions');
      it.todo('scale preserves provenance.rotateDeg if previously set');
      it.todo('scale snapshot is captured by zundo (undoable)');
    });
    ```

    **File 2 — app/src/stores/__tests__/editorStore.dragdrop.test.ts (EDIT-03 + Pitfall-5):**

    ```typescript
    // Phase 7 Plan 01 Wave 0: editorStore drag-drop test scaffold (EDIT-03, Pitfall-5).
    // Per RESEARCH §Pattern 3 + Pitfall 5: drop-only commit (no interim coord persistence during gesture).

    describe('editorStore > addElement (drop commit)', () => {
      it.todo('addElement appends a new PlanElementRow with kind="Beet" and the dropped coords');
      it.todo('addElement sets layer="infrastructure" for non-plant kinds');
      it.todo('addElement sets layer="seasonal" for kind="Pflanze"');
      it.todo('addElement sets provenance={ source: "manual" } for editor-created elements');
      it.todo('addElement sets isAccepted=true and confidence=null (manual elements have no AI confidence)');
    });

    describe('editorStore > updateElement (drag-in-progress vs commit) — Pitfall-5', () => {
      it.todo('updateElement during active gesture flag does NOT write to outbox (only store-level mutation)');
      it.todo('updateElement on gesture end (gestureActive=false) commits to store AND schedules a save');
      it.todo('updateElement preserves all fields not in the patch');
    });

    describe('editorStore > deleteElement (soft-delete)', () => {
      it.todo('deleteElement sets deletedAt timestamp; element is NOT removed from the array');
      it.todo('deleteElement leaves other elements untouched');
      it.todo('deleted elements no longer appear in selection candidates');
    });
    ```

    **File 3 — app/src/stores/__tests__/editorStore.polygon.test.ts (EDIT-05):**

    ```typescript
    // Phase 7 Plan 01 Wave 0: editorStore polygon-tool test scaffold (EDIT-05).
    // Per CONTEXT D-09: tap-corner mode + explicit "Beet abschließen" commit, mind. 3 points.

    describe('editorStore > polygonAddPoint', () => {
      it.todo('first tap initializes polygonInProgress with one point');
      it.todo('subsequent taps append points to polygonInProgress.pointsM');
      it.todo('polygonAddPoint does NOT commit to elements (only on polygonCommit)');
    });

    describe('editorStore > polygonCommit', () => {
      it.todo('throws when polygonInProgress is null');
      it.todo('throws when polygonInProgress.pointsM.length < 3 with message "polygon needs at least 3 points"');
      it.todo('commits a new Beet element with xM/yM = bbox centroid (via geometry.bedLayout.polygonToBbox)');
      it.todo('commits with widthM/heightM = bbox width/height');
      it.todo('commits with provenance.polygonPointsM = original tap coords (preserve polygon shape)');
      it.todo('commits with layer="infrastructure" (Beete are infrastructure)');
      it.todo('clears polygonInProgress and sets tool back to "select" after commit');
    });

    describe('editorStore > polygonCancel', () => {
      it.todo('clears polygonInProgress without committing');
      it.todo('sets tool back to "select"');
    });
    ```

    **File 4 — app/src/stores/__tests__/editorStore.undoredo.test.ts (EDIT-11 + Pitfall-3):**

    ```typescript
    // Phase 7 Plan 01 Wave 0: editorStore zundo temporal middleware test scaffold (EDIT-11, Pitfall-3).
    // Per RESEARCH §Pattern 8 + Pitfall 3: limit=20, partialize={elements}, equality shallow on a.elements===b.elements.

    describe('editorStore.temporal > undo/redo basics (EDIT-11)', () => {
      it.todo('addElement then undo() removes the element');
      it.todo('addElement then undo() then redo() restores the element');
      it.todo('undo when pastStates is empty is a no-op (does not throw)');
      it.todo('redo when futureStates is empty is a no-op (does not throw)');
    });

    describe('editorStore.temporal > 20-step limit (EDIT-11)', () => {
      it.todo('after 25 addElement mutations, pastStates.length === 20 (oldest 5 are dropped)');
      it.todo('after 25 mutations and 20 undos, restored state matches state after mutation 5 (not mutation 0)');
    });

    describe('editorStore.temporal > partialize excludes selection/viewport/tool (Pitfall-3)', () => {
      it.todo('changing selection does NOT add a snapshot to pastStates');
      it.todo('changing viewport (pan/zoom) does NOT add a snapshot');
      it.todo('changing tool does NOT add a snapshot');
      it.todo('snapshot equality function uses reference check (a.elements === b.elements) to dedup');
    });

    describe('editorStore.temporal > selection cleared on undo (Open Q 4)', () => {
      it.todo('after undo, selection is reset to null (avoid stale pointer to deleted element)');
      it.todo('after redo, selection stays null (user reselects explicitly)');
    });
    ```

    All `it.todo()` only — no production imports.
  </action>
  <verify>
    <automated>cd /d/AiProjects/garden-app && pnpm --filter app exec jest --testPathPattern='editorStore' --selectProjects editor 2>&1 | grep -E '(Tests:|todo)' | head -10</automated>
  </verify>
  <acceptance_criteria>
    - File app/src/stores/__tests__/editorStore.transform.test.ts exists and contains `provenance.rotateDeg`
    - File app/src/stores/__tests__/editorStore.dragdrop.test.ts exists and contains `Pitfall-5` and `gestureActive`
    - File app/src/stores/__tests__/editorStore.polygon.test.ts exists and contains `polygon needs at least 3 points`
    - File app/src/stores/__tests__/editorStore.undoredo.test.ts exists and contains `20-step limit` and `pastStates.length === 20`
    - All 4 files use `it.todo(` only
    - Running `pnpm --filter app exec jest --testPathPattern='editorStore' --selectProjects editor` exits 0 with >=25 todo entries
  </acceptance_criteria>
  <done>4 editorStore test stub files committed; editor jest project runs them and reports todos.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Scaffold 5 component test stub files + rowMappers.layer test (PlanEditor.smoke, ElementPalette, EditorToolbar, DraftsTray, rowMappers.layer)</name>
  <files>
    app/src/components/editor/__tests__/PlanEditor.smoke.test.tsx,
    app/src/components/editor/__tests__/ElementPalette.test.tsx,
    app/src/components/editor/__tests__/EditorToolbar.test.tsx,
    app/src/components/editor/__tests__/DraftsTray.test.tsx,
    app/src/lib/__tests__/rowMappers.layer.test.ts
  </files>
  <read_first>
    - .planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-VALIDATION.md (lines 41-86 — EDIT-01, EDIT-02, EDIT-08, DRAFT-01..03 test rows)
    - .planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-UI-SPEC.md (toolbar layout, palette structure, tray snap-points)
    - .planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-PATTERNS.md §"rowMappers.layer.test.ts" lines 1192-1221 and §"DraftsTray.test.tsx" lines 1226-1262
    - app/src/components/__tests__/ImportReview.test.tsx (component test mock skeleton analog — Pattern I)
  </read_first>
  <action>
    Create 5 NEW test stub files. The 4 component tests live in the NEW editor directory `app/src/components/editor/__tests__/` (jest editor project picks them up). The 1 mapper test lives in the existing `app/src/lib/__tests__/` (node project; existing mapper-tests pattern). All use `it.todo()`.

    **File 1 — app/src/components/editor/__tests__/PlanEditor.smoke.test.tsx (EDIT-01):**

    ```typescript
    // Phase 7 Plan 01 Wave 0: smoke-mount test for the plan editor screen (EDIT-01).
    // Mocks Skia/gesture-handler/reanimated globally via setup.ts. No real native modules.

    describe('PlanEditor smoke (EDIT-01)', () => {
      it.todo('renders the editor Canvas, Toolbar, and Palette without throwing in jsdom');
      it.todo('grid is visible by default (showGrid=true) and uses #D6CFC4 at 40% opacity per UI-SPEC');
      it.todo('toggling grid via toolbar button hides the grid Skia group');
      it.todo('shows empty-plan empty-state heading when elements array is empty (i18n key editor.emptyPlan.heading)');
    });
    ```

    **File 2 — app/src/components/editor/__tests__/ElementPalette.test.tsx (EDIT-02):**

    ```typescript
    // Phase 7 Plan 01 Wave 0: element-palette test scaffold (EDIT-02).
    // Per UI-SPEC §Toolbar / Palette: 3 tabs — Beete / Pflanzen / Infrastruktur.

    describe('ElementPalette (EDIT-02)', () => {
      it.todo('renders 3 tabs with testIDs palette-tab-beete / palette-tab-pflanzen / palette-tab-infrastruktur');
      it.todo('Beete tab renders one PaletteCard with kind="Beet"');
      it.todo('Infrastruktur tab renders Weg/Laube/Zaun/Wasserstelle/Kompost/Baum/Sitzplatz/Sonstiges cards (8 kinds per UI-SPEC)');
      it.todo('Pflanzen tab shows empty-plants hint when no Beete exist yet (i18n key editor.palette.emptyPlants)');
      it.todo('active tab carries the accent green underline (testID-checkable className contains border-[#4A7C59])');
      it.todo('PaletteCard long-press flips the shared dragging value with the correct kind');
    });
    ```

    **File 3 — app/src/components/editor/__tests__/EditorToolbar.test.tsx (EDIT-08, EDIT-11, EDIT-09 manual save):**

    ```typescript
    // Phase 7 Plan 01 Wave 0: editor toolbar test scaffold (EDIT-08, EDIT-11, EDIT-09 manual save).
    // Per UI-SPEC §Toolbar layout: 9 buttons left-to-right.

    describe('EditorToolbar > layer toggle (EDIT-08)', () => {
      it.todo('cycle 1 → both layers visible (Eye icon, default)');
      it.todo('cycle 2 → infrastructure only (EyeOff with seasonal-strike)');
      it.todo('cycle 3 → seasonal only (EyeOff with infra-strike)');
      it.todo('cycle 4 → back to both');
      it.todo('layer-toggle button has testID editor-layer-toggle-button');
    });

    describe('EditorToolbar > undo/redo (EDIT-11)', () => {
      it.todo('undo button calls useEditorStore.temporal.getState().undo');
      it.todo('redo button calls useEditorStore.temporal.getState().redo');
      it.todo('undo button disabled (opacity 0.4) when pastStates is empty');
      it.todo('redo button disabled when futureStates is empty');
    });

    describe('EditorToolbar > manual save (EDIT-09)', () => {
      it.todo('save button calls flushAllPendingSaves with mode + byId selector');
      it.todo('save button cycles state: idle → saving (Loader2) → saved (Check 2s) → idle');
      it.todo('save error triggers an InlineBanner variant=warning with editor.saveError copy');
    });

    describe('EditorToolbar > polygon mode (EDIT-05)', () => {
      it.todo('polygon-start button activates polygon tool (toolbar background accent green)');
      it.todo('polygon-finish button appears only when polygonInProgress.pointsM.length >= 3');
      it.todo('polygon-finish disabled with caption "Mindestens 3 Ecken" when length < 3');
    });
    ```

    **File 4 — app/src/components/editor/__tests__/DraftsTray.test.tsx (DRAFT-01, DRAFT-02, DRAFT-03):**

    ```typescript
    // Phase 7 Plan 01 Wave 0: drafts-tray test scaffold (DRAFT-01, DRAFT-02, DRAFT-03).
    // Mocks importRepo.loadPendingDraftsWithImportedAt + draftPromotionRepo.promoteBedDraft.

    describe('DraftsTrayBottomSheet > render (DRAFT-01)', () => {
      it.todo('hidden when zero pending drafts (drafts.length === 0)');
      it.todo('collapsed chip shows "Letzte Importe (N)" with count, testID=tray-chip');
      it.todo('expanded panel renders DraftReviewCard for each bed/plant/observation in scroll list');
      it.todo('expanded panel shows three filter chips: Alle / Aktuell / Stale (testID tray-filter-{value})');
    });

    describe('DraftsTrayBottomSheet > bed-draft drop (DRAFT-02)', () => {
      it.todo('long-press on bed-draft card flips shared dragging.value with { kind:"bed-draft", draftId, importItemId }');
      it.todo('on screen-root pan onEnd, handleBedDraftDrop is called with (draftId, importItemId, xM, yM)');
      it.todo('handleBedDraftDrop calls promoteBedDraft with mode, draft, dims, elements, importItemId, { xM, yM } — finalCoords param (6th arg) carries the drop position');
    });

    describe('DraftsTrayBottomSheet > plant-draft tap (DRAFT-02 plant path)', () => {
      it.todo('Auf Beet anwenden button opens BedPickerModal with all current Beet elements');
      it.todo('selecting a bed in modal calls promotePlantDraft with the chosen parentBedElement');
    });

    describe('DraftsTrayBottomSheet > stale badge (DRAFT-03)', () => {
      it.todo('Date.now mocked 31 days after importedAt → TrafficLightBadge state=amber label=Stale appears in card details slot');
      it.todo('Date.now mocked 29 days after importedAt → no stale badge');
      it.todo('filter Stale chip excludes fresh drafts (only stale cards visible)');
      it.todo('filter Aktuell chip excludes stale drafts (only fresh cards visible)');
      it.todo('filter Alle shows everything');
    });
    ```

    **File 5 — app/src/lib/__tests__/rowMappers.layer.test.ts (Pitfall-8 + Migration 018 round-trip):**

    ```typescript
    // Phase 7 Plan 01 Wave 0: rowMappers layer field test scaffold (Pitfall-8 + Migration 018).
    // Per RESEARCH §Code Examples §2 + 07-PATTERNS.md §rowMappers.layer.test.ts.

    // process.env stubs (matches gardenPlanRepo.test.ts:5-6 pattern for node-project tests)
    process.env['EXPO_PUBLIC_SUPABASE_URL'] = 'https://test.example';
    process.env['EXPO_PUBLIC_SUPABASE_ANON_KEY'] = 'test-anon-key';

    describe('rowMappers layer field (Phase 7)', () => {
      it.todo('round-trips layer="infrastructure" through planElementToDb → planElementToLocal');
      it.todo('round-trips layer="seasonal" through planElementToDb → planElementToLocal');
      it.todo('planElementToDb writes "layer" snake_case key (camelToSnake)');
    });

    describe('rowMappers layer pre-018 lazy defaults (Pitfall-8)', () => {
      it.todo('pre-018 row with layer=undefined AND element_type="Pflanze" → planElementToLocal returns layer="seasonal"');
      it.todo('pre-018 row with layer=undefined AND element_type="Beet" → planElementToLocal returns layer="infrastructure"');
      it.todo('pre-018 row with layer=null AND element_type="Pflanze" → layer="seasonal"');
      it.todo('row with explicit layer="seasonal" overrides element_type-based default');
      it.todo('row with explicit layer="infrastructure" overrides element_type-based default');
    });
    ```

    All `it.todo()` only. File 5 lives in node project (existing path `app/src/lib/__tests__/`), Files 1-4 live in the new editor project (path `app/src/components/editor/__tests__/`).
  </action>
  <verify>
    <automated>cd /d/AiProjects/garden-app && pnpm --filter app exec jest --testPathPattern='PlanEditor.smoke|ElementPalette|EditorToolbar|DraftsTray|rowMappers.layer' 2>&1 | grep -E '(Tests:|todo)' | head -10</automated>
  </verify>
  <acceptance_criteria>
    - File app/src/components/editor/__tests__/PlanEditor.smoke.test.tsx exists and contains `EDIT-01`
    - File app/src/components/editor/__tests__/ElementPalette.test.tsx exists and contains `palette-tab-beete`
    - File app/src/components/editor/__tests__/EditorToolbar.test.tsx exists and contains all three: `layer toggle`, `undo/redo`, `manual save`
    - File app/src/components/editor/__tests__/DraftsTray.test.tsx exists and contains `Stale` (filter) + `bed-draft drop` + `finalCoords`
    - File app/src/lib/__tests__/rowMappers.layer.test.ts exists and contains both `pre-018` and `Pitfall-8`
    - All 5 files use `it.todo(` only (no fake assertions, no production imports)
    - Running `pnpm --filter app exec jest --testPathPattern='PlanEditor.smoke|ElementPalette|EditorToolbar|DraftsTray|rowMappers.layer'` exits 0 with >=30 todo entries
  </acceptance_criteria>
  <done>5 stub files committed; full Wave-0 quick command `pnpm --filter app exec jest --testPathPattern='editor|geometry|stale-badge|rowMappers.layer'` exits 0 with >=55 total todo entries across all 15 Wave-0 files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (test scaffold + deps install) | Wave-0 test files run in isolated jest sandbox; no production data touched. Dependency install touches package.json — verified pin against npm registry. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | Wrong Skia version pulled (Pitfall-1) | mitigate | Use explicit `pnpm --filter app add @shopify/react-native-skia@1.12.4` — NEVER `expo install` (Pitfall-1). package.json acceptance check asserts literal "1.12.4" string. |
| T-07-02 | Tampering | Supply-chain risk on three new deps | accept | All three pinned to exact verified versions (RESEARCH `[VERIFIED]` against npm registry on 2026-05-12). Lockfile pnpm-lock.yaml commits the integrity hash. |
| T-07-03 | Information Disclosure | Test stub fixture leaks PII | accept | No real garden_id/user_id values in stubs — all `it.todo()` descriptions only, no fixtures yet. |
| T-07-04 | DoS | Wave-0 jest config breaks existing 5 projects | mitigate | New `editor` project APPENDS to projects array; existing 5 untouched. Acceptance criterion verifies all original projects still listed. |
</threat_model>

<verification>
- Quick command exits 0: `pnpm --filter app exec jest --testPathPattern='editor|geometry|stale-badge|rowMappers.layer'`
- All 15 stub files committed (4 geometry/save + 4 editorStore + 5 component/mapper + 1 setup file + 1 i18n placeholder = 15 file-modifications).
- Three deps pinned exactly in app/package.json.
- Editor jest project block present in app/jest.config.ts; existing 5 projects untouched.
- `pnpm --filter app typecheck` still green (no broken imports — stubs use no production imports).
</verification>

<success_criteria>
- 15 Wave-0 test stub files exist.
- All it.todo() shells match the 14 Phase-7 requirements (EDIT-01..09, EDIT-11, EDIT-12, DRAFT-01..03) per 07-VALIDATION.md §Per-Task Verification Map.
- Three new runtime deps pinned exactly: @shopify/react-native-skia@1.12.4, react-native-gesture-handler@2.31.2, zundo@2.3.0.
- New `editor` jest project block exists with transformIgnorePatterns covering all four new ESM libs.
- Editor setup file mocks Skia + gesture-handler + reanimated (alongside existing NativeWind + lucide mocks).
- `editor.*` block placeholder seeded in de.json — concrete strings deferred to Plan 04.
- Quick test command exits 0; jest reports >=55 todo entries.
- No production code touched (Plans 02-05 add the impls).
</success_criteria>

<output>
After completion, create `.planning/phases/07-plan-editor-drafts-integration-m2-m07-5/07-01-SUMMARY.md` documenting: stub count per category, pinned dep versions confirmed in pnpm-lock.yaml, jest editor project added, full Wave-0 quick command exit code + todo count.
</output>
