---
plan: 02
phase: 1
wave_sequence: [1, 2]
type: execute
wave: 2
depends_on: [01]
files_modified:
  - supabase/config.toml
  - supabase/migrations/20260416000001_foundation.sql
  - supabase/seed.sql
  - supabase/tests/rls_foundation.sql
  - app/src/lib/supabase.ts
  - app/src/hooks/useFlag.ts
  - app/src/hooks/__tests__/useFlag.test.ts
  - app/src/lib/enqueueAiJob.ts
  - packages/shared/src/types/database.ts
  - app/.env.example
  - supabase/.env.example
autonomous: false
requirements: [FOUND-03, FOUND-04, FOUND-07, FOUND-08]
user_setup:
  - service: supabase
    why: "Backend für Auth, Postgres mit RLS, pgmq-Queue und Edge Functions (D-03: Frankfurt EU-Region)"
    env_vars:
      - name: EXPO_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard → Settings → API → Project URL"
      - name: EXPO_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard → Settings → API → anon/public key"
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard → Account → Access Tokens (für `supabase db push` im non-TTY Modus)"
      - name: SUPABASE_PROJECT_REF
        source: "Supabase Dashboard → Settings → General → Reference ID"
    dashboard_config:
      - task: "Neues Projekt erstellen in Region 'Europe (Frankfurt)' (D-03)"
        location: "Supabase Dashboard → New Project → Region = eu-central-1 (Frankfurt)"
      - task: "pgmq Extension aktivieren"
        location: "Supabase Dashboard → Database → Extensions → pgmq → Enable"
      - task: "Zwei Test-User anlegen (User A + User B) für RLS-Verifikation"
        location: "Supabase Dashboard → Authentication → Users → Add user"
must_haves:
  truths:
    - "Migration 001 ist in Supabase Cloud Frankfurt applied"
    - "feature_flags, ai_jobs, ai_results existieren mit user_id FK"
    - "RLS ist auf allen drei Tabellen enabled; User A sieht keine Zeilen von User B"
    - "pgmq Extension ist aktiv; Queue 'ai_jobs' existiert"
    - "useFlag('example_flag') liest den aktuellen Wert aus Supabase (cached 5 Minuten)"
    - "Toggle in Dashboard ändert den Flag — keine Redeployment nötig"
  artifacts:
    - path: "supabase/migrations/20260416000001_foundation.sql"
      provides: "Migration 001: feature_flags + ai_jobs + ai_results + RLS + pgmq queue"
      contains: "enable row level security"
    - path: "supabase/tests/rls_foundation.sql"
      provides: "SQL test: User B liest 0 Zeilen von User A"
      contains: "auth.uid()"
    - path: "app/src/lib/supabase.ts"
      provides: "app-seitiger Supabase-Client (D-05: eigenständig)"
      min_lines: 10
    - path: "app/src/hooks/useFlag.ts"
      provides: "Feature-Flag-Hook mit TanStack-Query-Caching"
      exports: ["useFlag"]
    - path: "packages/shared/src/types/database.ts"
      provides: "Supabase-codegen TypeScript-Types (überschreibt Placeholder)"
      contains: "Database"
  key_links:
    - from: "app/src/hooks/useFlag.ts"
      to: "feature_flags Tabelle"
      via: "supabase.from('feature_flags').select('enabled')"
      pattern: "from\\(['\"]feature_flags['\"]\\)"
    - from: "app/src/lib/enqueueAiJob.ts"
      to: "pgmq_public schema"
      via: "supabase.schema('pgmq_public').rpc('send')"
      pattern: "pgmq_public"
    - from: "supabase/migrations/20260416000001_foundation.sql"
      to: "pgmq Extension"
      via: "select pgmq.create('ai_jobs')"
      pattern: "pgmq\\.create"
---

# Plan 02: Supabase Foundation Schema + RLS + pgmq + Feature Flags

> Legt Migration 001 mit genau den drei Foundation-Tabellen an (D-01), aktiviert RLS auf allen (D-02), stellt die pgmq-Queue bereit (FOUND-07), baut den Feature-Flag-Hook `useFlag()` (FOUND-04), pusht das Schema zur Supabase-Cloud-Instanz Frankfurt (D-03) und verifiziert die Cross-User-Isolation.

## Context

Dieser Plan bringt das Backend-Fundament live. Migration 001 scaffolded **nur** die drei Foundation-Tabellen — `garden_plans`, `seed_inventory` etc. kommen später in ihren jeweiligen Phasen (D-01). Jede Tabelle hat `user_id` FK + `auth.uid()`-Policy ab Tag 1 (D-02). pgmq wird via `pgmq_public`-Schema angesprochen (RESEARCH Pitfall 3), damit RLS und Supabase-Dashboard-Integration funktionieren. Der Feature-Flag-Hook `useFlag('example_flag')` ist die Akzeptanzprobe: Toggle im Dashboard → Reload der App → neuer Wert sichtbar, ohne Redeploy.

Per D-05 hat `app/` einen **eigenen** Supabase-Client; ein gemeinsamer Client mit den Edge Functions ist wegen Deno-vs-RN-Inkompatibilität nicht vorgesehen.

**Blocking-Gate:** Nach dem Bearbeiten der Migration **muss** `supabase db push` mit `SUPABASE_ACCESS_TOKEN` ausgeführt werden. Typecheck allein reicht nicht — Schema muss live sein, sonst ergibt RLS-Verifikation keinen Sinn.

## Decisions Implemented

| ID | Decision | Task |
|----|----------|------|
| D-01 | Migration 001 nur feature_flags/ai_jobs/ai_results | 1-02-02 |
| D-02 | RLS + user_id auf allen Tabellen | 1-02-02 |
| D-03 | Supabase Cloud, EU-Region Frankfurt | 1-02-01 (Dashboard-Setup) |
| D-05 | Kein geteilter DB-Client — app/ initialisiert eigen | 1-02-04 |

## Tasks

### Wave 1: Supabase-Projekt-Setup + Test-Stubs

#### Task 1-02-01: Supabase-Projekt anlegen + Env-Konfiguration
- **files_modified:** supabase/config.toml, app/.env.example, supabase/.env.example
- **depends_on:** 01 (packages/shared existiert)
- **type:** checkpoint:human-action
- **autonomous:** false
- **requirement:** FOUND-03

**What to do (Mensch + Claude kombiniert):**

1. **Mensch:** Supabase-Projekt in Region **"Europe (Frankfurt)"** anlegen (D-03). Name: `spatenstich-dev` für Dev-Environment (D-07).
2. **Mensch:** `SUPABASE_ACCESS_TOKEN` generieren (Account → Access Tokens). Diesen Token exportieren:
   - macOS/Linux: `export SUPABASE_ACCESS_TOKEN=<token>`
   - Windows: `setx SUPABASE_ACCESS_TOKEN <token>` oder in `.env.local` für die Shell-Session
3. **Mensch:** Im Dashboard → **Database → Extensions**: `pgmq` aktivieren (Toggle).
4. **Mensch:** Zwei Test-User anlegen (Authentication → Users → Add user, Passwort wählen):
   - `test-a@spatenstich.local`
   - `test-b@spatenstich.local`
   Beide User-UUIDs notieren — werden in Task 1-02-03 für RLS-Test benötigt.
5. **Claude:** `supabase init` im `supabase/`-Verzeichnis ausführen. Die generierte `supabase/config.toml` prüfen und sicherstellen, dass `project_id` leer/lokal bleibt (Verknüpfung mit Cloud erfolgt über `supabase link`).
6. **Claude:** `supabase link --project-ref $SUPABASE_PROJECT_REF` (non-interactive; setzt Token voraus).
7. **Claude:** `app/.env.example` anlegen:
   ```
   EXPO_PUBLIC_SUPABASE_URL=https://<project>.supabase.co
   EXPO_PUBLIC_SUPABASE_ANON_KEY=<anon-key>
   ```
8. **Claude:** `supabase/.env.example` anlegen (nur Doku — echte Secrets kommen in Plan 03 in Edge-Function-Secrets):
   ```
   # Nur für lokale/CI Tools. Edge-Function-Secrets werden via `supabase secrets set` verwaltet.
   SUPABASE_ACCESS_TOKEN=<personal access token>
   SUPABASE_PROJECT_REF=<ref>
   ```
9. **Mensch:** Im Projekt-Root `.env.local` (nicht committen — steht in `.gitignore` aus Plan 01) befüllen mit `EXPO_PUBLIC_SUPABASE_URL` + `EXPO_PUBLIC_SUPABASE_ANON_KEY`.

**Resume-Signal:** Antworte mit "linked" sobald `supabase link` erfolgreich war und beide Test-User existieren. Nenne Projekt-Ref und beide User-UUIDs.

**Verify:**
```bash
supabase projects list | grep spatenstich
test -f supabase/config.toml
test -f app/.env.example
```

**Done:**
- Supabase-Projekt in Frankfurt existiert und ist verlinkt (`supabase link` erfolgreich)
- `pgmq` Extension in Dashboard aktiviert
- Zwei Test-User existieren, UUIDs sind dem Claude bekannt
- `.env.example`-Dateien committet (ohne Secrets)

---

#### Task 1-02-02: Test-Stubs: useFlag + RLS-SQL-Test-Skeleton
- **files_modified:** app/src/hooks/__tests__/useFlag.test.ts, supabase/tests/rls_foundation.sql
- **depends_on:** 1-02-01
- **type:** test
- **autonomous:** true
- **requirement:** FOUND-04, FOUND-03

**What to do:**

1. `app/src/hooks/__tests__/useFlag.test.ts` — Stubs für spätere Implementierung:
   ```ts
   import { describe, it } from '@jest/globals';
   describe('useFlag', () => {
     it.todo('returns false when flag is missing');
     it.todo('returns true when feature_flags row has enabled=true');
     it.todo('updates within 5 minutes after dashboard toggle (cache staleTime)');
   });
   ```
2. `supabase/tests/rls_foundation.sql` — SQL-Test-Skeleton:
   ```sql
   -- Run with: supabase db execute --file supabase/tests/rls_foundation.sql
   -- Erwartet: 0 Zeilen sichtbar für User B wenn User A Daten schreibt.
   -- Wird in Task 1-02-05 (Wave 2) vollständig ausgeführt.
   BEGIN;
     -- Platzhalter für set_config('request.jwt.claim.sub', '<user-a>', true);
     -- INSERT als User A, dann als User B lesen, 0 Rows erwarten.
   ROLLBACK;
   ```

**Verify:**
```bash
test -f app/src/hooks/__tests__/useFlag.test.ts && test -f supabase/tests/rls_foundation.sql && echo OK
```

**Done:**
- Beide Stub-Dateien existieren
- useFlag-Test ist `todo` (wird in 1-02-04 aktiviert)

---

### Wave 2: Migration 001 + Client + Schema Push + RLS-Verifikation

#### Task 1-02-03: Migration 001 schreiben (feature_flags, ai_jobs, ai_results, pgmq)
- **files_modified:** supabase/migrations/20260416000001_foundation.sql, supabase/seed.sql
- **depends_on:** 1-02-01
- **type:** migration
- **autonomous:** true
- **requirement:** FOUND-03, FOUND-07, FOUND-08

**What to do (per D-01, D-02 — exakt drei Tabellen, RLS auf allen):**

1. Erstelle `supabase/migrations/20260416000001_foundation.sql` (aus RESEARCH Pattern 3):
   ```sql
   -- Phase 1 / Migration 001 — Foundation Tables
   -- D-01: NUR feature_flags, ai_jobs, ai_results. Andere Tabellen in späteren Phasen.
   -- D-02: RLS auf allen Tabellen aktiv; user_id FK + auth.uid()-Policy.

   -- ────────────────────────────────────────────────────────────────
   -- 1. feature_flags
   -- ────────────────────────────────────────────────────────────────
   create table public.feature_flags (
     id uuid primary key default gen_random_uuid(),
     user_id uuid references auth.users(id) on delete cascade, -- NULL = global flag (A5 assumption)
     flag_key text not null,
     enabled boolean not null default false,
     created_at timestamptz not null default now(),
     updated_at timestamptz not null default now(),
     unique (user_id, flag_key)
   );
   create index feature_flags_key_idx on public.feature_flags(flag_key);

   alter table public.feature_flags enable row level security;

   create policy "feature_flags_read_own_and_global" on public.feature_flags
     for select using (auth.uid() = user_id or user_id is null);
   -- Client darf KEINE Flags schreiben/ändern — nur service_role via Dashboard/CI.
   create policy "feature_flags_service_insert" on public.feature_flags
     for insert with check (auth.role() = 'service_role');
   create policy "feature_flags_service_update" on public.feature_flags
     for update using (auth.role() = 'service_role');

   -- ────────────────────────────────────────────────────────────────
   -- 2. ai_jobs (Tracking — pgmq ist die Queue, diese Tabelle ist Audit)
   -- ────────────────────────────────────────────────────────────────
   create table public.ai_jobs (
     id uuid primary key default gen_random_uuid(),
     user_id uuid not null references auth.users(id) on delete cascade,
     job_type text not null,                          -- 'photo_analysis' | 'seed_extraction' | 'rules_extraction'
     status text not null default 'queued' check (status in ('queued','processing','done','failed')),
     payload jsonb not null default '{}'::jsonb,
     pgmq_msg_id bigint,
     last_error text,
     created_at timestamptz not null default now(),
     updated_at timestamptz not null default now()
   );
   create index ai_jobs_user_status_idx on public.ai_jobs(user_id, status);

   alter table public.ai_jobs enable row level security;

   create policy "ai_jobs_read_own" on public.ai_jobs
     for select using (auth.uid() = user_id);
   create policy "ai_jobs_insert_own" on public.ai_jobs
     for insert with check (auth.uid() = user_id);
   create policy "ai_jobs_update_service" on public.ai_jobs
     for update using (auth.role() = 'service_role');

   -- ────────────────────────────────────────────────────────────────
   -- 3. ai_results (FOUND-08: raw + parsed vollständig persistiert)
   -- ────────────────────────────────────────────────────────────────
   create table public.ai_results (
     id uuid primary key default gen_random_uuid(),
     user_id uuid not null references auth.users(id) on delete cascade,
     job_id uuid not null references public.ai_jobs(id) on delete cascade,
     raw_response jsonb not null,
     parsed_result jsonb,
     model_used text,
     latency_ms integer,
     created_at timestamptz not null default now()
   );
   create index ai_results_user_idx on public.ai_results(user_id, created_at desc);
   create index ai_results_job_idx on public.ai_results(job_id);

   alter table public.ai_results enable row level security;

   create policy "ai_results_read_own" on public.ai_results
     for select using (auth.uid() = user_id);
   create policy "ai_results_insert_service" on public.ai_results
     for insert with check (auth.role() = 'service_role');

   -- ────────────────────────────────────────────────────────────────
   -- pgmq-Queue für asynchrone KI-Jobs (FOUND-07)
   -- pgmq Extension wird via Dashboard aktiviert (Task 1-02-01).
   -- Queue erstellen (idempotent):
   -- ────────────────────────────────────────────────────────────────
   do $$
   begin
     if not exists (
       select 1 from pgmq.meta where queue_name = 'ai_jobs'
     ) then
       perform pgmq.create('ai_jobs');
     end if;
   end $$;

   -- updated_at-Trigger (DRY)
   create or replace function public.tg_set_updated_at()
   returns trigger language plpgsql as $$
   begin new.updated_at = now(); return new; end $$;

   create trigger feature_flags_updated_at before update on public.feature_flags
     for each row execute function public.tg_set_updated_at();
   create trigger ai_jobs_updated_at before update on public.ai_jobs
     for each row execute function public.tg_set_updated_at();
   ```
2. `supabase/seed.sql` — seed den Flag (FOUND-04 Akzeptanzprobe braucht eine initial Zeile):
   ```sql
   -- Setzt example_flag als globalen Flag (user_id NULL) auf false.
   insert into public.feature_flags(user_id, flag_key, enabled)
   values (null, 'example_flag', false)
   on conflict (user_id, flag_key) do nothing;
   ```
   *Hinweis:* UNIQUE-Constraint mit NULL ist Postgres-quirky — wenn ON CONFLICT nicht greift, mit `where flag_key='example_flag' and user_id is null` vorher prüfen.

**Verify:**
```bash
# SQL-Syntax lokal validieren (ohne Push)
supabase migration list
grep -q "enable row level security" supabase/migrations/20260416000001_foundation.sql
grep -q "pgmq.create" supabase/migrations/20260416000001_foundation.sql
test -f supabase/seed.sql
```

**Done:**
- Migration-Datei enthält exakt drei Tabellen (D-01)
- Alle drei Tabellen haben `enable row level security` (D-02)
- Alle drei Tabellen haben `user_id` FK auf `auth.users(id)` (D-02)
- pgmq-Queue `ai_jobs` wird idempotent erstellt
- seed.sql setzt globalen `example_flag`

---

#### Task 1-02-04: [BLOCKING] Schema-Push gegen Supabase Cloud + Type-Generation
- **files_modified:** packages/shared/src/types/database.ts
- **depends_on:** 1-02-03
- **type:** migration
- **autonomous:** true
- **requirement:** FOUND-03

**What to do:**

1. Set `SUPABASE_ACCESS_TOKEN` + `SUPABASE_PROJECT_REF` aus Task 1-02-01.
2. Pushe das Schema non-interaktiv:
   ```bash
   supabase db push --yes --linked
   ```
   Falls interaktiver Prompt erscheint: `echo "y" | supabase db push --linked` oder `SUPABASE_NON_INTERACTIVE=1 supabase db push --linked`.
3. Seed ausführen:
   ```bash
   supabase db execute --file supabase/seed.sql --linked
   ```
4. TypeScript-Types generieren und Placeholder in `packages/shared/src/types/database.ts` überschreiben:
   ```bash
   supabase gen types typescript --linked > packages/shared/src/types/database.ts
   ```
5. Typecheck über Monorepo laufen lassen, damit die neuen Types konsumierbar sind.

**Verify:**
```bash
supabase db push --yes --linked && supabase gen types typescript --linked > packages/shared/src/types/database.ts && pnpm -r run typecheck && echo OK
```

**Done:**
- `supabase db push` exitet 0; in `supabase migration list` ist `20260416000001_foundation` als remote applied markiert
- `packages/shared/src/types/database.ts` enthält `Database` Typ mit `feature_flags`, `ai_jobs`, `ai_results`
- `pnpm -r run typecheck` grün

**⚠️ CRITICAL:** Ohne diesen Push existiert das Schema nur in der Migration-Datei. Typecheck/Build passieren dennoch — **Silent False-Positive**. Dieser Task ist das Gate für Task 1-02-05.

---

#### Task 1-02-05: Supabase-Client + useFlag-Hook + enqueueAiJob-Helper + RLS-Verifikation
- **files_modified:** app/src/lib/supabase.ts, app/src/hooks/useFlag.ts, app/src/hooks/__tests__/useFlag.test.ts, app/src/lib/enqueueAiJob.ts, supabase/tests/rls_foundation.sql
- **depends_on:** 1-02-04
- **type:** implementation
- **autonomous:** false
- **requirement:** FOUND-04, FOUND-07, FOUND-03
- **tdd:** true

**Behavior:**
- `useFlag('example_flag')` → `false` initial (seed.sql default)
- Nach Dashboard-Toggle auf `true` + Reload → Hook liefert `true`
- Hook cached für 5 Minuten (staleTime); innerhalb der 5 Minuten keine weiteren Netzcalls
- `enqueueAiJob({ job_type: 'photo_analysis', data: {...} })` erzeugt Zeile in `ai_jobs` UND Nachricht in pgmq `ai_jobs`-Queue
- RLS: User B liest 0 Zeilen von User A's `ai_jobs`

**What to do:**

1. **Supabase-Client (D-05 — app/ eigenständig)** `app/src/lib/supabase.ts`:
   ```ts
   import 'react-native-url-polyfill/auto';
   import { createClient } from '@supabase/supabase-js';
   import type { Database } from '@spatenstich/shared';

   const url = process.env.EXPO_PUBLIC_SUPABASE_URL!;
   const anonKey = process.env.EXPO_PUBLIC_SUPABASE_ANON_KEY!;
   if (!url || !anonKey) {
     throw new Error('Missing EXPO_PUBLIC_SUPABASE_URL or EXPO_PUBLIC_SUPABASE_ANON_KEY');
   }
   export const supabase = createClient<Database>(url, anonKey, {
     auth: { persistSession: true, autoRefreshToken: true, detectSessionInUrl: false },
   });
   ```
   Hinweis: `react-native-url-polyfill` als dep in `app/package.json` ergänzen (`pnpm --filter app add react-native-url-polyfill`).
2. **useFlag-Hook** `app/src/hooks/useFlag.ts`:
   ```ts
   import { useQuery } from '@tanstack/react-query';
   import { supabase } from '../lib/supabase';
   import type { FlagKey } from '@spatenstich/shared';

   export function useFlag(flagKey: FlagKey | string): boolean {
     const { data } = useQuery({
       queryKey: ['feature_flag', flagKey],
       queryFn: async () => {
         const { data, error } = await supabase
           .from('feature_flags')
           .select('enabled')
           .eq('flag_key', flagKey)
           .maybeSingle();
         if (error) return false;
         return data?.enabled ?? false;
       },
       staleTime: 5 * 60 * 1000, // 5 Minuten Cache
     });
     return data ?? false;
   }
   ```
3. **useFlag-Tests aktivieren** `app/src/hooks/__tests__/useFlag.test.ts`:
   ```ts
   import { renderHook, waitFor } from '@testing-library/react-native';
   import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
   import { useFlag } from '../useFlag';

   jest.mock('../../lib/supabase', () => ({
     supabase: {
       from: jest.fn(() => ({
         select: jest.fn(() => ({
           eq: jest.fn(() => ({
             maybeSingle: jest.fn().mockResolvedValue({ data: { enabled: true }, error: null }),
           })),
         })),
       })),
     },
   }));

   function wrap(qc: QueryClient) {
     return ({ children }: any) => <QueryClientProvider client={qc}>{children}</QueryClientProvider>;
   }

   it('returns true when flag is enabled', async () => {
     const qc = new QueryClient();
     const { result } = renderHook(() => useFlag('example_flag'), { wrapper: wrap(qc) });
     await waitFor(() => expect(result.current).toBe(true));
   });
   ```
   `@testing-library/react-native` als devDep ergänzen.
4. **enqueueAiJob** `app/src/lib/enqueueAiJob.ts` (FOUND-07, via `pgmq_public`-Schema — Pitfall 3):
   ```ts
   import { supabase } from './supabase';
   import { QUEUES } from '@spatenstich/shared';

   export async function enqueueAiJob(input: {
     job_type: 'photo_analysis' | 'seed_extraction' | 'rules_extraction';
     data: unknown;
   }) {
     const { data: user } = await supabase.auth.getUser();
     if (!user?.user) throw new Error('Not authenticated');

     const { data: job, error: jobErr } = await supabase.from('ai_jobs')
       .insert({ user_id: user.user.id, job_type: input.job_type, payload: input.data as any })
       .select()
       .single();
     if (jobErr) throw jobErr;

     const { error: qErr } = await supabase.schema('pgmq_public').rpc('send', {
       queue_name: QUEUES.AI_JOBS,
       message: { job_id: job.id, job_type: input.job_type, payload: input.data },
       sleep_seconds: 0,
     });
     if (qErr) throw qErr;
     return job;
   }
   ```
5. **RLS-SQL-Test** `supabase/tests/rls_foundation.sql` finalisieren:
   ```sql
   -- Ausführung: supabase db execute --file supabase/tests/rls_foundation.sql --linked
   -- Erwartet: Am Ende müssen alle ASSERTs grün sein.
   -- User A UUID / User B UUID aus 1-02-01 via psql-Variablen setzen:
   --   supabase db execute --file supabase/tests/rls_foundation.sql --linked -v user_a='<uuid>' -v user_b='<uuid>'

   BEGIN;
     -- Setze JWT-Claim auf User A und schreibe eine Zeile
     select set_config('request.jwt.claim.sub', :'user_a', true);
     select set_config('request.jwt.claim.role', 'authenticated', true);
     insert into public.ai_jobs(user_id, job_type, payload)
       values (:'user_a'::uuid, 'photo_analysis', '{"test":"A"}'::jsonb);

     -- Wechsle zu User B — darf User A's Zeile NICHT sehen
     select set_config('request.jwt.claim.sub', :'user_b', true);
     do $$
     declare cnt int;
     begin
       select count(*) into cnt from public.ai_jobs where user_id = :'user_a'::uuid;
       if cnt <> 0 then
         raise exception 'RLS breach: user B sees % rows of user A', cnt;
       end if;
       raise notice 'RLS ok: user B sieht 0 Zeilen von user A';
     end $$;
   ROLLBACK;
   ```
6. **Checkpoint — Manuelle Akzeptanztests (Mensch):**

   **Checkpoint A — Feature Flag Toggle (FOUND-04):**
   - App starten (`pnpm --filter app run web`)
   - App liest `useFlag('example_flag')` → muss `false` sein
   - Supabase Dashboard öffnen → Table Editor → `feature_flags` → Row mit `flag_key='example_flag'` → `enabled = true` setzen
   - App neu laden (nicht neu deployen!)
   - Erwartung: `useFlag('example_flag')` liefert jetzt `true`

   **Checkpoint B — RLS User-Isolation (FOUND-03):**
   - `supabase db execute --file supabase/tests/rls_foundation.sql --linked -v user_a='<A>' -v user_b='<B>'`
   - Output muss enthalten: `RLS ok: user B sieht 0 Zeilen von user A`
   - Zusätzlich im Dashboard-SQL-Editor: als User A via Impersonation Reihe lesen → 1 Row; als User B → 0 Rows.

**Resume-Signal:** Antworte mit "flag toggle verified" sobald der Dashboard-Toggle in der App sichtbar wurde, UND mit "rls verified" sobald das SQL-Skript fehlerfrei durchläuft.

**Verify:**
```bash
# Automated
pnpm --filter app test -- useFlag
pnpm --filter app run typecheck
# Manual gates
supabase db execute --file supabase/tests/rls_foundation.sql --linked -v user_a="$USER_A_UUID" -v user_b="$USER_B_UUID"
```

**Done:**
- useFlag-Test grün
- Supabase-Client kompiliert mit generierten Types
- RLS-SQL-Test exitet 0 (keine Exception geworfen)
- Dashboard-Toggle auf `example_flag` ändert App-Verhalten nach Reload ohne Redeploy
- `enqueueAiJob` kann manuell eine Test-Message in pgmq einreihen (wird in Plan 03 End-to-End getestet)

---

## Verification (gesamter Plan)

```bash
# Schema ist live
supabase migration list --linked | grep 20260416000001
# Types synchron
pnpm -r run typecheck
# Hook-Tests
pnpm --filter app test -- useFlag
# RLS live-test
supabase db execute --file supabase/tests/rls_foundation.sql --linked -v user_a="$USER_A_UUID" -v user_b="$USER_B_UUID"
```

## Success Criteria

1. Migration 001 ist als Remote applied markiert; `supabase migration list` zeigt den Eintrag
2. `packages/shared/src/types/database.ts` enthält `Database` mit den drei Tabellen
3. `useFlag`-Tests sind grün; manueller Toggle im Dashboard wirkt nach App-Reload ohne Redeploy
4. RLS-SQL-Test: User B liest 0 Zeilen von User A
5. `enqueueAiJob` schreibt Job + pgmq-Message (Zeile in `ai_jobs` ist abfragbar; Queue-Read im Dashboard zeigt Nachricht)

## Output

Nach Abschluss erstelle: `.planning/phases/01-foundation/01-02-SUMMARY.md`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (app/) ↔ Supabase Postgres | Unauthentifizierte/authentifizierte Anfragen — RLS ist die einzige Zugriffskontrolle |
| client ↔ pgmq | Jobs werden via `pgmq_public`-RPC eingereiht. Edge Function konsumiert mit service_role |
| Dashboard Admin ↔ feature_flags | Admin (Dirk) toggelt Flags per Dashboard → Server-seitig verifiziert via `auth.role()='service_role'` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | Elevation of Privilege | RLS fehlt auf einer Tabelle | mitigate | Migration 001 setzt `enable row level security` auf alle drei Tabellen; Task 1-02-05 verifiziert per SQL-Skript mit zwei Usern |
| T-2-02 | Information Disclosure | User A sieht Jobs/Results von User B | mitigate | `auth.uid() = user_id` Policy auf SELECT für alle drei Tabellen; expliziter Cross-User-SQL-Test |
| T-2-03 | Tampering | Client setzt `enabled=true` auf feature_flags (um Features freizuschalten) | mitigate | INSERT/UPDATE-Policies mit `auth.role()='service_role'` — Client darf nur lesen |
| T-2-04 | Tampering | Client manipuliert fremde ai_jobs (z.B. setzt Status auf done) | mitigate | UPDATE-Policy nur `service_role`; INSERT-Policy erzwingt `user_id = auth.uid()` |
| T-2-05 | Information Disclosure | pgmq-Nachricht enthält PII im payload und ist für service_role-Edge-Functions lesbar | accept | pgmq_public wirft RLS nicht auf die Queue selbst an — Edge Function läuft mit service_role (notwendig). Payload wird in Phase 4 vor Einreihung validiert/gefiltert |
| T-2-06 | Denial of Service | Fehlende Policy für UPDATE erlaubt Race zwischen Client und Service | mitigate | ai_results hat keine UPDATE-Policy für Client — nur INSERT durch service_role; ai_jobs UPDATE nur service_role |
| T-2-07 | Repudiation | User streitet ab, einen Job angestoßen zu haben | accept | `ai_jobs` speichert `user_id` + `created_at`; ausreichend für Single-User MVP |

## ASVS L1 Controls Addressed

- V4.1.3 (Least Privilege): INSERT/UPDATE auf feature_flags und ai_results nur für service_role
- V4.2.1 (Access Control — Least Privilege Deny-by-default): `enable row level security` ohne Default-Policies = default deny
- V8.3.4 (Sensitive Data — minimal exposure): payload wird explizit in ai_jobs getrennt vom Auth-Kontext gespeichert
- V10.3.2 (Configuration — Verified Migrations): `supabase db push` + `supabase migration list` als Verifikation, dass remote state = migration file
</threat_model>
