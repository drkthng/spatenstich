---
phase: 06-import-flow-companion-prompt-m07-3-m07-4
plan: 04
type: execute
wave: 4
depends_on: [06-01, 06-03]
files_modified: []
autonomous: false
requirements: [IMPORT-04, IMPORT-06, IMPORT-08]
must_haves:
  truths:
    - "Supabase migration 016 applied successfully to remote database"
    - "Import flow works end-to-end: paste valid JSON → preview → confirm → drafts saved"
    - "Invalid JSON shows error with schema copy button"
    - "Home screen import button navigates to import screen"
  artifacts: []
  key_links: []
---

<objective>
Push database migration to Supabase and verify the complete import flow end-to-end via human testing.

Purpose: Migration 016 must be applied to the remote Supabase database before the import flow can sync drafts. Human verification confirms the UI works correctly on a real device/browser.

Output: Applied migration + human sign-off on import flow
</objective>

<execution_context>
@D:/AiProjects/garden-app/.claude/get-shit-done/workflows/execute-plan.md
@D:/AiProjects/garden-app/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:/AiProjects/garden-app/.planning/ROADMAP.md
@D:/AiProjects/garden-app/.planning/phases/06-import-flow-companion-prompt-m07-3-m07-4/06-CONTEXT.md
@D:/AiProjects/garden-app/.planning/phases/06-import-flow-companion-prompt-m07-3-m07-4/06-01-SUMMARY.md
@D:/AiProjects/garden-app/.planning/phases/06-import-flow-companion-prompt-m07-3-m07-4/06-02-SUMMARY.md
@D:/AiProjects/garden-app/.planning/phases/06-import-flow-companion-prompt-m07-3-m07-4/06-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Push Supabase Migration 016 to Remote Database</name>
  <files></files>
  <read_first>
    - supabase/migrations/20260509000016_import_drafts.sql
  </read_first>
  <action>
Run `supabase db push` to apply migration 016 to the remote Supabase database.

If `SUPABASE_ACCESS_TOKEN` env var is set, the command runs non-interactively:
```bash
cd D:/AiProjects/garden-app && supabase db push
```

If the push requires interactive confirmation (e.g., destructive changes warning), flag for manual intervention. Migration 016 is purely additive (CREATE TABLE only), so no destructive warning should appear.

After push, verify the tables exist:
```bash
supabase db inspect --linked | grep -E "imports|import_items|bed_drafts|plant_drafts|observation_drafts"
```

If `supabase db push` fails due to auth, create a checkpoint for the user to authenticate and retry.
  </action>
  <verify>
    <automated>cd D:/AiProjects/garden-app && supabase db push --dry-run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` exits 0 (or dry-run shows no pending migrations)
    - All 5 tables exist in remote Supabase: imports, import_items, bed_drafts, plant_drafts, observation_drafts
    - RLS is enabled on all 5 tables in remote database
  </acceptance_criteria>
  <done>Migration 016 applied to remote Supabase; 5 import draft tables live with RLS</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Import Flow (M07.3 + M07.4):
- Companion prompt at prompts/garden-project-system-prompt.md
- Import Entry screen with paste fallback + file picker
- Import Preview screen with entity cards, confidence chips, toggles
- Error handling with "Schema kopieren" button
- Share-intent config for JSON files
- Home screen "Aus Claude.ai importieren" button
- 5 Supabase draft tables with RLS
- Offline-first draft persistence via outbox sync
  </what-built>
  <how-to-verify>
**Test 1: Import via Paste (Desktop)**
1. Start the app: `cd app && pnpm start --web`
2. On the Home screen, tap "Aus Claude.ai importieren"
3. Copy the contents of `schemas/examples/full.json`
4. Paste into the textarea on the Import Entry screen
5. Tap "Importieren"
6. Verify: Preview screen shows Beete, Pflanzen, Beobachtungen sections with confidence chips
7. Toggle OFF one entity, verify count changes
8. Tap "Ausgewählte übernehmen"
9. Verify: Navigates back to Home screen

**Test 2: Invalid JSON Error**
1. Navigate to Import screen
2. Paste `{"invalid": true}` (missing schemaVersion)
3. Tap "Importieren" or blur the textarea
4. Verify: Error banner appears with actionable message
5. Tap "Schema kopieren" → verify clipboard has the JSON schema

**Test 3: Confidence Filtering**
1. Paste the contents of `schemas/examples/edge-cases.json`
2. Verify: Low-confidence entities (< 0.6) have red chips and are toggled OFF by default
3. Toggle ON a low-confidence entity → verify warning text "Niedrige Erkennungssicherheit" appears

**Test 4: Home Screen Button**
1. Verify "Aus Claude.ai importieren" button visible on Home empty state
2. Verify button navigates to import screen

**Test 5: Companion Prompt Review**
1. Open `prompts/garden-project-system-prompt.md`
2. Verify it's in German, mentions BKleingG, includes JSON output format instruction
3. Verify setup instructions for Dirk are present (project name, Knowledge-Files, Opus 4.7)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| supabase db push | Migration applied to remote database — verified by dry-run before push |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-06-12 | Tampering | Remote migration | mitigate | Migration is additive only (CREATE TABLE), dry-run verification before push |
</threat_model>

<verification>
- `supabase db push` succeeds
- Human verifies all 5 test scenarios pass
</verification>

<success_criteria>
- Migration 016 applied to remote Supabase
- Human confirms: paste import works end-to-end
- Human confirms: invalid JSON shows actionable error
- Human confirms: confidence chips display correctly
- Human confirms: companion prompt is complete and in German
</success_criteria>

<output>
After completion, create `.planning/phases/06-import-flow-companion-prompt-m07-3-m07-4/06-04-SUMMARY.md`
</output>
