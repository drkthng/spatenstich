---
phase: 6.5
slug: draft-sichtung-promotion
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-12
---

# Phase 6.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Jest 29.7.0 + ts-jest 29.1.2 + @testing-library/react-native 13.3.3 (5 jest projects: node, hooks, stores, photos, components) |
| **Config file** | `app/jest.config.ts` |
| **Quick run command** | `pnpm --filter app test -- --testPathPattern='draftPromotion\|review\|i18n.review'` |
| **Full suite command** | `pnpm --filter app test` |
| **Estimated runtime** | ~25s quick / ~90s full |

---

## Sampling Rate

- **After every task commit:** Run quick command (≤25s)
- **After every plan wave:** Run full suite (~90s)
- **Before `/gsd-verify-work`:** Full suite green + manual browser smoke (Import → Sichtung → Home shows plan)
- **Max feedback latency:** 25 seconds

---

## Per-Task Verification Map

> Filled by planner during plan creation. Each task gets a row mapping to a Wave 0 test file.
> Tasks without an automated verify must appear in `## Manual-Only Verifications` below.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD by planner | — | — | DRAFT-01/02, Crit-1..7 | RLS member check | Promote respects garden membership | unit/component | `pnpm --filter app test -- <pattern>` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Test stub files to create in Wave 0 BEFORE implementation tasks:

- [ ] `app/src/lib/__tests__/draftPromotionRepo.test.ts` — covers DRAFT-01/02, Crit-3 (annehmen → plan_element + provenance), Crit-4 (verwerfen → deletedAt), Pitfall-3 (observation no plan_element)
- [ ] `app/src/lib/__tests__/draftPromotionRepo.idempotency.test.ts` — covers Pitfall-1 (double-promote no duplicate)
- [ ] `app/src/lib/__tests__/draftPromotionRepo.layout.test.ts` — covers Pitfall-2 (default geometry stacks beds without overlap)
- [ ] `app/src/components/__tests__/ImportReview.test.tsx` — covers Crit-2 (renders grouped sections)
- [ ] `app/src/components/__tests__/ImportReview.autopromote.test.tsx` — covers Crit-5 (auto-promote ≥ 0.8)
- [ ] `app/src/components/__tests__/preview-navigation.test.tsx` — covers Crit-1 (one-line redirect verification)
- [ ] `app/src/lib/__tests__/i18n.review-keys.test.ts` — covers Crit-7 (`import.review.*` keys present)
- [ ] `supabase/migrations/20260512000017_plan_elements_provenance.sql` — covers schema requirement (assertions inline via `DO $$` block following Migration 014/016 pattern)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Home shows imported plan after Sichtung | Crit-6 | Cross-screen integration (router + IndexedDB read + Skia render) — slow to set up in Jest, fast as browser smoke | 1. `pnpm --filter app web` 2. Login as Dirk 3. Import sample claude.ai JSON 4. Click "Ausgewählte übernehmen" 5. Verify navigates to `/import/review` 6. Click "Annehmen" on each draft + "Fertig" 7. Verify Home shows plan elements (no empty-state "Noch kein Gartenplan") |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (8 stub files listed above)
- [ ] No watch-mode flags
- [ ] Feedback latency < 25s
- [ ] `nyquist_compliant: true` set in frontmatter (after planner fills task map)

**Approval:** pending
