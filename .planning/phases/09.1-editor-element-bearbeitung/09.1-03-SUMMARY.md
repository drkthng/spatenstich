---
phase: 09.1-editor-element-bearbeitung
plan: "03"
subsystem: editor
tags: [canvas-handles, resize, rotation, skia, svg, gesture, tdd]
dependency_graph:
  requires: [09.1-02]
  provides: [ResizeHandle, RotationHandle, WebResizeHandle, WebRotationHandle, handleGeometry-GREEN]
  affects: [EditorCanvas.tsx, WebPlanEditor.tsx]
tech_stack:
  added: []
  patterns:
    - Pattern S3 — gestureActive discipline (setGestureActive true/false on drag)
    - Pattern S2 — provenance spread before rotateDeg update
    - Pattern S5 — Shift-key INPUT/TEXTAREA guard (WebRotationHandle)
    - Pitfall 8 — try/finally ensures gestureActive released on error
    - Pitfall 7 — INPUT/TEXTAREA guard prevents modal Shift hijacking
key_files:
  created:
    - app/src/components/editor/ResizeHandle.tsx
    - app/src/components/editor/RotationHandle.tsx
    - app/src/components/editor/web/WebResizeHandle.tsx
    - app/src/components/editor/web/WebRotationHandle.tsx
    - app/src/components/editor/__tests__/ResizeHandle.test.tsx
    - app/src/components/editor/__tests__/RotationHandle.test.tsx
    - app/src/components/editor/__tests__/WebResizeHandle.test.tsx
    - app/src/components/editor/__tests__/WebRotationHandle.test.tsx
  modified:
    - app/src/lib/editor/handleGeometry.ts
    - app/src/lib/editor/__tests__/handleGeometry.test.ts
    - app/src/components/editor/EditorCanvas.tsx
    - app/src/components/editor/web/WebPlanEditor.tsx
decisions:
  - "computeCornerHandles + computeRotationHandle implemented as pure geometry functions; no TS throw remaining"
  - "MVP carve-out: ResizeHandle hidden when rotateDeg !== 0 — enforced at render gate in both EditorCanvas and WebPlanEditor"
  - "RotationHandle.tsx uses absolute angle (commitRotationAbsolute) not delta, unlike EditorCanvas.commitRotation which is delta-based"
  - "All 4 handle components implement Pitfall 8 try/finally for gestureActive reset"
  - "WebRotationHandle Shift-listener has INPUT/TEXTAREA guard to prevent Pitfall 7 modal hijacking"
metrics:
  duration: "~45 minutes"
  completed: "2026-05-29"
  tasks_completed: 2
  tasks_total: 2
  tests_written: 18
  tests_green: 18
---

# Phase 09.1 Plan 03: Canvas Handles Summary

Canvas handles for element resize and rotation on both Skia (mobile) and SVG (web) renderers. Fills `handleGeometry.ts` Wave-0 stub GREEN and mounts all 4 handle components in both editor renderers.

## One-liner

Skia + Web resize/rotation handles with Pattern S3 gestureActive discipline, snapRotation snap, and MVP carve-out guarding rotated elements.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Skia handles GREEN + handleGeometry filled | 7e3e95e | handleGeometry.ts, ResizeHandle.tsx, RotationHandle.tsx, EditorCanvas.tsx + 3 tests |
| 2 | Web handles + WebPlanEditor mount | efc5e3b | WebResizeHandle.tsx, WebRotationHandle.tsx, WebPlanEditor.tsx + 2 tests |

## Handle Component Summary

| Component | Lines | Platform | Key Features |
|-----------|-------|----------|--------------|
| ResizeHandle.tsx | 138 | Skia | Pan gesture, 4-corner anchoring, Pitfall 8 try/finally |
| RotationHandle.tsx | 97 | Skia | Pan gesture, snapRotation(deg,false), commitRotationAbsolute |
| WebResizeHandle.tsx | 145 | Web SVG | window mousemove, nwse/nesw cursor, Pitfall 8 try/finally |
| WebRotationHandle.tsx | 109 | Web SVG | window mousemove, Shift-key listener, INPUT/TEXTAREA guard |
| handleGeometry.ts | 66 | shared | computeCornerHandles + computeRotationHandle pure functions |

## Test Results

- handleGeometry.test.ts: 5/5 GREEN (was 5 it.todo)
- ResizeHandle.test.tsx: 2/2 GREEN (new)
- RotationHandle.test.tsx: 2/2 GREEN (new)
- WebResizeHandle.test.tsx: 4/4 GREEN (new)
- WebRotationHandle.test.tsx: 5/5 GREEN (new)
- **Total: 18/18 tests GREEN**

## Acceptance Criteria Verification

### handleGeometry.ts
- No `throw new Error('TODO Plan` remaining: CONFIRMED (grep count = 0)
- Exports `computeCornerHandles` and `computeRotationHandle` with real bodies: CONFIRMED (2 functions)
- JSDoc contains `rotateDeg !== 0` MVP carve-out comment: CONFIRMED

### EditorCanvas.tsx (Skia)
- ResizeHandle conditionally rendered guarded by `rotateDeg === 0`: CONFIRMED
- findElementAtPoint from Plan 02 unmodified: CONFIRMED
- `.minDuration(500)` LongPress from Plan 02 unmodified: CONFIRMED
- ResizeHandle.tsx imports `Gesture, GestureDetector`: CONFIRMED

### WebPlanEditor.tsx (Web)
- WebResizeHandle guarded by `rotateDeg === 0`: CONFIRMED
- Plan-02 `onDoubleClick` present: CONFIRMED
- Plan-02 `MOUSE_DRAG_THRESHOLD_PX` and `pendingDragRef` present: CONFIRMED

### WebRotationHandle.tsx
- `isShiftDown` variable: 4 occurrences
- `snapRotation(rawDeg, isShiftDown)` call: CONFIRMED
- INPUT/TEXTAREA guard: CONFIRMED (tag === 'INPUT' || tag === 'TEXTAREA')

### Pattern S3 / Pitfall 8
- Both `setGestureActive(true)` and `setGestureActive(false)` present in all 4 handle files: CONFIRMED
- try/finally in WebResizeHandle onUp + WebRotationHandle onUp: CONFIRMED
- try/finally in ResizeHandle commitResize + RotationHandle commitRotationAbsolute: CONFIRMED

## MVP Carve-Out Evidence

```
grep result — EditorCanvas.tsx:
  "Phase 09.1 Plan 03: Resize handles — only shown when rotateDeg === 0 (MVP carve-out A1)"
  if (rotateDeg === 0) {

grep result — WebPlanEditor.tsx:
  "5. Resize handles — only shown when selection && rotateDeg === 0 (MVP carve-out A1)"
  if (rotateDeg === 0) {
```

## Plan-02 Code Integrity Confirmed

Both EditorCanvas.tsx and WebPlanEditor.tsx had Plan-02 code (LongPress/findElementAtPoint/onDoubleClick/MOUSE_DRAG_THRESHOLD_PX/pendingDragRef) preserved unchanged. Only handle mounts and new imports were added.

## Deviations from Plan

None — plan executed exactly as written. All 8 required files created/modified.

## Threat Mitigations Confirmed

| Threat ID | Mitigation | Evidence |
|-----------|-----------|---------|
| T-09.1-HANDLE-DRIFT | MVP carve-out: ResizeHandle hidden when rotateDeg !== 0 | rotateDeg === 0 gate in EditorCanvas + WebPlanEditor |
| T-09.1-SHIFT-HIJACK | INPUT/TEXTAREA guard in WebRotationHandle | tag === 'INPUT' \|\| tag === 'TEXTAREA' check |
| T-09.1-RESUME-LEAK | try/finally in all 4 handle components | Pitfall 8 mitigations in commitResize, commitRotationAbsolute, onUp handlers |
| T-09.1-FILE-RACE | Wave-3 sequentialization | Implemented after Plan 02 was already merged |

## Self-Check: PASSED

All files confirmed present on disk. Both task commits confirmed in git log.

| Check | Result |
|-------|--------|
| ResizeHandle.tsx exists | FOUND |
| RotationHandle.tsx exists | FOUND |
| WebResizeHandle.tsx exists | FOUND |
| WebRotationHandle.tsx exists | FOUND |
| handleGeometry.ts exists | FOUND |
| Task 1 commit 7e3e95e | FOUND |
| Task 2 commit efc5e3b | FOUND |
