---
phase: 09.1-editor-element-bearbeitung
plan: 05
type: execute
wave: 3
depends_on: [09.1-02]
files_modified:
  - packages/shared/src/i18n/de.json
  - app/src/i18n/__tests__/de.editorElementEditor.test.ts
  - app/src/lib/__tests__/rowMappers.provenance.test.ts
  - .planning/phases/09.1-editor-element-bearbeitung/09.1-HUMAN-VERIFY.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [D-09, D-10, D-11, D-19, D-21, D-23, D-24]
tags: [i18n, mapper, verification, roadmap, german]
supabase_project: vitrqkzxkiqvadqfzrcx

must_haves:
  truths:
    - "Alle 28+ Modal-Strings sind in packages/shared/src/i18n/de.json unter editor.elementEditor.* vorhanden (D-23 Locked-Core + Claude's Discretion UX-Affordances)"
    - "Alle deutschen Strings nutzen native UTF-8 Umlaute (ä/ö/ü/ß), keine ASCII-Ersetzungen (D-24, feedback_german_umlauts.md)"
    - "rowMappers round-trippen provenance-Felder zOrder/note/plantedAt/accentColor verlustfrei (D-19)"
    - "label-Spalte und widthM/heightM-Spalten werden NICHT in provenance dupliziert (D-20/D-21)"
    - "09.1-HUMAN-VERIFY.md enthält alle 7 Manual-Only Verifications aus VALIDATION.md"
    - "ROADMAP.md Phase 09.1 zeigt **Plans:** 6 plans mit Plan-Liste, ersetzt **Goal:** [Urgent work - to be planned] mit echtem Goal aus CONTEXT.md"
  artifacts:
    - path: "packages/shared/src/i18n/de.json"
      provides: "editor.elementEditor.* i18n block mit ≥ 28 Keys (Locked D-23 Core + Claude's Discretion UX)"
      contains: "elementEditor"
    - path: "app/src/i18n/__tests__/de.editorElementEditor.test.ts"
      provides: "Smoke-Test der i18n-Keys + UTF-8-Umlaut-Assertion"
    - path: "app/src/lib/__tests__/rowMappers.provenance.test.ts"
      provides: "Round-Trip-Test für provenance jsonb mit allen 09.1-Feldern"
    - path: ".planning/phases/09.1-editor-element-bearbeitung/09.1-HUMAN-VERIFY.md"
      provides: "Manuelle Verifikations-Checkliste für Skia/SVG Gesten + UTF-8 visual check"
    - path: ".planning/ROADMAP.md"
      provides: "Aktualisierte Phase 09.1 Entry mit Goal + Plans-Liste"
  key_links:
    - from: "app/src/components/editor/ElementEditorModal.tsx"
      to: "packages/shared/src/i18n/de.json"
      via: "t('editor.elementEditor.<key>')"
      pattern: "editor\\.elementEditor\\."
    - from: "packages/shared/src/lib/mappers/rowMappers.ts"
      to: "PlanElementRow.provenance"
      via: "passthrough Record<string, unknown>"
      pattern: "provenance:"
---

<objective>
Schließt Phase 09.1 ab durch:
(1) Einfügen des `editor.elementEditor.*` i18n-Blocks in `packages/shared/src/i18n/de.json`
    mit allen 28+ Keys (D-09/D-10/D-11/D-23 Locked Core + Claude's Discretion UX-Affordances)
    und nativen UTF-8-Umlauten (D-24).
(2) GREEN-fill der i18n-Smoke-Tests + Mapper-Round-Trip-Tests, die alle in
    `provenance` jsonb persistenten Felder (zOrder/note/plantedAt/accentColor) verlustfrei
    durch `planElementToLocal` und `planElementToDb` laufen lassen (D-19).
(3) Erzeugung von `09.1-HUMAN-VERIFY.md` aus den 7 Manual-Only Verifications in VALIDATION.md
    — abgedeckt: Skia Long-Press, Web Doppelklick, Android Hardware-Back, Web ESC,
    15°-Snap UX, Drag-Latency, Color-Picker Cross-Platform.
(4) Aktualisierung von ROADMAP.md Phase 09.1: Ersatz des Placeholder-Goals durch ein
    konkretes Outcome-formuliertes Goal aus CONTEXT.md §domain, Setzen von
    `**Plans:** 6 plans` und Auflistung aller sechs PLAN.md-Dateien.
(5) Finaler D-XX Coverage Audit (D-01..D-24) über alle sechs Plans.

Purpose: Ohne Plan 05 würden die Modal-Strings als raw-key Fallback rendern (`editor.elementEditor.title`
statt "Element bearbeiten"), und die Manual-Only Verifications wären nirgendwo dokumentiert.
Außerdem stellt nur dieser Plan sicher, dass die in Plan 02 abgelegten provenance-Felder
beim nächsten Supabase-Sync (und nach offline reload) intakt wieder auftauchen.

Output: Sechs PLAN.md im Phase-Verzeichnis vollständig, ROADMAP.md auf Phase 09.1 aktualisiert,
HUMAN-VERIFY.md als finaler manueller Gate vor /gsd-verify-work, sämtliche automatisierten
Tests (Wave 0..3) grün, D-01..D-24 jeweils in mindestens einem Plan-`requirements`-Frontmatter referenziert.

**Revision Note (Checker Feedback):**
- Task 1 Action erhält explizite Discretion-Erklärung für die D-23 Key-Erweiterung auf 28+ Keys.
- Task 3 Audit-Tabelle: D-21 Row updated zu "09.1-02, 09.1-05" (Implementation lebt in Plan 02
  per Modal-Save Logik, Plan 05 trägt nur den Round-Trip-Audit für die Column-Discipline).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09.1-editor-element-bearbeitung/09.1-CONTEXT.md
@.planning/phases/09.1-editor-element-bearbeitung/09.1-PATTERNS.md
@.planning/phases/09.1-editor-element-bearbeitung/09.1-RESEARCH.md
@.planning/phases/09.1-editor-element-bearbeitung/09.1-VALIDATION.md

# Wave 0 stubs to GREEN-fill
@app/src/i18n/__tests__/de.editorElementEditor.test.ts
@app/src/lib/__tests__/rowMappers.provenance.test.ts

# Files to MODIFY
@packages/shared/src/i18n/de.json
@.planning/ROADMAP.md

# Mapper that must round-trip — read-only verify
@app/src/lib/mappers/rowMappers.ts
@packages/shared/src/types/entities.ts

# Analog test for i18n smoke
@app/src/lib/__tests__/i18n.review-keys.test.ts

# Memory feedback policies
# @ feedback_german_umlauts.md (UTF-8 native Umlaute strict, no ASCII fallback)
# @ feedback_show_project_id.md (Supabase project ref in answer header)
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Editor UI → i18n lookup | Modal-Komponenten lesen Keys via `t('editor.elementEditor.<x>')` — fehlende Keys führen zu raw-key Anzeige in UI (silent bug) |
| Local provenance → Supabase jsonb | provenance-Objekt wird via Supabase Storage als jsonb gespeichert — Mapper darf KEINE Felder verlieren |
| Mapper round-trip | Felder die nur in Plan 09.1 hinzukommen (zOrder, note, plantedAt, accentColor) müssen ohne Schema-Migration durchlaufen, da D-19 keine neue Migration vorschreibt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-I18N | Tampering | i18n-Key drift (Modal-Callsites vs de.json) | mitigate | Smoke-Test asserted, dass alle in Plans 02-04 verwendeten `t('editor.elementEditor.<x>')` Keys in `de.json` definiert sind. Iteriert eine fest definierte Key-Liste UND prüft Modal-Source-Files per grep, dass keine Modal-Komponente einen Key referenziert, der nicht in de.json existiert. |
| T-09.1-UTF8-FALLBACK | Tampering | Deutsche Strings | mitigate | Test prüft alle Strings im `editor.elementEditor`-Block gegen `\bae|\boe|\bue|\bss\b` ASCII-Ersetzung. Korrekte Schreibung "Höhe" statt "Hoehe", "Löschen" statt "Loeschen", "Größe" statt "Groesse". Memory feedback_german_umlauts.md ist verbindlich. |
| T-09.1-MAPPER-LOSS | Information Disclosure / Loss | rowMappers.ts provenance passthrough | mitigate | Round-Trip-Test seedet PlanElementRow mit allen 09.1-Feldern in provenance + benannten Spalten (widthM/heightM/label), ruft `planElementToDb` → `planElementToLocal` → vergleicht deep-equal. Kein Feldverlust toleriert. |
| T-09.1-LABEL-COLLISION | Tampering | label-Spalte vs provenance.label | mitigate | Round-Trip-Test setzt label="Beet A" und versucht NICHT, denselben Wert in provenance zu duplizieren. Modal-Save (Plan 02) muss label nur an dedizierte Spalte schreiben (D-21). Test asserted, dass `el.provenance` KEIN `label`-Key enthält. |
| T-09.1-PII-NOTE | Information Disclosure | provenance.note (Freitext bis 500 chars) | accept | Memo persistiert lokal + Supabase EU-Frankfurt (DSGVO-konform, CLAUDE.md). Kein zusätzliches Hashing/Encryption nötig — gleiche Schutzklasse wie restliche Plan-Daten. Modal-Validierung beschränkt auf maxLength=500 (Plan 02 Mitigation T-09.1-DOS-NOTE). |
| T-09.1-DECISION-COVERAGE | Tampering | D-XX Coverage Audit | mitigate | Plan 05 Task 3 führt eine ausführliche Audit-Tabelle aus, die D-01..D-24 gegen die `requirements`-Frontmatter aller sechs Plans matched. Output dokumentiert in SUMMARY.md. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: editor.elementEditor.* i18n-Block in de.json einfügen + Tests GREEN-fillen</name>
  <files>
    packages/shared/src/i18n/de.json,
    app/src/i18n/__tests__/de.editorElementEditor.test.ts,
    app/src/lib/__tests__/rowMappers.provenance.test.ts
  </files>
  <read_first>
    - `packages/shared/src/i18n/de.json` aktueller `editor`-Block ab Line 232 (siehe PATTERNS.md §MODIFIED de.json) — gesamte editor-Section einsehen vor Edit
    - `.planning/phases/09.1-editor-element-bearbeitung/09.1-RESEARCH.md` Lines 883-928 (verbatim Key-Liste für editor.elementEditor.*)
    - `.planning/phases/09.1-editor-element-bearbeitung/09.1-PATTERNS.md` Lines 609-635 (Test-Pattern Analog `i18n.review-keys.test.ts`)
    - `app/src/lib/__tests__/i18n.review-keys.test.ts` (Komplettes Analog für Test-Strukturierung)
    - `app/src/lib/mappers/rowMappers.ts` Lines 394-446 (provenance passthrough in `planElementToLocal` und `planElementToDb`)
    - `packages/shared/src/types/entities.ts` (`PlanElementRow` Felder, vor allem `widthM`, `heightM`, `label`, `provenance`)
    - Wave 0 Stubs: `app/src/i18n/__tests__/de.editorElementEditor.test.ts` (it.todo()-Slots) + `app/src/lib/__tests__/rowMappers.provenance.test.ts` (it.todo()-Slots)
    - Memory Reference: feedback_german_umlauts.md — UTF-8 native Umlaute strict, KEIN `ae/oe/ue/ss` als ASCII-Ersatz
  </read_first>
  <behavior>
    - de.json: Innerhalb des bestehenden `"editor": { ... }` Objekts wird ein NEUER Key `"elementEditor": { ... }` hinzugefügt (NICHT ein neues Top-Level-Objekt). Der Block enthält mindestens diese 18 Pflicht-Keys: title, name, namePlaceholder, width, height, unitCm, unitDeg, rotation, rotationPlaceholder, maintainRatio, zOrder, bringToFront, bringForward, sendBackward, sendToBack, moreFields, lessFields, note, notePlaceholder, plantedAt, accentColor, accentColorReset, cancel, save, delete, deleteConfirmTitle, deleteConfirmBody, deleteConfirmYes, deleteConfirmNo + `validation` Sub-Objekt mit widthMin/widthMax/heightMin/heightMax.
    - Alle Strings in nativem UTF-8: "Höhe" (nicht "Hoehe"), "Löschen" (nicht "Loeschen"), "Ja, löschen" (nicht "Ja, loeschen"), "Verhältnis beibehalten" (nicht "Verhaeltnis beibehalten"), "Ganz nach vorne", "Eine Ebene zurück" (nicht "zurueck"), "Größe" wo angebracht. Im Cmd-Aufruf des Edits muss UTF-8 erhalten bleiben (Memory feedback_german_umlauts.md).
    - JSON-Validität: das resultierende de.json muss `JSON.parse` ohne Fehler bestehen (Trailing-Commas verboten).
    - de.editorElementEditor.test.ts: GREEN-fillen aller it.todo()-Slots (≥ 20 laut Plan 00). Tests folgen Pattern aus i18n.review-keys.test.ts mit Supabase env mocks (Pattern S8).
      - "defines de.editor.elementEditor block"
      - "defines title" (assert === "Element bearbeiten")
      - "defines all 18 core keys" (iterate)
      - "uses real UTF-8 umlauts" (regex check for ä/ö/ü/ß presence)
      - "rejects ASCII fallbacks" (regex `\b(ae|oe|ue|ss)\b` MUST NOT match any value in the block)
      - "validation sub-object has widthMin/widthMax/heightMin/heightMax"
      - "validation widthMin contains numeric value 5"
      - "all values are non-empty strings"
    - rowMappers.provenance.test.ts: GREEN-fillen mit Round-Trip-Tests.
      - "round-trips provenance.zOrder through DB and back" (number 7 in, number 7 out)
      - "round-trips provenance.note" (string "Hallo Welt mit Umlauten ÄÖÜß" verlustfrei)
      - "round-trips provenance.plantedAt as ISO date" (ISO-String "2026-04-15" intakt)
      - "round-trips provenance.accentColor" ("#a1b2c3" Hex bleibt erhalten)
      - "round-trips provenance.rotateDeg" (45.0 bleibt 45.0)
      - "round-trips provenance.source" (existing field "manual" bleibt)
      - "round-trips provenance.plantSlug" (existing field bleibt — Pitfall 5 regression-guard)
      - "label and widthM/heightM live on dedicated columns, NOT in provenance" — D-20/D-21 Sicherung
      - "provenance survives null → empty-object normalization"
  </behavior>
  <action>
    **Claude's Discretion Block — D-23 Key-Liste Erweiterung:**

    D-23 in CONTEXT.md lockt eine Core-Key-Liste mit ~18 Pflicht-Keys (title, name, width, height,
    rotation, zOrder + 4 Z-Order-Buttons, maintainRatio, note, plantedAt, accentColor, cancel,
    save, delete + Delete-Confirm-Triplett). Plan 05 erweitert diese Liste unter
    **Claude's Discretion** auf 28+ Keys mit den folgenden UX-Affordance-Keys:

    - `namePlaceholder` ("z.B. Tomatenbeet") — UX-Hilfe für leeres Name-Feld
    - `rotationPlaceholder` ("0–359") — Bereichs-Hinweis im Number-Input
    - `notePlaceholder` ("Bemerkung zum Element …") — UX-Hilfe für leeres Note-Feld
    - `unitCm` ("cm") — Einheit-Suffix für Width/Height (statt Inline-Hardcoding pro Komponente)
    - `unitDeg` ("°") — Einheit-Suffix für Rotation
    - `moreFields` ("Erweitert") / `lessFields` ("Weniger") — Toggle-Labels für Default-Collapsed
      Erweiterten-Sektion (D-11)
    - `accentColorReset` ("Zurücksetzen") — Reset-Knopf-Label im Color-Picker
    - `validation.widthMin` / `widthMax` / `heightMin` / `heightMax` — Form-Validierungsmeldungen
      mit konkreten Grenzwerten (Sub-Objekt `validation`, 4 Keys)

    Begründung: Diese Keys sind UX-Affordances (Placeholder, Einheit-Suffixe, Validierungs-
    Meldungen) die NICHT in D-23 explizit gelocked sind, aber für eine konsistente,
    untranslierbar-saubere UI erforderlich sind. Inline-String-Hardcoding würde gegen die
    i18n-Konvention verstoßen und Phase-13 (mögliche EN-Translation) blockieren.

    **Garantie:** Alle Locked D-23 Core-Keys aus CONTEXT.md sind vollständig erhalten —
    keine Umbenennung, keine Auslassung. Die Erweiterung ist additiv.

    ---

    Schritt 1 — de.json Edit:
    - Read `packages/shared/src/i18n/de.json` vollständig (mindestens den editor-Block).
    - Lokalisiere den schließenden `}` des `editor`-Objekts. Direkt davor (mit korrekter Komma-Position vor der vorhergehenden Key-Definition) füge den `elementEditor`-Block ein.
    - Verwende den Edit-Tool und übergebe den exakten String. ACHTUNG: Im JSON-File MÜSSEN native UTF-8-Bytes enthalten sein. Da die Bash-Shell auf Windows manchmal Codepage-Probleme produziert, NUTZE den Write/Edit-Tool direkt (keinen `cat << EOF`-Heredoc — siehe `critical_rules`).
    - Exakte Key-Liste (alle Strings auf Deutsch, UTF-8 native — Locked-Core + Claude's Discretion):
      ```
      title:                "Element bearbeiten"          # D-23 Locked Core
      name:                 "Name"                        # D-23 Locked Core
      namePlaceholder:      "z.B. Tomatenbeet"            # Claude's Discretion (UX placeholder)
      width:                "Breite"                      # D-23 Locked Core
      height:               "Höhe"                        # D-23 Locked Core
      unitCm:               "cm"                          # Claude's Discretion (unit suffix)
      unitDeg:              "°"                           # Claude's Discretion (unit suffix)
      rotation:             "Rotation"                    # D-23 Locked Core
      rotationPlaceholder:  "0–359"                       # Claude's Discretion (range hint)
      maintainRatio:        "Verhältnis beibehalten"      # D-23 Locked Core
      zOrder:               "Ebene"                       # D-23 Locked Core
      bringToFront:         "Ganz nach vorne"             # D-23 Locked Core
      bringForward:         "Eine Ebene vor"              # D-23 Locked Core
      sendBackward:         "Eine Ebene zurück"           # D-23 Locked Core
      sendToBack:           "Ganz nach hinten"            # D-23 Locked Core
      moreFields:           "Erweitert"                   # Claude's Discretion (D-11 toggle)
      lessFields:           "Weniger"                     # Claude's Discretion (D-11 toggle)
      note:                 "Notiz"                       # D-23 Locked Core
      notePlaceholder:      "Bemerkung zum Element …"     # Claude's Discretion (UX placeholder)
      plantedAt:            "Pflanzdatum"                 # D-23 Locked Core
      accentColor:          "Akzentfarbe"                 # D-23 Locked Core
      accentColorReset:     "Zurücksetzen"                # Claude's Discretion (reset button)
      cancel:               "Abbrechen"                   # D-23 Locked Core
      save:                 "Speichern"                   # D-23 Locked Core
      delete:               "Löschen"                     # D-23 Locked Core
      deleteConfirmTitle:   "Element löschen?"            # D-23 Locked Core
      deleteConfirmBody:    "Du kannst die Aktion sofort rückgängig machen."   # D-23 Locked Core
      deleteConfirmYes:     "Ja, löschen"                 # D-23 Locked Core
      deleteConfirmNo:      "Abbrechen"                   # D-23 Locked Core
      validation: {                                       # Claude's Discretion (form validation)
        widthMin:           "Breite muss mindestens 5 cm sein"
        widthMax:           "Breite darf höchstens 500 cm sein"
        heightMin:          "Höhe muss mindestens 5 cm sein"
        heightMax:          "Höhe darf höchstens 500 cm sein"
      }
      ```
    - Nach dem Edit: `node -e "JSON.parse(require('fs').readFileSync('packages/shared/src/i18n/de.json','utf-8'))"` muss exit 0 zurückgeben.

    Schritt 2 — de.editorElementEditor.test.ts:
    - Open `app/src/i18n/__tests__/de.editorElementEditor.test.ts` (Wave 0 Stub mit it.todo()-Slots).
    - Header übernehmen aus i18n.review-keys.test.ts:
      ```
      process.env['EXPO_PUBLIC_SUPABASE_URL'] = 'https://test.example';
      process.env['EXPO_PUBLIC_SUPABASE_ANON_KEY'] = 'test-anon-key';
      import de from '@spatenstich/shared/i18n/de';
      ```
    - Konvertiere it.todo()-Slots zu echten it()-Tests gemäß `<behavior>`-Liste.
    - Für UTF-8-Check:
      ```
      const block = (de as any).editor.elementEditor;
      const flatValues: string[] = [];
      const walk = (obj: any) => { for (const v of Object.values(obj)) { if (typeof v === 'string') flatValues.push(v); else if (typeof v === 'object' && v) walk(v); } };
      walk(block);
      // Positiv: mind. ein Wert enthält ein deutsches Sonderzeichen
      expect(flatValues.some(v => /[äöüÄÖÜß]/.test(v))).toBe(true);
      // Negativ: kein Wert nutzt ASCII-Fallback (Wortgrenze um false-positives wie "blue" zu vermeiden ist explizit gewollt)
      for (const v of flatValues) {
        expect(v).not.toMatch(/Hoehe|Loeschen|Groesse|zurueck|Erlaeuterung|Verhaeltnis|moegli|fuer|ueber/);
      }
      ```
    - Update Test-Datei-Header-Kommentar: `// Phase 09.1 Plan 05 GREEN: editor.elementEditor.* keys + UTF-8 Umlaute (D-23, D-24).`

    Schritt 3 — rowMappers.provenance.test.ts:
    - Open `app/src/lib/__tests__/rowMappers.provenance.test.ts`.
    - Verwende Pattern aus rowMappers.ts §Self-analog (PATTERNS.md Lines 597-604 — passthrough lesen + schreiben).
    - Test-Setup:
      ```
      import { planElementToDb, planElementToLocal } from '@/src/lib/mappers/rowMappers';
      // makeEl Helper aus Pattern S7 (zentral oder lokal definiert)
      ```
    - Pro Test: konstruiere PlanElementRow mit benanntem Subset von provenance-Feldern + dedizierten Spalten, runde durch `planElementToDb` → `planElementToLocal`, asserte `deep equal` für relevante Felder.
    - Für den D-20/D-21-Sicherungstest:
      ```
      const local = makeEl({ widthM: 1.5, heightM: 2.0, label: 'Beet A', provenance: { zOrder: 0, source: 'manual' } });
      const db = planElementToDb(local);
      // label ist eigene Spalte, kein provenance-Duplikat
      expect((db.provenance as any)?.label).toBeUndefined();
      expect((db.provenance as any)?.widthM).toBeUndefined();
      expect((db.provenance as any)?.heightM).toBeUndefined();
      // Round-Trip preserviert label intakt
      const back = planElementToLocal(db);
      expect(back.label).toBe('Beet A');
      ```
    - Update Test-Datei-Header-Kommentar: `// Phase 09.1 Plan 05 GREEN: provenance round-trip + D-19/D-20/D-21 column-discipline.`

    Schritt 4 — Tests laufen lassen:
    - `pnpm --filter app exec vitest run src/i18n/__tests__/de.editorElementEditor.test.ts src/lib/__tests__/rowMappers.provenance.test.ts --reporter=verbose`.
    - Bei Fehler: KEIN Anpassen der Tests — stattdessen de.json korrigieren oder rowMappers prüfen.

    Anti-Patterns:
    - KEIN Hartcodieren von ASCII-Fallbacks für Umlaute (Memory).
    - KEIN Schreiben von `label`/`widthM`/`heightM` in provenance (D-20/D-21).
    - KEIN Inline-Mock von rowMappers — die echte Implementierung MUSS round-trippen.
    - KEIN Edit von de.json via Bash-Heredoc oder echo — nur Write/Edit Tool (UTF-8-Sicherheit).
  </action>
  <verify>
    <automated>cd app && pnpm exec tsc --noEmit</automated>
    <automated>node -e "JSON.parse(require('fs').readFileSync('packages/shared/src/i18n/de.json','utf-8'))"</automated>
    <automated>cd app && pnpm exec vitest run src/i18n/__tests__/de.editorElementEditor.test.ts src/lib/__tests__/rowMappers.provenance.test.ts --reporter=verbose</automated>
    <automated>cd app && pnpm exec vitest run --reporter=dot</automated>
  </verify>
  <done>
    - de.json JSON-valid, enthält `editor.elementEditor.*` Block mit allen Pflicht-Keys (Locked Core + Claude's Discretion UX)
    - UTF-8 Umlaute nativ (Browser/Editor zeigt "Höhe" korrekt)
    - de.editorElementEditor.test.ts grün, alle it.todo() entfernt
    - rowMappers.provenance.test.ts grün, mindestens 9 it()-Tests
    - Vollständiger vitest-Lauf grün (Regression-Schutz)
    - `grep -c 'editor.elementEditor' packages/shared/src/i18n/de.json` ≥ 1 (Block existiert)
  </done>
</task>

<task type="auto">
  <name>Task 2: 09.1-HUMAN-VERIFY.md erstellen + ROADMAP.md auf Phase 09.1 aktualisieren</name>
  <files>
    .planning/phases/09.1-editor-element-bearbeitung/09.1-HUMAN-VERIFY.md,
    .planning/ROADMAP.md
  </files>
  <read_first>
    - `.planning/phases/09.1-editor-element-bearbeitung/09.1-VALIDATION.md` (Manual-Only Verifications Tabelle Lines 69-81)
    - `.planning/ROADMAP.md` (gesamter File, vor allem Phase 09.1 Entry)
    - `.planning/phases/09.1-editor-element-bearbeitung/09.1-CONTEXT.md` §domain (Phase Boundary für Goal-Formulierung)
    - Memory: feedback_show_project_id.md (Supabase project-ref `vitrqkzxkiqvadqfzrcx` im HUMAN-VERIFY header zeigen)
    - Memory: feedback_language.md (User-facing Prosa Deutsch)
  </read_first>
  <behavior>
    - 09.1-HUMAN-VERIFY.md enthält:
      - Header mit Phase-Name, Supabase project-ref `vitrqkzxkiqvadqfzrcx (Frankfurt, EU)`, Sprache: Deutsch (User-facing).
      - Sektion "Voraussetzungen": tsc grün, full vitest grün, kein offener Wave-Stub.
      - 7 Test-Cases als Checkliste mit konkreten Schritten (jeweils 4-7 Zeilen):
        1. Skia Long-Press öffnet Modal (iOS-Simulator)
        2. Web Doppelklick öffnet Modal (`expo start --web`)
        3. Hardware-Back schließt Modal (Android-Build)
        4. ESC schließt Modal (Web-Build)
        5. 15°-Snap UX-Feel (Drag Rotations-Handle auf Web)
        6. Drag-Latency < 16ms (Resize-Handle auf iOS-Simulator, 60fps-Schwellwert)
        7. Color-Picker Cross-Platform (iOS + Android + Web, Hex eintippen + persistieren)
      - Jeder Test-Case enthält: **Voraussetzung**, **Schritte** (nummeriert), **Erwartung**, **Bei Fehlschlag** (welcher Plan/Threat refs).
      - Footer: Abschluss-Sektion "Verifikations-Sign-Off" mit `[ ] Alle 7 Tests bestanden` + Datum-Feld + Signatur-Feld.
    - ROADMAP.md Phase 09.1 Entry wird aktualisiert:
      - `**Goal:**` Zeile ersetzt `[Urgent work - to be planned]` durch konkretes outcome-formuliertes Goal aus CONTEXT.md §domain. Vorschlag (anpassen wenn ROADMAP-Konvention abweicht): "Vollständige Element-Bearbeitung im Plan-Editor: Resize-Handles, Rotations-Handle, Properties-Modal (Name/Breite/Höhe/Rotation/Notiz/Pflanzdatum/Akzentfarbe) und Z-Order-Steuerung — auf Web (Doppelklick → Modal) und Mobile (Long-Press → Modal) konsistent."
      - `**Requirements**:` Zeile von `TBD` zu `D-01..D-24` (alle CONTEXT-Decisions, da Phase 09.1 keine REQ-IDs verwendet — siehe planning_context).
      - `**Plans:**` von `0 plans` zu `6 plans`.
      - Plan-Liste:
        ```
        - [ ] 09.1-00-test-scaffold-helper-modules-PLAN.md — Wave 0: Test-Stubs + Picker-Installs + 4 Helper-Module
        - [ ] 09.1-01-store-layer-PLAN.md — Wave 1: editorStore.editingElementId + zundo pause/resume + zOrder/rotationSnap algorithms
        - [ ] 09.1-02-modal-trigger-PLAN.md — Wave 2: ElementEditorModal + Doppelklick (Web) + LongPress (Skia) + hitTest.ts GREEN + Mount in plan/index.tsx
        - [ ] 09.1-03-canvas-handles-PLAN.md — Wave 3: ResizeHandle/RotationHandle für Skia + Web (4+1 Komponenten)
        - [ ] 09.1-04-render-pipeline-PLAN.md — Wave 3: sortByZOrder + Rotation-Transform in beiden Renderern
        - [ ] 09.1-05-i18n-verification-PLAN.md — Wave 3: editor.elementEditor.* keys + Mapper round-trip + HUMAN-VERIFY
        ```
  </behavior>
  <action>
    Schritt 1 — 09.1-HUMAN-VERIFY.md anlegen via Write-Tool:
    - Pfad: `.planning/phases/09.1-editor-element-bearbeitung/09.1-HUMAN-VERIFY.md`.
    - Header (Frontmatter optional): Title `# Phase 09.1 — Manuelle Verifikation`, Subheader `Supabase Projekt: vitrqkzxkiqvadqfzrcx (Frankfurt, EU)` (Memory feedback_show_project_id.md), Status `Pending`.
    - Voraussetzungs-Sektion auflisten (vitest grün, tsc grün, Plan 04 abgeschlossen).
    - Pro Test-Case (1-7) eine Sub-Sektion mit:
      ```
      ### Test N: <Behavior aus VALIDATION.md>
      **Plattform:** iOS-Simulator / Web / Android / etc.
      **Requirement:** D-XX
      **Voraussetzung:** App läuft auf Zielplattform, Element mit ID el-1 platziert.
      **Schritte:**
      1. ...
      2. ...
      **Erwartung:** ...
      **Bei Fehlschlag:** Plan 09.1-NN (Komponente X) erneut prüfen; Threat T-09.1-YYY.
      - [ ] Bestanden am: ____ durch: ____
      ```
    - Test 5 (15°-Snap): explizit erwähnen, dass Shift-Halten (Web) Snap deaktiviert (D-07) und im Modal die exakte Grad-Eingabe ohne Snap möglich ist.
    - Test 6 (Drag-Latency): empfiehlt Skia FPS-Overlay (`Skia.SkiaDomView.debugFlags`) oder visuelle Beurteilung.
    - Test 7 (Color-Picker): explizit alle drei Plattformen — wenn iOS/Android Native (`reanimated-color-picker@3.0.6`) und Web (Web-Fallback `<input type="color">` oder Library-Web-Build) abweichen, beide separat ankreuzen.
    - Footer "Verifikations-Sign-Off" mit Checkbox + Datum + Initialen-Feld.

    Schritt 2 — ROADMAP.md updaten via Edit-Tool:
    - Read ROADMAP.md, lokalisiere `### Phase 09.1: Editor-Element-Bearbeitung (INSERTED)`.
    - Edit den Block:
      - `**Goal:** [Urgent work - to be planned]` → `**Goal:** Vollständige Element-Bearbeitung im Plan-Editor — Resize-Handles (Eck-4-Punkt), Rotations-Handle (15°-Snap mit Shift-Bypass auf Web), Properties-Modal mit Name/Breite/Höhe/Rotation/Notiz/Pflanzdatum/Akzentfarbe und Photoshop-Style Z-Order-Buttons. Konsistent auf Web (Doppelklick öffnet Modal) und Mobile (Long-Press öffnet Modal).`
      - `**Requirements**: TBD` → `**Requirements**: D-01..D-24 (siehe 09.1-CONTEXT.md)`
      - `**Plans:** 0 plans` → `**Plans:** 6 plans`
      - Wenn unter "Plans:" bereits eine leere Sektion existiert, füge die 6 Markdown-Bullets ein (Behavior-Liste oben). Andernfalls Sektion neu anlegen.
    - WICHTIG: Andere Phasen NICHT verändern.

    Schritt 3 — Sanity-Check:
    - `grep -c 'editor-element-bearbeitung' .planning/ROADMAP.md` ≥ 6 (mind. 6 Plan-Bullets)
    - HUMAN-VERIFY.md existiert und enthält das Token `vitrqkzxkiqvadqfzrcx`
    - HUMAN-VERIFY.md enthält mind. 7 `### Test N:` Headlines

    Anti-Patterns:
    - KEINE Bash-Heredoc-Erzeugung (Memory + critical_rules) — Write/Edit Tool verwenden für UTF-8-Sicherheit.
    - KEIN Schreiben von Phase-09.1-Goals als Task-shape (`Build/Implement/Add ...`) — outcome-shape required (`User can ...`).
    - KEIN Touchen anderer Phasen in ROADMAP.md.
  </action>
  <verify>
    <automated>test -f .planning/phases/09.1-editor-element-bearbeitung/09.1-HUMAN-VERIFY.md && echo HUMAN-VERIFY-EXISTS</automated>
    <automated>grep -c 'editor-element-bearbeitung' .planning/ROADMAP.md</automated>
    <automated>grep -c 'vitrqkzxkiqvadqfzrcx' .planning/phases/09.1-editor-element-bearbeitung/09.1-HUMAN-VERIFY.md</automated>
    <automated>grep -cE '^### Test [1-7]:' .planning/phases/09.1-editor-element-bearbeitung/09.1-HUMAN-VERIFY.md</automated>
  </verify>
  <done>
    - 09.1-HUMAN-VERIFY.md existiert mit allen 7 Test-Cases, jeweils mit Schritten + Erwartung + Sign-Off-Checkbox
    - Supabase project-ref im HUMAN-VERIFY Header (Memory feedback_show_project_id.md)
    - ROADMAP.md Phase 09.1 zeigt korrekten Goal-Outcome + `**Plans:** 6 plans` + 6 PLAN.md-Bullets
    - Andere Phasen-Entries in ROADMAP.md unverändert (manual diff prüfen)
  </done>
</task>

<task type="auto">
  <name>Task 3: D-XX Coverage Audit über alle sechs Plans</name>
  <files>
    .planning/phases/09.1-editor-element-bearbeitung/09.1-05-SUMMARY.md
  </files>
  <read_first>
    - `.planning/phases/09.1-editor-element-bearbeitung/09.1-CONTEXT.md` (alle D-01..D-24)
    - Alle sechs PLAN.md `requirements:` Frontmatter-Felder via grep
  </read_first>
  <behavior>
    - Erzeuge eine 24-zeilige Audit-Tabelle (D-01..D-24), Spalten:
      | D-XX | Topic (kurz) | Covered by Plan(s) | Status |
    - "Covered by Plan(s)" listet jeden PLAN.md, dessen `requirements:` Frontmatter D-XX enthält.
    - "Status" ist ✅ wenn mindestens ein Plan D-XX trägt, ❌ wenn keiner. ❌ ist ein BLOCKING ERROR.
    - Bei BLOCKING ERROR: füge unter der Tabelle eine "## Gaps" Sektion ein und benenne, welcher Plan ergänzt werden muss.
    - Erwartete Verteilung (nach Planung-Distribution):
      - D-01..D-03 (Trigger-Gesten): Plan 02
      - D-04..D-07 (Resize+Rotation Interaktion): Plans 01, 03
      - D-08 (Skia 2-Finger-Rotation bleibt aktiv): Plan 03
      - D-09..D-11 (Modal-Felder): Plans 02, 05
      - D-12..D-15 (Z-Order): Plans 00, 01, 02, 04
      - D-16..D-18 (Modal-Pattern, ESC, Hardware-Back): Plan 02
      - D-19 (provenance jsonb keine neue Migration): Plans 01, 05
      - D-20/D-21 (widthM/heightM/label dedizierte Spalten): Plans 02, 04, 05
      - D-22 (gestureActive zundo pause/resume): Plans 01, 03
      - D-23/D-24 (i18n editor.elementEditor + UTF-8): Plans 00, 02, 05
  </behavior>
  <action>
    Schritt 1 — Frontmatter-Inventar erstellen:
    - `grep -nE '^requirements:' .planning/phases/09.1-editor-element-bearbeitung/09.1-*-PLAN.md` zur Übersicht der gelisteten D-XX-Mengen.
    - Pro Plan extrahiere die `requirements:`-Liste.

    Schritt 2 — Audit-Tabelle generieren:
    - Erstelle `.planning/phases/09.1-editor-element-bearbeitung/09.1-05-SUMMARY.md` (das ist gleichzeitig die End-of-Plan SUMMARY und Coverage-Audit).
    - Struktur:
      ```
      # 09.1-05 SUMMARY — i18n + Verification + Coverage Audit

      **Phase:** 09.1-editor-element-bearbeitung
      **Plan:** 05
      **Supabase Projekt:** vitrqkzxkiqvadqfzrcx (Frankfurt, EU)

      ## What Shipped
      - editor.elementEditor.* i18n-Block in de.json (28+ keys, UTF-8 nativ)
      - de.editorElementEditor.test.ts grün (20+ assertions)
      - rowMappers.provenance.test.ts grün (9+ round-trip assertions)
      - 09.1-HUMAN-VERIFY.md erstellt (7 Manual-Only Verifications)
      - ROADMAP.md Phase 09.1 finalisiert (Goal + 6 plans)

      ## D-XX Coverage Audit

      | D-XX | Topic | Covered by Plan(s) | Status |
      |------|-------|---------------------|--------|
      | D-01 | Trigger-Geste plattform-spezifisch | 09.1-02 | ✅ |
      | D-02 | Long-Press disambiguation | 09.1-02 | ✅ |
      | D-03 | Kein Edit-Toolbar-Button | 09.1-02 | ✅ |
      | D-04 | Hybrid Modal + Handles | 09.1-01, 09.1-03 | ✅ |
      | D-05 | Eck-Handles 4-Punkt | 09.1-03 | ✅ |
      | D-06 | Rotations-Handle mittig | 09.1-03 | ✅ |
      | D-07 | 15° Snap + Shift-Bypass | 09.1-01, 09.1-03 | ✅ |
      | D-08 | Skia 2-Finger-Rotation bleibt | 09.1-03 | ✅ |
      | D-09 | Modal-Kern-Felder | 09.1-02, 09.1-05 | ✅ |
      | D-10 | Modal-Erweiterte-Felder | 09.1-02, 09.1-05 | ✅ |
      | D-11 | Default-Collapsed | 09.1-02, 09.1-05 | ✅ |
      | D-12 | Photoshop-Style Z-Order-Buttons | 09.1-00, 09.1-02 | ✅ |
      | D-13 | provenance.zOrder render-sort | 09.1-00, 09.1-01, 09.1-04 | ✅ |
      | D-14 | Lazy non-renumbering Konflikt | 09.1-00, 09.1-01, 09.1-04 | ✅ |
      | D-15 | Z-Order plan-weit | 09.1-00, 09.1-01, 09.1-04 | ✅ |
      | D-16 | Modal-Pattern (Card-Shell) | 09.1-02 | ✅ |
      | D-17 | Esc / Backdrop-Tap schließt | 09.1-02 | ✅ |
      | D-18 | Hardware-Back schließt | 09.1-02 | ✅ |
      | D-19 | provenance jsonb keine Migration | 09.1-00, 09.1-01, 09.1-05 | ✅ |
      | D-20 | widthM/heightM dedizierte Spalten | 09.1-04, 09.1-05 | ✅ |
      | D-21 | label dedizierte Spalte | 09.1-02, 09.1-05 | ✅ |
      | D-22 | gestureActive zundo pause/resume | 09.1-00, 09.1-01, 09.1-03 | ✅ |
      | D-23 | editor.elementEditor.* Keys | 09.1-00, 09.1-05 | ✅ |
      | D-24 | UTF-8 native Umlaute | 09.1-00, 09.1-05 | ✅ |

      ## D-21 Coverage Note
      D-21 (label dedizierte Spalte, NICHT in provenance) wird in zwei Plänen abgedeckt:
      - **09.1-02:** Implementation — Modal-Save schreibt `label` an dedizierte Spalte
        (handleSave: `onSave({ widthM, heightM, label, provenance: nextProvenance })`),
        provenance enthält KEIN label-Key. Test 14 in ElementEditorModal.test.tsx
        assertet die Column-Discipline.
      - **09.1-05:** Audit — rowMappers.provenance.test.ts round-trip-Test prüft,
        dass `planElementToDb(local).provenance.label === undefined` UND
        `planElementToLocal(db).label === 'Beet A'` (D-20/D-21-Sicherungstest).

      ## Test Results
      - tsc: green
      - vitest full run: green
      - JSON.parse de.json: ok

      ## Next Steps
      - User: Run `/gsd-verify-work 09.1` after manual verification per 09.1-HUMAN-VERIFY.md
      - User: Mark each ROADMAP.md plan-checkbox `[x]` after `/gsd-verify-work` greenlight
      ```
    - Validiere: Falls ein D-XX als ❌ markiert ist, STOP und füge stattdessen eine "## ⚠ Coverage Gap" Sektion ein, die der Orchestrator weiterleitet. KEIN Plan committen, wenn Coverage incomplete.

    Schritt 3 — Validation der Plan-Strukturen via gsd-sdk:
    - Für jeden 09.1-*-PLAN.md:
      ```
      gsd-sdk query frontmatter.validate <PLAN_PATH> --schema plan
      gsd-sdk query verify.plan-structure <PLAN_PATH>
      ```
    - Bei `valid: false` oder errors: SUMMARY um Fix-Required-Sektion erweitern; sonst SUMMARY bestätigt all-green.

    Anti-Patterns:
    - KEIN Vortäuschen ✅ wenn der Plan-`requirements:` Frontmatter das D-XX nicht enthält.
    - KEIN Editieren anderer Plans um Coverage zu erzwingen — wenn ein D-XX fehlt, eskaliere als Gap.
  </action>
  <verify>
    <automated>test -f .planning/phases/09.1-editor-element-bearbeitung/09.1-05-SUMMARY.md && echo SUMMARY-EXISTS</automated>
    <automated>grep -cE '^\| D-(0[1-9]|1[0-9]|2[0-4]) ' .planning/phases/09.1-editor-element-bearbeitung/09.1-05-SUMMARY.md</automated>
    <automated>grep -c '❌' .planning/phases/09.1-editor-element-bearbeitung/09.1-05-SUMMARY.md</automated>
  </verify>
  <done>
    - 09.1-05-SUMMARY.md enthält alle 24 D-XX Audit-Zeilen
    - Audit-Result: 0 Vorkommen von ❌ (alle D-01..D-24 covered)
    - frontmatter.validate + verify.plan-structure für alle sechs Plans grün (SUMMARY berichtet)
  </done>
</task>

</tasks>

<verification>
**Plan-weite Checks:**
- `cd app && pnpm exec tsc --noEmit` exit 0
- `cd app && pnpm exec vitest run --reporter=dot` exit 0 (alle Phase 09.1 Tests grün)
- `node -e "JSON.parse(require('fs').readFileSync('packages/shared/src/i18n/de.json','utf-8'))"` exit 0
- `grep -c 'elementEditor' packages/shared/src/i18n/de.json` ≥ 1
- `grep -cE '\\b(ae|oe|ue|ss)\\b' packages/shared/src/i18n/de.json` → 0 für den editor.elementEditor-Block (UTF-8 strict — gröberer Grep kann false-positives liefern wenn andere Blöcke englische Wörter enthalten; Test-File übernimmt fein-granularen Block-Check)
- 09.1-HUMAN-VERIFY.md existiert mit Supabase-Token + 7 Tests
- 09.1-05-SUMMARY.md existiert mit D-XX Audit-Tabelle ohne ❌
- ROADMAP.md Phase 09.1: `**Plans:** 6 plans` + 6 Bullet-Einträge
</verification>

<success_criteria>
- Alle 28+ Modal-Strings auf Deutsch in de.json (D-09/D-10/D-11/D-23 Locked Core + Claude's Discretion UX-Affordances)
- UTF-8 Umlaute nativ in allen Strings (D-24, Memory)
- rowMappers round-trippen alle 09.1-relevanten provenance-Felder (D-19) verlustfrei
- label/widthM/heightM exklusiv auf dedizierten Spalten, nie in provenance (D-20/D-21)
- 09.1-HUMAN-VERIFY.md deckt alle 7 Manual-Only Verifications aus VALIDATION.md ab
- ROADMAP.md zeigt finalen Phase-09.1 Eintrag (Goal + 6 Plans)
- D-XX Audit über alle sechs Plans: alle 24 Decisions covered, keine Gaps; D-21 listet beide Plans (09.1-02 + 09.1-05)
- gsd-sdk frontmatter.validate + verify.plan-structure für alle sechs Plans grün
- Plan-Sample-Run innerhalb 30 Sekunden Feedback-Latency-Budget
</success_criteria>

<output>
Create `.planning/phases/09.1-editor-element-bearbeitung/09.1-05-SUMMARY.md` als Teil von Task 3 (siehe action).
Die SUMMARY enthält gleichzeitig den End-of-Plan-Report UND den D-XX Coverage Audit.

Final Commit (durch Orchestrator nach allen Wave-3-Plans):
`git commit -m "docs(09.1): create phase plan" -- .planning/phases/09.1-editor-element-bearbeitung/09.1-*-PLAN.md .planning/ROADMAP.md`
</output>
</content>
</invoke>