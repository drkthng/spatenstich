---
phase: 09.1-editor-element-bearbeitung
plan: 03
type: execute
wave: 3
depends_on:
  - 09.1-02
files_modified:
  - app/src/components/editor/ResizeHandle.tsx
  - app/src/components/editor/RotationHandle.tsx
  - app/src/components/editor/web/WebResizeHandle.tsx
  - app/src/components/editor/web/WebRotationHandle.tsx
  - app/src/components/editor/EditorCanvas.tsx
  - app/src/components/editor/web/WebPlanEditor.tsx
  - app/src/lib/editor/handleGeometry.ts
  - app/src/lib/editor/__tests__/handleGeometry.test.ts
autonomous: true
requirements:
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-22

must_haves:
  truths:
    - "Selected Element zeigt 4 Eck-Resize-Handles + 1 Rotations-Handle (D-05/D-06) — Web SVG <circle> und Skia primitives"
    - "Resize-Handle Drag aktualisiert widthM/heightM in Echtzeit (Live-Preview); Commit über updateElement bei onEnd/mouseup"
    - "Rotation-Handle snapped auf 15° per snapRotation; Shift-Taste (Web) deaktiviert Snap (D-07)"
    - "2-Finger Skia Rotations-Geste bleibt aktiv (additiv, nicht ersetzt — D-08)"
    - "MVP Carve-Out: Resize-Handles sind versteckt wenn rotateDeg !== 0 (RESEARCH A1) — User auf Modal-Numeric-Input zurückgeführt"
    - "Drag-Phase gestureActive=true ist gesetzt → zundo pausiert → eine Snapshot bei onEnd (D-22)"
    - "INPUT/TEXTAREA-Guard auf Shift-Listener verhindert hijacking während Modal-Eingabe (Pitfall 7)"
  artifacts:
    - path: "app/src/components/editor/ResizeHandle.tsx"
      provides: "Skia corner handle with Pan-Gesture + commitResize callback"
      min_lines: 40
    - path: "app/src/components/editor/RotationHandle.tsx"
      provides: "Skia top handle with Pan-Gesture + snapRotation + commitRotationAbsolute callback"
      min_lines: 40
    - path: "app/src/components/editor/web/WebResizeHandle.tsx"
      provides: "SVG circle + window mousemove → widthM/heightM update + setGestureActive lifecycle"
      min_lines: 50
    - path: "app/src/components/editor/web/WebRotationHandle.tsx"
      provides: "SVG circle + window mousemove → snapRotation(deg, isShiftDown) + provenance.rotateDeg update"
      contains: "isShiftDown"
      min_lines: 50
    - path: "app/src/lib/editor/handleGeometry.ts"
      provides: "GREEN implementations of computeCornerHandles + computeRotationHandle; MVP carve-out documented inline"
      min_lines: 30
  key_links:
    - from: "app/src/components/editor/EditorCanvas.tsx"
      to: "app/src/components/editor/ResizeHandle.tsx + RotationHandle.tsx"
      via: "conditional render when selection && rotateDeg === 0 (for Resize)"
      pattern: "selection.*ResizeHandle|ResizeHandle.*selection"
    - from: "app/src/components/editor/web/WebPlanEditor.tsx"
      to: "WebResizeHandle + WebRotationHandle"
      via: "conditional render when selection set"
      pattern: "selection.*WebResizeHandle|WebResizeHandle.*selection"
    - from: "app/src/components/editor/web/WebRotationHandle.tsx"
      to: "app/src/lib/editor/rotationSnap.ts"
      via: "snapRotation(rawDeg, isShiftDown) call"
      pattern: "snapRotation\\s*\\("
    - from: "app/src/components/editor/RotationHandle.tsx"
      to: "app/src/lib/editor/rotationSnap.ts"
      via: "snapRotation(rawDeg, false) call (no Shift on mobile)"
      pattern: "snapRotation\\s*\\("
---

<objective>
Wave-3 Canvas-Handles: 4 Eck-Resize-Handles + 1 Rotations-Handle als direkte Manipulation auf beiden Renderern (Web SVG + Skia). Drag aktualisiert widthM/heightM/rotateDeg live; Commit bei onEnd/mouseup. 15°-Snap auf Rotation mit Shift-Bypass (Web).

**Supabase Project (immer anzeigen):** vitrqkzxkiqvadqfzrcx (Frankfurt, EU)

Purpose: D-04 Hybrid-Ansatz (Modal + Handles) zweite Hälfte — schnelle Anpassung direkt auf Canvas. Plan 02 deckt Modal + Trigger-Gesten ab; dieser Plan deckt Handles ab.

**Revision Note (Checker BLOCKER #1 + WARNING #2):**
- Wave geändert von 2 → 3, `depends_on` von `[09.1-01]` → `[09.1-02]`. Grund: Plans 02 + 03 modifizieren beide `EditorCanvas.tsx` und `WebPlanEditor.tsx`. Sequentialisierung verhindert File-Race zwischen parallelen Waves.
- Tasks gesplittet: Task 1 = Skia handles (ResizeHandle, RotationHandle, EditorCanvas-Mount). Task 2 = Web handles (WebResizeHandle, WebRotationHandle, WebPlanEditor-Mount). Pro Task eigene Wave-0-Tests + komponenten-spezifische `automated verify` (vitest run).
- Wave-0-Tests für die 4 Handle-Komponenten existieren bereits in Plan 00 (`__tests__/...rotation/longPress.test.tsx` plus ggf. weitere bei Bedarf). Plan 03 ergänzt minimalste Render-Tests pro Handle (siehe Behavior).

Output: 4 neue Handle-Komponenten + 2 modifizierte Renderer + GREEN handleGeometry.ts + handleGeometry-Tests GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/09.1-editor-element-bearbeitung/09.1-CONTEXT.md
@.planning/phases/09.1-editor-element-bearbeitung/09.1-PATTERNS.md
@.planning/phases/09.1-editor-element-bearbeitung/09.1-VALIDATION.md
@.planning/phases/09.1-editor-element-bearbeitung/09.1-01-SUMMARY.md
@.planning/phases/09.1-editor-element-bearbeitung/09.1-02-SUMMARY.md

# Reference patterns
@app/src/components/editor/EditorCanvas.tsx
@app/src/components/editor/web/WebPlanEditor.tsx
@app/src/lib/editor/handleGeometry.ts
@app/src/lib/editor/rotationSnap.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Skia Handles — handleGeometry GREEN + ResizeHandle + RotationHandle + EditorCanvas-Mount</name>
  <files>
    app/src/lib/editor/handleGeometry.ts,
    app/src/lib/editor/__tests__/handleGeometry.test.ts,
    app/src/components/editor/ResizeHandle.tsx,
    app/src/components/editor/RotationHandle.tsx,
    app/src/components/editor/EditorCanvas.tsx
  </files>
  <behavior>
    - Test 1 (handleGeometry): computeCornerHandles tl == (xM - widthM/2, yM - heightM/2); tr/bl/br symmetrisch
    - Test 2 (handleGeometry): computeCornerHandles für element mit xM=2 yM=3 widthM=1 heightM=0.5 → tl=(1.5, 2.75), br=(2.5, 3.25)
    - Test 3 (handleGeometry): computeRotationHandle returns position offset above top-center, offsetPx scaled by viewport scale
    - Test 4 (handleGeometry): MVP carve-out documented — JSDoc on file mentions rotateDeg !== 0 hides resize
    - Test 5 (handleGeometry): exports do not throw (smoke check that Wave 0 throws are gone)
    - Test 6 (ResizeHandle render-smoke): renders 4 Skia Circle handles when corner prop is set per-instance (1 Circle per render)
    - Test 7 (RotationHandle render-smoke): renders a Skia Circle and invokes snapRotation on simulated pan-end
    - Test 8 (EditorCanvas mount): ResizeHandle conditionally rendered only when selection && rotateDeg === 0; RotationHandle rendered when selection (any rotateDeg)
  </behavior>
  <read_first>
    - app/src/lib/editor/handleGeometry.ts (Wave-0 Stubs)
    - app/src/lib/geometry/bedLayout.ts (Pure-Math-Analog — polygonToBbox Pattern)
    - app/src/lib/editor/rotationSnap.ts (GREEN from Plan 01)
    - app/src/components/editor/EditorCanvas.tsx (Lines 87-94 onGestureBegin/onGestureEnd useCallback Pattern S3; Lines 98-113 commitRotation; Lines 117-127 Pan gesture pattern; Lines 157-171 Rotation gesture; Plan-02-Änderungen für findElementAtPoint-Import)
    - .planning/phases/09.1-editor-element-bearbeitung/09.1-PATTERNS.md (§ResizeHandle.tsx, §RotationHandle.tsx, §handleGeometry.ts, §Pattern S3 gestureActive discipline)
    - .planning/phases/09.1-editor-element-bearbeitung/09.1-RESEARCH.md §Pattern 3 (Resize+Rotation Handles Geometry) + §Pitfall 4 (resize-handle drift after rotation) + §Pitfall 8 (zundo pause not resumed after error)
    - app/src/components/editor/__tests__/EditorCanvas.rotation.test.tsx (Wave-0 Tests für transform-Array — bleibt Plan 04, hier nur als Pattern-Referenz für Skia-Render-Tests)
  </read_first>
  <action>
    Schritt 1 — handleGeometry.ts GREEN (per PATTERNS.md §handleGeometry.ts + RESEARCH §Pattern 3):

    a) `computeCornerHandles(el: PlanElementRow): { tl, tr, bl, br }`:
       - Each corner = `{ xM: el.xM ± el.widthM/2, yM: el.yM ± el.heightM/2 }`.
       - tl = top-left, tr = top-right, bl = bottom-left, br = bottom-right.
       - Pure function, no React, no state.

    b) `computeRotationHandle(el: PlanElementRow, offsetPx: number, scale: number): { xM, yM }`:
       - Position mittig über element top-edge: `xM = el.xM`, `yM = el.yM - el.heightM/2 - (offsetPx / scale)`.
       - offsetPx default 20 (D-06: 16–24px Offset, mid-Bereich).
       - scale = viewport scale (px per garden meter), nötig damit offset-in-px konstant aussieht beim Zoomen.

    c) JSDoc oben am file (MVP Carve-Out per RESEARCH A1):
       - "TODO v1.1: rotated-resize math currently NOT implemented. Caller MUST hide resize handles when (el.provenance?.rotateDeg ?? 0) !== 0. Until then, user falls back to Modal numeric width/height input (D-04 Hybrid: handles are the optional fast-path, Modal is precise-path)."

    Schritt 2 — handleGeometry.test.ts GREEN-fill:
       - Import + makeEl factory (Pattern S7).
       - 5 it() bodies pro Tests 1-5 oben.

    Schritt 3 — ResizeHandle.tsx (Skia, per PATTERNS.md §ResizeHandle.tsx):

    a) Props: `{ elementId: string; corner: 'tl' | 'tr' | 'bl' | 'br'; xM: number; yM: number; scale: number; }`.

    b) Imports: `Circle` aus `@shopify/react-native-skia`; `Gesture, GestureDetector` aus `react-native-gesture-handler`; `runOnJS, useSharedValue` aus `react-native-reanimated`; `useEditorStore`.

    c) Pan gesture pattern (verbatim aus EditorCanvas:117-127):
       - `.onBegin(() => { runOnJS(onGestureBegin)(); })` — setGestureActive(true).
       - `.onChange((e) => { /* SharedValue accumulation für Live-Preview */ })`.
       - `.onEnd((e) => { runOnJS(commitResize)(finalW, finalH); runOnJS(onGestureEnd)(); })` — setGestureActive(false).

    d) Resize-Math (per RESEARCH §Pattern 3):
       - Anchored at opposite corner. Für `corner === 'br'`: newWidthM = startWidthM + e.translationX / scale, newHeightM = startHeightM + e.translationY / scale.
       - Für `tl`: -e.translationX / scale, -e.translationY / scale, plus xM/yM shift weil Anker bewegt sich.
       - Math wird in worklet als pure helper aufgerufen (z.B. `computeResizeFromDrag(corner, startW, startH, dxPx, dyPx, scale)`).
       - MIN_WIDTH_M = 0.05 (5cm — CONTEXT.md discretion), clamp.

    e) commitResize callback (JS-thread):
       - useEditorStore.getState().updateElement(elementId, { widthM: finalW, heightM: finalH }).
       - KEIN provenance touch (Pattern S2 ist hier nicht relevant; widthM/heightM sind dedicated columns per D-20).

    f) Render `<Circle cx={xM} cy={yM} r={6 / scale} color="#2DD4BF" />` mit cyan-Akzent (passend zu selection-stroke per CONTEXT.md discretion).

    g) Pitfall 8 mitigation: wrap commitResize + onGestureEnd in try/finally — auch bei Error setGestureActive(false).

    Schritt 4 — RotationHandle.tsx (Skia, per PATTERNS.md §RotationHandle.tsx):

    a) Props: `{ elementId: string; xM: number; yM: number; centerXM: number; centerYM: number; scale: number; }`.

    b) Pan gesture: `.onBegin` setGestureActive(true); `.onChange` compute angle = `Math.atan2(currentY - centerYM, currentX - centerXM) * 180 / PI`, apply `snapRotation(angle, false)` (no Shift on mobile per A7), live-preview via SharedValue.

    c) `.onEnd((e) => { runOnJS(commitRotationAbsolute)(snappedDeg); runOnJS(onGestureEnd)(); })`.

    d) commitRotationAbsolute(deg): reuses Pattern S2 — read prev provenance, spread, set rotateDeg = deg. Differs from existing commitRotation (which is DELTA-based) — siehe PATTERNS.md §RotationHandle.tsx note "sibling commitRotationAbsolute(deg)".

    e) Render `<Circle cx={xM} cy={yM} r={6 / scale} color="#2DD4BF" />`.

    f) D-08 additive: this handle co-exists with existing 2-finger Skia rotation gesture in EditorCanvas. Both paths converge on updateElement.

    Schritt 5 — EditorCanvas.tsx Handle-Mount:
    - After element render section, conditional render handles for selected element:
       - `{selection && selectedEl && rotateDeg === 0 && computeCornerHandles(selectedEl) → 4 <ResizeHandle> }` (MVP Carve-Out hide-when-rotated).
       - `{selection && selectedEl && <RotationHandle ... />}` (always show, even when rotated — user can still rotate).
    - Achtung: Plan 02 hat in EditorCanvas.tsx bereits `findElementAtPoint`-Import und LongPress-Handler ergänzt. Diese NICHT überschreiben oder duplizieren — nur die neuen Handle-Mounts hinzufügen.

    Schritt 6 — Komponenten-Tests (ergänzend zu Wave-0):
    - Falls Wave-0 keine dedizierten Render-Tests für ResizeHandle/RotationHandle hat, lege minimalste Smoke-Tests in `app/src/components/editor/__tests__/ResizeHandle.test.tsx` und `RotationHandle.test.tsx` an (1-2 it() pro Datei, gemockte Gesture/Skia). Tests 6-7 in der Behavior-Liste.
    - Falls die Tests bereits via EditorCanvas.rotation.test.tsx (Plan 04) oder EditorCanvas.longPress.test.tsx (Plan 02) abgedeckt sind, dokumentiere im Plan-Body warum keine eigenen Tests nötig sind. Test 8 (EditorCanvas-Mount-Gating) wird in EditorCanvas.rotation.test.tsx in Plan 04 mitgetestet (siehe bestehende sortByZOrder + Group transform Tests).

    KEINE fenced code im action — Component-Bodies + math live in PATTERNS.md + RESEARCH §Pattern 3.
  </action>
  <verify>
    <automated>cd app && pnpm exec vitest run handleGeometry --reporter=dot 2>&1 | tail -25</automated>
    <automated>cd app && pnpm exec tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `handleGeometry.ts` no longer contains `throw new Error('TODO Plan` (grep returns 0).
    - `handleGeometry.ts` exports `computeCornerHandles` and `computeRotationHandle` with real bodies (grep `^export function (computeCornerHandles|computeRotationHandle)` returns 2 hits).
    - `handleGeometry.ts` JSDoc contains literal `rotateDeg !== 0` to document MVP carve-out (grep `rotateDeg\s*!==\s*0` returns 1+ hit in comments).
    - Skia handle component files exist (ls `ResizeHandle.tsx` and `RotationHandle.tsx`).
    - `ResizeHandle.tsx` imports `Gesture, GestureDetector` (grep returns 1+ hit).
    - `RotationHandle.tsx` invokes `snapRotation(` (grep returns 1+ hit).
    - EditorCanvas.tsx conditionally renders ResizeHandle guarded by `rotateDeg === 0` (grep `rotateDeg\s*===\s*0` returns 1+ hit in EditorCanvas.tsx) — MVP Carve-Out.
    - EditorCanvas.tsx LongPress + findElementAtPoint Code aus Plan 02 unverändert vorhanden (grep `findElementAtPoint` returns 1+ hit; grep `\.minDuration\(500\)` returns 1+ hit).
    - `handleGeometry.test.ts` zero `it.todo`; 5 `it(` blocks.
    - `pnpm --filter app exec vitest run handleGeometry` exits 0.
    - `pnpm --filter app exec tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    Handle-Geometry-Math ist GREEN; 2 Skia-Handle-Komponenten existieren mit Pattern S3 + Pitfall 8 try/finally; EditorCanvas mounted die Handles unter MVP Carve-Out (rotateDeg === 0 für Resize). Plan 02 Code (LongPress + findElementAtPoint) bleibt intakt.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Web Handles — WebResizeHandle + WebRotationHandle (Shift-Bypass) + WebPlanEditor-Mount</name>
  <files>
    app/src/components/editor/web/WebResizeHandle.tsx,
    app/src/components/editor/web/WebRotationHandle.tsx,
    app/src/components/editor/web/WebPlanEditor.tsx
  </files>
  <behavior>
    - Test 1 (WebResizeHandle): renders a single SVG <circle> per corner with cursor 'nwse-resize' or 'nesw-resize'
    - Test 2 (WebResizeHandle): onMouseDown calls setGestureActive(true); window mouseup calls setGestureActive(false); try/finally on cleanup
    - Test 3 (WebResizeHandle): widthM/heightM update on mousemove via updateElement (no provenance touch)
    - Test 4 (WebRotationHandle): renders SVG <circle> with cursor 'grab'
    - Test 5 (WebRotationHandle): keydown 'Shift' sets isShiftDown=true; keyup sets false; INPUT/TEXTAREA target guards listener (Pitfall 7)
    - Test 6 (WebRotationHandle): on mousemove computes deg via atan2 then snapRotation(rawDeg, isShiftDown); updateElement called with provenance spread including rotateDeg=newDeg
    - Test 7 (WebPlanEditor-Mount): renders 4 WebResizeHandle + 1 WebRotationHandle when selection && rotateDeg === 0; rotates-only case skips Resize
  </behavior>
  <read_first>
    - app/src/components/editor/web/WebPlanEditor.tsx (Lines 99-114 Web keydown effect; Lines 119-152 window mousemove drag effect; Lines 199-215 handleElementMouseDown; nach Plan 02: onDoubleClick + MOUSE_DRAG_THRESHOLD_PX vorhanden)
    - .planning/phases/09.1-editor-element-bearbeitung/09.1-PATTERNS.md (§WebResizeHandle.tsx, §WebRotationHandle.tsx, §Pattern S5 Shift-Listener)
    - .planning/phases/09.1-editor-element-bearbeitung/09.1-RESEARCH.md §Pattern 4 (Shift-key for free rotation) + §Pitfall 7 (Shift key INPUT/TEXTAREA guard) + §Pitfall 8 (zundo pause not resumed)
    - app/src/components/editor/web/WebPlanEditor.tsx (Plan-02-Änderungen: onDoubleClick + Drag-Threshold)
    - app/src/lib/editor/rotationSnap.ts (GREEN aus Plan 01)
    - app/src/lib/editor/handleGeometry.ts (GREEN aus Plan 03 Task 1)
  </read_first>
  <action>
    Schritt 1 — WebResizeHandle.tsx (per PATTERNS.md §WebResizeHandle.tsx + Window mousemove pattern):

    a) Props: `{ elementId: string; corner: 'tl' | 'tr' | 'bl' | 'br'; xPx: number; yPx: number; scale: number; }`.

    b) Render `<circle cx={xPx} cy={yPx} r={6} fill="#2DD4BF" stroke="#0F766E" strokeWidth={1} style={{ cursor: ... }} />` (cursor = 'nwse-resize' für tl/br, 'nesw-resize' für tr/bl).

    c) onMouseDown handler: e.stopPropagation(), capture startMouseX/Y + elStartWidthM/HeightM, setGestureActive(true) via store, register dragRef.

    d) Window mousemove + mouseup (verbatim Pattern aus WebPlanEditor.tsx:119-152):
       - useEffect when dragRef set: window.addEventListener('mousemove', onMove); onMove computes new widthM/heightM per corner anchoring (use computeResizeFromDrag pure helper if extracted); call useEditorStore.getState().updateElement(...).
       - onMouseUp: clear dragRef, setGestureActive(false).
       - Cleanup window listeners on unmount.

    e) Pitfall 8: try/finally around drag-cleanup — auch bei Error muss setGestureActive(false) feuern.

    Schritt 2 — WebRotationHandle.tsx (per PATTERNS.md §WebRotationHandle.tsx + Pattern S5 Shift-Listener):

    a) Props: `{ elementId: string; xPx: number; yPx: number; centerXPx: number; centerYPx: number; }`.

    b) Shift-key listener (Pattern S5 + RESEARCH §Pitfall 7):
       - `const [isShiftDown, setIsShiftDown] = useState(false);`
       - useEffect with `window.addEventListener('keydown'/'keyup', onKey)`; INPUT/TEXTAREA guard via `if ((e.target as HTMLElement)?.tagName === 'INPUT' || (e.target as HTMLElement)?.tagName === 'TEXTAREA') return;`.
       - onKey sets isShiftDown to e.shiftKey.

    c) onMouseDown + Window mousemove:
       - onMouseDown setGestureActive(true) + dragRef set.
       - onMove computes raw deg via `Math.atan2(mouseY - centerYPx, mouseX - centerXPx) * 180 / PI`, then `const newDeg = snapRotation(rawDeg, isShiftDown);`.
       - updateElement(elementId, { provenance: { ...prev, rotateDeg: newDeg } }) — Pattern S2.
       - onMouseUp: clear dragRef, setGestureActive(false).
       - Cleanup window listeners on unmount.

    d) Render `<circle ... style={{ cursor: 'grab' }} ... />`.

    e) D-07 Shift-Override: when isShiftDown, snap is bypassed; die `snapRotation` Funktion handhabt das bereits intern.

    f) Pitfall 8 mitigation: try/finally um Drag-Cleanup.

    Schritt 3 — WebPlanEditor.tsx Handle-Mount:
    - Im SVG-Render-Block, nach den existierenden Element-Renders: conditional render handles für selected element.
       - `{selection && selectedEl && rotateDeg === 0 && computeCornerHandles(selectedEl) → 4 <WebResizeHandle xPx={corner.xM * scale} yPx={corner.yM * scale} ... />}` (MVP Carve-Out).
       - `{selection && selectedEl && <WebRotationHandle ... />}` (always show).
    - WICHTIG: Plan 02 Code (onDoubleClick, MOUSE_DRAG_THRESHOLD_PX, pendingDragRef, window mousemove drag listener) bleibt UNVERÄNDERT. Nur die Handle-Mounts werden ergänzt.

    Schritt 4 — Komponenten-Tests:
    - Falls noch nicht in Wave-0 angelegt, erstelle `__tests__/WebResizeHandle.test.tsx` und `__tests__/WebRotationHandle.test.tsx` mit den Behavior-Tests 1-6.
    - Test 7 (WebPlanEditor-Mount-Gating) lebt entweder als Render-Test in `WebPlanEditor.dblclick.test.tsx` (von Plan 02) oder neu angelegt als `WebPlanEditor.handles.test.tsx` (1-2 it()).

    KEINE fenced code im action — Component-Bodies + math live in PATTERNS.md + RESEARCH §Pattern 3.
  </action>
  <verify>
    <automated>cd app && pnpm exec vitest run WebResizeHandle WebRotationHandle --reporter=dot 2>&1 | tail -25</automated>
    <automated>cd app && pnpm exec tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Web handle component files exist (ls `WebResizeHandle.tsx` and `WebRotationHandle.tsx`).
    - `WebRotationHandle.tsx` contains literal `isShiftDown` variable (grep returns 2+ hits — declaration + usage).
    - `WebRotationHandle.tsx` invokes `snapRotation(` with `isShiftDown` as 2nd arg (grep `snapRotation\s*\([^)]*,\s*isShiftDown` returns 1+ hit).
    - `WebRotationHandle.tsx` contains INPUT/TEXTAREA guard (grep `tagName.*INPUT|INPUT.*TEXTAREA` returns 1+ hit) — Pitfall 7.
    - `WebResizeHandle.tsx` and `WebRotationHandle.tsx` invoke `setGestureActive(true)` AND `setGestureActive(false)` (grep `setGestureActive\(true\).*setGestureActive\(false\)|setGestureActive\(false\).*setGestureActive\(true\)` multiline >= 1 each file).
    - WebPlanEditor.tsx conditionally renders WebResizeHandle guarded by `rotateDeg === 0` (grep `rotateDeg\s*===\s*0` returns 1+ hit) — MVP Carve-Out.
    - WebPlanEditor.tsx Plan-02-Code unverändert: grep `onDoubleClick` returns 1+ hit; grep `MOUSE_DRAG_THRESHOLD_PX|pendingDragRef` returns 1+ hit.
    - `pnpm --filter app exec vitest run WebResizeHandle WebRotationHandle` exits 0.
    - `pnpm --filter app exec tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    2 Web-Handle-Komponenten existieren mit korrekter Mouse-Lifecycle, Pattern S5 Shift-Listener (INPUT/TEXTAREA-Guard) und Pitfall 8 try/finally; WebPlanEditor mounted die Handles unter MVP Carve-Out. Plan 02 Code (Doppelklick, Drag-Threshold) bleibt intakt.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User Touch/Mouse Drag → SharedValue / DragRef | Live-preview state on worklet/JS-thread; commit only at onEnd → updateElement crossing into store + zundo. |
| Window Shift-Listener → modal text input focus | Without INPUT/TEXTAREA guard, Shift-key in modal note field is hijacked by handle (Pitfall 7). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.1-HANDLE-DRIFT | Tampering | Resize anchoring on rotated elements (Pitfall 4) | mitigate | MVP carve-out: ResizeHandle hidden when `provenance.rotateDeg !== 0`. JSDoc in handleGeometry.ts documents the constraint. EditorCanvas + WebPlanEditor enforce render-gate. User fallback: numeric Modal inputs (D-04). ASVS V5. |
| T-09.1-SHIFT-HIJACK | Tampering | WebRotationHandle Shift-key listener (Pitfall 7) | mitigate | INPUT/TEXTAREA tag guard in keydown/keyup handler (Pattern S5). Acceptance criterion checks grep evidence. ASVS V5. |
| T-09.1-RESUME-LEAK | Denial of Service | gestureActive remains true after drag error (Pitfall 8) | mitigate | try/finally around commit + onGestureEnd in all 4 handle components. Pattern S3 discipline. zundo would stay paused otherwise → user sees no undo capture forever. ASVS V14. |
| T-09.1-SNAP-CONSISTENCY | Tampering | snapRotation invariants on rotation handle | accept | Pure function tested in Plan 01; called identically by Skia (always snap) and Web (Shift bypass). No new attack surface. ASVS V5. |
| T-09.1-FILE-RACE | Tampering | EditorCanvas + WebPlanEditor concurrent edits (Plan 02 + Plan 03) | mitigate | Wave-3 sequentialisierung (depends_on: [09.1-02]) eliminiert die Race-Condition. Plan 02 schließt EditorCanvas-/WebPlanEditor-Änderungen vor Plan 03-Start ab. |
</threat_model>

<verification>
- `pnpm --filter app exec vitest run handleGeometry rotationSnap zOrder WebResizeHandle WebRotationHandle` exits 0
- `pnpm --filter app exec tsc --noEmit` exits 0
- `pnpm --filter app exec vitest run` full suite exits 0 (kein PlanEditor.smoke Regression; Plan-02-Tests bleiben grün)
- Plan-02 LongPress + Doppelklick funktional unverändert (grep evidence: findElementAtPoint + onDoubleClick + MOUSE_DRAG_THRESHOLD_PX persistent)
</verification>

<success_criteria>
1. 4 Eck-Resize-Handles erscheinen auf selected element (versteckt bei rotateDeg !== 0).
2. 1 Rotations-Handle erscheint mittig oben über selected element.
3. Skia + Web beide funktional mit konsistentem gestureActive lifecycle.
4. 15°-Snap (Skia immer; Web mit Shift-Bypass) ist deterministisch testbar via rotationSnap.
5. Pattern S2/S3 + Pitfall 7/8 enforced.
6. handleGeometry.test GREEN — bestätigt math invariants.
7. Plan-02-Code in EditorCanvas + WebPlanEditor bleibt funktional unverändert (kein Conflict-Overwrite).
</success_criteria>

<output>
Create `.planning/phases/09.1-editor-element-bearbeitung/09.1-03-SUMMARY.md` when done — summarize:
- Anzahl neuer Handle-Komponenten + LOC pro Task
- MVP Carve-Out grep evidence (rotateDeg === 0 in EditorCanvas + WebPlanEditor)
- handleGeometry Tests GREEN/total
- Pitfall 7/8 mitigations confirmed
- Plan-02-Code-Integrität bestätigt (findElementAtPoint + onDoubleClick noch vorhanden)
</output>
