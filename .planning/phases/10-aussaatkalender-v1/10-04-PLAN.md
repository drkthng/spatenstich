---
phase: 10-aussaatkalender-v1
plan: 04
type: execute
wave: 4
depends_on: [10-01, 10-02, 10-03]
files_modified:
  - app/src/components/kalender/FruchtfolgeWarnung.tsx
  - app/app/(app)/kalender/[slug].tsx
  - app/src/components/kalender/__tests__/PflanzenDetail.test.tsx
autonomous: false
requirements: [CAL-05, CAL-06]
user_setup: []
must_haves:
  truths:
    - "The detail route /(app)/kalender/[slug] shows the full-width GanttStreifen + month labels + GanttLegende for the resolved plant"
    - "The detail screen shows Phase-8 plant infos (Mindestabstand, Sonnenbedarf, Familie) from the plant DB"
    - "The 'Auf welchem Beet?' section lists beds returned by findBeeteForPlant, or a 'Noch nicht im Plan' hint"
    - "A 'Zu Plan hinzufügen' button calls addPlantToPlan (CAL-05) when a Beet exists; otherwise the keinBeetImPlan InlineBanner replaces it"
    - "A Fruchtfolge warning (CAL-06) appears when a same-family plant already sits in the target/any bed (pruefeEinfacheFruchtfolge)"
  artifacts:
    - path: "app/app/(app)/kalender/[slug].tsx"
      provides: "Pflanzen-Detail route: Gantt + infos + Auf welchem Beet + CTA"
      contains: "useLocalSearchParams"
    - path: "app/src/components/kalender/FruchtfolgeWarnung.tsx"
      provides: "Thin InlineBanner wrapper for CAL-06"
      exports: ["FruchtfolgeWarnung"]
  key_links:
    - from: "app/app/(app)/kalender/[slug].tsx"
      to: "app/src/hooks/useKalenderData.ts"
      via: "addPlantToPlan + elements"
      pattern: "addPlantToPlan"
    - from: "app/app/(app)/kalender/[slug].tsx"
      to: "app/src/lib/kalenderBeete.ts"
      via: "findBeeteForPlant"
      pattern: "findBeeteForPlant"
    - from: "app/app/(app)/kalender/[slug].tsx"
      to: "@spatenstich/shared kalenderEngine"
      via: "pruefeEinfacheFruchtfolge"
      pattern: "pruefeEinfacheFruchtfolge"
---

<objective>
Build the Pflanzen-Detail screen and the Fruchtfolge warning, closing CAL-05 (add to plan) and CAL-06 (simple crop-rotation warning).

Purpose: The detail screen is the destination of every plant tap in plan 03. It shows the full Jahres-Gantt, the Phase-8 plant facts, the "Auf welchem Beet?" answer (reusing plan 02's findBeeteForPlant), and the primary CTA "Zu Plan hinzufügen" (CAL-05 via the hook's addPlantToPlan). The Fruchtfolge warning (CAL-06) surfaces when the plant's family already occupies a bed, using the engine's pruefeEinfacheFruchtfolge. A blocking human-verify checkpoint confirms the visual/interaction contract on a real device since Gantt rendering and the add-to-plan round-trip are not fully provable by unit tests alone.

Output: FruchtfolgeWarnung component, the `[slug].tsx` detail route, a detail unit test, and a HUMAN-VERIFY checkpoint.
</objective>

<artifacts_this_phase_produces>
NEW symbols introduced by this plan (treat as newly-created in drift passes):
- `FruchtfolgeWarnung` component (props: grund, onDismiss?)
- Route `app/app/(app)/kalender/[slug].tsx` (default export Pflanzen-Detail screen)
- Module-local `MONATE` 3-letter month label array (Jan…Dez)
(Consumes plan-01 `pruefeEinfacheFruchtfolge`/`getFensterFuerPflanze`, plan-02 `findBeeteForPlant`/`useKalenderData`, plan-03 `GanttStreifen`/`GanttLegende`.)
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-aussaatkalender-v1/10-UI-SPEC.md
@.planning/phases/10-aussaatkalender-v1/10-PATTERNS.md
@.planning/phases/10-aussaatkalender-v1/10-02-SUMMARY.md
@.planning/phases/10-aussaatkalender-v1/10-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: FruchtfolgeWarnung component + Pflanzen-Detail [slug] screen (CAL-05, CAL-06)</name>
  <files>app/src/components/kalender/FruchtfolgeWarnung.tsx, app/app/(app)/kalender/[slug].tsx, app/src/components/kalender/__tests__/PflanzenDetail.test.tsx</files>
  <read_first>
    - app/app/(app)/profile/vereinsregeln/index.tsx (analog screen: sectioned ScrollView, Stack.Screen with dynamic title, t() helper, section layout with gap)
    - app/src/components/InlineBanner.tsx (props: message, variant, onDismiss, testID — FruchtfolgeWarnung is a thin wrapper around variant="warning")
    - app/src/hooks/useKalenderData.ts (from plan 02: addPlantToPlan(plant), hasBeetImPlan, elements, dimensions, klimazone, refresh)
    - app/src/lib/kalenderBeete.ts (from plan 02: findBeeteForPlant(elements, plantSlug); getPlantSlug)
    - app/src/hooks/usePlants.ts (usePlants() → data PlantRow[]; resolve the route plant by slug from this list)
    - packages/shared/src/lib/kalenderEngine.ts (pruefeEinfacheFruchtfolge(neuePflanze, beetPflanzen); getFensterFuerPflanze — all from '@spatenstich/shared')
    - app/src/components/kalender/GanttStreifen.tsx + GanttLegende.tsx (from plan 03: render full Gantt height=20 + legend)
    - packages/shared/src/types/plants.ts (PlantRow: nameDe, family, minSpacingCm, sunRequirement — for the Phase-8 info section)
    - .planning/phases/10-aussaatkalender-v1/10-UI-SPEC.md §Screen 2 (anatomy) + §Pflanzen-Detail Interaktionen + §Copywriting Contract (detail.* keys + zuPlanHinzufuegen + planOeffnen + hinzugefuegtBanner + aufWelchemBeet + nochNichtImPlan + keinBeetImPlan) + §Error States + 10-PATTERNS.md §kalender/[slug].tsx + §FruchtfolgeWarnung
  </read_first>
  <action>
    Create `app/src/components/kalender/FruchtfolgeWarnung.tsx` per 10-PATTERNS.md §FruchtfolgeWarnung: thin wrapper exporting `function FruchtfolgeWarnung({ grund, onDismiss }: { grund: string; onDismiss?: () => void }): React.JSX.Element` that returns `<InlineBanner variant="warning" message={grund} onDismiss={onDismiss} testID="fruchtfolge-warnung" />` (import InlineBanner from '../InlineBanner'); render nothing-guarding is the caller's job. Create the route `app/app/(app)/kalender/[slug].tsx` per 10-PATTERNS.md §kalender/[slug].tsx + 10-UI-SPEC §Screen 2: read `const { slug } = useLocalSearchParams<{ slug: string }>()`; copy the `t()` helper; use `usePlants()` and resolve `const plant = plants.find(p => p.slug === slug)`; use `useKalenderData()` for `elements`, `klimazone`, `hasBeetImPlan`, `addPlantToPlan`. Render `<Stack.Screen options={{ headerTitle: plant?.nameDe ?? slug }} />` and a sectioned ScrollView (`flex-1 bg-[#F9F7F4] dark:bg-[#1C1917]`, contentContainerStyle `{ padding:16, gap:16 }`). When `plant` is undefined, render a "nicht gefunden" InlineBanner (variant="warning") and stop. When `klimazone == null`, render the PLZ-warning InlineBanner like the index screen. SECTION 1 (Gantt): `<GanttStreifen plant={plant} klimazone={klimazone ?? 4} height={20} />` followed by a `flex-row justify-between` row of 12 month labels from a module-level `const MONATE = ['Jan','Feb','Mrz','Apr','Mai','Jun','Jul','Aug','Sep','Okt','Nov','Dez']` (`text-xs text-stone-400`), then `<GanttLegende />`. SECTION 2 (Phase-8 infos): render `t('kalender.detail.mindestabstand', ...)` with `plant.minSpacingCm`, `t('kalender.detail.sonnenbedarf', ...)` with `plant.sunRequirement`, `t('kalender.detail.familie', ...)` with `plant.family` — perform `{var}` substitution inline (string replace, matching the de.json single-brace convention). Compute the CAL-06 warning: derive the beds the plant would join via `findBeeteForPlant(elements, slug)`; for each such bed (or, if not yet placed, for all beds) gather the other Pflanze elements in that bed and their families (resolve family via the plants list keyed by getPlantSlug); call `pruefeEinfacheFruchtfolge({ family: plant.family, slug }, beetPflanzenFamilies)`; when `warnung` render `<FruchtfolgeWarnung grund={result.grund!} />`. SECTION 3 ("Auf welchem Beet?"): header `t('kalender.aufWelchemBeet')`; list the bed labels from findBeeteForPlant, or render `t('kalender.nochNichtImPlan')` when empty. Then the CTA: when `hasBeetImPlan` render a `Button variant="default" className="bg-[#4A7C59]"` labeled `t('kalender.zuPlanHinzufuegen')`, accessibilityLabel `${plant.nameDe} zu Plan hinzufügen`, that on press calls `await addPlantToPlan(plant)`, sets a success banner state showing `t('kalender.hinzugefuegtBanner', { name: plant.nameDe })`, and disables the button while in flight; on failure show an `InlineBanner variant="error"` with the German "Hinzufügen fehlgeschlagen. Versuche es erneut." text (button stays active). When NOT `hasBeetImPlan` render `<InlineBanner variant="warning" message={t('kalender.keinBeetImPlan')} />` instead of the add button (CAL-04 guard, Fallstrick 5). Finally a `Button variant="outline"` labeled `t('kalender.planOeffnen')` → `router.push('/(app)/plan' as any)`. Write `app/src/components/kalender/__tests__/PflanzenDetail.test.tsx` in the components jest project: mock `expo-router` `useLocalSearchParams` to return a known slug, mock `usePlants` to return a plant with that slug + a family, mock `useKalenderData` (jest.mock the hook module) to control `hasBeetImPlan`/`elements`/`addPlantToPlan`; assert (a) Phase-8 info strings render (Mindestabstand value present), (b) with hasBeetImPlan=false the keinBeetImPlan banner renders and the add button is absent, (c) with hasBeetImPlan=true pressing the add button calls the mocked addPlantToPlan once with the plant, (d) a same-family plant already in a bed renders the `fruchtfolge-warnung` testID. Keep each mock minimal and colocated.
  </action>
  <verify>
    <automated>pnpm --filter app exec jest --testPathPattern=PflanzenDetail && pnpm --filter app exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter app exec jest --testPathPattern=PflanzenDetail` exits 0 with all four assertions passing.
    - `pnpm --filter app exec tsc --noEmit` exits 0.
    - `app/app/(app)/kalender/[slug].tsx` contains `useLocalSearchParams`, imports `findBeeteForPlant` from '../../../src/lib/kalenderBeete' (or the project's `@/src/...` alias), imports `pruefeEinfacheFruchtfolge` and `getFensterFuerPflanze` from `'@spatenstich/shared'`, and renders `<GanttStreifen` + `<GanttLegende`.
    - The add CTA calls `addPlantToPlan` and the no-bed branch renders `t('kalender.keinBeetImPlan')` (verify both `addPlantToPlan` and `keinBeetImPlan` strings present in the file).
    - `FruchtfolgeWarnung.tsx` exports `FruchtfolgeWarnung` and renders `<InlineBanner variant="warning"` with `testID="fruchtfolge-warnung"`.
    - No import from `'@spatenstich/shared/lib/kalenderEngine'` (subpath invalid) anywhere in the file.
  </acceptance_criteria>
  <done>Detail screen renders Gantt + infos + Auf-welchem-Beet + CTA; CAL-05 add wired to hook; CAL-06 Fruchtfolge warning via engine; detail tests green; typecheck clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify Aussaatkalender on device (CAL-01..CAL-06 visual + add round-trip)</name>
  <files>.planning/phases/10-aussaatkalender-v1/10-HUMAN-VERIFY.md</files>
  <action>
    Checkpoint — no code is written in this task. After Task 1 lands and the full phase suite is green (`pnpm --filter @spatenstich/shared exec jest && pnpm --filter app exec jest`), present the numbered steps in <how-to-verify> to the user and pause for the resume signal. The user exercises the running app on a real device (or web) and confirms the visual + interaction contract for CAL-01..CAL-06 plus UTF-8 Umlaute. Record per-criterion outcomes (pass / issue) in 10-HUMAN-VERIFY.md. Do not advance the wave until the user types "approved" or all reported issues are resolved.
  </action>
  <what-built>
    Full Phase-10 Aussaatkalender: Wochen-View at /(app)/kalender (KW card "Diese Woche" + "Nur meine Pflanzen" filter chip + Jahresübersicht plant list with Miniatur-Gantt), Pflanzen-Detail at /(app)/kalender/[slug] (full 12-month Gantt + month labels + legend + Phase-8 infos + "Auf welchem Beet?" + "Zu Plan hinzufügen" + Fruchtfolge warning), and the "Zum Kalender" button on the Home screen. Klimazonen offset, CAL-01..CAL-06 are unit-tested; this checkpoint confirms the visual contract and the add-to-plan round-trip on a real device.
  </what-built>
  <how-to-verify>
    1. Start the app (account mode, a garden with at least one Beet and ≥1 plant placed via the editor). Run `pnpm --filter app start` (or the project's dev command) and open on iPhone (Expo Go) or web.
    2. From Home, tap "Zum Kalender" (testID home-kalender-button) → the Wochen-View opens with header "Aussaatkalender" and "KW {n} · 2026".
    3. Confirm the "Diese Woche" card lists current-week actions with colored badges (Vorkultur violett / Direktsaat grün / Auspflanzen blau / Ernte orange) and the correct German labels (CAL-03). If no actions this week, the empty-state text appears.
    4. Toggle the "Nur meine Pflanzen" chip: active = green (#4A7C59) filled, inactive = outline; the Jahresübersicht list narrows to plan plants vs. all plants (CAL-04 filter / Success Criterion 4).
    5. Tap a plant row → Pflanzen-Detail opens. Confirm the full-width 12-month Gantt with colored phase bars, month labels Jan…Dez, and the 4-color legend (CAL-01 + CAL-02). Confirm Mindestabstand / Sonnenbedarf / Familie show Phase-8 data (Success Criterion 6).
    6. Change the profile PLZ to a different klimazone (e.g. a Zone-1 vs Zone-7 PLZ) and reopen a plant detail (e.g. Tomate) → the Gantt phase bars shift by ~1–4 KW (CAL-02 / Success Criterion 3).
    7. In "Auf welchem Beet?" confirm placed plants list their bed, unplaced show "Noch nicht im Plan". Tap "Zu Plan hinzufügen" → success banner "{name} wurde dem Plan hinzugefügt" appears and the plant is added (verify it shows in the Plan editor) (CAL-05). With a plan that has no Beet, confirm the keinBeetImPlan banner replaces the add button.
    8. Place two plants of the same family (e.g. Tomate + Paprika, Solanaceae) in one bed, then open a third same-family plant's detail → the Fruchtfolge warning banner appears (CAL-06). Confirm German UTF-8 Umlaute render correctly throughout (ä/ö/ü/ß, e.g. "Jahresübersicht", "hinzugefügt", "öffnen").
  </how-to-verify>
  <verify>User confirms each numbered step in how-to-verify; per-criterion outcomes recorded in 10-HUMAN-VERIFY.md.</verify>
  <resume-signal>Type "approved" or describe issues (per-criterion).</resume-signal>
  <done>User typed "approved" (or all reported issues resolved); CAL-01..CAL-06 confirmed on device with UTF-8 Umlaute correct; 10-HUMAN-VERIFY.md updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| [slug] route param → plant resolution | The dynamic path segment is matched against the validated plant list |
| addPlantToPlan (detail CTA) → plan_elements write | CAL-05 write crosses local-storage + outbox + RLS boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08 | Tampering | `slug` from useLocalSearchParams | mitigate | slug is resolved via `plants.find(p => p.slug === slug)`; an unknown/forged slug yields undefined → "nicht gefunden" InlineBanner, no crash, no write. Expo Router decodes the segment; no eval/SQL. |
| T-10-09 | Elevation / Spoofing | detail "Zu Plan hinzufügen" (CAL-05) | mitigate | Delegates to plan-02 `addPlantToPlan` → `writePlanElement(mode, el)` with `assertAccount` + Supabase RLS member-check; non-account mode throws and surfaces the error banner. No direct storage access in the screen. |
| T-10-10 | Integrity | CAL-06 family resolution | mitigate | Family lookup goes through `getPlantSlug` type-guard + plant-list keying; elements without a resolvable slug are skipped, so the warning never fires on malformed data and never throws. |
| T-10-SC | Tampering | npm installs | accept | No new packages (RESEARCH §Package Legitimacy Audit). FruchtfolgeWarnung wraps the existing InlineBanner. |
</threat_model>

<verification>
- `pnpm --filter app exec jest --testPathPattern="PflanzenDetail"` green.
- `pnpm --filter app exec tsc --noEmit` exits 0.
- Full phase suite green before /gsd-verify-work: `pnpm --filter @spatenstich/shared exec jest && pnpm --filter app exec jest`.
- Human-verify checkpoint approved (CAL-01..CAL-06 on device + UTF-8 Umlaute).
</verification>

<success_criteria>
- CAL-05: "Zu Plan hinzufügen" writes a Pflanze element via the hook; success banner shown; guarded when no Beet.
- CAL-06: same-family-in-bed renders the Fruchtfolge warning via pruefeEinfacheFruchtfolge.
- Detail screen shows Gantt + Phase-8 infos + "Auf welchem Beet?"; klimazone shift visible on device.
</success_criteria>

<output>
Create `.planning/phases/10-aussaatkalender-v1/10-04-SUMMARY.md` when done.
Append device-verification results to `.planning/phases/10-aussaatkalender-v1/10-HUMAN-VERIFY.md`.
</output>
