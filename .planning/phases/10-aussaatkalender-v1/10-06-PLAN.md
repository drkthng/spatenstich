---
phase: 10-aussaatkalender-v1
plan: "06"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/lib/kalenderBeete.ts
  - app/src/lib/__tests__/kalenderBeete.test.ts
autonomous: true
gap_closure: true
requirements: [CAL-04, CAL-06]
must_haves:
  truths:
    - "beetToPolygon baut das Rechteck aus center ± half-dimensions (gleiche Konvention wie useCompanionDetection.getBedPolygon), NICHT aus top-left"
    - "findBeeteForPlant nutzt {x: pflanze.xM, y: pflanze.yM} als Pflanzen-Mittelpunkt (konsistent mit findBedForPlant), nicht xM + widthM/2"
    - "findPflanzenInBeet(elements, beet) liefert genau die nicht-gelöschten Pflanze-Elemente, deren Mittelpunkt im Beet-Polygon liegt"
  artifacts:
    - path: "app/src/lib/kalenderBeete.ts"
      provides: "Center-basierte beetToPolygon + neue Export findPflanzenInBeet"
      exports: ["getPlantSlug", "findBeeteForPlant", "findPflanzenInBeet"]
    - path: "app/src/lib/__tests__/kalenderBeete.test.ts"
      provides: "Center-Konvention-Fixtures + findPflanzenInBeet-Tests"
      contains: "findPflanzenInBeet"
  key_links:
    - from: "app/src/lib/kalenderBeete.ts"
      to: "app/src/hooks/useCompanionDetection.ts"
      via: "beetToPolygon spiegelt getBedPolygon (center ± half)"
      pattern: "xM - halfW|widthM / 2"
    - from: "app/src/lib/kalenderBeete.ts"
      to: "lib/geometry/bedLayout"
      via: "pointInPolygon für findPflanzenInBeet"
      pattern: "pointInPolygon"
---

<objective>
Schließt WR-01 (falsche Koordinaten-Konvention in `beetToPolygon` — top-left statt bbox-CENTER) und liefert den neuen reinen Helper `findPflanzenInBeet`, den der Detail-Screen-Fix (Plan 10-07, WR-02) für die beet-scoped Fruchtfolge-Prüfung benötigt.

`app/src/lib/kalenderBeete.ts` ist ein reines TypeScript-Modul (kein React/RN). Die Codebase-Konvention ist `xM/yM = bbox CENTER` — dokumentiert in `bedLayout.ts:3-4`, in `editorStore.polygonCommit` umgesetzt und in der ausgelieferten Phase 9 `useCompanionDetection.getBedPolygon` (`xM ± widthM/2`) implementiert. `kalenderBeete.beetToPolygon` baut dagegen ein Top-Left-Rechteck und `findBeeteForPlant` berechnet den Pflanzen-Mittelpunkt fälschlich als `xM + widthM/2`. Für jedes Claude.ai-importierte Beet (ohne `provenance.polygonPointsM`, das den Fallback-Pfad trifft) ist das Polygon um (+w/2, +h/2) verschoben — "Auf welchem Beet?" (CAL-04) und das CAL-06-Beet-Scoping liefern falsche Ergebnisse.

Wave 1, unabhängig: reiner Geometrie-Fix ohne Abhängigkeit von anderen Gap-Plänen. Plan 10-07 (Detail-Screen) und 10-08 (Hook) hängen von diesem Plan ab, weil sie die korrigierte Konvention bzw. den neuen `findPflanzenInBeet`-Export konsumieren.

Purpose: CAL-04 "Auf welchem Beet?" und CAL-06 Beet-Scoping liefern für importierte Beete korrekte Ergebnisse, konsistent mit der restlichen Codebase (Phase 9).
Output: Center-basierte `beetToPolygon`, korrigierter Pflanzen-Mittelpunkt in `findBeeteForPlant`, neuer Export `findPflanzenInBeet`, an die Center-Konvention angeglichene Tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-aussaatkalender-v1/10-REVIEW.md
@.planning/phases/10-aussaatkalender-v1/10-02-SUMMARY.md
@app/src/hooks/useCompanionDetection.ts
@app/src/lib/geometry/bedLayout.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: beetToPolygon auf center ± half-dimensions umstellen + Pflanzen-Mittelpunkt korrigieren (WR-01)</name>
  <files>app/src/lib/kalenderBeete.ts, app/src/lib/__tests__/kalenderBeete.test.ts</files>
  <read_first>
    - app/src/lib/kalenderBeete.ts (zu ändernde Datei — `beetToPolygon` Z. 26-42 Top-Left-Fallback, Pflanzen-Center Z. 75-78)
    - app/src/hooks/useCompanionDetection.ts (Source-of-Truth: `getBedPolygon` Z. 58-72 center ± half; `findBedForPlant` Z. 91 `{x: plant.xM, y: plant.yM}` als Pflanzen-Center)
    - app/src/lib/geometry/bedLayout.ts (Konvention dokumentiert Z. 1-4: "xM/yM = bbox center"; `pointInPolygon` Z. 23-36)
    - app/src/lib/__tests__/kalenderBeete.test.ts (bestehende Tests — Fixtures kodieren AKTUELL die falsche Top-Left-Annahme, siehe Kommentar Z. 62-63; müssen auf Center umgestellt werden)
    - .planning/phases/10-aussaatkalender-v1/10-REVIEW.md (WR-01 Z. 77-99 — exakte Fix-Empfehlung inkl. Code)
  </read_first>
  <behavior>
    - WR-01 (RED→GREEN): Ein Beet mit `xM=1, yM=1, widthM=2, heightM=2` (Center-Konvention) hat das Polygon-Rechteck `[(0,0),(2,0),(2,2),(0,2)]` (center ± half). Eine Pflanze mit `xM=1, yM=1` (Mittelpunkt) liegt darin. Test: `findBeeteForPlant([beet, pflanze@(1,1)], slug)` → Beet zurückgegeben.
    - WR-01 Außerhalb: Eine Pflanze mit `xM=2.5, yM=2.5` (außerhalb des Center-Rechtecks `[0,0]→[2,2]`) → leeres Array.
    - WR-01 Konsistenz mit Phase 9: Für identische Beet-Daten liefert `kalenderBeete.beetToPolygon` dieselben 4 Ecken wie `useCompanionDetection.getBedPolygon` (Center ± half). (Kann als direkter Vergleichstest formuliert werden, wenn beetToPolygon für den Test exportiert wird — optional; alternativ über das Verhalten von findBeeteForPlant verifiziert.)
    - Regression: Der Polygon-Pfad über `provenance.polygonPointsM` (Dreieck-Test) bleibt unverändert grün.
  </behavior>
  <action>
    Korrigiere in `kalenderBeete.ts` die Koordinaten-Konvention auf bbox-CENTER (spiegelt `useCompanionDetection.getBedPolygon` exakt):

    (1) `beetToPolygon`-Fallback (Z. 31-41): Ersetze das Top-Left-Rechteck durch center ± half-dimensions: `halfW = beet.widthM / 2; halfH = beet.heightM / 2;` und gib `[{x: xM - halfW, y: yM - halfH}, {x: xM + halfW, y: yM - halfH}, {x: xM + halfW, y: yM + halfH}, {x: xM - halfW, y: yM + halfH}]` zurück. Entferne die irreführenden Kommentare (Z. 32-34), die die Top-Left-Konvention behaupten; der korrekte Doc-Kommentar (Z. 23-24) "built from center ± half-dimensions" stimmt dann mit dem Code überein. Der primäre Pfad über `provenance.polygonPointsM` bleibt unverändert.

    (2) Pflanzen-Mittelpunkt in `findBeeteForPlant` (Z. 75-78): Ersetze `{x: pflanze.xM + pflanze.widthM/2, y: pflanze.yM + pflanze.heightM/2}` durch `{x: pflanze.xM, y: pflanze.yM}` — konsistent mit `findBedForPlant` (useCompanionDetection.ts:91), wo `xM/yM` bereits der Mittelpunkt ist.

    Gleiche die Tests in `kalenderBeete.test.ts` an die Center-Konvention an: Die Fixtures kodieren aktuell Top-Left (Kommentar Z. 62-63 "Pflanze at (0.85,0.85) has center (1.0,1.0)"). Stelle Beet- und Pflanzen-Koordinaten auf Center-Werte um, sodass die Containment-/Außerhalb-/Dedup-Tests die korrekte Geometrie prüfen. WICHTIG: Die alten Fixtures dürfen NICHT 1:1 übernommen werden — sie maskierten den Defekt (10-VERIFICATION.md: "bestehende Tests kodieren evtl. die falsche Konvention").
  </action>
  <verify>
    <automated>pnpm --filter app exec jest --testPathPattern="kalenderBeete"</automated>
  </verify>
  <acceptance_criteria>
    - `beetToPolygon`-Fallback nutzt `widthM / 2` und `heightM / 2` (Grep: `grep -c "widthM / 2" app/src/lib/kalenderBeete.ts` >= 1) und enthält keine `xM + widthM` / `yM + heightM` Top-Left-Konstruktion mehr (Grep: `grep -c "xM + widthM" app/src/lib/kalenderBeete.ts` ergibt 0 außerhalb von Kommentaren).
    - `findBeeteForPlant` nutzt `{ x: pflanze.xM, y: pflanze.yM }` als Center (Grep: `grep -c "pflanze.xM + pflanze.widthM" app/src/lib/kalenderBeete.ts` ergibt 0).
    - Test: Pflanze@(1,1) in Center-Beet (xM=1,yM=1,w=2,h=2) → gefunden; Pflanze@(2.5,2.5) → nicht gefunden — grün.
    - Polygon-Fallback-Test (Dreieck via provenance.polygonPointsM) — unverändert grün.
    - `pnpm --filter app exec jest --testPathPattern="kalenderBeete"` — 0 failures.
    - `pnpm --filter app exec tsc --noEmit` — exit 0.
  </acceptance_criteria>
  <done>beetToPolygon und der Pflanzen-Mittelpunkt verwenden die bbox-CENTER-Konvention; Tests prüfen Center-Geometrie und sind grün.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: findPflanzenInBeet Helper exportieren (für WR-02 Beet-Scoping)</name>
  <files>app/src/lib/kalenderBeete.ts, app/src/lib/__tests__/kalenderBeete.test.ts</files>
  <read_first>
    - app/src/lib/kalenderBeete.ts (nach Task 1 korrigiert — `beetToPolygon`, `getPlantSlug`, `pointInPolygon`-Import; gleiche Geometrie wiederverwenden)
    - app/src/hooks/useCompanionDetection.ts (`computeConflicts` Z. 126-164 als Analog: Pflanzen pro Beet via findBedForPlant/PiP gruppieren — gleiches Muster, andere Granularität)
    - .planning/phases/10-aussaatkalender-v1/10-REVIEW.md (WR-02 Z. 101-109 — fordert exakt `findPflanzenInBeet(elements, beet)` Export)
  </read_first>
  <behavior>
    - findPflanzenInBeet (RED→GREEN): `findPflanzenInBeet(elements, beet)` liefert genau die nicht-gelöschten `elementType='Pflanze'`-Elemente, deren Mittelpunkt `{x: xM, y: yM}` im Polygon von `beet` liegt (via `beetToPolygon` + `pointInPolygon`).
    - Scoping: Eine Pflanze in einem ANDEREN Beet (außerhalb des übergebenen `beet`-Polygons) wird NICHT zurückgegeben.
    - Ausschlüsse: soft-deleted Pflanzen (deletedAt gesetzt) und Nicht-Pflanze-Elemente werden ausgeschlossen.
    - Leerfälle: leere Elementliste → leeres Array; Beet ohne enthaltene Pflanzen → leeres Array.
  </behavior>
  <action>
    Exportiere eine neue reine Funktion `findPflanzenInBeet(elements: PlanElementRow[], beet: PlanElementRow): PlanElementRow[]` aus `kalenderBeete.ts`. Implementierung: filtere `elements` auf nicht-gelöschte `elementType==='Pflanze'`, rekonstruiere das Beet-Polygon einmal via `beetToPolygon(beet)` (Task-1-korrigierte Funktion), und behalte jede Pflanze, deren Mittelpunkt `{ x: pflanze.xM, y: pflanze.yM }` per `pointInPolygon` im Polygon liegt. Keine Slug-Filterung hier — der Aufrufer (Plan 10-07) filtert den eigenen Slug separat heraus. Diese Funktion ist das beet-scoped Gegenstück zur plan-globalen Filterung, die WR-02 verursacht hat.

    Erweitere `kalenderBeete.test.ts` um eine `describe('findPflanzenInBeet')`-Gruppe: (a) Pflanze im Beet → enthalten, (b) Pflanze in anderem/außerhalb des Beets → NICHT enthalten (Kern-Scoping-Beweis), (c) soft-deleted Pflanze ausgeschlossen, (d) Nicht-Pflanze-Element ausgeschlossen, (e) leere Liste → []. Center-Koordinaten-Konvention (Task 1) durchgängig verwenden.
  </action>
  <verify>
    <automated>pnpm --filter app exec jest --testPathPattern="kalenderBeete"</automated>
  </verify>
  <acceptance_criteria>
    - `findPflanzenInBeet` ist aus `kalenderBeete.ts` exportiert (Grep: `grep -c "export function findPflanzenInBeet" app/src/lib/kalenderBeete.ts` === 1).
    - Test: Pflanze im Ziel-Beet enthalten; Pflanze in anderem Beet NICHT enthalten — grün (beweist Beet-Scoping).
    - Test: soft-deleted Pflanze + Nicht-Pflanze-Element ausgeschlossen — grün.
    - `pnpm --filter app exec jest --testPathPattern="kalenderBeete"` — 0 failures (alle bisherigen + neuen Tests).
    - `pnpm --filter app exec tsc --noEmit` — exit 0.
  </acceptance_criteria>
  <done>findPflanzenInBeet exportiert und durch Scoping-Tests verifiziert; bereit für Konsum durch Plan 10-07.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Geänderte/neue Symbole dieses Plans:
- `beetToPolygon` (intern, `app/src/lib/kalenderBeete.ts`) — Fallback auf center ± half-dimensions umgestellt.
- `findBeeteForPlant` — Pflanzen-Mittelpunkt korrigiert auf `{x: xM, y: yM}`.
- NEU `findPflanzenInBeet(elements, beet): PlanElementRow[]` — exportiert, beet-scoped Pflanzen-Lookup für WR-02 (konsumiert von Plan 10-07).
- Angeglichene Center-Konvention-Fixtures + neue `findPflanzenInBeet`-Tests in `kalenderBeete.test.ts`.
Öffentliche Exporte: `getPlantSlug`, `findBeeteForPlant` (unverändert), `findPflanzenInBeet` (neu).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| importierte Beet-Daten → Geometrie | provenance.polygonPointsM / xM/yM aus Claude.ai-Import oder Editor; untrusted Form, aber rein lesend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06-01 | Tampering | provenance.plantSlug / polygonPointsM | mitigate | `getPlantSlug` type-guard (`typeof === 'string'`) bleibt; `beetToPolygon` prüft `Array.isArray(polygonPointsM) && length >= 3` vor Nutzung — degenerierte Polygone fallen auf Rechteck zurück |
| T-10-06-02 | Information Disclosure | falsche Beet-Zuordnung | mitigate | Center-Konvention-Fix beseitigt die (+w/2,+h/2)-Verschiebung — korrekte Beet-Membership verhindert falsch zugeordnete Fruchtfolge-/Beet-Anzeigen |
| T-10-06-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Packages — reine Korrektur + Helper in bestehendem Modul |
</threat_model>

<verification>
- `pnpm --filter app exec jest --testPathPattern="kalenderBeete"` — alle Tests grün
- `pnpm --filter app exec tsc --noEmit` — exit 0
- Manuelle Konsistenzprüfung: beetToPolygon-Fallback identisch zu useCompanionDetection.getBedPolygon (center ± half)
</verification>

<success_criteria>
- WR-01 geschlossen: beetToPolygon + Pflanzen-Mittelpunkt nutzen bbox-CENTER-Konvention
- findPflanzenInBeet exportiert und scoping-getestet (Voraussetzung für Plan 10-07 / WR-02)
- Tests prüfen Center-Geometrie, nicht die alte Top-Left-Annahme
</success_criteria>

<output>
Create `.planning/phases/10-aussaatkalender-v1/10-06-SUMMARY.md` when done
</output>
