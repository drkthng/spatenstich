---
phase: 10-aussaatkalender-v1
plan: "05"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/lib/kalenderEngine.ts
  - packages/shared/src/lib/__tests__/kalenderEngine.test.ts
  - app/src/components/kalender/GanttStreifen.tsx
  - app/src/components/kalender/__tests__/GanttStreifen.test.tsx
autonomous: true
gap_closure: true
requirements: [CAL-01, CAL-02]
must_haves:
  truths:
    - "getAktuelleKw() liefert an Sonntagen die laufende KW (nicht die Folge-KW)"
    - "Ein Klimazonen-verschobenes Fenster mit ISO-Wochen-Wrap (startKw > endKw) verschwindet NICHT aus filterAktiveAktionen und erzeugt keinen Gantt-Balken mit negativer Breite"
    - "GanttStreifen rendert keinen Balken mit negativer/0-Breite für ein gewrapptes Fenster"
  artifacts:
    - path: "packages/shared/src/lib/kalenderEngine.ts"
      provides: "getAktuelleKw mit Math.floor; addWindow mit Wrap-Normalisierung"
      contains: "Math.floor"
    - path: "app/src/components/kalender/GanttStreifen.tsx"
      provides: "Defensiver Guard gegen clampedEnd < clampedStart"
      contains: "clampedEnd"
  key_links:
    - from: "packages/shared/src/lib/kalenderEngine.ts"
      to: "doyToIsoKw"
      via: "getAktuelleKw nutzt korrekte DOY-Berechnung (Math.floor)"
      pattern: "Math\\.floor"
    - from: "app/src/components/kalender/GanttStreifen.tsx"
      to: "getFensterFuerPflanze"
      via: "Fenster-Balken mit Breiten-Guard gerendert"
      pattern: "clampedEnd"
---

<objective>
Schließt WR-03 (getAktuelleKw Off-by-one durch Math.ceil → falsche KW an Sonntagen) und WR-04 (fehlende Behandlung von ISO-Wochen-Wrap `startKw > endKw` → verschwundene Fenster + negative Gantt-Balken-Breiten) aus dem Phase-10 Code-Review.

Beide Defekte liegen in der reinen Kalender-Engine (`packages/shared`) bzw. der reinen View-bar-Berechnung in `GanttStreifen.tsx` — keine Datenbank-, Auth- oder Netzwerk-Pfade betroffen. Dieser Plan ist die Wurzel der Korrektheitskette: WR-03/WR-04 sind die einzigen Engine-Defekte und blockieren keine UI-Plan-Fixes, daher Wave 1, unabhängig von allen anderen Gap-Plänen.

Purpose: Die Wochen-Ansicht (CAL-01/CAL-03) zeigt sonntags und in Jahreswechsel-Fenstern korrekte Aktionen statt Folge-Woche bzw. verschwindender Fenster; der Jahres-Gantt (CAL-01) rendert keine kaputten/negativen Balken.
Output: Korrigierte `kalenderEngine.ts` + `GanttStreifen.tsx`, jeweils mit neuen Regressionstests, die die alten (off-by-one / wrap) Pfade explizit abdecken.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-aussaatkalender-v1/10-REVIEW.md
@.planning/phases/10-aussaatkalender-v1/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getAktuelleKw Off-by-one (WR-03) + ISO-Wochen-Wrap-Normalisierung (WR-04) in kalenderEngine</name>
  <files>packages/shared/src/lib/kalenderEngine.ts, packages/shared/src/lib/__tests__/kalenderEngine.test.ts</files>
  <read_first>
    - packages/shared/src/lib/kalenderEngine.ts (zu ändernde Datei — `getAktuelleKw` Z. 105-111, `addWindow` Z. 79-92, `doyToIsoKw` Z. 46-53)
    - packages/shared/src/lib/__tests__/kalenderEngine.test.ts (bestehende Tests — `getAktuelleKw`-Test Z. 148-155 prüft nur 1..53; muss verschärft werden)
    - .planning/phases/10-aussaatkalender-v1/10-REVIEW.md (WR-03 Z. 111-125, WR-04 Z. 127-145 — exakte Fix-Beschreibung)
  </read_first>
  <behavior>
    - WR-03 (RED→GREEN): `getAktuelleKw()` mit injizierbarem Datum (neuer optionaler Parameter `now: Date = new Date()`, NICHT die Signatur der bestehenden no-arg-Aufrufer brechen — Default behält Rückwärtskompatibilität) für einen Sonntag (z. B. `new Date(2026, 5, 14)` = So 14.06.2026) liefert KW 24, NICHT KW 25. Test schlägt mit dem aktuellen `Math.ceil` fehl und wird mit `Math.floor` grün.
    - WR-03 Regression: Für einen Montag (z. B. `new Date(2026, 5, 15)` = Mo 15.06.2026) liefert `getAktuelleKw` KW 25.
    - WR-04 (RED→GREEN): Ein Fenster, dessen Klimazonen-verschobener DOY in den Jahresanfang fällt (Zone-1-Offset −30: z. B. `sowIndoorDoyStart = 5`, `sowIndoorDoyEnd = 20` ergibt nach −30 negativen DOY → clamp auf 1 → KW 1) erzeugt KEIN Fenster mit `startKw > endKw`. Assertion: für jedes Fenster aus `getFensterFuerPflanze(pflanze, 1)` gilt `startKw <= endKw`.
    - WR-04 Jahresende: Ein Fenster mit `endDoy >= 359` (späte Dezember-DOY, die in ISO-KW 1 des Folgejahres wrappt) wird auf `endKw = 53` normalisiert, sodass `startKw <= endKw`.
  </behavior>
  <action>
    Korrigiere zwei Engine-Defekte in `kalenderEngine.ts`:

    (1) WR-03 — `getAktuelleKw`: Ersetze `Math.ceil` durch `Math.floor` bei der DOY-Berechnung (an Sonntagen liefert `ceil` einen um 1 zu hohen DOY, was über `doyToIsoKw` die Folge-KW ergibt). Mache die Funktion testbar: füge einen optionalen Parameter `now: Date = new Date()` hinzu und nutze `now` statt eines internen `new Date()`. Bestehende no-arg-Aufrufer (`useKalenderData.aktuelleKw`, GanttStreifen) bleiben unverändert. Behalte den `Math.min(53, doyToIsoKw(doy))`-Clamp.

    (2) WR-04 — `addWindow`-Closure in `getFensterFuerPflanze`: Nach der KW-Konvertierung (`startKw`/`endKw` aus `doyToIsoKw`) normalisiere invertierte Fenster aus dem ISO-Wochen-Jahreswechsel-Wrap. Per 10-REVIEW.md Fix-Empfehlung: wenn `startKw > endKw`, pinne die Wrap-Grenze an die Kalenderkante — `if (s <= 7) startKw = 1;` (früher Jahresanfang wrappt auf KW 52/53 der Vorjahres-ISO-Nummerierung) und `if (e >= 359) endKw = 53;` (spätes Jahresende wrappt auf KW 1). Die `let`-Deklaration der KW-Variablen entsprechend anpassen. Der bestehende DOY-Clamp (1..365, Fallstrick 1) und KW-Clamp (Math.min(53), Fallstrick 4) bleiben erhalten.

    Erweitere `kalenderEngine.test.ts`: Verschärfe den bestehenden `getAktuelleKw`-Test um Sonntags-/Montags-Assertions mit injiziertem Datum. Füge eine neue `describe`-Gruppe für die Wrap-Invariante hinzu (für jedes Fenster aus Zone 1 mit jahresanfang-/jahresende-nahen DOY gilt `startKw <= endKw`).

    Keine neuen Dependencies. Reine UTC-Arithmetik beibehalten (kein date-fns, RESEARCH-Vorgabe).
  </action>
  <verify>
    <automated>pnpm --filter @spatenstich/shared exec jest kalenderEngine</automated>
  </verify>
  <acceptance_criteria>
    - `getAktuelleKw` enthält `Math.floor` und keinen `Math.ceil` mehr (Grep: `grep -v '^\s*//' packages/shared/src/lib/kalenderEngine.ts | grep -c "Math.ceil"` ergibt 0 in getAktuelleKw — doyToIsoKw darf sein eigenes ceil behalten, prüfe gezielt die getAktuelleKw-Funktion).
    - Neuer Test: `getAktuelleKw(new Date(2026, 5, 14))` === 24 (Sonntag) — grün.
    - Neuer Test: `getAktuelleKw(new Date(2026, 5, 15))` === 25 (Montag) — grün.
    - Neuer Test: alle Fenster aus `getFensterFuerPflanze(wrapPflanze, 1)` erfüllen `f.startKw <= f.endKw` — grün.
    - `pnpm --filter @spatenstich/shared exec jest kalenderEngine` — alle bestehenden 15 Tests + neue Tests grün, 0 failures.
    - `pnpm --filter @spatenstich/shared typecheck` — exit 0.
  </acceptance_criteria>
  <done>getAktuelleKw liefert an Sonntagen die laufende KW; gewrappte Fenster haben startKw <= endKw; alle Engine-Tests grün.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GanttStreifen Breiten-Guard gegen negative Balken (WR-04 Komponenten-Hälfte)</name>
  <files>app/src/components/kalender/GanttStreifen.tsx, app/src/components/kalender/__tests__/GanttStreifen.test.tsx</files>
  <read_first>
    - app/src/components/kalender/GanttStreifen.tsx (zu ändernde Datei — Balken-Berechnung Z. 46-65: `clampedStart`/`clampedEnd`/`width`)
    - app/src/components/kalender/__tests__/GanttStreifen.test.tsx (bestehende Tests — Render-/Farb-Pattern als Vorlage)
    - .planning/phases/10-aussaatkalender-v1/10-REVIEW.md (WR-04 Z. 127-145, speziell GanttStreifen.tsx:50 negative width)
  </read_first>
  <behavior>
    - WR-04 (RED→GREEN): Wird `GanttStreifen` mit einer Pflanze gerendert, deren (nach Engine-Fix theoretisch nicht mehr, aber defensiv abzusichernd) Fenster `endKw < startKw` hätte, rendert die Komponente für dieses Fenster KEINEN Balken (kein `gantt-bar` mit negativer width). Da Task 1 die Engine-Quelle bereits normalisiert, ist dieser Guard die zweite Verteidigungslinie. Test: ein Fenster mit `endKw < startKw` (direkt als Prop-Fenster konstruiert oder via Mock von getFensterFuerPflanze) erzeugt 0 Balken für dieses Fenster.
    - Regression: Die bestehenden 3 Balken (Vorkultur/Auspflanzen/Ernte) für MOCK_PLANT bleiben unverändert gerendert (kein false-positive-Skip gültiger Fenster).
  </behavior>
  <action>
    Füge in der `fenster.map`-Schleife von `GanttStreifen.tsx` einen defensiven Guard hinzu: nach Berechnung von `clampedStart` und `clampedEnd`, wenn `clampedEnd < clampedStart`, gib für dieses Fenster `null` zurück (kein Balken). Dies verhindert negative `width`-Prozentwerte (10-REVIEW.md WR-04: `width = (clampedEnd - clampedStart + 1) / TOTAL_KW * 100` würde negativ). Die bestehende `key`/`testID="gantt-bar"`/`FARBEN[f.typ]`-Logik für gültige Fenster bleibt identisch.

    Erweitere `GanttStreifen.test.tsx`: Da `GanttStreifen` `getFensterFuerPflanze` intern aufruft, mocke `@spatenstich/shared` in einem neuen Test so, dass `getFensterFuerPflanze` ein invertiertes Fenster (`startKw: 30, endKw: 5`) zurückgibt, und assertiere `queryAllByTestId('gantt-bar')` hat Länge 0 für dieses Fenster (bzw. das invertierte wird übersprungen, gültige bleiben). Halte den Mock minimal und isoliert (nur dieser Test mockt das Modul, damit die bestehenden 4 Tests unverändert die echte Engine nutzen — ggf. separate Testdatei oder `jest.isolateModules`/`jest.doMock`, je nach jest-Setup; bevorzugt einen separaten `describe` mit lokalem Fenster-Array, falls die Komponente refaktoriert werden kann, ohne die Render-Signatur zu brechen — bevorzugte Variante: belasse die Komponenten-API unverändert und teste über `getFensterFuerPflanze`-Mock).
  </action>
  <verify>
    <automated>pnpm --filter app exec jest --testPathPattern="GanttStreifen"</automated>
  </verify>
  <acceptance_criteria>
    - `GanttStreifen.tsx` enthält einen Guard `if (clampedEnd < clampedStart) return null;` (oder äquivalent) innerhalb der `fenster.map`.
    - Neuer Test: invertiertes Fenster (startKw > endKw) → 0 `gantt-bar` für dieses Fenster — grün.
    - Bestehende 4 GanttStreifen-Tests (3 Balken, 0 Balken bei leerer Pflanze, accessibilityLabel, Farben) — unverändert grün.
    - `pnpm --filter app exec jest --testPathPattern="GanttStreifen"` — 0 failures.
    - `pnpm --filter app exec tsc --noEmit` — exit 0.
  </acceptance_criteria>
  <done>GanttStreifen rendert keinen Balken mit negativer Breite; bestehende Render-Tests unverändert grün.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Geänderte/neue Symbole dieses Plans:
- `getAktuelleKw(now?: Date)` in `packages/shared/src/lib/kalenderEngine.ts` — neuer optionaler `now`-Parameter (rückwärtskompatibel), `Math.ceil` → `Math.floor`.
- `addWindow`-Closure in `getFensterFuerPflanze` — Wrap-Normalisierung (`startKw > endKw` → Kantenpinning).
- Breiten-Guard in `GanttStreifen` (`app/src/components/kalender/GanttStreifen.tsx`) — überspringt invertierte Fenster.
- Neue Regressionstests in `kalenderEngine.test.ts` (Sonntags-/Montags-KW, Wrap-Invariante) und `GanttStreifen.test.tsx` (invertiertes Fenster).
Keine neuen öffentlichen Exporte; Signatur von `getAktuelleKw` additiv erweitert.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keine neue | Reine Berechnungs-/Rendering-Logik; kein neuer Input-Pfad, keine DB-Mutation, kein Netzwerk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05-01 | Tampering | kalenderEngine Datums-/DOY-Eingabe | accept | Reine Funktion; `now`-Parameter nur in Tests injiziert, Produktiv-Default `new Date()`; KW/DOY-Clamps (1..53 / 1..365) bestehen weiter und begrenzen jeden Wertebereich |
| T-10-05-02 | Denial of Service | GanttStreifen Render mit Fehl-Fenster | mitigate | Breiten-Guard verhindert negative width → kein kaputter Render/Layout-Crash bei degeneriertem Fenster |
| T-10-05-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Packages installiert — reine Code-Korrektur in bestehenden Modulen |
</threat_model>

<verification>
- `pnpm --filter @spatenstich/shared exec jest kalenderEngine` — alle Tests grün
- `pnpm --filter app exec jest --testPathPattern="GanttStreifen"` — alle Tests grün
- `pnpm --filter @spatenstich/shared typecheck` — exit 0
- `pnpm --filter app exec tsc --noEmit` — exit 0
</verification>

<success_criteria>
- WR-03 geschlossen: getAktuelleKw an Sonntagen korrekt (durch Test bewiesen)
- WR-04 geschlossen: keine Fenster mit startKw > endKw aus der Engine; GanttStreifen rendert keine negativen Balken
- Keine Regression in bestehenden Engine-/Gantt-Tests
</success_criteria>

<output>
Create `.planning/phases/10-aussaatkalender-v1/10-05-SUMMARY.md` when done
</output>
