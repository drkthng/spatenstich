---
phase: 10-aussaatkalender-v1
plan: 03
type: execute
wave: 3
depends_on: [10-01, 10-02]
files_modified:
  - app/src/components/kalender/GanttStreifen.tsx
  - app/src/components/kalender/GanttLegende.tsx
  - app/src/components/kalender/KalenderWochenCard.tsx
  - app/src/components/kalender/PflanzenKalenderZeile.tsx
  - app/app/(app)/kalender/index.tsx
  - app/app/(app)/index.tsx
  - app/src/components/kalender/__tests__/GanttStreifen.test.tsx
autonomous: true
requirements: [CAL-01, CAL-03]
must_haves:
  truths:
    - "GanttStreifen renders one positioned View bar per KalenderFenster, colored per Aktionstyp (CAL-01 12-Monats-Streifen)"
    - "The Wochen-View screen at /(app)/kalender shows the KalenderWochenCard (diese Woche) and the Jahresübersicht plant list with a 'Nur meine Pflanzen' filter chip"
    - "Aktions-Badges distinguish Vorkultur/Direktsaat/Auspflanzen/Ernte by color + German label (CAL-03)"
    - "When klimazone is null the screen shows the PLZ-warning InlineBanner with an action to /(app)/profile/plz"
    - "The Home screen has a 'Zum Kalender' button that navigates to /(app)/kalender"
  artifacts:
    - path: "app/src/components/kalender/GanttStreifen.tsx"
      provides: "12-month View-bar Gantt strip, height-configurable, color per Aktionstyp"
      exports: ["GanttStreifen"]
    - path: "app/app/(app)/kalender/index.tsx"
      provides: "Wochen-View route: KW card + filter chip + plant list"
      contains: "kalender.title"
    - path: "app/src/components/kalender/KalenderWochenCard.tsx"
      provides: "Diese-Woche action list with colored Aktions-Badges"
      exports: ["KalenderWochenCard"]
  key_links:
    - from: "app/app/(app)/kalender/index.tsx"
      to: "app/src/hooks/useKalenderData.ts"
      via: "useKalenderData()"
      pattern: "useKalenderData"
    - from: "app/src/components/kalender/GanttStreifen.tsx"
      to: "@spatenstich/shared kalenderEngine"
      via: "getFensterFuerPflanze"
      pattern: "getFensterFuerPflanze"
    - from: "app/app/(app)/index.tsx"
      to: "/(app)/kalender"
      via: "router.push"
      pattern: "/\\(app\\)/kalender"
---

<objective>
Build the Phase-10 calendar UI primitives and the main Wochen-View screen, and wire the Home screen entry point.

Purpose: This plan renders the "diese Woche" answer (CAL-01 timeline + CAL-03 phase distinction). It delivers the four kalender components (GanttStreifen, GanttLegende, KalenderWochenCard, PflanzenKalenderZeile) and the `/(app)/kalender` route per the approved UI-SPEC, then adds the "Zum Kalender" button to the Home screen. The detail screen (CAL-05/CAL-06) is plan 04.

Output: 4 components, the Wochen-View screen, the Home-screen button, and the filled GanttStreifen test (CAL-01).
</objective>

<artifacts_this_phase_produces>
NEW symbols introduced by this plan (treat as newly-created in drift passes):
- `GanttStreifen` component (props: plant, klimazone, height?)
- `GanttLegende` component (no props)
- `KalenderWochenCard` component (props: aktionen, aktuelleKw, onPlantPress)
- `PflanzenKalenderZeile` component (props: plant, klimazone, onPress, testID?)
- Route `app/app/(app)/kalender/index.tsx` (default export Wochen-View screen)
- Home-screen button testID `home-kalender-button` → `/(app)/kalender`
- Module-local `FARBEN` Record<AktionsTyp,string>, `LEGENDE_ITEMS`, `MONATE` constants
</artifacts_this_phase_produces>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-aussaatkalender-v1/10-UI-SPEC.md
@.planning/phases/10-aussaatkalender-v1/10-PATTERNS.md
@.planning/phases/10-aussaatkalender-v1/10-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GanttStreifen + GanttLegende components + GanttStreifen test (CAL-01)</name>
  <files>app/src/components/kalender/GanttStreifen.tsx, app/src/components/kalender/GanttLegende.tsx, app/src/components/kalender/__tests__/GanttStreifen.test.tsx</files>
  <read_first>
    - app/src/components/VereinsregelRow.tsx (View-based layout row imports + className-only styling + accessibilityLabel convention)
    - app/src/components/InlineBanner.tsx (color-dot + label pattern reference for the Legende; props surface)
    - app/app/(app)/index.tsx lines 15-16 (the `t()` i18n helper to copy verbatim into each component that renders text)
    - packages/shared/src/lib/kalenderEngine.ts (getFensterFuerPflanze signature + AktionsTyp + KalenderFenster — imported from '@spatenstich/shared')
    - .planning/phases/10-aussaatkalender-v1/10-PATTERNS.md §GanttStreifen + §GanttLegende (verbatim render code) and 10-UI-SPEC.md §Gantt-Rendering-Kontrakt (TOTAL_KW=52, KW53→52 clamp, track #E5E7EB, 20px/8px heights) + §Gantt-Phasen-Farben (exact hex per Aktionstyp)
    - app/src/components/kalender/__tests__/GanttStreifen.test.tsx (the existing it.todo stub from plan 01 — replace it)
  </read_first>
  <behavior>
    - Test (CAL-01): rendering GanttStreifen for a plant with a Vorkultur, an Auspflanzen, and an Ernte window produces 3 positioned bar Views (queryable by testID `gantt-bar`), each with a backgroundColor matching the FARBEN map for its Aktionstyp.
    - Test: a plant with no DOY windows renders the track View but zero `gantt-bar` children.
    - Test: the outer track View carries an accessibilityLabel that includes the plant's nameDe.
  </behavior>
  <action>
    Create `app/src/components/kalender/GanttStreifen.tsx` per 10-PATTERNS.md §GanttStreifen (verbatim structure): module-level `const TOTAL_KW = 52` and `const FARBEN: Record<AktionsTyp, string> = { Vorkultur:'#A78BFA', Direktsaat:'#34D399', Auspflanzen:'#60A5FA', Ernte:'#FB923C' }`. Export `function GanttStreifen({ plant, klimazone, height = 20 }: { plant: PlantRow; klimazone: number; height?: number }): React.JSX.Element`. Call `getFensterFuerPflanze(plant, klimazone)` (imported from `'@spatenstich/shared'`). Render an outer track `<View style={{ flexDirection:'row', height, backgroundColor:'#E5E7EB', borderRadius:4 }} accessibilityLabel={`Gantt-Diagramm für ${plant.nameDe}`}>`. For each fenster compute `clampedStart = Math.max(1, f.startKw)`, `clampedEnd = Math.min(TOTAL_KW, f.endKw)`, `left = ((clampedStart-1)/TOTAL_KW)*100`, `width = ((clampedEnd-clampedStart+1)/TOTAL_KW)*100`, and render an absolutely-positioned `<View testID="gantt-bar" style={{ position:'absolute', left:`${left}%`, width:`${width}%`, height:'100%', backgroundColor:FARBEN[f.typ], borderRadius:3 }} />`. Import `PlantRow` from `'@spatenstich/shared'` and `AktionsTyp` from `'@spatenstich/shared'`. Create `app/src/components/kalender/GanttLegende.tsx` per 10-PATTERNS.md §GanttLegende: module-level `LEGENDE_ITEMS` array (4 entries, typ + farbe hex + i18n key `kalender.legende.<typ-lowercase>`) in the order Vorkultur→Direktsaat→Auspflanzen→Ernte; copy the `t()` helper; render `<View className="flex-row flex-wrap gap-x-3 gap-y-1">` with per-item color dot (`{ width:10, height:10, borderRadius:5, backgroundColor:item.farbe }`) + `<Text className="text-xs text-stone-600 dark:text-stone-300">{t(item.key)}</Text>`. Replace the stub `app/src/components/kalender/__tests__/GanttStreifen.test.tsx` with real tests using the components jest project render util (check a neighboring component test for the established `render`/`screen` import) covering the behavior block; use a local PlantRow mock with DOY fields that yield the three expected windows.
  </action>
  <verify>
    <automated>pnpm --filter app exec jest --testPathPattern=GanttStreifen</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter app exec jest --testPathPattern=GanttStreifen` exits 0 with the three behavior tests passing and no remaining `it.todo`.
    - `app/src/components/kalender/GanttStreifen.tsx` exports `GanttStreifen`, imports `getFensterFuerPflanze` from `'@spatenstich/shared'`, and renders bars with `testID="gantt-bar"`.
    - The FARBEN hex values are exactly `#A78BFA / #34D399 / #60A5FA / #FB923C` (verify: `grep -nE "#A78BFA|#34D399|#60A5FA|#FB923C" app/src/components/kalender/GanttStreifen.tsx` shows all four).
    - `GanttLegende.tsx` exports `GanttLegende` and references `kalender.legende.` i18n keys.
    - No import from `'@spatenstich/shared/lib/kalenderEngine'` exists (subpath invalid).
  </acceptance_criteria>
  <done>Both Gantt components render via View-bars (no SVG), CAL-01 test green, exact phase colors, i18n legend keys wired.</done>
</task>

<task type="auto">
  <name>Task 2: KalenderWochenCard + PflanzenKalenderZeile + Wochen-View screen + Home button (CAL-03)</name>
  <files>app/src/components/kalender/KalenderWochenCard.tsx, app/src/components/kalender/PflanzenKalenderZeile.tsx, app/app/(app)/kalender/index.tsx, app/app/(app)/index.tsx</files>
  <read_first>
    - app/app/(app)/index.tsx (FULL — the analog screen: loading guard, ScrollView + contentContainerStyle padding 16, t() helper, Button navigation with router.push and testID, screen background `bg-[#F9F7F4] dark:bg-[#1C1917]`; this task ADDS a "Zum Kalender" button here)
    - app/src/components/VereinsregelRow.tsx (Pressable row: onPress, accessibilityRole="button", accessibilityLabel, min-h-[44px] className, testID)
    - app/src/components/ui/badge.tsx (Badge props/usage for the Aktions-Typ label) and app/src/components/ui/button.tsx (Button variant="outline"/"default")
    - app/src/components/InlineBanner.tsx (props: message, variant, actionLabel, onAction — used for klimazone==null and empty states)
    - app/src/hooks/useKalenderData.ts (the hook from plan 02: returns wochenAktionen, meinePflanzenslugs, aktuelleKw, klimazone, hasBeetImPlan, loading; accepts `{ nurMeinePflanzen }`)
    - .planning/phases/10-aussaatkalender-v1/10-UI-SPEC.md §Screen 1 (anatomy) + §Filter-Chip-Kontrakt (active/inactive className + default-on rule) + §Copywriting Contract + §Empty States + §Navigation-Kontrakt (Home button "Zum Kalender" placement) + 10-PATTERNS.md §KalenderWochenCard + §PflanzenKalenderZeile + §kalender/index.tsx
  </read_first>
  <action>
    Create `app/src/components/kalender/KalenderWochenCard.tsx` per 10-PATTERNS.md §KalenderWochenCard: props `{ aktionen: Array<{ plant: PlantRow; fenster: KalenderFenster }>; aktuelleKw: number; onPlantPress: (slug: string) => void }`; copy the `t()` helper; render the card (`bg-stone-200 dark:bg-stone-800 rounded-xl p-4`) with a header row showing `t('kalender.dieseWoche')` (text-xl font-semibold) and the KW label; for each aktion render a Pressable row (`min-h-[44px]`, accessibilityRole="button", onPress → onPlantPress(plant.slug)) containing a Badge whose backgroundColor is the FARBEN value for `fenster.typ` and whose text is `t('kalender.legende.' + fenster.typ.toLowerCase())` (CAL-03 colored phase distinction) plus `plant.nameDe` (text-sm). Define/import the same FARBEN map (re-export from GanttStreifen.tsx or duplicate a local const — prefer importing `FARBEN` if GanttStreifen exports it; otherwise duplicate per UI-SPEC colors). When `aktionen.length === 0` render the empty-state text `t('kalender.keineAktionenDieseWoche')` inside the card. Create `app/src/components/kalender/PflanzenKalenderZeile.tsx` per 10-PATTERNS.md §PflanzenKalenderZeile: props `{ plant: PlantRow; klimazone: number; onPress: (slug: string) => void; testID?: string }`; a Pressable row (`flex-row items-center gap-3 py-2 min-h-[44px]`, accessibilityRole="button", accessibilityLabel=`${plant.nameDe}, Kalenderdetails öffnen`) with the plant name (flex-1, text-sm font-semibold) and a fixed-width (`w-32`) Miniatur-`<GanttStreifen plant={plant} klimazone={klimazone} height={8} />`. Create the route `app/app/(app)/kalender/index.tsx` per 10-PATTERNS.md §kalender/index.tsx + 10-UI-SPEC §Screen 1: default-export a screen with `<Stack.Screen options={{ headerTitle: t('kalender.title') }} />`, screen background `flex-1 bg-[#F9F7F4] dark:bg-[#1C1917]`, a vertical ScrollView (contentContainerStyle padding 16). Use `useKalenderData`. When `klimazone == null` render an `InlineBanner variant="warning" message={t('kalender.plzFehlt')} actionLabel={t('kalender.plzJetztEingeben')} onAction={() => router.push('/(app)/profile/plz' as any)}`. Render a Filter-Chip Pressable per §Filter-Chip-Kontrakt: local `React.useState(nurMeinePflanzen)` initialized to `meinePflanzenslugs.size > 0`; active className `bg-[#4A7C59] rounded-full px-3 py-1` + white text, inactive `border border-stone-300 dark:border-stone-700 rounded-full px-3 py-1`; accessibilityRole="checkbox" + accessibilityState={{ checked }}; label `t('kalender.filterMeinePflanzen')`. Pass the chip state into the hook (or re-derive the filtered list locally). Render `<KalenderWochenCard aktionen={wochenAktionen} aktuelleKw={aktuelleKw} onPlantPress={(slug) => router.push(('/(app)/kalender/' + slug) as any)} />`. Below it render a `t('kalender.jahresuebersicht')` section header (text-sm font-semibold) followed by the list of `<PflanzenKalenderZeile>` for the active (filtered) plant set, each navigating to `/(app)/kalender/[slug]`. Handle the loading state (usePlants cold-start) with the existing centered `text-stone-500 "..."` pattern, and the "filter active + 0 slugs" empty state per §Empty States (`kalender.keinePflanzenImPlan` heading + body + outline "Plan öffnen" button → `/(app)/plan`). Finally, edit `app/app/(app)/index.tsx`: add a `Button variant="outline"` with `testID="home-kalender-button"` and label `t('kalender.title')` that calls `router.push('/(app)/kalender' as any)`, placed in BOTH the has-plan branch (after the existing "Plan öffnen"/import buttons) and the empty-state branch (after the existing buttons), per §Navigation-Kontrakt.
  </action>
  <verify>
    <automated>pnpm --filter app exec jest --testPathPattern="kalender|home" && pnpm --filter app exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter app exec tsc --noEmit` exits 0.
    - `pnpm --filter app exec jest --testPathPattern="GanttStreifen"` still green (no regression from the FARBEN export decision).
    - `app/app/(app)/kalender/index.tsx` exists, default-exports a component, references `t('kalender.title')`, uses `useKalenderData`, and pushes `'/(app)/kalender/' + slug` on plant press.
    - The Filter-Chip uses `accessibilityRole="checkbox"` and the active style `bg-[#4A7C59]` (verify both strings present in kalender/index.tsx).
    - `app/app/(app)/index.tsx` contains a button with `testID="home-kalender-button"` that pushes `/(app)/kalender` (verify: `grep -n "home-kalender-button" app/app/(app)/index.tsx` matches and `grep -n "/(app)/kalender'" app/app/(app)/index.tsx` matches).
    - `KalenderWochenCard.tsx` renders a Badge colored by Aktionstyp using a label from `kalender.legende.*` (CAL-03) — verify `grep -n "kalender.legende" app/src/components/kalender/KalenderWochenCard.tsx` matches.
    - All new components use `min-h-[44px]` on Pressable rows (verify present in PflanzenKalenderZeile.tsx and KalenderWochenCard.tsx).
  </acceptance_criteria>
  <done>Wochen-View screen renders KW card + filter chip + plant list; Home has "Zum Kalender" entry; colored Aktions-Badges distinguish all 4 phases; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route param / navigation → screen | The plant slug placed into the navigation path crosses from UI list into the [slug] route |
| klimazone (UI) → render | Null/invalid klimazone reaching render path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Tampering / Injection | slug in `router.push('/(app)/kalender/' + slug)` | mitigate | slug originates from PlantRow.slug in the bundled/validated plant DB (Phase 8 schema enforces slug format), not user free-text; the [slug] route (plan 04) resolves it against the plant list and renders a "nicht gefunden" state on no match — no eval/SQL, Expo Router path-encodes the segment. |
| T-10-07 | Information Disclosure | klimazone==null render | mitigate | Screen renders the PLZ-warning InlineBanner and the hook returns empty wochenAktionen — no NaN/undefined leaks into UI (Fallstrick 2). |
| T-10-SC | Tampering | npm installs | accept | No new packages (RESEARCH §Package Legitimacy Audit). All UI primitives (Card/Button/Badge/InlineBanner) pre-exist. |
</threat_model>

<verification>
- `pnpm --filter app exec jest --testPathPattern="kalender|GanttStreifen|home"` green.
- `pnpm --filter app exec tsc --noEmit` exits 0.
- Home button `home-kalender-button` present and wired to `/(app)/kalender`.
</verification>

<success_criteria>
- CAL-01: GanttStreifen renders 12-month View-bars per window; test green.
- CAL-03: Aktions-Badges distinguish Vorkultur/Direktsaat/Auspflanzen/Ernte by color + German label.
- Wochen-View screen live at `/(app)/kalender` with filter chip + plant list; Home entry button added.
</success_criteria>

<output>
Create `.planning/phases/10-aussaatkalender-v1/10-03-SUMMARY.md` when done.
</output>
