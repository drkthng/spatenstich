---
phase: 10-aussaatkalender-v1
plan: "07"
type: execute
wave: 2
depends_on: ["10-06"]
files_modified:
  - app/app/(app)/kalender/[slug].tsx
  - packages/shared/src/i18n/de.json
  - app/src/components/kalender/__tests__/PflanzenDetail.test.tsx
autonomous: true
gap_closure: true
requirements: [CAL-01, CAL-05, CAL-06]
must_haves:
  truths:
    - "Der Detail-Screen crasht NICHT bei unbekanntem Slug nach Ladeabschluss (Rules-of-Hooks eingehalten — Guard nach allen Hooks)"
    - "Die CAL-06 Fruchtfolge-Warnung erscheint NUR wenn eine gleiche-Familie-Pflanze im JEWEILIGEN Ziel-Beet sitzt, nicht plan-global"
    - "Eine gleiche-Familie-Pflanze in einem ANDEREN Beet löst KEINE Fruchtfolge-Warnung aus"
    - "Drei zuvor hartkodierte deutsche UI-Strings laufen über de.json/t()"
  artifacts:
    - path: "app/app/(app)/kalender/[slug].tsx"
      provides: "Hook-stabiler Detail-Screen, beet-scoped Fruchtfolge, de.json-Strings, kein toter Import"
      contains: "findPflanzenInBeet"
    - path: "packages/shared/src/i18n/de.json"
      provides: "Neue kalender-Keys: nichtGefunden, hinzufuegenFehler"
      contains: "nichtGefunden"
    - path: "app/src/components/kalender/__tests__/PflanzenDetail.test.tsx"
      provides: "Not-found-Pfad-Test + Anderes-Beet-kein-Warnung-Test"
      contains: "anderes Beet"
  key_links:
    - from: "app/app/(app)/kalender/[slug].tsx"
      to: "app/src/lib/kalenderBeete.ts"
      via: "findPflanzenInBeet für beet-scoped Fruchtfolge"
      pattern: "findPflanzenInBeet"
    - from: "app/app/(app)/kalender/[slug].tsx"
      to: "packages/shared/src/i18n/de.json"
      via: "t('kalender.nichtGefunden') / t('kalender.hinzufuegenFehler')"
      pattern: "kalender\\.(nichtGefunden|hinzufuegenFehler)"
---

<objective>
Schließt den Phasenziel-BLOCKER CR-01 (Rules-of-Hooks-Verletzung — Detail-Screen crasht bei unbekanntem Slug) und BLOCKER WR-02 (CAL-06 Fruchtfolge nicht beet-scoped) im Pflanzen-Detail-Screen `app/app/(app)/kalender/[slug].tsx`. Nimmt zusätzlich IN-01 (toter Import `getFensterFuerPflanze`) und IN-02 (3 hartkodierte deutsche Strings) mit.

CR-01 ist das Herzstück des Phasenziels — "Klick auf Pflanze → Detail-View" (Truth 6) — und crasht aktuell mit "Rendered fewer hooks than expected", weil der Early-Return-Guard (Z. 59-68) VOR `React.useMemo` (Z. 75 fruchtfolgeGrund, Z. 121 meineBeete) und `React.useCallback` (Z. 127 handleAddToPlan) steht. WR-02: die innere `otherPflanzenInBeet`-Filterung (Z. 89-97) ignoriert die Loop-Variable `beet` und filtert plan-global statt beet-scoped — die Warnung feuert bei gleicher Familie irgendwo im Plan.

Wave 2, hängt von 10-06 ab: WR-02 wird mit dem dort neu exportierten `findPflanzenInBeet(elements, beet)` korrekt beet-scoped. Kein File-Overlap mit dem parallelen Wave-2-Plan 10-08 (Hook).

Purpose: Truth 6 (Detail-View) wird crash-frei erreicht; CAL-06 entspricht der Spec ("gleiche Familie im Ziel-Beet"); i18n-Konvention (NFR-06) eingehalten.
Output: Hook-stabiler `[slug].tsx`, beet-scoped Fruchtfolge über `findPflanzenInBeet`, 2 neue de.json-Keys, Not-found- und Anderes-Beet-Tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/10-aussaatkalender-v1/10-REVIEW.md
@.planning/phases/10-aussaatkalender-v1/10-VERIFICATION.md
@.planning/phases/10-aussaatkalender-v1/10-04-SUMMARY.md
@app/src/lib/kalenderBeete.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: CR-01 Rules-of-Hooks fixen + IN-01/IN-02 mitnehmen (Detail-Screen)</name>
  <files>app/app/(app)/kalender/[slug].tsx, packages/shared/src/i18n/de.json</files>
  <read_first>
    - app/app/(app)/kalender/[slug].tsx (zu ändernde Datei — Early-Return Z. 59-68 vs. Hooks Z. 75/121/127; toter Import Z. 16; hartkodierte Strings Z. 64 + Z. 136)
    - .planning/phases/10-aussaatkalender-v1/10-REVIEW.md (CR-01 Z. 55-73 mit Fix-Skizze; IN-01 Z. 192-196; IN-02 Z. 198-202)
    - .planning/phases/10-aussaatkalender-v1/10-VERIFICATION.md (Gap 1 truth/missing Z. 9-16)
    - packages/shared/src/i18n/de.json (kalender-Block Z. 384-414 — Einfügepunkt für neue Keys; UTF-8 Umlaute literal, single-brace {var}-Konvention)
  </read_first>
  <action>
    (1) CR-01 — Hook-Reihenfolge stabilisieren: Verschiebe den "Plant not found guard" (aktuell Z. 59-68, `if (!loading && !plant) return ...`) NACH alle Hook-Deklarationen, d. h. unter `fruchtfolgeGrund` (useMemo), `meineBeete` (useMemo) und `handleAddToPlan` (useCallback). Die useMemos null-guarden `plant` bereits intern (`if (!plant || ...) return ...`), laufen also bei `plant === undefined` gefahrlos. Nach der Umstellung ist die Hook-Anzahl über alle Render-Pfade konstant (kein "Rendered fewer hooks than expected"-Crash mehr). Die `useState`-Calls (addLoading/successMessage/errorMessage) bleiben oben; nur der Early-Return wandert unter die Hooks.

    (2) IN-01 — toter Import: Entferne `getFensterFuerPflanze` aus dem `import { pruefeEinfacheFruchtfolge, getFensterFuerPflanze } from '@spatenstich/shared'` (Z. 16) — es wird im File nicht referenziert (GanttStreifen importiert es selbst). Behalte `pruefeEinfacheFruchtfolge`.

    (3) IN-02 — i18n: Füge zwei Keys zum `kalender`-Block in `de.json` hinzu (UTF-8-Umlaute literal):
      - `"nichtGefunden": "Pflanze \"{slug}\" nicht gefunden."`
      - `"hinzufuegenFehler": "Hinzufügen fehlgeschlagen. Versuche es erneut."`
    Ersetze im `[slug].tsx` die hartkodierten Strings: Z. 64 `` `Pflanze "${slug}" nicht gefunden.` `` → `t('kalender.nichtGefunden', { slug: slug ?? '' })`; Z. 136 `'Hinzufügen fehlgeschlagen. Versuche es erneut.'` → `t('kalender.hinzufuegenFehler')`. Der vorhandene `t()`-Helper unterstützt bereits den `vars`-Parameter (single-brace-Substitution).

    KEINE neuen fenced code blocks im Screen außer den Edits. UTF-8-Umlaute literal (ä/ö/ü/ß), keine ASCII-Ersetzung (Projekt-Policy + MEMORY feedback_german_umlauts).
  </action>
  <verify>
    <automated>pnpm --filter app exec jest --testPathPattern="PflanzenDetail" && pnpm --filter @spatenstich/shared exec jest i18n.kalender</automated>
  </verify>
  <acceptance_criteria>
    - Der `if (!loading && !plant) return` Guard steht NACH `handleAddToPlan` (dem letzten useCallback) — manuell verifizierbar: Guard-Zeilennummer > useCallback-Zeilennummer.
    - `getFensterFuerPflanze` wird im Screen nicht mehr importiert (Grep: `grep -c "getFensterFuerPflanze" "app/app/(app)/kalender/[slug].tsx"` === 0).
    - de.json enthält `kalender.nichtGefunden` und `kalender.hinzufuegenFehler` mit korrekten UTF-8-Umlauten (Grep: `grep -c "nichtGefunden" packages/shared/src/i18n/de.json` === 1; `hinzufuegenFehler` enthält "Hinzufügen").
    - Keine hartkodierten deutschen Banner-Strings mehr im Screen (Grep: `grep -c "nicht gefunden\\.\`" "app/app/(app)/kalender/[slug].tsx"` für den Inline-String === 0).
    - `pnpm --filter @spatenstich/shared exec jest i18n.kalender` — grün (bestehende i18n-Tests + ggf. neue Key-Assertion).
    - `pnpm --filter app exec tsc --noEmit` — exit 0.
  </acceptance_criteria>
  <done>Hook-Reihenfolge stabil (kein Crash bei unbekanntem Slug); toter Import entfernt; Banner-Strings über de.json.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-02 Fruchtfolge beet-scoped machen + Not-found- und Anderes-Beet-Tests</name>
  <files>app/app/(app)/kalender/[slug].tsx, app/src/components/kalender/__tests__/PflanzenDetail.test.tsx</files>
  <read_first>
    - app/app/(app)/kalender/[slug].tsx (nach Task 1 — `fruchtfolgeGrund` useMemo Z. ~75-118, speziell die `otherPflanzenInBeet`-Filterung Z. 89-97, die `beet` ignoriert)
    - app/src/lib/kalenderBeete.ts (nach Plan 10-06 — `findPflanzenInBeet(elements, beet)` Export; `getPlantSlug`)
    - .planning/phases/10-aussaatkalender-v1/10-REVIEW.md (WR-02 Z. 101-109 mit Fix-Empfehlung `findPflanzenInBeet(elements, beet).filter(...)`)
    - .planning/phases/10-aussaatkalender-v1/10-VERIFICATION.md (Gap 3 truth/missing Z. 26-34)
    - app/src/components/kalender/__tests__/PflanzenDetail.test.tsx (bestehende 4 Tests; Mock-Struktur — useKalenderData/usePlants/kalenderBeete/@spatenstich/shared gemockt; muss kalenderBeete-Mock um findPflanzenInBeet erweitern)
  </read_first>
  <behavior>
    - WR-02 Scoping (RED→GREEN): Eine gleiche-Familie-Pflanze (Solanaceae) in einem ANDEREN Beet als dem/den Ziel-Beet(en) der betrachteten Pflanze löst KEINE Fruchtfolge-Warnung aus. Test: `findPflanzenInBeet` so mocken, dass das Ziel-Beet KEINE Fremd-Pflanze enthält → `pruefeEinfacheFruchtfolge` wird mit leerer/eigener Liste aufgerufen → keine `fruchtfolge-warnung`.
    - WR-02 Positiv (Regression): gleiche-Familie-Pflanze IM Ziel-Beet → `findPflanzenInBeet` liefert sie → Warnung erscheint (bestehender Test (d) bleibt grün, ggf. an findPflanzenInBeet-Mock angepasst).
    - CR-01 Not-found (neu): Render mit unbekanntem Slug nach `loading=false` (`usePlants` liefert leere/nicht-matchende Liste) crasht NICHT und zeigt den nichtGefunden-Banner. Test rendert ohne Throw und findet den Banner-Text/-Marker.
  </behavior>
  <action>
    Mache die `fruchtfolgeGrund`-Berechnung in `[slug].tsx` beet-scoped: Ersetze in der `for (const beet of targetBeete)`-Schleife die plan-globale `otherPflanzenInBeet`-Filterung (die `beet` ignoriert) durch `findPflanzenInBeet(elements, beet)` (Import aus `@/src/lib/kalenderBeete`), gefolgt von einem `.filter((e) => getPlantSlug(e) !== plant.slug)` zum Ausschluss der eigenen Pflanze (T-10-10). Das Ergebnis wird wie bisher über `getPlantSlug` + `familyBySlug.get(ps)` in `{ family, slug }` gemappt und an `pruefeEinfacheFruchtfolge` übergeben. So wird pro Beet nur dessen tatsächlich enthaltene Pflanzen-Population geprüft — entsprechend der CAL-06-Spec ("gleiche Familie bereits im Ziel-Beet").

    Erweitere `PflanzenDetail.test.tsx`:
    - Ergänze den `kalenderBeete`-Mock um `findPflanzenInBeet: jest.fn()`.
    - Neuer Test (Anderes-Beet): `findBeeteForPlant` liefert das Ziel-Beet, aber `findPflanzenInBeet` für dieses Beet liefert KEINE Fremd-Pflanze (leer oder nur die eigene) → assertiere `queryByTestId('fruchtfolge-warnung')` ist null (gleiche Familie in anderem Beet löst nichts aus).
    - Neuer Test (CR-01 Not-found): `usePlants` liefert `{ data: [] }` (Slug 'tomate' nicht auffindbar), `useKalenderData` liefert `loading: false`. `render(<PflanzenDetailScreen />)` wirft NICHT und zeigt den nichtGefunden-Banner (Text via de.json oder Marker). Dieser Test schlägt mit der alten Hook-Reihenfolge fehl (Crash) und ist nach Task 1 grün.
    - Passe den bestehenden positiven Fruchtfolge-Test (d) an, sodass `findPflanzenInBeet` für das Ziel-Beet die Paprika-Pflanze zurückgibt (statt der plan-globalen elements-Filterung) — Warnung bleibt grün.
  </action>
  <verify>
    <automated>pnpm --filter app exec jest --testPathPattern="PflanzenDetail"</automated>
  </verify>
  <acceptance_criteria>
    - `[slug].tsx` ruft `findPflanzenInBeet(elements, beet)` in der Fruchtfolge-Schleife auf (Grep: `grep -c "findPflanzenInBeet" "app/app/(app)/kalender/[slug].tsx"` >= 1) und enthält keine plan-globale `elements.filter` mehr in dieser Schleife, die `beet` ignoriert.
    - Neuer Test (Anderes-Beet): gleiche Familie in anderem Beet → KEINE `fruchtfolge-warnung` — grün.
    - Neuer Test (Not-found): Render mit unbekanntem Slug + loading=false → kein Crash, nichtGefunden-Banner sichtbar — grün.
    - Bestehende 4 PflanzenDetail-Tests (inkl. positiver Fruchtfolge) — grün (positiver Test ggf. an findPflanzenInBeet-Mock angepasst).
    - `pnpm --filter app exec jest --testPathPattern="PflanzenDetail"` — 0 failures.
    - `pnpm --filter app exec tsc --noEmit` — exit 0.
  </acceptance_criteria>
  <done>Fruchtfolge ist beet-scoped (anderes Beet löst nichts aus); Not-found-Pfad crasht nicht mehr und ist getestet.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Geänderte/neue Symbole dieses Plans:
- `[slug].tsx` Detail-Screen — Hook-Reihenfolge stabilisiert (Guard nach allen Hooks); `getFensterFuerPflanze`-Import entfernt; `fruchtfolgeGrund` nutzt `findPflanzenInBeet` (beet-scoped); Banner-Strings über `t()`.
- `de.json` — neue Keys `kalender.nichtGefunden` ({slug}-Var) und `kalender.hinzufuegenFehler`.
- `PflanzenDetail.test.tsx` — `findPflanzenInBeet`-Mock; neuer Not-found-Test; neuer Anderes-Beet-kein-Warnung-Test; angepasster positiver Fruchtfolge-Test.
Keine neuen öffentlichen Exporte (Screen ist default-export, unverändert).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL-Slug → Screen | `useLocalSearchParams().slug` ist untrusted; via `plants.find` aufgelöst (T-10-08) |
| Plan-Elemente → Fruchtfolge | provenance.plantSlug aus Import/Editor; rein lesend, type-guarded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08 | Tampering | slug aus URL | mitigate | `plants.find(p => p.slug === slug)` → unbekannter/geforgter Slug ergibt undefined → nichtGefunden-Banner (jetzt crash-frei via Hook-Fix), kein Write |
| T-10-10 | Tampering | provenance.plantSlug / family lookup | mitigate | `getPlantSlug` type-guard + `familyBySlug.get(ps)`; Elemente ohne auflösbaren Slug/Family werden vor `pruefeEinfacheFruchtfolge` herausgefiltert |
| T-10-07-01 | Denial of Service | Hook-Count-Instabilität | mitigate | Guard nach allen Hooks → konstante Hook-Anzahl, kein React-Crash bei degeneriertem Render-Pfad (Datenintegrität der Anzeige) |
| T-10-07-SC | Tampering | npm/pip/cargo installs | accept | Keine neuen Packages — reine Screen-Korrektur + 2 i18n-Keys |
</threat_model>

<verification>
- `pnpm --filter app exec jest --testPathPattern="PflanzenDetail"` — alle Tests grün (inkl. Not-found + Anderes-Beet)
- `pnpm --filter @spatenstich/shared exec jest i18n.kalender` — grün
- `pnpm --filter app exec tsc --noEmit` — exit 0
</verification>

<success_criteria>
- CR-01 geschlossen: kein Rules-of-Hooks-Crash; Not-found-Pfad getestet
- WR-02 geschlossen: Fruchtfolge beet-scoped via findPflanzenInBeet; anderes-Beet-Test grün
- IN-01/IN-02 mitgenommen: toter Import weg, Strings über de.json
</success_criteria>

<output>
Create `.planning/phases/10-aussaatkalender-v1/10-07-SUMMARY.md` when done
</output>
